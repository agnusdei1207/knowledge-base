---
title: "드롭아웃 (Dropout)"
date: "2026-07-09"
tags:
  - "cspe-basic-theory"
weight: 52
extra:
  question_no: "052"
  exam_status: "미출제"
  exam_history: ""
  exam_note: ""
---

## 미리 알고가기

- 과적합: 모델이 훈련 데이터의 잡음까지 학습해 새 데이터에서 성능이 떨어지는 상태임
- 드롭 확률 $p$: 학습 중 뉴런 출력을 0으로 만들 확률이며 규제 강도를 정함
- 베르누이 마스크: 0 또는 1을 확률적으로 뽑아 뉴런 사용 여부를 정하는 이진 벡터임
- 인버티드 드롭아웃: 학습 시 남은 출력을 $1/(1-p)$로 키워 추론 보정을 줄이는 방식임
- 공적응: 여러 뉴런이 서로에게 과도하게 의존해 독립적인 특징을 못 배우는 현상임

## Ⅰ. 개요

- **정의/개념**: 드롭아웃은 학습 중 뉴런 출력 일부를 확률적으로 0으로 마스킹해 특정 경로 의존을 낮추는 신경망 규제 기법임
- **배경/필요성**: 파라미터가 많은 신경망은 훈련 데이터를 쉽게 암기하므로 여러 부분망을 묵시적으로 학습시키는 일반화 장치가 필요함

## Ⅱ. 특징

- 매 반복마다 다른 부분망을 학습해 단일 모델 안에서 앙상블과 유사한 효과를 냄
- 뉴런 간 공적응을 줄여 일부 특징에 의존한 불안정한 판단을 완화함
- 학습 시에는 무작위 마스크를 쓰고 추론 시에는 모든 뉴런을 사용해 기대 출력을 유지함
- 드롭 확률 $p$가 크면 규제가 강하지만 과하면 표현력이 부족해 과소적합 위험이 커짐

## Ⅲ. 종류 및 비교

| 판단 기준 | 드롭아웃 | 가중치 감쇠 | 데이터 증강 |
|:---|:---|:---|:---|
| 통제 대상 | 뉴런 출력 경로 | 가중치 크기 | 입력 데이터 다양성 |
| 적용 방식 | 확률적 마스킹 | 손실 함수에 패널티 추가 | 변환된 학습 샘플 생성 |
| 적합 조건 | 큰 신경망의 은닉층 | 대부분의 파라미터 모델 | 이미지·음성 등 변환 가능 데이터 |
| 주요 위험 | 과도하면 과소적합 | 너무 강하면 경계가 둔화 | 실제 분포와 다른 변환 가능 |

> 요약: 드롭아웃은 모델 내부 경로를 흔들고, 가중치 감쇠와 데이터 증강은 각각 파라미터와 입력을 통제함.

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| 활성값 $h$ | 드롭아웃이 적용되기 전 은닉층 출력이며 마스킹 대상임 |
| 드롭 확률 $p$ | 제거할 뉴런 비율이며 일반화와 표현력의 균형을 정함 |
| 마스크 $m$ | $m_i \sim Bernoulli(1-p)$로 생성되는 뉴런별 선택값임 |
| 스케일링 | 남은 출력을 $1/(1-p)$로 보정해 학습·추론 기대값을 맞춤 |
| 실행 모드 | 학습 모드에서는 마스크를 쓰고 평가 모드에서는 마스크를 끔 |

```text
+----------+      +----------+      +-----------+      +----------+
| hidden h | ---> | mask m   | ---> | scale     | ---> | output   |
+----------+      +----------+      +-----------+      +----------+
```

> 요약: 드롭아웃은 학습 중 무작위 마스크와 스케일링으로 경로 의존을 낮춤.

## Ⅴ. 원리 및 절차 흐름도

```text
+----------+      +----------+      +----------+      +----------+
| forward  | ---> | masking  | ---> | training | ---> | inference|
+----------+      +----------+      +----------+      +----------+
```

1. **확률 설정**: 층별 과적합 위험에 따라 드롭 확률 $p$를 정함
2. **마스크 생성**: 베르누이 분포로 뉴런별 0 또는 1 값을 생성함
3. **출력 보정**: $\tilde{h} = m \odot h / (1-p)$로 남은 신호의 기대값을 맞춤
4. **추론 전환**: 평가 모드에서는 마스크를 제거하고 전체 뉴런으로 예측함

> 요약: 드롭 확률은 일반화와 표현력 사이의 균형을 만들며 평가 모드에서는 마스크를 꺼야 함.

## Ⅵ. 실무 적용 및 유의점

1. 작은 데이터의 완전연결층은 드롭아웃으로 과적합을 줄이되 $p$를 검증 손실과 학습·검증 격차로 조정함
2. BatchNorm과 함께 쓰면 통계 변동이 커지므로 위치와 비율을 분리 실험하고 평가 모드 성능으로 확인함

## Ⅶ. 결론

- 드롭아웃은 무작위 부분망 학습으로 일반화를 높이는 규제 기법이며, 드롭 확률과 실행 모드 관리가 성능을 좌우함

## 작성 근거(검토용)

- 개요는 마스킹과 경로 의존 완화를 함께 제시해 드롭아웃의 동작과 목적을 분리하지 않음
- 특징은 앙상블 효과, 공적응 억제, 모드 차이, 과소적합 위험을 핵심 축으로 선택함
- 비교표는 규제 기법 간 통제 대상이 다르다는 판단 기준을 중심에 둠
- 구성요소는 $h$, $p$, $m$, 스케일링, 실행 모드로 실제 구현 단위를 일치시킴
- 절차는 확률 설정에서 추론 전환까지 이어져 학습·서비스 차이를 드러냄
- 유의점은 과도한 규제와 BatchNorm 병행 문제를 대표 운영 리스크로 선정함
