---
title: "SVM 서포트 벡터 머신 (Support Vector Machine)"
date: "2026-07-09"
tags:
  - "cspe-basic-theory"
weight: 61
extra:
  question_no: "061"
  exam_status: "기출"
  exam_history: "132회"
  exam_note: ""
---

## 미리 알고가기

- 초평면: 특징 공간에서 클래스를 나누는 결정 경계임
- 마진: 결정 경계와 가장 가까운 학습 데이터 사이의 거리임
- 서포트 벡터: 마진을 결정하는 경계 근처의 핵심 표본임
- 커널 트릭: 고차원으로 직접 변환하지 않고 내적 계산만으로 비선형 경계를 형성함
- 규제 계수 $C$: 마진 확대와 오분류 허용 사이를 조절함
- 커널 감도 $\gamma$: RBF 커널에서 한 표본의 영향 범위를 조절함

## 작성 근거(검토용)

- 개요는 SVM을 마진을 최대화하는 지도 학습 모델로 정의해 출제 키워드와 연결함
- 마진·서포트 벡터·커널·볼록 최적화는 SVM 고유 판단축임
- Ⅲ은 실무 선택에 필요한 하드 마진과 소프트 마진의 오차 허용 차이를 중심으로 구성함
- Ⅳ는 수식보다 경계 형성 요소를 우선해 초평면, 서포트 벡터, 커널, $C$의 관계를 드러냄
- 데이터 규모와 커널 튜닝은 SVM의 성능·비용을 가르는 조건임

## Ⅰ. 개요

- **정의/개념**: SVM은 클래스 사이의 마진을 최대화하는 초평면을 찾아 분류·회귀를 수행하는 지도 학습 모델임
- **배경/필요성**: 표본이 적고 차원이 높은 데이터의 일반화 성능을 확보하기 위해 최대 마진 분류 기준이 필요함

## Ⅱ. 특징

- 마진을 넓혀 경계 주변 잡음에 덜 흔들림
- 서포트 벡터만 결정 경계에 직접 영향을 줌
- 커널 함수로 선형 분리가 어려운 데이터를 처리함
- 볼록 최적화로 전역 최적해를 찾는 구조임

## Ⅲ. 종류 및 비교

| 판단 기준 | 하드 마진 SVM | 소프트 마진 SVM |
|:---|:---|:---|
| 전제 | 완전 선형 분리 | 일부 오분류 존재 |
| 오차 허용 | 허용하지 않음 | $C$ 값으로 허용 폭 조절 |
| 최적화 항 | 마진을 최대화하는 가중치 항 | 가중치 항과 슬랙 변수 위반 비용 |
| 한계 | 이상치에 민감함 | $C$와 커널 튜닝 필요 |

> 요약: 하드 마진은 분리 가능 데이터에, 소프트 마진은 잡음이 있는 실무 데이터에 적합함.

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| 입력 특징 벡터 | 분류할 데이터를 수치 특징 공간으로 표현함 |
| 초평면 | $w \cdot x + b = 0$ 형태의 결정 경계임 |
| 서포트 벡터 | 마진 폭과 경계 위치를 결정하는 표본임 |
| 커널 함수 | 선형·다항·RBF 등 경계 형태를 결정함 |
| 규제 계수 $C$ | 큰 $C$는 오차를 줄이고 작은 $C$는 마진을 넓힘 |

> 요약: 초평면과 서포트 벡터가 경계를 만들고 커널과 $C$가 유연성을 조절함.

```text
Class A          support vectors
     o      o        o
-------------------------  margin boundary
=========================  decision hyperplane
-------------------------  margin boundary
     x      x        x
Class B          support vectors
```

## Ⅴ. 원리 및 절차 흐름도

```text
학습 데이터 -> 커널 적용 (Kernel mapping) -> 마진 최적화 -> 서포트 벡터 확정 -> 결정 함수
```

1. 학습 데이터를 특징 벡터로 변환하고 필요 시 커널을 선택함
2. 클래스 간 마진이 최대가 되는 초평면을 최적화함
3. 마진에 닿는 서포트 벡터와 계수를 확정함
4. 신규 데이터는 결정 함수의 부호로 클래스를 판정함

> 요약: SVM은 커널 선택과 마진 최적화를 거쳐 서포트 벡터만으로 결정 경계를 정의함.

## Ⅵ. 실무 사례

1. 소규모 고차원 분류에는 SVM이 적합하지만 표본 수가 커지면 학습 비용이 급증하므로 선형 SVM이나 표본 축소로 학습 시간과 정확도를 함께 확인함
2. RBF 커널은 비선형 경계를 잘 만들지만 $C$와 $\gamma$에 민감하므로 교차 검증으로 과적합 여부를 확인하고 검증 점수를 비교함

## Ⅶ. 결론

- SVM은 데이터 규모와 커널 조건에 따라 마진 안정성과 학습 비용의 균형을 선택해야 하는 경계 기반 분류 모델임
