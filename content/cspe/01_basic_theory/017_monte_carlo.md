---
title: "몬테카를로 방법 (Monte Carlo Method)"
date: "2026-07-14T00:10:00+09:00"
tags:
  - "cspe-basic-theory"
weight: 17
extra:
  question_no: "017"
  exam_status: "기출"
  exam_history: "131회"
  exam_note: ""
---

## 미리 알고가기

```text
확률분포 정의 → 난수 표본 생성 → 반복 실행 → 근삿값 계산 → 오차 평가
```

### 무작위 입력

| 개념 | 쉬운 뜻 | 주사위 실험 예시 |
|:---|:---|:---|
| 확률 (Probability) | 사건이 일어날 가능성 | 각 눈이 나올 가능성이 같음 |
| 확률변수 (Random Variable) | 무작위 결과를 숫자로 나타낸 것 | 나온 주사위 눈을 숫자로 기록함 |
| 확률분포 (Probability Distribution) | 값별 발생 가능성을 나타낸 규칙 | 1부터 6까지 비슷한 비율로 나타남 |
| 의사난수 (Pseudo-random Number) | 알고리즘으로 만든 난수 | 프로그램이 주사위 눈을 무작위처럼 생성함 |
| 독립 표본 (Independent Sample) | 앞선 결과가 다음 결과에 영향을 주지 않는 표본 | 직전 눈과 관계없이 다음 주사위를 던짐 |

### 표본과 추정

| 개념 | 쉬운 뜻 | 주사위 실험 예시 |
|:---|:---|:---|
| 모집단 (Population) | 추정하려는 전체 값이나 사례 | 주사위를 계속 던질 때 나올 모든 결과 |
| 표집 (Sampling) | 모집단에서 일부 값을 뽑는 과정 | 주사위를 1,000번 던짐 |
| 표본 (Sample) | 표집으로 얻은 값들의 집합 | 주사위 1,000회의 눈 |
| 모수 (Parameter) | 모집단이 가진 고정된 특성값 | 공정한 주사위의 실제 평균 3.5 |
| 통계량 (Statistic) | 표본에서 계산한 요약값 | 1,000회 결과에서 구한 평균·분산 |
| 표본평균 (Sample Mean) | 표본에서 구한 평균 | 1,000회 결과의 평균 3.48 |
| 기댓값 (Expected Value) | 실험을 오래 반복할 때 평균이 가까워지는 값 | 시행 횟수를 늘리면 평균이 3.5에 가까워짐 |
| 분산 (Variance) | 평균에서 먼 값에 더 큰 비중을 주어 퍼진 정도를 나타낸 값 | 1과 6처럼 평균에서 먼 눈의 차이를 크게 반영함 |
| 표준편차 (Standard Deviation) | 개별 값이 평균에서 퍼진 정도 | 값이 평균 주변에 모이면 작아짐 |
| 오차 (Error) | 추정값과 실제값의 차이 | 표본평균 3.48과 실제 평균 3.5의 차이는 0.02임 |

### 추정값의 불확실성

| 개념 | 쉬운 뜻 | 주사위 실험 예시 |
|:---|:---|:---|
| 표집분포 (Sampling Distribution) | 표집을 반복해 얻은 표본평균들의 분포 | 반복 실험의 평균 3.48, 3.51, 3.49 등이 이루는 분포 |
| 표준오차 (Standard Error) | 반복 표집에서 표본평균들이 달라지는 정도 | 작을수록 반복 실험의 평균들이 가깝게 모임 |
| 신뢰구간 (Confidence Interval) | 오차를 고려해 모수를 추정한 범위 | 주사위의 실제 평균이 있을 범위를 제시함 |
| 큰 수의 법칙 (Law of Large Numbers) | 표본을 늘리면 표본평균이 기댓값에 가까워지는 원리 | 시행 횟수를 늘릴수록 평균이 3.5에 가까워짐 |

### 계산 방법

| 개념 | 쉬운 뜻 | 적용 예시 |
|:---|:---|:---|
| 수치 근사 (Numerical Approximation) | 정확한 답 대신 계산 가능한 가까운 값을 구하는 방법 | 반복 실험의 평균으로 답을 추정함 |
| 격자법 (Grid Method) | 입력 범위를 일정한 간격으로 나누어 계산하는 방법 | 구간을 같은 간격의 점으로 나누어 값을 합산함 |
| 중요도 표본추출 (Importance Sampling) | 드문 사건을 더 자주 뽑고 그 영향을 보정하는 방법 | 매우 드문 장애 시나리오를 집중 실행해 장애 확률을 추정함 |

- 표본평균은 모수를 추정하며 표본마다 달라짐
- 표준편차는 개별 결과, 표준오차는 표본평균이 퍼진 정도임
- 표본 수를 늘리면 표준오차는 줄지만 표준편차가 반드시 줄지는 않음
- 몬테카를로 방법은 큰 수의 법칙으로 근삿값을 얻고 표준오차와 신뢰구간으로 품질을 판단함

## 작성 근거(검토용)

- **개요 구성**: 기출 키워드이므로 난수 반복, 통계량, 근사라는 정의 요소를 짧게 고정함
- **Ⅱ. 특징 선정**: 적용성, 병렬성, 오차-계산량 관계, 신뢰구간은 몬테카를로의 장단점을 직접 설명함
- **Ⅲ. 비교축 선정**: 계산 방식, 차원 증가 영향, 주요 위험은 격자법과 몬테카를로 방법의 선택을 가름
- **Ⅳ. 구조 선정**: 확률 모델, 난수 생성기, 시뮬레이션 함수, 통계 집계기, 수렴 기준은 실험 재현에 필요한 구성임
- 난수 품질과 표본 수 증가에 따른 점진적 오차 감소는 계산 자원 산정의 판단 기준임

## Ⅰ. 개요

- **정의/개념**: 몬테카를로 방법은 난수 표본을 반복 생성하고 통계 결과로 근삿값을 구하는 수치 계산 기법임
- **배경/필요성**: 계산 비용이 크거나 정확한 해를 구하기 어려운 문제를 풀기 위해 표본 기반 근사가 필요함

### 쉽게 이해하기 (학습용)
- 눈을 감고 과녁에 화살을 수백 번 쏜 뒤, 화살이 맞은 자리들의 평균 위치로 과녁 중심을 짐작하는 것과 같음

## Ⅱ. 특징

- 확률 모델로 표현한 문제의 근삿값을 표본 평균·빈도 등의 통계량으로 구함
- 각 표본 시행이 독립적이므로 여러 노드에서 병렬 실행한 뒤 통계량을 합산할 수 있음
- 표본 수를 늘릴수록 표준오차는 감소하지만 개선 폭은 점차 작아지고 계산량은 계속 증가함
- 추정 결과는 표준오차와 신뢰구간으로 함께 제시해야 함

### 쉽게 이해하기 (학습용)
- 화살을 쏠 때마다 서로 영향이 없어 여러 사람이 동시에 쏴서 결과만 합쳐도 됨(병렬 가능)
- 정밀도를 조금 더 높이려 해도 표본 수를 크게 늘려야 하므로 정확도와 계산 비용을 함께 판단해야 함

## Ⅲ. 종류 및 비교

| 판단 기준 | 결정론적 수치 해석 (격자법) | 몬테카를로 방법 (통계법) |
|:---|:---|:---|
| 계산 방식 | 규칙 격자점의 결정론적 합산 | 난수 표본의 통계량 추정 |
| 차원 증가 영향 | 격자점 수가 지수적으로 증가 | 차원 수와 무관하게 표본 증가에 따른 점진적 오차 감소 특성 유지 |
| 주요 위험 | 격자 이산화 오차 | 난수 생성 편향 |

> 요약: 저차원 구간을 규칙적으로 분할하면 격자법, 차원 증가로 격자 수가 급증하면 몬테카를로 방법을 적용함.

### 쉽게 이해하기 (학습용)
- 격자법은 과녁 전체를 촘촘한 그물로 나눠 일일이 계산해 변수(차원)가 늘면 그물코가 폭발적으로 늘어나지만, 몬테카를로는 무작위로 툭툭 쏘기만 하면 되니 차원이 늘어도 크게 느려지지 않음

## Ⅳ. 구성요소 및 구조

| 설계 요소 | 설명 |
|:---|:---|
| 확률 모델 | 원문제의 불확실성을 나타내는 확률변수와 표본의 확률분포 |
| 난수 생성기 | 확률 분포에 따른 표본 입력 생성 |
| 시뮬레이션 함수 | 표본을 결과값으로 변환하는 실행 모델 |
| 통계 집계기 | 근삿값과 오차 범위 산출 |
| 수렴 기준 | 목표 오차에 따른 반복 종료 조건 |

> 요약: 몬테카를로 방법은 확률 모델의 난수 표본을 반복 실행하고 통계량으로 근삿값과 오차 범위를 산출함.

### 쉽게 이해하기 (학습용)
- 주사위를 굴리는 규칙(확률 모델), 실제로 굴리는 도구(난수 생성기), 굴린 결과를 기록하는 장부(통계 집계기)가 각자 역할을 나눠 맡음

## Ⅴ. 원리 및 절차 흐름도

```text
확률 모델 정의 → 난수 표본 생성 → 시뮬레이션·통계 갱신
                                      ↓
                                목표 오차 판정
                         ├─ 미달 → 다음 표본 반복
                         └─ 충족 → 추정값·오차 범위 반환
```

1. **확률 모델링**: 확률변수의 분포와 결과 산출 기준을 정의함
2. **표본 생성**: 난수 생성기로 독립 표본을 반복 추출함
3. **반복 실험**: 각 표본을 시뮬레이션 함수에 넣고 결과를 저장함
4. **오차 평가**: 표준오차와 신뢰구간으로 목표 정밀도 도달 여부를 판단함

> 요약: 난수 표본의 통계량으로 근삿값을 구하고 표준오차와 신뢰구간으로 정밀도를 평가함.

### 쉽게 이해하기 (학습용)
- 규칙을 정하고, 주사위를 굴리고, 결과를 쌓아가다가, 오차가 충분히 작아지면 멈추는 반복 과정임

## Ⅵ. 실무 사례

1. 서버 용량 계획은 부하 분포를 반복 표본화하고 허용 용량 초과 확률로 증설 여부를 정함
2. 시스템 신뢰도 평가는 장애 시나리오를 반복 실행해 장애 확률과 신뢰구간을 구함

### 쉽게 이해하기 (학습용)
- 서버를 몇 대 준비할지 미리 알 수 없을 때, 여러 상황을 무작위로 수백 번 시뮬레이션해서 감당 못 할 확률을 미리 가늠하는 것과 같음

## Ⅶ. 결론

- 몬테카를로 방법은 무작위 실험을 반복해 답을 근사하며, 시행 횟수와 계산 비용 사이에서 필요한 정밀도를 정해야 함
- 수식으로 직접 풀기 어렵고 입력 분포를 모델링할 수 있을 때 적용하며, 드문 장애는 중요도 표본추출로 추정 오차를 줄여야 함

### 쉽게 이해하기 (학습용)
- 정확히 계산하기 힘든 문제는 많이 시도해 평균을 내는 방식으로 근사치를 구하되, 시도 횟수와 계산 비용의 균형을 스스로 정해야 함
