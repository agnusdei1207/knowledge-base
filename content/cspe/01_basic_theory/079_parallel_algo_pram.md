---
title: "병렬 알고리즘 — PRAM 모델 (Parallel Algorithm PRAM)"
date: "2026-07-09"
tags:
  - "cspe-basic-theory"
weight: 79
extra:
  question_no: "079"
  exam_status: "미출제"
  exam_history: ""
  exam_note: ""
---

## 미리 알고가기

- 병렬 알고리즘: 독립 작업을 여러 처리 장치에서 동시에 수행하도록 설계한 알고리즘임
- PRAM: 동기식 공유 메모리와 다수 프로세서를 가정한 병렬 계산 모델임
- Work: 모든 프로세서가 수행한 총 연산량임
- Span: 의존성 때문에 병렬화해도 줄지 않는 최장 경로 시간임
- EREW·CREW·CRCW: 공유 메모리의 동시 읽기·쓰기 허용 범위로 나눈 PRAM 유형임
- CRCW 충돌 규칙: Priority는 우선순위 프로세서의 쓰기, Common은 동일한 쓰기값, Arbitrary는 임의 프로세서의 쓰기를 반영함

## 작성 근거(검토용)

- 개요는 병렬 알고리즘과 PRAM을 함께 정의해 설계 대상과 분석 모델을 연결함
- 특징은 이상 모델, Work/Span, 접근 충돌, 실제 하드웨어 제약을 병렬 판단 기준으로 선정함
- Ⅲ은 동시 읽기·쓰기 허용 여부를 중심으로 PRAM 변종의 본질을 비교함
- 구조 도식은 리덕션의 단계별 병합과 Span 감소 관계를 보여줌
- 부하 불균형과 공유 메모리 동기화는 병렬 성능을 제한하는 주요 조건임

## Ⅰ. 개요

- **정의/개념**: 병렬 알고리즘은 작업을 동시 실행 단위로 나누며, PRAM은 공유 메모리 병렬 계산의 이론적 한계를 분석하는 모델임
- **배경/필요성**: 대규모 연산의 처리 시간을 줄이기 위해 병렬성·동기화·메모리 접근 비용의 설계 기준이 필요함

## Ⅱ. 특징

- PRAM은 공유 메모리 접근을 단위 시간으로 가정해 알고리즘 자체의 병렬성을 분석함
- Work와 Span을 분리해 총 비용과 이론적 최소 시간을 함께 평가함
- 동시 읽기·쓰기 허용 범위에 따라 표현 가능한 병렬 연산과 충돌 정책이 달라짐
- 실제 하드웨어에서는 캐시, 메모리 대역폭, 동기화 비용이 성능을 제한함

## Ⅲ. 종류 및 비교

| 판단 기준 | EREW | CREW | CRCW |
|:---|:---|:---|:---|
| 읽기·쓰기 허용 | 동시 읽기·쓰기 불가 | 동시 읽기 가능·동시 쓰기 불가 | 동시 읽기·쓰기 가능 |
| 충돌 처리 | 같은 메모리 셀의 동시 접근 배제 | 동시 읽기 허용·동시 쓰기 배제 | Priority·Common·Arbitrary로 동시 쓰기 결과 결정 |
| 대표 적용 사례 | 병렬 정렬·누적 합 분석 | 공유 테이블 병렬 조회 | 병렬 최솟값·빈도 집계 분석 |

> 요약: EREW는 동시 읽기·쓰기를 배제하고, CREW는 동시 읽기만 허용하며, CRCW는 동시 쓰기 결과를 정책으로 결정함.

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| 프로세서 집합 | 독립 작업을 동시에 실행하는 처리 단위임 |
| 공유 메모리 | 모든 프로세서가 접근하는 논리적 저장 공간임 |
| 동기 클록 | 단계별 병렬 연산의 진행 시점을 동기화함 |
| 충돌 정책 | 같은 주소 접근 시 허용 여부와 우선순위를 정함 |
| 리덕션 트리 | 합계·최댓값처럼 부분 결과를 단계적으로 합치는 구조임 |

```text
[ 병렬 리덕션 ]
단계 0: x1  x2  x3  x4
단계 1: x1+x2   x3+x4
단계 2: (x1+x2)+(x3+x4)
```

> 요약: 병렬 리덕션은 독립 부분 계산을 단계적으로 병합해 Span을 줄이는 구조임.

## Ⅵ. 실무 사례

1. 멀티코어 태스크 런타임은 워크 스틸링·동적 스케줄링을 적용하고 처리량·작업별 대기 시간으로 검증함
2. 공유 메모리 집계는 로컬 집계 후 병합하고 캐시 미스 횟수·동기화 대기 시간으로 검증함

## Ⅶ. 결론

- 병렬 알고리즘은 Work·Span·메모리 충돌을 기준으로 설계하고 캐시 미스·메모리 대역폭·동기화 대기로 이론적 속도 향상을 검증해야 함
