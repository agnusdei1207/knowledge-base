---
title: "메모리 계층 구조 (Memory Hierarchy)"
date: "2026-07-13T18:00:00+09:00"
tags:
  - "cspe-hardware"
weight: 27
extra:
  question_no: "027"
  exam_status: "기출"
  exam_history: "123회"
  exam_note: ""
---

## 미리 알고가기

- 메모리 계층은 속도·용량·비용이 다른 저장 자원을 층으로 배치한 구조임
- 상위 계층일수록 빠르지만 비싸고 작으며 하위 계층일수록 느리지만 크고 저렴함
- 지역성은 계층 구조가 평균 접근 시간을 낮출 수 있게 하는 전제임
- 캐시 블로킹(Cache Blocking): 데이터를 캐시 크기에 맞는 작은 블록으로 나눠 상위 계층 재사용을 늘리는 기법임

## 작성 근거(검토용)

- 메모리 계층은 장치 나열보다 적중·실패와 상하위 계층 이동을 중심으로 설명함
- 상위·하위 계층을 접근 지연, 용량·비트당 비용, 관리 전략으로 비교함
- 접근 빈도와 계층별 지연·용량을 데이터 배치 기준으로 정리함

## Ⅰ. 개요

- **정의/개념**: 메모리 계층 구조는 레지스터·캐시·주기억장치·보조저장장치를 속도·용량·비용 특성에 따라 계층화하고, 자주 쓰는 데이터를 상위 층에 유지해 평균 접근 시간을 최소화하는 시스템 설계 원리임
- **배경/필요성**: 속도·용량·비용 요구를 함께 만족하기 위해 서로 다른 저장 자원을 결합한 지역성 기반 계층 구조가 필요함

### 쉽게 이해하기 (학습용)
- 책상 위(레지스터), 책장(캐시), 서재(주기억장치), 창고(보조저장장치) 순으로 자주 쓰는 물건일수록 가까운 곳에 두는 것과 같음

## Ⅱ. 특징

- 상위 계층은 낮은 지연과 작은 용량을, 하위 계층은 높은 용량과 낮은 비용을 담당함
- 데이터 이동 정책과 지역성 활용 수준이 평균 접근 시간과 처리량을 좌우함
- 상위 계층에서 데이터가 없으면 하위 계층의 추가 접근 지연이 발생함
- 최근에는 HBM·CXL·SSD 계층이 추가되어 단일 서버 안에서도 계층이 복잡해짐

### 쉽게 이해하기 (학습용)
- 책상은 좁아도 바로 손이 가고, 창고는 넓지만 가지러 가는 데 오래 걸림
- 필요한 물건을 미리 옮겨두는 습관과 자주 쓰는 물건을 얼마나 잘 파악하는지가 전체 작업 속도를 좌우함
- 책상에 물건이 없으면 서재나 창고까지 가야 해서 그만큼 시간이 더 걸림

## Ⅲ. 종류 및 비교

| 판단 기준 | 상위 계층 | 하위 계층 |
|:---|:---|:---|
| 접근 지연 | 짧음 | 상위 계층보다 김 |
| 용량·비트당 비용 | 용량이 작고 비트당 비용이 높음 | 상위 계층보다 용량이 크고 비트당 비용이 낮음 |
| 관리 전략 | 라인 교체·사전 인출로 적중률을 조정함 | 배치·압축·계층 이동으로 접근량을 조정함 |

> 요약: 상위 계층은 지연을 줄이고, 하위 계층은 용량과 비용을 담당함.

### 쉽게 이해하기 (학습용)
- 책상 위는 좁지만 바로 손이 가고, 창고는 크고 저렴하지만 가지러 가는 시간이 오래 걸리는 것처럼 위·아래 계층이 역할을 나눠 맡음

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| 레지스터 | 현재 명령이 사용하는 피연산자와 중간 결과를 저장함 |
| 캐시 계층 | L1·L2·L3가 최근 사용한 데이터를 보관해 DRAM 접근 횟수를 줄임 |
| 주기억장치 | 실행 중인 작업 집합을 보관하고 캐시 실패 시 데이터를 공급함 |
| 보조·확장 메모리 | SSD·원격 메모리·CXL 풀이 주기억장치보다 큰 용량을 제공함 |

```text
+-----------+
| 레지스터  |
+-----------+
      |
      v
+-----------+
| L1 / L2   |
+-----------+
      |
      v
+-----------+
| L3 캐시    |
+-----------+
      |
      v
+-----------+
| DRAM      |
+-----------+
      |
      v
+-----------+
| SSD / CXL |
+-----------+
```

> 요약: 레지스터, 캐시, DRAM, SSD/CXL은 용량과 지연을 나눠 맡아 평균 접근 시간을 낮춤.

### 쉽게 이해하기 (학습용)
- 지금 쓰는 펜(레지스터), 책상 서랍(캐시), 서재(DRAM), 창고(보조·확장 메모리)가 순서대로 놓여 있어 가까운 곳부터 찾아보고 없으면 더 먼 곳으로 감

## Ⅴ. 원리 및 절차 흐름도

```text
+-------------+     +-------------+     +-------------+     +-------------+     +-------------+
| CPU 요청 발생   | --> | 상위 계층 조회   | --> | 실패 시 하위 계층 조회 | --> | 데이터 상위 적재   | --> | 다음 요청 처리   |
+-------------+     +-------------+     +-------------+     +-------------+     +-------------+
```

1. **CPU 요청 발생**: 명령이 필요한 데이터를 우선 상위 계층에서 찾음
2. **상위 계층 조회**: 레지스터와 캐시에서 적중 여부를 판단함
3. **실패 시 하위 계층 조회**: 없으면 DRAM이나 저장장치에서 데이터를 찾음
4. **데이터 상위 적재**: 찾은 데이터를 상위 계층에 적재함
5. **다음 요청 처리**: 재참조되면 적재된 상위 계층에서 데이터를 제공함

> 요약: 요청은 상위 계층부터 조회하고 없으면 하위 계층에서 가져와 상위 계층에 적재함.

### 쉽게 이해하기 (학습용)
- 필요한 물건을 먼저 책상 서랍에서 찾고 없으면 서재로, 그래도 없으면 창고까지 가서 찾아온 뒤 다음에 또 쓰기 좋게 책상 서랍에 옮겨둠

## Ⅵ. 실무 사례

1. 수치 계산 응용은 캐시 블로킹을 적용하고 캐시 실패율과 메모리 대기 사이클 비율을 확인함
2. CXL 확장 메모리 서버는 사용 빈도 기반 계층 배치를 적용하고 상위 계층 적중률과 종단 메모리 지연을 확인함

### 쉽게 이해하기 (학습용)
- 자주 쓰는 자료는 미리 작은 묶음으로 나눠 책상 서랍에 옮겨두고, 창고를 늘려 쓰는 곳은 자주 찾는 물건만 골라 가까운 자리로 옮겨둠

## Ⅶ. 결론

- 메모리 계층 구조는 속도·용량·비용이 다른 저장 자원을 층으로 배치하고 지역성을 활용해 평균 접근 시간을 낮추는 설계 원리임
- 데이터의 접근 빈도와 계층별 지연·용량을 기준으로 저장 위치를 정하고, 캐시 블로킹처럼 재사용을 늘리는 기법으로 상위 계층 적중률을 높여야 함

### 쉽게 이해하기 (학습용)
- 자주 쓰는 물건일수록 손 닿는 자리에 두고, 얼마나 자주 찾는지와 각 자리의 크기·거리를 함께 따져야 함
