---
title: "가상 메모리 페이징·세그멘테이션 (Virtual Memory Paging Segmentation)"
date: "2026-07-08"
tags:
  - "cspe-hardware"
weight: 22
extra:
  question_no: "022"
  exam_status: "기출"
  exam_history: "120회, 121회, 125회"
---

## 미리 알고가기

- 가상 메모리는 프로세스마다 독립적인 주소 공간을 제공하는 추상화임
- 페이징은 고정 크기 단위로, 세그멘테이션은 논리 단위의 가변 크기로 메모리를 관리함
- 페이지 폴트가 발생하면 운영체제가 보조기억장치에서 필요한 내용을 적재함

## Ⅰ. 개요

- **정의/개념**: 가상 메모리는 프로세스가 보는 주소 공간과 실제 물리 메모리를 분리해 보호·공유·확장을 가능하게 하는 구조이며, 구현 방식은 고정 크기 페이지를 쓰는 페이징과 논리 단위 세그먼트를 쓰는 세그멘테이션으로 나뉨
- **배경/필요성**: 한정된 물리 메모리 위에서 여러 프로그램을 안전하게 동시에 실행하려면 주소 충돌을 막고 필요한 데이터만 적재하는 관리 기법이 필요함

## Ⅱ. 특징

- 프로세스마다 독립 주소 공간을 제공해 메모리 보호와 격리를 쉽게 만듦
- 물리 메모리보다 큰 작업 집합도 필요한 시점에 나눠 적재해 실행할 수 있음
- 공유 라이브러리와 메모리 매핑 파일처럼 공통 데이터를 효율적으로 재사용할 수 있음
- 페이지 폴트와 교체 정책이 잘못 맞물리면 쓰레싱으로 전체 처리량이 급락할 수 있음

## Ⅲ. 종류 및 비교

| 판단 기준 | 페이징 | 세그멘테이션 |
|:---|:---|:---|
| 관리 단위 | 고정 크기 페이지와 프레임으로 관리함 | 코드·데이터·스택 같은 논리 단위 세그먼트로 관리함 |
| 장점 | 외부 단편화 없이 배치와 교체가 단순함 | 보호와 공유를 논리 경계에 맞춰 적용하기 쉬움 |
| 한계 | 내부 단편화와 페이지 테이블 오버헤드가 생김 | 외부 단편화와 compaction 부담이 생김 |
| 현대 활용 | 대부분의 범용 OS 기본 메커니즘임 | 제한된 보호 모델이나 특수 목적 구조로 보조 활용됨 |

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| Virtual Address Space | 프로세스별로 독립된 주소 범위를 제공해 같은 주소라도 서로 다른 물리 위치로 매핑되게 함 |
| Page or Segment Table | 각 가상 단위를 물리 위치와 연결하고 유효성 및 권한 정보를 함께 관리함 |
| MMU and TLB | 주소 변환을 하드웨어로 수행하며 최근 변환 결과를 캐시해 지연을 줄임 |
| Physical Memory and Swap Area | 자주 쓰는 데이터는 DRAM에 두고 부족한 영역은 보조기억장치로 넘겨 전체 주소 공간을 확장함 |

```text
+----------------------+     +----------------------+     +------------------+
| Process Virtual Space | --> | MMU / TLB            | --> | Physical Memory  |
+----------------------+     +----------------------+     +------------------+
                                      |
                                      v
                              +------------------+
                              | Disk Swap Area   |
                              +------------------+
```

## Ⅴ. 원리 및 절차 흐름도

```text
+-------------+     +-------------+     +-------------+     +-------------+     +-------------+
| 주소 접근        | --> | 변환 엔트리 조회   | --> | 메모리 존재 확인   | --> | 필요 시 page fault | --> | 실행 재개      |
+-------------+     +-------------+     +-------------+     +-------------+     +-------------+
```

1. **주소 접근**: CPU가 가상 주소로 메모리 참조를 요청함
2. **변환 엔트리 조회**: MMU가 페이지 또는 세그먼트 매핑 정보를 확인함
3. **메모리 존재 확인**: 유효 엔트리면 물리 주소를 만들고 없으면 예외를 발생시킴
4. **필요 시 page fault 처리**: 운영체제가 디스크에서 필요한 내용을 적재하고 엔트리를 갱신함
5. **실행 재개**: 동일 명령을 다시 수행해 정상 접근을 완료함

## Ⅵ. 문제점 및 해결 방안

1. 문제: 물리 메모리보다 큰 작업 집합을 무리하게 동시에 실행하면 page fault가 폭증해 쓰레싱이 발생함
   - 해결방안: working set과 admission control을 적용해 page fault frequency와 CPU utilization로 검증함
2. 문제: 페이지 테이블이 커질수록 주소 변환 메타데이터와 TLB 미스 비용이 함께 증가함
   - 해결방안: multi-level page table과 huge page를 조합하고 page table memory overhead와 TLB miss rate로 검증함
3. 문제: 세그멘테이션 기반의 가변 크기 배치는 외부 단편화와 재배치 비용을 키울 수 있음
   - 해결방안: paging 결합 구조나 compaction 정책을 적용하고 fragmentation ratio와 relocation time으로 검증함

## Ⅶ. 적용 사례

- 범용 서버 OS는 demand paging을 기반으로 필요한 시점에만 적재해 메모리 활용도를 높이고, page fault frequency와 swap in rate로 결과를 확인함
- 데이터베이스 서버는 huge page와 메모리 고정을 조합해 변환 비용을 줄이고, TLB miss rate와 query latency로 결과를 확인함
- 가상화 플랫폼은 nested paging을 활용해 guest 격리를 유지하면서 성능을 확보하고, VM memory overhead와 VM exit latency로 결과를 확인함

## Ⅷ. 결론

가상 메모리의 본질은 큰 주소 공간을 보여주는 데 있지 않고 보호와 공유와 적재 시점을 분리해 시스템 전체 효율을 높이는 데 있으므로, 설계 핵심은 page fault 비용과 메타데이터 오버헤드를 동시에 다루는 데 있음.
