---
title: "하이퍼스레딩·SMT (Simultaneous Multithreading)"
date: "2026-07-13T16:00:00+09:00"
tags:
  - "cspe-hardware"
weight: 16
extra:
  question_no: "016"
  exam_status: "미출제"
  exam_history: ""
  exam_note: "전망"
---

## 미리 알고가기

- SMT는 하나의 물리 코어에서 여러 스레드를 동시에 실행하는 기술임
- 하이퍼스레딩은 Intel의 SMT 구현 브랜드임
- 실행 유닛과 캐시는 공유하고 PC·레지스터 상태는 스레드별로 유지함
- PC(Program Counter): 다음에 실행할 명령어의 주소를 가리키는 레지스터임
- LLC(Last Level Cache): 여러 코어가 함께 쓰는 최상위 캐시임
- 논리 프로세서: 운영체제가 하나의 물리 코어를 SMT로 둘 이상의 실행 단위로 인식하는 단위임
- p99 응답시간: 전체 요청 중 99%가 이 시간 안에 처리됨을 나타내는 지연 지표임

## 작성 근거(검토용)

- SMT를 물리 코어 추가가 아니라 스레드별 상태 복제와 실행 자원 공유라는 구조로 설명함
- 멀티코어와 SMT를 물리 자원·처리량 조건·자원 간섭·격리로 비교함
- 처리량 증가와 공유 자원 간섭·격리 조건을 SMT 사용 판단으로 정리함

## Ⅰ. 개요

- **정의/개념**: SMT는 하나의 물리 코어 내부에서 여러 스레드의 아키텍처 상태를 유지하고, 각 스레드의 명령어를 동시에 발행해 공유 실행 자원의 유휴 슬롯을 줄이는 마이크로아키텍처 기법임
- **배경/필요성**: 메모리·분기 대기 중인 실행 유닛의 활용률을 높이기 위해 여러 스레드를 동시에 발행하는 구조가 필요함

### 쉽게 이해하기 (학습용)
- 한 사람(코어)이 신분증 두 개(스레드 상태)를 들고 번갈아 일을 처리해 쉬는 시간을 줄이는 것과 같음
- 사람 자체를 늘리지 않고 그 사람이 노는 시간을 다른 일로 채우는 방식임

## Ⅱ. 특징

- 유휴 실행 슬롯을 다른 스레드로 채워 처리량을 높임
- 코어 전체가 아니라 스레드별 PC·레지스터 상태를 추가하고 실행 유닛과 캐시는 공유함
- L1 캐시와 실행 유닛을 공유하므로 간섭이 발생하기 쉬움
- 한 스레드의 대기 슬롯을 다른 스레드가 사용하므로 단일 스레드 지연보다 코어 전체 처리량을 대상으로 함

### 쉽게 이해하기 (학습용)
- 손이 쉬는 순간(유휴 슬롯)에 다른 스레드의 일감을 끼워 넣어 손을 놀리지 않음
- 신분증(PC·레지스터)만 두 벌 준비하고 작업대(실행 유닛)와 공구함(캐시)은 같이 씀
- 같은 작업대를 같이 쓰다 보니 서로 부딪히는 순간(간섭)이 생김

## Ⅲ. 종류 및 비교

| 판단 기준 | 멀티코어 | SMT |
|:---|:---|:---|
| 물리 자원 | 코어와 상태를 완전히 분리 | 실행 유닛·캐시는 공유하고 스레드별 PC·레지스터만 분리 |
| 처리량 조건 | 코어별 독립 스레드가 필요 | 한 스레드의 실행 슬롯이 비는 구간이 필요 |
| 자원 간섭·격리 | 공유 LLC·메모리에서 간섭이 발생하며 격리 단위는 물리 코어임 | 실행 유닛·L1 캐시부터 간섭이 발생하며 격리 단위는 논리 프로세서임 |

> 요약: SMT는 코어를 추가하지 않고 공유 실행 자원의 빈 슬롯을 다른 스레드로 채움.

### 쉽게 이해하기 (학습용)
- 멀티코어는 사람마다 작업대를 따로 주는 것이고, SMT는 한 작업대를 두 사람이 번갈아 쓰는 것임
- 작업대를 나눠 쓰는 SMT는 한 사람이 잠깐 멈춘 틈에 다른 사람이 바로 그 자리를 채워야 이득이 생김

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| 스레드 문맥 | 각 스레드의 PC와 레지스터 상태를 별도로 보관함 |
| 공유 실행 유닛 | ALU·FPU·로드스토어 유닛을 여러 스레드가 함께 사용함 |
| 공유 캐시 | L1·L2 캐시 경쟁이 SMT 간섭과 처리량 변동을 발생시킴 |
| 스케줄러 | 준비된 스레드 중 어떤 명령을 먼저 발행할지 결정함 |

```text
+------------------+   +------------------+
| 스레드 문맥 1      |   | 스레드 문맥 2      |
+------------------+   +------------------+
          \                    /
           \                  /
        +----------------------+
        | 스케줄러              |
        +----------------------+
                  |
                  v
        +----------------------+
        | 공유 실행 유닛·캐시     |
        +----------------------+
```

> 요약: SMT는 스레드별 상태를 분리하되 실행 유닛과 캐시는 공유해 처리량을 높임.

### 쉽게 이해하기 (학습용)
- 각 스레드는 자기 이름표(문맥)만 따로 갖고, 작업 도구(실행 유닛)와 창고(캐시)는 같이 씀
- 누가 먼저 도구를 쓸지는 스케줄러가 순서를 정해줌

## Ⅴ. 원리 및 절차 흐름도

```text
+-------------+     +-------------+     +-------------+     +-------------+
| 스레드 상태 유지  | --> | 발행 후보 선택    | --> | 공유 자원 실행    | --> | 결과 반영      |
+-------------+     +-------------+     +-------------+     +-------------+
```

1. **스레드 상태 유지**: 논리 프로세서별 상태를 저장함
2. **발행 후보 선택**: 준비된 스레드 명령 중 우선순위를 정함
3. **공유 자원 실행**: 실행 유닛과 캐시를 함께 사용함
4. **결과 반영**: 각 스레드 상태에 결과를 반영함

> 요약: SMT는 준비된 스레드 명령을 선택해 공유 자원에서 실행하고 결과를 각 스레드 상태에 반영함.

### 쉽게 이해하기 (학습용)
- 이름표를 챙긴 스레드 중 준비된 사람을 골라 공용 작업대에 세우고, 끝나면 그 사람 이름표에 결과를 적어 넣는 과정임

## Ⅵ. 실무 사례

1. 서버 운영체제는 같은 코어의 실행 유닛·캐시 경합을 줄이기 위해 SMT 인지 스케줄링을 적용하고, 자원 경합률과 처리량 증가율로 검증함
2. 지연시간 민감 서비스는 논리 프로세서 간 간섭을 줄이기 위해 SMT를 비활성화하고, p99 응답시간과 처리량으로 검증함

### 쉽게 이해하기 (학습용)
- 여러 요청을 동시에 처리하는 서버는 작업대 경합을 계산해 SMT를 켜 처리량을 늘림
- 응답 속도가 생명인 서비스는 옆 사람과 부딪히는 걸 막기 위해 SMT를 꺼서 혼자 쓰게 함

## Ⅶ. 결론

- SMT는 유휴 실행 슬롯을 다른 스레드로 채워 처리량을 높이지만, 실행 유닛·캐시 공유로 인한 간섭 위험을 동반함
- 처리량 증가율과 p99 지연·격리 위험을 함께 측정해, 지연 민감 서비스는 SMT를 비활성화하고 처리량 중심 서버는 SMT 인지 스케줄링으로 전환해야 함

### 쉽게 이해하기 (학습용)
- 유휴 슬롯을 줄여 얻는 처리량과 이웃 스레드와 부딪혀 생기는 지연을 저울질해 SMT 사용 여부를 정해야 함
