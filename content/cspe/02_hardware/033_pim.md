---
title: "PIM 메모리 내 처리 (Processing-in-Memory)"
date: "2026-07-08"
tags:
  - "cspe-hardware"
weight: 33
extra:
  question_no: "033"
  exam_status: "기출"
  exam_history: "129회, 131회"
---

## 미리 알고가기

- PIM은 메모리 내부에 연산 기능을 넣어 데이터 이동을 줄이려는 구조임
- 핵심 목표는 연산보다 데이터 이동 비용이 큰 메모리 병목 작업을 줄이는 것임
- 범용 CPU 대체가 아니라 반복적이고 병렬적인 연산을 메모리로 이관하는 접근임

## Ⅰ. 개요

- **정의/개념**: PIM은 DRAM이나 HBM 같은 메모리 내부에 경량 연산 로직을 통합해 데이터를 외부 프로세서로 대량 이동시키지 않고 저장 위치 근처에서 직접 처리하는 메모리 중심 컴퓨팅 구조임
- **배경/필요성**: AI·빅데이터의 데이터 이동 에너지와 지연을 줄이기 위해 저장 위치 근처의 연산 기능이 필요함

## Ⅱ. 특징

- 데이터 이동량을 줄여 에너지 효율과 실효 처리량을 동시에 개선할 수 있음
- 메모리 뱅크 병렬성을 그대로 활용해 단순 반복 연산을 대량으로 분산 처리하기 쉬움
- 복잡한 제어 흐름보다는 MAC, reduce, filter 같은 규칙적 연산에서 효과가 큼
- 캐시 일관성·프로그래밍 모델·메모리 공정 제약이 상용화 속도를 좌우함

## Ⅲ. 종류 및 비교

| 판단 기준 | CPU/GPU 중심 처리 | PIM |
|:---|:---|:---|
| 연산 위치 | 외부 프로세서 코어에서 수행함 | 메모리 내부 로직에서 수행함 |
| 데이터 이동 | 메모리와 프로세서 사이 왕복이 큼 | 내부 처리로 왕복을 줄임 |
| 처리 특성 | 범용 명령과 분기 제어를 수행함 | 메모리 외부 전송 바이트와 연산당 에너지를 줄임 |
| 한계 | 메모리 장벽과 데이터 이동 전력 부담이 큼 | 범용성과 프로그래밍 모델 성숙도가 낮음 |

> 요약: CPU/GPU는 범용성이 높고, PIM은 데이터 이동이 큰 반복 연산에서 이득이 큼.

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| 메모리 배열 | 원본 데이터를 저장하며 대용량 병렬 접근의 기반이 됨 |
| 메모리 내 연산 유닛 | 메모리 내부의 경량 연산 로직으로 벡터 연산이나 누산 같은 반복 작업을 수행함 |
| 명령·작업 이관 인터페이스 | 호스트가 처리할 데이터와 연산을 지시해 CPU와 PIM의 역할을 분리함 |
| 실행환경·데이터 배치 정책 | 이관 대상과 데이터 위치를 조정해 내부 병렬성을 실제 성능으로 연결함 |

```text
+-------------+     +----------------------+     +------------------+
| Host CPU/GPU | --> | PIM Command Interface| --> | In-Memory Compute|
+-------------+     +----------------------+     +------------------+
                                               |
                                               v
                                       +------------------+
                                       | Memory Array     |
                                       +------------------+
```

> 요약: PIM은 메모리 배열 가까이에 연산 로직과 오프로딩 인터페이스를 두어 이동량을 줄임.

## Ⅴ. 원리 및 절차 흐름도

```text
+-------------+     +-------------+     +-------------+     +-------------+     +-------------+
| 작업 후보 식별   | --> | PIM 오프로딩 결정 | --> | 메모리 내부 병렬 처리 | --> | 결과 축약 반환   | --> | 후속 연산 연계   |
+-------------+     +-------------+     +-------------+     +-------------+     +-------------+
```

1. **작업 후보 식별**: 반복적이고 데이터 이동 비중이 큰 연산을 선택함
2. **PIM 오프로딩 결정**: 호스트가 명령과 대상 데이터를 메모리 쪽에 넘김
3. **메모리 내부 병렬 처리**: 뱅크 단위 연산 로직이 데이터를 메모리 내부에서 처리함
4. **결과 축약 반환**: 전체 데이터를 보내는 대신 필요한 결과만 호스트로 돌려줌
5. **후속 연산 연계**: CPU나 GPU가 제어 중심 작업과 후처리를 계속 수행함

> 요약: PIM은 데이터 이동 비중이 큰 작업만 선별해 메모리 내부에서 처리하고 축약 결과를 반환함.

## Ⅵ. 실무 적용 및 유의점

1. DRAM 공정은 로직에 최적화되지 않으므로 경량 연산 집합을 적용하고 supported offload ratio, area overhead로 확인함
2. CPU 캐시와 PIM이 같은 데이터를 따로 다루면 일관성 비용이 커지므로 비캐시 영역이나 일관성 프로토콜을 적용하고 coherence violation count, offload latency로 확인함
3. 소프트웨어가 PIM 작업 이관을 지원하지 않으면 이동 절감 효과가 사라지므로 컴파일러와 실행환경을 보완하고 offload hit ratio, energy per operation으로 확인함

## Ⅶ. 결론

PIM은 연산 속도보다 불필요한 데이터 이동을 줄여 시스템 전체 에너지와 지연 구조를 바꾸는 기술임.

## 작성 근거(검토용)

- PIM은 CPU 대체가 아니라 메모리 병목 반복 작업의 데이터 이동 감소 구조로 설명함
- 모호한 표현은 supported offload ratio, coherence violation count, energy per operation으로 구체화함
- 결론은 연산 속도보다 데이터 이동 감소와 에너지·지연 구조 변화로 정리함
