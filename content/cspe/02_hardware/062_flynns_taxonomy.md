---
title: "병렬 컴퓨터 분류 — Flynn 분류 (Flynn's Taxonomy)"
date: "2026-07-06"
tags:
  - "cspe-hardware"
weight: 62
---

# 병렬 컴퓨터 분류 - Flynn 분류 (Flynn's Taxonomy)

## 미리 알고가기

- Instruction Stream: 제어 장치가 실행하는 명령 흐름의 수를 의미함
- Data Stream: 명령이 적용되는 데이터 흐름의 수를 의미함
- SISD/SIMD/MISD/MIMD: 단일 명령 단일 데이터(Single Instruction Single Data, SISD), 단일 명령 다중 데이터(Single Instruction Multiple Data, SIMD), 다중 명령 단일 데이터(Multiple Instruction Single Data, MISD), 다중 명령 다중 데이터(Multiple Instruction Multiple Data, MIMD)는 명령 흐름과 데이터 흐름의 단일·다중 여부로 나눈 4분류임
- 병렬성: 여러 연산을 동시에 수행해 처리량이나 응답시간을 개선하는 특성임

## Ⅰ. 개요

- **정의/개념**: Flynn 분류는 컴퓨터 아키텍처가 제공하는 병렬성을 명령어 스트림 수와 데이터 스트림 수라는 두 기준으로 SISD, SIMD, MISD, MIMD로 나누어 워크로드 적합성을 판단하는 병렬 컴퓨터 분류 체계임.
- **배경/필요성**: 병렬 컴퓨터는 중앙처리장치(Central Processing Unit, CPU), 그래픽 처리 장치(Graphics Processing Unit, GPU), 벡터 프로세서, 클러스터처럼 구조가 다양해 단순 성능 수치만으로 차이를 설명하기 어려움. Flynn 분류는 명령과 데이터 흐름이라는 공통 축으로 아키텍처 차이를 빠르게 정리하게 함.
- **비유**: 한 지휘자와 한 연주자, 한 지휘자와 여러 연주자, 여러 지휘자와 여러 연주자로 공연 형태를 나누는 것과 같음.

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| 병렬 컴퓨터의 기본 분류와 적용 예 설명 | SISD, SIMD, MISD, MIMD, 명령/데이터 흐름 | 용어 암기만 제시 |

> 요약: Flynn 분류는 명령 흐름과 데이터 흐름의 수로 병렬 컴퓨터 구조를 구분하는 기준임.

## Ⅱ. 특징 및 비교

| 판단 기준 | SIMD | MIMD |
|:---|:---|:---|
| 명령 흐름 | 하나의 명령을 여러 데이터에 동시에 적용함 | 여러 프로세서가 서로 다른 명령을 독립 실행함 |
| 적용 워크로드 | 벡터, 행렬, 이미지, GPU 커널처럼 규칙적 데이터 병렬성에 적합함 | 멀티코어, 클러스터, 서버처럼 작업 병렬성과 비동기 처리가 적합함 |
| 주요 한계 | 분기와 불규칙 데이터에서 효율이 낮아짐 | 동기화, 통신, 부하분산 비용이 커질 수 있음 |

> 요약: Flynn 분류는 병렬성을 데이터 병렬 중심으로 볼지 작업 병렬 중심으로 볼지 판단하게 함.

## Ⅲ. 구성요소/구조

```text
+----------------+        +----------------+
| Instruction(s) | -----> | Processing Elem|
+----------------+        +----------------+
                                |
                                v
                         +--------------+
                         | Data Stream  |
                         +--------------+
```

| 구성요소 | 설명 | 비유 |
|:---|:---|:---|
| 명령 스트림 | 제어 장치가 실행하는 명령 흐름의 개수를 나타냄 | 지휘자의 지시 |
| 데이터 스트림 | 명령이 적용되는 데이터 집합의 개수를 나타냄 | 연주자별 악보 |
| 처리 요소 | 명령과 데이터를 받아 실제 연산을 수행하는 프로세서 또는 코어임 | 연주자 |
| 분류 매트릭스 | SISD, SIMD, MISD, MIMD 네 영역으로 구조를 구분함 | 좌표 지도 |

> 요약: Flynn 분류는 명령, 데이터, 처리 요소의 관계를 2차원 매트릭스로 구조화함.

### 원리/흐름도

```text
+----------+     +----------+     +----------+     +----------+
| Inspect  | --> | Count    | --> | Classify | --> | Assess   |
+----------+     +----------+     +----------+     +----------+
```

1. **구조 확인**: 대상 시스템의 제어 장치, 처리 요소, 데이터 배치 방식을 확인함
2. **흐름 계수**: 동시에 존재하는 명령 스트림과 데이터 스트림이 단일인지 다중인지 판단함
3. **유형 분류**: SISD, SIMD, MISD, MIMD 중 가장 적합한 범주로 분류함
4. **적용 판단**: 워크로드의 데이터 병렬성, 작업 병렬성, 통신 비용을 기준으로 적합성을 평가함

> 요약: Flynn 분류 적용은 구조의 명령·데이터 흐름을 세고 워크로드 적합성으로 해석하는 과정임.

## Ⅳ. 문제점 및 개선방안

- **P1 현대 하이브리드 표현 한계**: GPU 클러스터처럼 SIMD와 MIMD가 결합된 구조는 단일 범주로 설명하기 어려움
- **P1 대응**: CPU-GPU, 노드-클러스터처럼 계층별로 SIMD/MIMD를 병기함 (확인: 계층별 분류 명확성)
- **P2 MISD 모호성**: 실무 사례가 적고 해석이 다양해 시험 답안에서 구체 예시 제시가 어려움
- **P2 대응**: MISD는 중복 실행이나 파이프라인형 특수 사례로 한정해 설명하고 핵심 비교는 SIMD/MIMD에 둠 (확인: 예시의 구조 일치)
- **P3 메모리·인터커넥트 누락**: 분류가 명령과 데이터 흐름에 집중해 캐시, 비균일 메모리 접근(Non-Uniform Memory Access, NUMA), 네트워크 병목을 설명하지 못함
- **P3 대응**: 메모리 계층, 공유/분산 메모리, 인터커넥트 토폴로지를 보조 축으로 추가함 (확인: 병목 설명 가능성)

> 요약: Flynn 분류는 단독 결론이 아니라 계층, 메모리, 인터커넥트 관점과 함께 사용할 때 실무성이 높아짐.

## Ⅴ. 실무 적용 사례

| 적용 영역 | 적용 방식 | 확인 지표 |
|:---|:---|:---|
| CPU 벡터화 분석 | CPU 코어 내부는 SIMD, 멀티코어 실행은 MIMD로 계층 구분함 | 벡터 lane 활용률, thread imbalance |
| GPU 가속 워크로드 | 워프 단위 실행은 SIMD/SIMT 관점, 다중 커널·노드 실행은 MIMD 관점으로 분리함 | branch divergence, memory bandwidth |
| 클러스터 설계 | 노드 내부 병렬성과 노드 간 메시지 전달을 별도 분류해 병목을 식별함 | 통신 지연, 동기화 대기시간, speedup |

> 요약: 실무에서는 한 장비를 하나의 범주로 고정하지 않고 계층별 병렬 모델과 성능 지표를 함께 해석함.

## Ⅵ. 결론

- **발전 방향**: Flynn 분류는 CPU SIMD, GPU 단일 명령 다중 스레드(Single Instruction Multiple Threads, SIMT), MIMD cluster, 인공지능(Artificial Intelligence, AI) accelerator가 혼합된 현대 병렬 구조를 설명하는 상위 분류 틀로 계속 활용됨
- **기술사적 판단**: 명령 흐름, 데이터 흐름, 메모리 공유 방식, 통신 비용을 분리해 워크로드를 어느 병렬 모델에 배치할지 결정함; speedup, lane utilization, synchronization overhead, memory bandwidth, communication latency를 측정해 분류와 실제 성능의 차이를 확인함
- **기술사 제언**: Flynn 분류는 암기 표가 아니라 병렬성 선택의 출발점이며, 현대 시스템에서는 메모리와 인터커넥트 병목까지 연결해야 함
