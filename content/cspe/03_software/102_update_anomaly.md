---
title: "이상 현상 — 삽입·삭제·갱신 (Update Anomaly)"
date: "2026-07-08"
tags:
  - "cspe-software"
weight: 102
extra:
  question_no: "102"
  exam_status: "미출제"
  exam_note: "기본"
---

## 미리 알고가기

- 이상 현상은 테이블 구조가 잘못돼 데이터를 넣고 지우고 바꾸는 과정에서 예상하지 않은 문제가 생기는 현상임
- 삽입 이상은 넣어야 할 사실을 넣지 못하는 문제이고, 삭제 이상은 한 행을 지우다가 다른 사실까지 함께 잃는 문제임
- 갱신 이상은 같은 사실을 여러 행에서 반복 수정해야 해 일부만 바뀌는 문제임

## Ⅰ. 개요

- **정의/개념**: 삽입·삭제·갱신 이상은 한 테이블에 여러 의미의 데이터를 함께 담아 두었을 때 새 사실을 넣기 어렵거나, 한 행 삭제로 다른 사실까지 사라지거나, 같은 사실을 여러 곳에서 반복 수정해야 하는 구조적 데이터 조작 문제임.
- **배경/필요성**: 하나의 사실을 여러 행에 저장하거나 한 행에 여러 개체의 속성을 결합하면 삽입·삭제·갱신 시 정보 손실과 불일치가 발생하므로 함수 종속성을 기준으로 분해해야 함.

## Ⅱ. 특징

- 삽입 이상은 필수와 선택 속성이 한 행에 묶여 있어 일부 정보만 먼저 저장하지 못하게 만듦
- 삭제 이상은 한 행이 여러 사실을 동시에 담고 있어 하나를 지우다 다른 사실까지 잃게 만듦
- 갱신 이상은 같은 사실이 여러 행에 퍼져 있어 일부만 수정되면 불일치가 남음
- 세 이상 현상 모두 데이터 중복과 잘못된 속성 결합에서 시작됨

## Ⅲ. 종류 및 비교

| 판단 기준 | 삽입 이상 | 삭제 이상 | 갱신 이상 |
|:---|:---|:---|:---|
| 대표 원인 | 서로 다른 개체 정보를 한 행에 함께 둠 | 한 행이 두 가지 이상 사실을 동시에 표현함 | 같은 사실을 여러 행에 반복 저장함 |
| 나타나는 문제 | 일부 값이 없어도 저장해야 할 사실을 넣지 못함 | 한 행 삭제가 다른 사실의 소실로 이어짐 | 같은 값을 여러 군데 고쳐야 해 일부만 바뀔 수 있음 |
| 예방 방향 | 개체를 분리하고 선택 속성을 재배치함 | 관계를 분리해 행 하나가 한 사실만 갖게 함 | 중복 속성을 제거하고 키를 다시 설계함 |
| 확인 신호 | 다른 개체 값 없이는 새 사실 저장 불가 | 행 삭제 시 별도 개체 정보도 소실 | 같은 사실을 가진 여러 행을 함께 수정 |

## Ⅳ. 구성요소 및 구조

| 구성요소 | 설명 |
|:---|:---|
| 혼합 개체 테이블 | 서로 다른 개체의 속성이 한 테이블에 섞여 이상 현상을 유발함 |
| 중복 속성 | 같은 사실을 여러 행에 저장해 갱신 부담을 키움 |
| 종속성 위반 | 함수 종속성과 키 구조가 어긋난 지점이 구조 문제의 근거가 됨 |
| 재설계 방향 | 개체와 관계를 분리해 각 테이블이 한 의미만 갖도록 다시 나눔 |

## Ⅴ. 원리 및 절차 흐름도

```text
+-------------+     +-------------+     +-------------+     +-------------+
| 현재 스키마 분석 | --> | 이상 유형 식별  | --> | 종속성 재정의   | --> | 테이블 재구성   |
+-------------+     +-------------+     +-------------+     +-------------+
```

1. **현재 스키마 분석**: 속성과 키와 중복 구조를 점검함
2. **이상 유형 식별**: 삽입과 삭제와 갱신 중 어떤 문제가 나타나는지 구분함
3. **종속성 재정의**: 어떤 속성이 어떤 개체에 속하는지 다시 정리함
4. **테이블 재구성**: 정규화 기준으로 구조를 분리함

## Ⅵ. 실무 적용 및 유의점

1. 레거시 통합 테이블에서 같은 값 수정이 반복되거나 삭제 시 정보가 함께 사라진다면 임시 쿼리보다 스키마 재설계를 우선하고 이상 현상 재발 건수와 중복 속성 개수로 확인함.
2. 정규화로 구조를 나눈 뒤에는 애플리케이션 매핑과 입력 경로도 함께 고쳐야 하므로 계약 테스트를 수행하고 매핑 오류 건수와 배포 후 결함 건수로 확인함.

## Ⅶ. 결론

삽입·삭제·갱신 이상은 쿼리 문제가 아니라 한 테이블에 여러 사실을 섞어 둔 구조 문제이므로 정규화와 재설계로 풀어야 함.
