---
title: "교착상태 조건·예방·회피·탐지·복구 (Deadlock)"
date: "2026-07-02"
tags:
  - "cspe-software"
weight: 9
---

# 📖 【암기용】 개념 완전 이해

> 목적: 이 개념을 처음 보는 사람도 완벽히 이해하게 만든다. 시험 답안 양식이 아니라, 이해를 위한 친절한 설명이다.

## 한눈에
- **개요**: **교착상태(Deadlock)**는 다중 프로세스의 **동시성 제어(Concurrency Control)**에서, 둘 이상의 프로세스가 서로 상대가 점유한 자원을 요청한 채 영구적으로 대기하여 그룹 전체가 멈추는 현상이다.
- **왜 필요한가**: 멀티스레드 프로그램은 공유 자원을 보호하려고 뮤텍스 같은 락(Lock)을 건다. 락을 거는 순서가 서로 엇갈리면 A는 B가 쥔 락을, B는 A가 쥔 락을 영원히 기다리게 되므로, 이를 언제 어떻게 막을지가 OS 설계의 핵심 과제다.
- **핵심 직관**: 외나무다리에서 마주친 두 자동차. 다리는 한 번에 한 대만 지나가고(상호 배제), 이미 다리를 반쯤 차지한 채(점유와 대기) 뒤로 물러나길 거부하며(비선점), 서로 상대가 비키기만 기다리면(환형 대기) 둘 다 영원히 멈춘다.

## 핵심 용어 정리 (내부에 등장하는 것들)

| 용어 | 의미 | 비유 |
|:---|:---|:---|
| 동시성 제어 | 여러 프로세스·스레드가 공유 자원에 동시 접근할 때 순서와 배타성을 조율하는 OS의 상위 과제 — 교착상태는 이 조율이 실패한 결과다 | 여러 명이 한 부엌을 같이 쓸 때의 교통정리 |
| 자원 할당 그래프 | 프로세스→자원(요청), 자원→프로세스(점유) 관계를 화살표로 그린 그래프. 단일 인스턴스 자원에서 화살표가 원형으로 돌면(사이클) 교착상태 발생을 의미 | 누가 누구 열쇠를 기다리는지 그린 지도 |
| 상호 배제 (Mutual Exclusion) | 자원을 한 번에 한 프로세스만 쓸 수 있음 | 화장실 칸은 한 번에 한 명 |
| 점유와 대기 (Hold and Wait) | 자원을 이미 쥔 채로 다른 자원을 추가로 요청 | 왼손에 포크를 쥔 채 오른쪽 포크를 더 달라고 함 |
| 비선점 (No Preemption) | 다른 프로세스가 쥔 자원을 강제로 뺏을 수 없음 | 남의 손에 든 포크를 강탈할 수 없음 |
| 환형 대기 (Circular Wait) | 프로세스들이 A→B→C→A 형태로 서로 자원을 기다리는 순환 고리를 형성 | 세 명이 서로 옆 사람 포크를 기다리는 원탁 |
| 코프만 조건 (Coffman Conditions) | 위 4가지 조건의 통칭 — 이 4개가 **동시에 모두** 성립해야 교착상태가 발생한다 | 자물쇠 4개가 다 잠겨야만 문이 안 열림 |
| 예방 (Prevention) | 4대 조건 중 하나를 원천적으로 성립 불가능하게 설계 | 애초에 1인당 포크 2개씩 지급 |
| 회피 (Avoidance) | 자원 요청을 승인하기 전에 미래 상태가 안전한지 계산해서, 위험하면 거절 | 은행원이 대출 전에 파산 가능성 계산 |
| 탐지 (Detection) | 일단 자원을 자유롭게 내주고, 주기적으로 자원 할당 그래프에 사이클이 생겼는지 검사 | 정기 재고 조사로 도난 적발 |
| 복구 (Recovery) | 탐지된 사이클을 끊기 위해 프로세스 하나(희생양, Victim)를 강제 종료 | 얽힌 매듭을 끊기 위해 실 하나를 자름 |
| 타조 알고리즘 (Ostrich Algorithm) | 교착상태 감시 비용이 더 크다고 보고 아예 대응하지 않는 정책 | 위험을 못 본 척 모래에 머리를 박는 타조 |

## 깊이 이해

### 왜 4가지 조건이 모두 필요한가
- 교착상태는 4개 조건의 논리곱(AND)이다. 하나라도 깨지면 순환 대기 자체가 성립하지 않는다. 예를 들어 상호 배제가 없다면(자원을 여럿이 동시에 쓸 수 있다면) 애초에 "기다릴 이유"가 없다.
- 실무에서는 상호 배제(자원의 물리적 특성상 불가피)와 비선점(강제 회수 시 데이터 오염 위험)을 깨기 어렵다. 그래서 **환형 대기**를 깨는 방법 — 모든 프로세스가 자원을 항상 정해진 순서(예: 자원 ID 오름차순)로만 요청하게 강제 — 가 실무에서 가장 널리 쓰이는 예방책이다.

### 자원 할당 그래프로 판별하기
- P1이 R1을 점유하고 R2를 요청, P2가 R2를 점유하고 R1을 요청하는 상황을 그리면 P1→R2→P2→R1→P1의 사이클이 만들어진다. 자원 종류마다 인스턴스가 1개뿐이면 이 사이클의 존재 자체가 곧 교착상태다.
- 자원 인스턴스가 여러 개(예: 같은 종류의 프린터 3대)라면 사이클이 있어도 다른 인스턴스로 요청이 풀릴 수 있어, 사이클 유무만으로 판별할 수 없고 별도의 탐지 알고리즘이 필요하다.

### 예방·회피·탐지·복구, 무엇을 언제 쓰나
- **예방**: 구현은 단순하지만 자원을 보수적으로 잠가야 해서 활용률이 떨어진다.
- **회피**: 매 요청마다 시뮬레이션 비용이 들고, 프로세스가 자신의 최대 자원 요구량을 미리 신고해야 한다는 비현실적 전제가 있다(010 은행원 알고리즘에서 상세).
- **탐지+복구**: 평소엔 자원을 자유롭게 내주다가 주기적으로 그래프를 검사해 사이클을 찾으면, 우선순위가 낮거나 되돌리기 쉬운 프로세스 하나를 강제 종료(Victim)해 사이클을 끊는다. 종료 대상 선정이 한쪽으로 편향되면 같은 프로세스만 계속 죽어 기아(Starvation)로 이어질 수 있다.

### 현실의 OS는 무엇을 쓰나
- 리눅스·윈도우 같은 범용 OS는 위 4가지 어디에도 속하지 않는 **타조 알고리즘**을 쓴다. 교착상태는 수개월에서 수년에 한 번 날까 말까 한데, 이를 막으려고 매 자원 요청마다 그래프를 검사하면 상시 비용이 훨씬 크다. 그래서 "터지면 사용자가 프로세스를 강제 종료한다"는 방침이 비용 대비 효과 면에서 합리적이다.
- 반면 DBMS(Oracle, MySQL 등)는 트랜잭션 실패의 손실이 크므로, 락 대기 시간에 타임아웃(예: 잠금 대기 50초)을 걸고 초과 시 트랜잭션 하나를 자동으로 롤백하는 탐지+복구 방식을 실제로 쓴다.

## 연결 개념
- **뮤텍스(Mutex)**: 상호 배제 조건을 구현하는 실제 도구. 뮤텍스 2개 이상을 서로 다른 순서로 중첩해서 잠그면 환형 대기가 만들어져 교착상태로 이어진다(012에서 상세).
- **은행원 알고리즘(Banker's Algorithm)**: 회피 전략의 대표 구현체. 자원 요청을 승인하기 전에 미래 상태가 안전한지 계산한다(010에서 상세).
- **기아 현상(Starvation)**: 복구 단계에서 특정 프로세스만 반복적으로 희생양(Victim)이 되면, 교착상태는 벗어나지만 대신 기아 상태에 빠질 수 있다(011에서 상세).

---

# 📝 【답안용】 시험 답안 템플릿

> 목적: 시험장에서 25분에 그대로 쓰는 답안 양식. 작성방식(추상표현 금지·수치·도식·문제유형 전환)을 엄격히 지킨다.
> 핵심: 이 시험은 정보를 많이 나열하는 시험이 아니라, 문제 신호어에서 출제자 의도를 읽고 핵심 논점을 선별해 쓰는 시험이다.

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 둘 이상의 프로세스가 공유 자원에 대한 상호 배제와 비선점 원칙을 지키면서, 서로 환형으로 자원을 점유·대기할 때 발생하는 시스템 무한 정지(Halt) 상태다.
> 2. **가치**: 동시성 제어 시 필연적으로 발생하는 자원 경합을 예방, 회피, 탐지, 복구의 4단계 아키텍처 관점으로 풀어내어 시스템 무결성을 방어한다.
> 3. **판단 포인트**: 코프만의 4가지 필요충분조건을 명시하고, 이를 OS 레벨에서 처리할 때 발생하는 자원 낭비(예방/회피)와 사후 복구 패널티 간의 트레이드오프를 짚어야 한다.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| 발생 메커니즘의 수학적 이해 | 코프만(Coffman) 4조건(상호배제, 점유대기, 비선점, 환형대기) 모두 만족 | 4조건 중 1개만 터져도 발생한다고 적는 치명적 오개념 (4개 동시 만족 필수) |
| OS의 데드락 제어 철학 및 4단계 방어 | 예방(사전), 회피(회피율 계산), 탐지(그래프), 복구(Kill) 전략 대조 | 현대 상용 OS(Linux/Win)가 예방/회피를 쓴다고 착각 (타조 알고리즘 명시 필수) |

> 요약: 데드락은 자원을 뺏지 못하는 선비(비선점)들이, 남의 것만 탐내며(점유와 대기), 서로 눈치만 보다(환형 대기) 다 같이 굶어 죽는 운영체제의 딜레마다.

---

## Ⅰ. 개요 및 필요성

- 정의: 프로세스 그룹 내의 모든 프로세스가 그룹 내 다른 프로세스가 점유한 자원을 요청하며 영구적으로 블로킹(Blocking)된 상태
- 배경: 다중 프로그래밍 환경에서 락(Lock)을 통한 임계 구역(Critical Section) 보호가 고도화됨에 따라 역설적으로 자원 요청 순서의 사이클(Cycle) 형성 유발
- 필요성: 시스템 가동 중단(Downtime)을 막기 위해 4대 발생 조건을 원천 차단하거나, 사후에 감지하여 트랜잭션을 롤백(Rollback)하는 체계적 통제 메커니즘 필수

---

## Ⅱ. 교착상태 4대 성립 조건 (Coffman Conditions) 도식

```text
[ 자원 할당 그래프 (환형 대기 Cycle 완성) ]
         (요청)               (점유)
프로세스 P1 ------> 자원 R2 ------> 프로세스 P2
    ^                                 |
    |                                 |
    +------- 자원 R1 <----------------+
          (점유)               (요청)
* R1과 R2는 서로 뺏을 수 없고(비선점), 이미 쥔 상태로 남의 것을 탐함(점유대기).
```

| 필요충분조건 | 메커니즘 및 발생 원인 | 조건 파괴를 통한 예방(Prevention) 한계점 |
|:---|:---|:---|
| **상호 배제** (Mutual Exclusion) | 자원은 한 번에 1개의 프로세스만 점유 가능 | 자원을 공유(Read-only) 가능하게 변경. 단, 프린터/락 등 물리적 한계로 불가능 |
| **점유와 대기** (Hold and Wait) | 자원을 보유한 상태에서 추가 자원 할당 대기 | "실행 전 모든 자원 일괄 할당". 효율성 폭락 및 미래 자원 예측 불가로 현실성 없음 |
| **비선점** (No Preemption) | 다른 프로세스의 자원을 강제로 뺏을 수 없음 | "대기 시 내 자원 강제 반납". 문맥 저장/복원 오버헤드 극심 및 데이터 오염(Corrupt) |
| **환형 대기** (Circular Wait) | 자원 대기 그래프가 순환 사이클(Cycle) 형성 | "자원 번호 순서로만 락 획득 강제". 가장 실무적으로 쓰이나 개발 제약(순서 강제) 심함 |

> 요약: 이론적으로 4개 중 1개만 부수면 예방되지만, 상호 배제를 부수면 데이터가 망가지고, 점유/비선점을 부수면 시스템 성능이 망가진다. 결국 '환형 대기 꼬리표 끊기'만이 유일한 현실적 예방책이다.

---

## Ⅲ. 교착상태 방어 아키텍처: 회피(Avoidance) vs 탐지·복구(Recovery)

가혹한 사전 예방(Prevention) 대신, 알고리즘을 통한 제어 기법이다.

| 방어 체계 | 핵심 철학 및 구현 알고리즘 | 트레이드오프 및 한계점 |
|:---|:---|:---|
| **회피 (Avoidance)** | 자원 요청 시 수학적으로 시스템이 '안전 상태(Safe State)'에 남는지 시뮬레이션 후 승인 | **은행원 알고리즘(Banker's)**. 매 요청마다 $O(M \cdot N^2)$ 행렬 연산 발생. 미래 최대 요구량을 미리 알아야 해 실무 적용 0% |
| **탐지 (Detection)** | 일단 조건 없이 다 빌려주고, 정기적으로 대기 그래프(Wait-for Graph)의 Cycle(사이클) 존재 여부 검사 | 탐지 주기를 짧게 하면 CPU 렉 걸리고, 길게 하면 이미 시스템 마비된 후 발견됨 |
| **복구 (Recovery)** | 탐지된 데드락 사이클을 박살 내기 위해 특정 프로세스를 희생양(Victim)으로 강제 킬(Kill) | 최소 비용 Victim(우선순위 낮거나 남은 시간 적은 놈) 선정 로직 및 빈번한 킬에 의한 기아 현상(Starvation) 우려 |

> 요약: 회피는 매번 주판을 튕기다 은행원이 지쳐 쓰러지고(오버헤드), 탐지/복구는 일단 터지게 놔뒀다가 한 놈을 총살(Kill)시켜 사태를 수습하는 거친 실전파다.

---

## Ⅳ. 현대 시스템의 데드락 제어 현실 (타조 알고리즘)

| 시스템 환경 | OS/커널의 실제 채택 정책 | 실무적 근거 및 해결의 외주화 |
|:---|:---|:---|
| **범용 OS** (Linux / Windows) | **무시 (타조 알고리즘, Ostrich Algorithm)** | 데드락은 수년에 한 번 날까 말까 함. 이걸 막겠다고 1초에 수만 번씩 장부를 뒤지는 건 최악의 낭비. 터지면 사용자가 `Ctrl+Alt+Del` 로 죽이게 둠 |
| **DBMS** (Oracle / MySQL) | **타임아웃(Timeout) + 복구(Kill)** 결합 | 락 획득 대기 시간(예: `innodb_lock_wait_timeout=50s`)이 지나면 데드락으로 간주하고, 강제로 트랜잭션 1개를 롤백(Rollback) 시킴 |

> 요약: 리눅스 커널 개발자들은 수학적 완벽함을 버리고 "모래밭에 머리를 박는 타조"처럼 데드락을 무시하는 경제학적 최적화를 택했다. 처리는 온전히 애플리케이션 개발자의 몫이 되었다.

---

## Ⅴ. 실무 적용 방안 및 결론

**적용 방안 3개:**
1. [코드 레벨 환형 대기(Circular Wait) 타파] Java/C++ 멀티 스레드 개발 시, 여러 뮤텍스(Mutex)를 중첩해서 획득해야 할 경우 무조건 해시(Hash)값이나 리소스 ID 기반의 오름차순(Ascending) 정렬된 고정 순서로만 Lock을 잡도록 아키텍처 규칙 강제 적용
2. [DB 트랜잭션 Timeout 격리] MSA 환경에서 트랜잭션이 길어지면 데드락 확률이 기하급수적으로 높아지므로, DB 세션의 `Lock Timeout`을 서비스 SLA(예: 3초) 내로 짧게 끊고 빠지게(Fail-fast) 하여 연쇄 마비(Cascading Failure) 원천 차단
3. [Go Lang 채널(Channel) 기반 락 프리] 뮤텍스 자체의 태생적 리스크를 피하기 위해, "메모리를 공유하여 통신하지 말고, 통신(Channel)을 통해 메모리를 공유하라"는 철학의 CSP 모델(Goroutine)을 도입해 개발자 실수 연루 데드락 배제

**결론:**
- 기술사 판단: 교착상태는 운영체제 이론의 화려한 꽃이지만, 실전에서는 코프만 4조건을 부수려는 하향식(Top-down) 통제보다, 락(Lock) 자체의 사용 빈도와 획득 순서를 단순화하는 상향식(Bottom-up) 코드 엔지니어링으로 해결하는 것이 절대 원칙이다.
- 향후 방향: 최근 동시성 제어 트렌드는 Lock을 잡고 대기하는 비관적(Pessimistic) 제어에서, 일단 충돌을 허용하되 버전을 비교해 재시도(CAS 연산)하는 낙관적 락(Optimistic Lock)이나 완전한 락 프리(Lock-free) 데이터 구조로 진화하여 데드락 발생 조건 자체를 형해화하고 있다.

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅲ 강조 | Ⅳ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "교착상태 발생 조건(4가지)과 해결 기법을 설명하시오" | 코프만 4조건과 예방/회피/탐지/복구 대조표 | 4조건 중 '환형 대기' 제거의 실효성과 타조 알고리즘 |
| 요구사항 명시형 | "교착상태 복구 과정의 기아 문제와 실무 예방 코딩 지침 설계" | 희생양(Victim) 선정 시 누적 롤백 횟수 반영(기아 방어) | DB 타임아웃 통제 및 오름차순 락(Lock) 획득 아키텍처 |
