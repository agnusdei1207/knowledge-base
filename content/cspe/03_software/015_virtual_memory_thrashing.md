---
title: 가상 메모리 및 스래싱 (Virtual Memory and Thrashing)
date: 2026-07-05
tags: [cspe-software]
weight: 15
---

## Ⅰ. 개요
- 정의: 물리 메모리보다 큰 프로그램을 실행하기 위한 가상 주소 기법과 메모리 과부하 현상
- 배경: 제한된 RAM에서 주소 공간 격리와 필요한 페이지만 적재하는 관리가 필요함
- 출제 의도: 요구 페이징(Demand Paging) 메커니즘과 스래싱 발생 시 시스템 성능 영향 파악

## Ⅱ. 구성요소
```
[ Virtual Addr ] -> [ MMU ] -> [ Physical Addr ]
      |               |              |
      +-> [Page Table]+              +-> [ RAM / Swap ]
```
| 구성요소 | 설명 | 판단 기준 |
| :--- | :--- | :--- |
| MMU | 가상 주소를 물리 주소로 변환하는 하드웨어 유닛 | 주소 변환 경로 |
| 페이지 폴트 | 필요한 페이지가 메모리에 없어 OS 처리가 필요한 예외 | 메모리 적재 여부 |
| 워킹 셋 | 프로세스가 일정 시간 동안 집중적으로 참조하는 페이지 집합 | 필요 프레임 규모 |
> 요약: 가상 메모리는 추상화를 통해 메모리 확장을, 스래싱은 과도한 스왑 오버헤드를 의미함.

## Ⅲ. 절차
```
[CPU Access] -> [Page Fault] -> [Swap In] -> [Update Table] -> [Restart]
```
1. CPU가 가상 주소를 참조하면 MMU가 페이지 테이블을 조회함
2. 해당 페이지가 메모리에 없으면(Invalid bit) 페이지 폴트 트랩 발생
3. OS는 디스크 스왑 영역에서 해당 페이지를 찾아 빈 프레임에 적재(Swap-in)
4. 페이지 테이블을 갱신하고 중단되었던 명령어를 다시 실행함
> 요약: 필요한 페이지를 늦게 적재해 메모리 사용량과 page fault 비용을 함께 관리함.

## Ⅳ. 문제점
- 원인: 다중 프로그래밍 정도가 너무 높아 프로세스당 프레임 수가 워킹 셋보다 적어짐
- 원인: CPU 이용률은 낮고 page in/out I/O가 반복되어 처리량이 급감함

## Ⅴ. 개선방안
- (단기) 워킹 셋 알고리즘이나 PFF(Page Fault Frequency)를 활용하여 적정 프레임 할당
- (중기) 다중 프로그래밍 정도(Degree of Multiprogramming)를 낮추어 메모리 여유 확보
- (장기) 고속 SSD 저장장치 도입 및 압축 메모리(ZRAM) 기술 적용으로 I/O 지연 단축

## Ⅵ. 전망
- 대용량 데이터 처리 환경에서도 주소 공간 격리와 working set 관리는 계속 필요함
- 비휘발성 메모리(NVM)와 압축 메모리는 swap 지연을 줄이는 보조 계층으로 활용됨

## 작성 근거(검토용)

- 가상 메모리와 스래싱은 메모리 확장 평가가 아니라 주소 변환, 페이지 적재, working set 대비 프레임 수로 설명해야 함
- 스래싱은 시스템 정지 단정보다 page in/out I/O 반복과 처리량 급감으로 쓰는 편이 기술적으로 정확함
