---
title: "3단계 스키마 - 외부·개념·내부 (Three-Level Schema)"
date: "2026-07-01"
tags:
  - "cspe-software"
weight: 128
---

# 📖 【암기용】 개념 완전 이해

> 목적: 3단계 스키마를 처음 봐도 완벽히 이해하게 만든다. 시험 답안 양식이 아니라, 이해를 위한 친절한 설명이다.

## 한눈에
- **개요**: DB 구조를 사용자 관점, 전체 논리 관점, 물리 저장 관점으로 나눈 ANSI/SPARC 구조
- **왜 필요한가**: 사용자 화면, 업무 데이터 모델, 실제 저장 방식이 한 덩어리로 묶이면 변경 때 영향 범위가 커진다. 3단계 스키마는 관점을 분리해 데이터 독립성을 확보한다.
- **핵심 직관**: 같은 도시를 시민용 지도, 행정구역 지도, 지하 배관 도면으로 나눠 관리하는 방식임

## 깊이 이해
- **배경·문제의식**: 파일 시스템 중심 데이터 처리는 프로그램이 레코드 배치와 저장 위치를 알아야 했다. ANSI/SPARC 3단계 스키마는 외부 스키마, 개념 스키마, 내부 스키마를 분리하여 사용자와 저장 구조를 직접 결합하지 않는다.
- **작동 원리**: 외부 스키마는 사용자·응용별 뷰, 개념 스키마는 조직 전체 논리 모델, 내부 스키마는 저장 파일·인덱스·페이지 구조를 정의한다. 계층 간 매핑이 질의 변환과 변경 영향 흡수를 담당한다.
- **비유**: 고객은 메뉴판만 보고, 매장은 전체 레시피를 관리하고, 주방은 재료 보관 위치를 따로 관리하는 구조임
- **구체 예시**: 영업팀은 고객명·등급 뷰만 보고, 개념 스키마는 고객·주문·결제 관계를 관리하며, 내부 스키마는 고객 테이블 파티션과 B+Tree 인덱스를 관리함
- **흔한 오해·주의점**: 3단계 스키마는 물리적으로 DB가 세 개라는 뜻이 아니다. 하나의 DBMS가 관점과 매핑을 분리해 관리하는 개념 구조다.

## 연결 개념
- 데이터 독립성 - 3단계 스키마의 목적
- 외부/개념 매핑 - 논리적 독립성 기반
- 개념/내부 매핑 - 물리적 독립성 기반

---

# 📝 【답안용】 시험 답안 템플릿

> 목적: 시험장에서 25분에 그대로 쓰는 답안 양식. 작성방식(추상표현 금지·수치·도식·문제유형 전환)을 엄격히 지킨다.
> 핵심: 3단계 스키마 답안은 각 계층 정의와 두 매핑, 데이터 독립성 효과를 함께 써야 함.

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 3단계 스키마는 외부·개념·내부 스키마로 DB 관점을 분리하는 ANSI/SPARC 구조임.
> 2. **가치**: 사용자 뷰, 전사 논리 모델, 물리 저장 구조 변경을 분리해 DB 변경 영향을 줄임.
> 3. **판단 포인트**: 외부/개념 매핑은 논리 독립성, 개념/내부 매핑은 물리 독립성과 연결됨.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| DBMS 기본 구조 확인 | external, conceptual, internal schema | 3계층 애플리케이션 구조와 혼동하지 않음 |
| 데이터 독립성 연결 확인 | 논리적·물리적 데이터 독립성 | 계층만 나열하고 매핑을 빠뜨리지 않음 |
| 실무 적용 이해 확인 | view, ER model, storage/index | 사용자별 권한·뷰 관점 누락 방지 |

> 요약: 3단계 스키마는 DB 관점 분리와 데이터 독립성을 설명하는 기본 구조임.

---

## Ⅰ. 개요 및 필요성

3단계 스키마는 DB 구조를 세 관점으로 분리한 모델이다. 사용자 요구, 전사 데이터 모델, 물리 저장 구조는 변경 주기와 책임자가 다르다. 세 관점을 분리하면 응용 변경과 저장 구조 변경의 결합도를 낮출 수 있다.

---

## Ⅱ. 구조 및 구성요소

```text
User/Application -> External Schema -> Conceptual Schema -> Internal Schema -> Physical Storage
                       +-> External/Conceptual Mapping
                                            +-> Conceptual/Internal Mapping
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| 외부 스키마 | 사용자·응용별 데이터 뷰 | view, 권한, 화면별 projection |
| 개념 스키마 | 전사 논리 데이터 모델 | entity, relationship, constraint |
| 내부 스키마 | 저장 파일·인덱스·접근 경로 | page, partition, B+Tree |
| 매핑 | 계층 간 변환 관계 | 독립성 확보 지점 |

> 요약: 3단계 스키마는 사용자 뷰, 논리 모델, 물리 저장을 분리하고 매핑으로 연결함.

---

## Ⅲ. 동작원리 및 흐름도

```text
User Query -> External View Resolve -> Conceptual Model Map -> Internal Access Path -> Storage Read/Write
```

| 단계 | 처리 내용 | 검증 기준 |
|:---:|:---|:---|
| 1 | 사용자별 외부 스키마 확인 | view 권한, column mask |
| 2 | 외부 질의를 개념 스키마로 변환 | view expansion |
| 3 | 개념 객체를 내부 접근 경로로 매핑 | optimizer plan |
| 4 | 인덱스·파티션·페이지 접근 | I/O count, buffer hit |
| 5 | 결과를 외부 뷰 형태로 반환 | projection correctness |

> 요약: 질의는 외부 뷰에서 시작해 개념 모델과 내부 저장 경로로 변환된 뒤 결과로 반환됨.

---

## Ⅳ. 특징

| 구분 | 외부 스키마 | 개념 스키마 | 내부 스키마 |
|:---|:---|:---|:---|
| 관점 | 사용자·응용 | 전사 논리 | 물리 저장 |
| 변경 예 | 화면 컬럼·권한 | 엔티티·관계·제약 | 인덱스·파일·파티션 |
| 독립성 | 외부 변경 격리 | 논리 기준점 | 물리 변경 격리 |

> 요약: 외부·개념·내부 스키마는 각각 사용자, 논리, 저장 관점을 분담함.

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | 기존/대안 | 본 키워드 | 선택 기준 |
|:---|:---|:---|:---|
| 구조 | 파일 처리 방식 | 3단계 스키마 | 프로그램과 저장 구조 분리 필요 |
| 비용/성능 | 저장 변경 시 프로그램 수정 | 매핑 계층으로 변경 흡수 | 변경 영향 모듈 수 |
| 운영/위험 | 관점 혼재 | 스키마 책임 분리 | 데이터 거버넌스 조직 |

> 요약: 3단계 스키마는 DB 변경관리와 데이터 거버넌스의 기준 구조로 활용됨.

| 리스크 | 원인 | 대응 방안 | 확인 지표 |
|:---|:---|:---|:---|
| 외부 뷰 난립 | 부서별 임의 생성 | view naming, owner 지정 | orphan view count |
| 개념 모델 불일치 | 업무 용어 중복 | data dictionary, ERD 승인 | duplicate entity count |
| 내부 구조 영향 | 인덱스 변경 후 계획 변동 | plan baseline, 통계 관리 | top SQL p95 변동 |

> 요약: 3단계 스키마 운영은 뷰 관리, 개념 모델 표준화, 실행계획 통제가 필요함.

| 점검 항목 | 목표 기준 | 측정 방법 |
|:---|:---|:---|
| 독립성 | 물리 변경 시 SQL 수정 0건 | release impact report |
| 거버넌스 | 개념 엔티티 중복 5% 이하 | data catalog |
| 접근통제 | 외부 스키마별 권한 누락 0건 | privilege audit |

> 요약: 3단계 스키마는 변경 영향, 모델 중복, 권한 감사로 관리한다.

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개 (필수 — 단계별 또는 항목별):**
1. 외부 스키마는 업무 API·view·권한 정책과 연결하고, 민감 컬럼은 masking view로 노출함.
2. 개념 스키마는 ERD와 data dictionary를 기준으로 용어·엔티티·제약조건을 관리함.
3. 내부 스키마는 인덱스·파티션·압축 변경을 실행계획 검증 후 배포하고 응용 SQL 의미를 유지함.

**결론 (2줄):**
- 기술사 판단: 3단계 스키마는 데이터 독립성과 DB 거버넌스를 설명하는 기준 모델임.
- 향후 방향: 데이터 카탈로그, 스키마 레지스트리, 정책 기반 접근제어가 외부·개념 스키마 관리와 결합됨.

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅲ 강조 | Ⅳ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "3단계 스키마를 설명하시오" | 질의 변환과 매핑 흐름 | 세 스키마 역할 비교 |
| 요구사항 명시형 | "데이터 독립성 방안을 제시하시오" | 매핑 계층별 변경 흡수 | 뷰·카탈로그·실행계획 통제 |

> 요약: 설명형은 계층 정의, 방안형은 독립성 확보 절차와 운영 지표를 중심으로 작성함.
