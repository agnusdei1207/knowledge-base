---
title: "MLOps 파이프라인 (MLOps Pipeline)"
date: "2026-07-01"
tags:
  - "cspe-software"
weight: 251
---

# 📖 【암기용】 개념 완전 이해

> 목적: MLOps 파이프라인을 처음 보는 사람도 완벽히 이해하게 만든다. 시험 답안 양식이 아니라, 이해를 위한 친절한 설명이다.

## 한눈에
- **개요**: MLOps 파이프라인은 데이터 수집부터 모델 학습, 배포, 감시까지 ML 생명주기를 자동화하는 운영 체계다.
- **왜 필요한가**: 모델은 코드와 달리 데이터 분포 변화, 재학습, 실험 재현성 문제가 함께 발생한다. 수작업 배포는 학습 데이터, 모델 파일, 서비스 버전 불일치를 만든다.
- **핵심 직관**: 소프트웨어 CI/CD에 데이터 검증, 모델 평가, 드리프트 감시를 붙인 ML 전용 공정 라인이다.

## 깊이 이해
- **배경·문제의식**: PoC 모델은 노트북에서 동작해도 운영 서비스는 데이터 품질, 재현 가능한 학습, 승인 절차, 롤백, 감시 지표가 필요하다.
- **작동 원리**: 데이터 적재 후 스키마와 결측치를 검증하고, 피처를 생성해 학습한다. 평가 기준을 통과한 모델만 레지스트리에 등록하고 CI/CD로 서빙 환경에 배포한다.
- **비유**: 연구실 레시피를 공장 생산 라인으로 옮길 때 원재료 검사, 공정 기록, 품질 검사, 출하 추적을 붙이는 것과 같다.
- **구체 예시**: 추천 모델은 AUC 0.78 이상, p95 추론 지연 100ms 이하, 드리프트 PSI 0.2 미만 조건을 통과할 때만 배포 후보가 된다.
- **흔한 오해·주의점**: MLOps는 도구 설치가 아니다. 데이터 계약, 모델 승인권자, 온라인 지표, 재학습 기준이 없으면 파이프라인은 단순 배치 작업에 머문다.

## 연결 개념
- 피처 스토어: 학습과 추론 피처 일관성 확보
- 모델 레지스트리: 모델 버전, 승인 상태, 배포 이력 관리
- 모델 모니터링: 데이터 드리프트와 예측 품질 저하 탐지

---

# 📝 【답안용】 시험 답안 템플릿

> 목적: 시험장에서 25분에 그대로 쓰는 답안 양식. 작성방식(추상표현 금지·수치·도식·문제유형 전환)을 엄격히 지킨다.
> 핵심: MLOps 답안은 CI/CD 나열이 아니라 데이터, 모델, 서비스, 모니터링을 하나의 통제 흐름으로 묶어야 한다.

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: MLOps 파이프라인은 ML 모델의 데이터 준비, 학습, 검증, 배포, 감시, 재학습을 자동화하는 운영 생명주기다.
> 2. **가치**: 재현 가능한 학습 실행, 승인 기반 배포, 드리프트 감시로 모델 변경 리스크를 지표 기반으로 통제한다.
> 3. **판단 포인트**: 모델 정확도만 보지 말고 데이터 품질, p95 추론 지연, 롤백 시간, 드리프트 임계값을 함께 설계해야 한다.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| ML 운영 생명주기 이해 확인 | DataOps, CI/CD, CT, 모델 레지스트리, 모니터링 | 학습 자동화만 설명하고 운영 감시 누락 |
| 현업 적용 판단 확인 | 데이터 검증, 평가 게이트, 승인, 배포 전략 | 정확도 수치만 제시하고 지연·비용·롤백 누락 |
| 리스크 통제 역량 확인 | 드리프트, 재현성, 편향, 모델 부패 대응 | 재학습 트리거와 책임자 승인 절차 누락 |

> 요약: MLOps 문제는 모델 개발보다 운영 배포와 지속 통제를 평가하는 문제다.

---

## Ⅰ. 개요 및 필요성

- 개요: MLOps는 ML 모델 생명주기 자동화 체계다.
- 배경: ML 서비스는 코드 변경 외에 데이터 분포, 피처 정의, 모델 파라미터가 예측 결과를 바꾼다.
- 필요성: 학습 재현성, 평가 게이트, 배포 승인, 운영 감시를 파이프라인으로 통제해야 한다.

---

## Ⅱ. 구조 및 구성요소

```text
Data Source -> Data Validation -> Feature Pipeline -> Training -> Evaluation -> Registry -> Serving -> Monitoring
                                      / Metadata Store
                                      / Approval Gate
```

| 구성요소 | 역할 | 통제 기준 |
|:---|:---|:---|
| 데이터 검증 | 스키마, 결측치, 이상치 확인 | Great Expectations, TFDV |
| 학습 파이프라인 | 피처 생성, 학습, 튜닝 실행 | Kubeflow, Airflow, MLflow |
| 모델 레지스트리 | 버전, 지표, 승인 상태 관리 | Staging, Production, Archived |
| 서빙·모니터링 | API 배포, 지연, 드리프트 감시 | p95, PSI, 오류율 |

> 요약: MLOps 구조는 데이터 검증부터 운영 감시까지 모델 변경을 추적 가능한 단계로 나눈다.

---

## Ⅲ. 동작원리 및 흐름도

```text
데이터 변경 감지 -> 검증 통과 -> 학습 실행 -> 평가 기준 확인 -> 승인 -> 배포 -> 지표 감시 -> 재학습 트리거
```

| 단계 | 처리 내용 | 검증 기준 |
|:---:|:---|:---|
| 1 | 원천 데이터 적재와 품질 검증 | 결측률 1% 이하, 스키마 일치 |
| 2 | 피처 생성과 학습 실행 | 코드 SHA, 데이터 버전, 파라미터 기록 |
| 3 | 오프라인 평가와 배포 승인 | AUC, F1, RMSE 목표 충족 |
| 4 | 온라인 서빙과 감시 | p95 지연 100ms, PSI 0.2 미만 |

> 요약: MLOps는 검증 기준을 통과한 모델만 승인 배포하고 운영 지표가 임계값을 넘으면 재학습을 시작한다.

---

## Ⅳ. 특징

| 구분 | 일반 DevOps | MLOps 파이프라인 | 판단 포인트 |
|:---|:---|:---|:---|
| 변경 대상 | 코드와 설정 | 코드, 데이터, 피처, 모델 | 데이터 버전 추적 필요 |
| 테스트 | 단위·통합 테스트 | 데이터 검증, 모델 평가, 편향 점검 | 정확도와 지연 동시 평가 |
| 배포 | 애플리케이션 릴리스 | 모델 레지스트리 승인 후 서빙 | 카나리, 섀도, 롤백 |

> 요약: MLOps는 DevOps에 데이터와 모델 품질 게이트를 추가해 ML 서비스 변경을 통제한다.

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | 기존/대안 | MLOps 파이프라인 | 선택 기준 |
|:---|:---|:---|:---|
| 구조 | 수동 노트북 학습 | 자동 파이프라인과 메타데이터 추적 | 월 1회 이상 재학습 |
| 비용/성능 | 개별 서버 실행 | 컨테이너, GPU 스케줄링 | GPU 사용률 60% 이상 목표 |
| 운영/위험 | 담당자 지식 의존 | 승인, 감사로그, 롤백 | 규제·감사 대상 모델 |

> 요약: 반복 학습과 운영 배포가 있는 모델은 MLOps를 적용하고, 단발 분석 모델은 경량 추적부터 시작한다.

| 리스크 | 원인 | 대응 방안 | 확인 지표 |
|:---|:---|:---|:---|
| 학습 재현 실패 | 데이터와 코드 버전 미기록 | DVC, Git SHA, MLflow Run 기록 | 재현 실행 성공률 |
| 모델 부패 | 운영 데이터 분포 변화 | PSI, KS 통계량, 재학습 트리거 | PSI 0.2 이상 알림 |
| 배포 장애 | 모델 파일과 서빙 이미지 불일치 | OCI 이미지, 레지스트리 승인 | 롤백 시간 10분 이하 |

> 요약: 핵심 리스크는 재현성, 드리프트, 배포 불일치이며 버전 추적과 임계값 기반 운영으로 통제한다.

| 점검 항목 | 목표 기준 | 측정 방법 |
|:---|:---|:---|
| 모델 품질 | AUC, F1, RMSE 기준 충족 | 검증 데이터, 교차검증 |
| 서비스 지표 | p95 지연 100ms, 오류율 0.1% 이하 | APM, 서빙 로그 |
| 운영 통제 | 승인 이력 100%, 감사로그 보존 | 레지스트리, CI/CD 로그 |

> 요약: MLOps 도입 여부는 모델 품질, 서비스 지표, 승인 증적을 함께 측정해 판단한다.

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개:**
1. MLflow 또는 Kubeflow로 학습 Run, 데이터 버전, 모델 아티팩트, 평가지표를 단일 메타데이터로 기록함
2. 레지스트리에 Staging, Production 단계를 두고 AUC 0.78 이상, p95 100ms 이하 조건 통과 모델만 배포함
3. Prometheus, Evidently, OpenTelemetry로 지연, 오류율, PSI를 감시하고 PSI 0.2 이상 시 재학습 티켓을 생성함

**결론 (2줄):**
- 기술사 판단: 운영 예측 결과가 업무 의사결정에 쓰이면 MLOps를 필수 적용하고, 실험성 모델은 추적과 평가 게이트부터 적용함
- 향후 방향: MLOps는 LLMOps, Feature Store, Model Governance와 결합해 모델 공급망 통제 체계로 확장됨

---

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅲ 강조 | Ⅳ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "MLOps를 설명하시오" | 데이터 검증부터 재학습까지 생명주기 | DevOps와 차이, 모델 운영 특성 |
| 요구사항 명시형 | "도입 방안을 제시하시오", "설계하시오" | 평가 게이트, 승인, 감시 지표 | 리스크 대응, 재학습 기준, 도구 선택 |

> 요약: 설명형은 생명주기 전체를, 설계형은 평가 게이트와 운영 지표를 중심으로 목차를 전환한다.
