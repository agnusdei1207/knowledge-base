---
title: "컨텍스트 윈도우 (Context Window)"
date: "2026-07-01"
tags:
  - "cspe-latest-tech"
weight: 40
---

# 📖 【암기용】 개념 완전 이해

> 목적: 컨텍스트 윈도우를 처음 봐도 완벽히 이해하게 만든다.

## 한눈에
- **개요**: LLM이 한 번의 요청에서 입력·출력으로 처리할 수 있는 최대 토큰 범위
- **왜 필요한가**: 모델은 이전 대화를 무한히 기억하지 못하므로, 윈도우 안에 들어간 토큰만 직접 참조함.
- **핵심 직관**: 모델의 작업 책상 크기임. 책상 위에 올라온 문서만 읽고 답하며, 밖으로 밀려난 문서는 보지 못함.

## 깊이 이해
- **배경·문제의식**: 초기 GPT 계열은 2K~4K 토큰만 처리해 긴 문서 QA와 다중 대화 유지에 한계가 있었음. 최신 LLM은 128K~1M 토큰까지 확장했지만 비용·지연·정확도 문제가 남음.
- **작동 원리**: 요청의 system prompt, user message, RAG 문맥, 대화 기록, 출력 예약 토큰이 모두 같은 윈도우를 공유함. 초과 토큰은 잘리거나 요약·검색으로 대체됨.
- **비유**: 시험장 책상 위에 참고자료를 올리되, 책상 크기를 넘으면 일부 자료를 치워야 하는 상황과 같음.
- **구체 예시**: 128K 윈도우에서 출력 4K를 예약하면 입력으로 실제 사용 가능한 범위는 약 124K 토큰임.
- **흔한 오해·주의점**: 윈도우가 크면 모든 위치의 정보를 동일하게 찾는다는 뜻이 아님. 긴 문맥에서는 중간 정보 검색 정확도가 낮아지는 Lost in the Middle 현상이 발생함.

## 연결 개념
- Long Context LLM — 컨텍스트 윈도우 확장 모델
- RAG — 윈도우 한계를 검색으로 보완
- KV Cache — 긴 윈도우 추론 메모리 병목


# 📝 【답안용】 시험 답안 템플릿

> 목적: 시험장에서 25분에 그대로 쓰는 답안 양식.

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: Context Window는 LLM이 한 요청에서 참조 가능한 입력·출력 토큰의 총량 제한임.
> 2. **가치**: 긴 문서·대화·코드 분석 범위를 결정하며 RAG, 요약, 메모리 전략의 기준이 됨.
> 3. **판단 포인트**: 토큰 예산, 출력 예약, 비용, Lost in the Middle, KV Cache 메모리를 함께 관리해야 함.


## Ⅰ. 개요 및 필요성

Context Window는 LLM의 최대 토큰 처리 범위임. 긴 문서 QA·고객 상담·코드 분석에서는 system prompt, 검색 문맥, 대화 이력, 출력 토큰을 한 윈도우 안에 배치해야 하므로 토큰 예산 설계가 필수임.


## Ⅱ. 구조 및 구성요소

```text
┌──────────────────── Context Window N tokens ────────────────────┐
│ System │ History │ Retrieved Context │ User Query │ Output Budget │
└──────────────────────────────────────────────────────────────────┘
                         │
                         ▼
                 LLM Attention + KV Cache
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| System Prompt | 역할·정책 고정 | 매 요청 반복 포함 |
| Conversation History | 대화 맥락 제공 | 오래된 대화 요약 필요 |
| Retrieved Context | RAG 검색 근거 | 상위 k개 청크 삽입 |
| Output Budget | 생성 토큰 예약 | 입력 한도에서 차감 |

> 요약: 컨텍스트 윈도우는 정책·대화·검색근거·질문·출력예산이 공유하는 제한 자원임.


## Ⅲ. 동작원리 및 흐름도

```text
요청 수신 → 토큰 수 계산 → 우선순위별 문맥 선택
    → 초과 시 요약/삭제/RAG 대체 → LLM 호출 → 출력 생성
```

| 단계 | 처리 내용 | 검증 기준 |
|:---:|:---|:---|
| 1 | 입력 요소별 토큰 수 산정 | tokenizer 기준 총량 |
| 2 | 출력 토큰 예산 예약 | 예: 4K output reserve |
| 3 | 문맥 우선순위 조정 | system > query > 근거 > history |
| 4 | 초과 문맥 처리 | summarization, sliding window, RAG |

> 요약: 토큰 예산을 먼저 계산하고 중요도 기준으로 문맥을 배치해 윈도우 초과와 근거 누락을 방지함.


## Ⅳ. 특징

| 구분 | 짧은 윈도우(4K~32K) | 긴 윈도우(128K~1M) | 수치·판단 포인트 |
|:---|:---|:---|:---|
| 문서 처리 | 청킹·요약 필수 | 원문 전체 입력 가능 | 계약서 50K 토큰 처리 |
| 비용·지연 | 토큰 비용 낮음 | 입력비·TTFT 증가 | 입력 토큰당 과금 |
| 정확도 | 필요한 근거 누락 가능 | Lost in the Middle 발생 | NIAH 평가 필요 |
| 운영 방식 | RAG 의존 | RAG+Long Context 혼합 | k값·윈도우 동시 조정 |

> 요약: 긴 윈도우는 문맥 누락을 줄이나 비용·지연·중간 위치 검색 정확도를 별도 지표로 관리해야 함.


## Ⅴ. 실무 적용 및 결론

**적용 방안 3개:**
1. 요청 전 토큰 예산표를 적용해 system 2K, RAG 20K, history 8K, output 4K처럼 상한을 분리
2. 대화형 서비스는 sliding window + 요약 메모리로 30턴 이상 대화에서 핵심 사실만 유지
3. 장문서 QA는 RAG Top-k와 128K 모델을 비교하고 NIAH 정확도 95% 이상·p95 TTFT 2초 이하 기준으로 선택

**결론 (2줄):**
- 기술사 판단: 빈번한 짧은 질의는 RAG 8K~32K, 전문 문서 전체 검토는 128K 이상 Long Context를 선택함.
- 향후 방향: 컨텍스트 윈도우 관리는 모델 확장보다 토큰 라우팅·메모리·검색 품질 최적화로 고도화됨.


### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅲ 강조 | Ⅳ 강조 |
|:---|:---|:---|:---|
| 포괄형 | 설명하시오, 기술하시오 | 토큰 예산 계산 흐름 | 짧은/긴 윈도우 비교 |
| 요구사항 명시형 | 설계하시오, 방안을 제시하시오 | 문맥 선택·초과 처리 절차 | RAG vs Long Context 선택 기준 |

> 요약: 설명형은 토큰 제한 구조, 설계형은 예산 배분과 문맥 선택 기준 중심으로 목차를 전환함.
