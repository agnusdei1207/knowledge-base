import os
import sys

base_dir = "/home/user/.gemini/antigravity-cli/brain/97e45c2b-339b-4cac-b553-8b2b12cb02ef/.system_generated/worktrees/subagent-CSPE-Rewriter-03-software-111-120-self-dbada255/content/cspe/03_software"

files = {}

files["111_partitioning.md"] = """---
title: "파티셔닝 — 범위·해시·리스트 (Partitioning)"
date: "2026-07-04"
tags:
  - "cspe-software"
weight: 111
---

# 📖 【암기용】 개념 완전 이해

## 한눈에
- **개요**: 대용량 테이블을 작은 논리적 단위(파티션)로 나누어 분산 저장·관리하는 데이터베이스 기법
- **왜 필요한가**: 수십억 건의 데이터가 쌓인 단일 테이블은 인덱스 트리가 거대해져 검색이 느려지고, 오래된 데이터를 지우는 배치 작업에 엄청난 락(Lock)과 리소스가 소모된다.
- **핵심 직관**: 백과사전을 가나다순으로 권별로 쪼개놓으면, '가'로 시작하는 단어를 찾을 때 '가' 권만 뒤지면 되고, 나중에 '가' 권만 따로 버리거나 교체하기도 쉽다.

## 핵심 용어 정리

| 용어/표기 | 의미 | 비유·예 |
|:---|:---|:---|
| 수평 파티셔닝 | 행(Row)을 기준으로 테이블을 분할 | 백과사전을 ㄱ~ㅎ 권으로 나눔 |
| 수직 파티셔닝 | 열(Column)을 기준으로 테이블을 분할 | 이름·전화번호부와 이름·주소부로 나눔 |
| 파티션 키 | 데이터를 나눌 기준이 되는 컬럼 | '가입일자'나 '고객ID' |
| 파티션 프루닝 (Pruning) | 쿼리 시 불필요한 파티션을 검색 대상에서 제외 | 2026년 조회 시 2025년 파티션 무시 |

## 깊이 이해
- **배경·문제의식**: VLDB(Very Large Database) 환경에서 테이블 크기가 커지면 인덱스의 깊이(Depth)가 깊어져 B-Tree 탐색 성능이 저하된다. 특정 기간의 데이터를 일괄 삭제(Purge)할 때 대량의 DELETE가 발생하여 트랜잭션 로그가 꽉 차고 성능 병목이 발생한다.
- **작동 원리**: 데이터베이스 내부적으로는 여러 개의 물리적 테이블로 쪼개지만, 애플리케이션(사용자) 입장에서는 하나의 논리적 테이블로 접근한다. 파티션 키에 따라 DBMS가 알아서 데이터를 적절한 파티션에 라우팅한다.
- **비유**: 회사 서류철을 연도별로 캐비닛에 보관(Range)하고, 5년 지난 캐비닛은 통째로 창고로 보낸다(Drop Partition). 캐비닛 안에서는 부서별로 폴더(List)를 나눈다.
- **구체 예시**: 로그 데이터를 `CREATE TABLE ... PARTITION BY RANGE (log_date)`로 생성하면, 매달 새로운 파티션을 추가하고 오래된 파티션을 `DROP PARTITION`으로 1초 만에 날릴 수 있다.
- **흔한 오해·주의점**: 파티셔닝은 성능을 무조건 향상시키지 않는다. 파티션 키를 조건에 포함하지 않는 쿼리는 모든 파티션을 다 뒤져야 하는 오버헤드(Partition Scan)가 발생해 단일 테이블보다 느려질 수 있다.

## 연결 개념
- 샤딩 (Sharding): 파티셔닝이 한 DB 내의 물리적 분할이라면, 샤딩은 물리적으로 다른 DB 서버로 쪼개는 수평 분할 기법
- 인덱스 분할 (Local/Global Index): 파티션 테이블에 인덱스를 생성할 때 파티션별로 종속시킬지 전체를 아우를지 결정하는 구조

---

# 📝 【답안용】 시험 답안 템플릿

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 파티셔닝은 초대용량 테이블(VLDB)을 파티션 키를 기준으로 다수의 물리적 세그먼트로 분할해 성능과 관리성을 높이는 기법이다.
> 2. **가치**: 불필요한 영역을 배제하는 파티션 프루닝(Pruning)으로 I/O를 최소화하고, Drop 파티션을 통해 삭제 트랜잭션 부하를 제거한다.
> 3. **판단 포인트**: 데이터 특성에 맞는 분할 방식(Range·Hash·List)을 선택하고, 파티션 인덱스(Local/Global) 전략을 병행해야 병목을 방지한다.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| VLDB 관리 및 튜닝 기법 이해 | Range·Hash·List 기법 비교, 파티션 프루닝 명시 | 파티셔닝과 샤딩(서버 간 분할) 혼동 |
| 파티션 인덱스 설계 역량 확인 | Local 인덱스와 Global 인덱스의 구조적 차이 비교 | 파티셔닝이 무조건 빠르다는 단정 |
| 데이터 생명주기 운영 역량 | 대용량 이력 데이터의 Drop Partition을 통한 Purge 효율화 | 범위 파티셔닝 시 MAXVALUE 설정 누락 |

> 요약: 단일 서버 내에서의 분할 기법인 파티셔닝 유형과 인덱스 전략, 그리고 이를 통한 VLDB 성능 및 관리 효율화 방안을 제시해야 한다.

---

## Ⅰ. 개요 및 필요성

- 개요: VLDB의 큰 테이블과 인덱스를 관리 가능한 작은 물리적 단위(파티션)로 분할하는 데이터베이스 관리 기법
- 배경: 단일 테이블이 기가/테라바이트 단위로 커지면 인덱스 깊이 증가로 탐색 비용이 급증하고 Lock 경합이 심화됨
- 필요성: 파티션 프루닝을 통한 디스크 I/O 90% 이상 절감 및 파티션 단위 DDL 작업을 통한 운영 중단 최소화 필요

---

## Ⅱ. 구조 및 구성요소

```text
Application Query -> DBMS Optimizer (Partition Pruning 적용) -> Partitioned Table
                                                                  |
                                                                  +-> Partition A (2026-01)
                                                                  +-> Partition B (2026-02)
                                                                  +-> Partition C (2026-03)
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| 파티션 키 (Partition Key) | 데이터를 물리적으로 나눌 기준 컬럼 | 변동이 적고 고르게 분포된 컬럼 권장(날짜 등) |
| 로컬 인덱스 (Local Index) | 개별 파티션과 1:1로 종속되어 생성되는 인덱스 | 파티션 DDL 수행 시 타 파티션 영향 없음 |
| 글로벌 인덱스 (Global Index) | 전체 파티션을 통합하여 단일 구조로 생성되는 인덱스 | 특정 파티션 Drop 시 전체 인덱스 무효화 위험 |

> 요약: 논리적으로는 단일 테이블로 보이나, 물리적으로는 파티션 키 기준의 개별 세그먼트로 나뉘며 인덱스 종속성에 따라 로컬과 글로벌로 구분된다.

---

## Ⅲ. 동작원리 및 흐름도

```text
SQL Request -> Partition Pruning (조건 분석) -> Target Partition 접근
-> Local/Global Index 탐색 -> Data Fetch -> Result Return
```

- 1단계 [쿼리 최적화]: Optimizer가 WHERE 절의 파티션 키 조건을 분석하여 접근할 파티션 목록 도출
- 2단계 [파티션 프루닝]: 불필요한 물리적 파티션을 디스크 I/O 스캔 대상에서 완전히 배제
- 3단계 [인덱스 스캔]: 선택된 파티션의 인덱스(Local 등)를 통해 데이터 블록의 ROWID 식별
- 4단계 [데이터 추출]: 도출된 ROWID로 디스크 블록에 접근하여 결과를 Fetch 후 사용자 반환

> 요약: Optimizer가 쿼리 조건을 분석해 스캔 범위를 특정 파티션으로 한정(Pruning)함으로써 불필요한 Full Scan 부하를 방지한다.

---

## Ⅳ. 특징

- 관리 편의성: 보관 주기가 지난 이력 데이터를 `DELETE` 대신 `DROP PARTITION`으로 처리해 트랜잭션 로그 부하 99% 절감
- 성능 향상: 데이터 스캔 범위를 물리적으로 한정하여 응답 속도를 보장하고 단일 인덱스 트리의 비대화 방지
- 가용성 증대: 파티션 단위의 독립적 백업·복구가 가능하며, 특정 파티션 손상이 전체 테이블 장애로 직결되지 않음

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | 범위 (Range) | 해시 (Hash) | 리스트 (List) |
|:---|:---|:---|:---|
| 분할 기준 | 연속된 값 (날짜, 숫자 구간) | 해시 함수를 통한 연산 값 | 사전에 정의된 특정 값의 목록 |
| 장점 | 이력 데이터(시계열) 보관·관리에 최적 | 데이터가 균등 분배되어 I/O 병목 방지 | 지역, 부서 등 명확한 불연속 값 분류 시 유리 |
| 단점 | 특정 최신 기간에 트래픽이 몰릴 수 있음 | 특정 범위(Range) 검색 시 프루닝 미동작 | 새로운 값 등장 시 파티션 수동 추가 필요 |

> 요약: 시계열 이력 데이터는 Range 파티션을 기본으로 하고, PK 조회가 많고 쏠림이 우려되면 Hash 파티션을 선택한다.

**리스크·대응:**
- 전체 파티션 스캔: 파티션 키 누락 쿼리 실행 시 Full Scan 부하 → 애플리케이션 쿼리 가이드라인에 파티션 키 포함 강제
- 인덱스 무효화 장애: 파티션 DDL 후 Global 인덱스 상태 변경(Unusable) → DDL 수행 시 `UPDATE GLOBAL INDEXES` 옵션 병행
- 특정 파티션 Hotspot: 트래픽 쏠림 현상 → Range 파티션 하위에 Hash 서브 파티션을 두는 복합 파티셔닝(Composite) 도입

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개:**
1. 시계열 데이터 파티셔닝: 대용량 로그 테이블(1TB 이상) 대상 Range 파티셔닝 적용 및 월단위 파티션 자동 생성 잡(Job) 스케줄링
2. 파티션 인덱스 최적화: 관리 독립성을 위해 Local Index를 원칙으로 하고, PK 등 글로벌 유일성 보장이 필수인 경우만 Global Index 제한적 사용
3. 데이터 아카이빙(Purge) 자동화: 보관 주기(Retention)를 초과한 파티션은 야간 배치 시간에 DDL(`DROP PARTITION`)을 통해 I/O 부하 없이 폐기

**결론 (2줄):**
- 기술사 판단: 파티셔닝은 단순한 테이블 분할이 아니라 I/O 제어와 아카이빙 효율성을 높이는 설계이므로 쿼리 패턴과 일치해야 한다.
- 향후 방향: 단일 DB의 스케일업 한계를 극복하기 위해, 점진적으로 분산 DB 환경의 수평 분할(Sharding)로 확장을 고려해야 한다.

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅱ·Ⅲ 강조 | Ⅴ·Ⅵ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "파티셔닝을 설명하시오" | 기법(Range/Hash/List) 비교, 프루닝 원리 | 파티션 인덱스(Local/Global) 장단점 |
| 요구사항 명시형 | "대용량 DB 성능향상 방안을 제시하시오" | 대용량 분할 흐름, DDL 처리 원리 | 이력 데이터 생명주기 방안, 리스크 대응 |
"""

files["112_sharding.md"] = """---
title: "샤딩 — 수평 분할 (Sharding)"
date: "2026-07-04"
tags:
  - "cspe-software"
weight: 112
---

# 📖 【암기용】 개념 완전 이해

## 한눈에
- **개요**: 같은 테이블 스키마를 가진 데이터를 여러 대의 독립적인 데이터베이스 서버(Shard)에 수평적으로 분산 저장하는 기법
- **왜 필요한가**: 단일 DB(Scale-up)로는 CPU, 메모리, 디스크 I/O의 물리적 한계에 부딪혀 수천만 명의 동시 접속과 페타바이트급 데이터를 감당할 수 없다.
- **핵심 직관**: 대형 도서관이 꽉 차면, '가~마' 저자 책은 1관에, '바~하' 책은 2관에 배치(서버 분리)하여 방문객을 분산시키는 것과 같다.

## 핵심 용어 정리

| 용어/표기 | 의미 | 비유·예 |
|:---|:---|:---|
| 샤드 (Shard) | 데이터를 쪼개어 저장하는 개별 물리적 DB 노드 | 도서관 1관, 2관 |
| 샤드 키 (Shard Key) | 데이터를 어느 샤드에 보낼지 결정하는 기준 컬럼 | '회원ID' (예: ID 1~1000은 1번 샤드) |
| 라우터 / 브로커 | 애플리케이션의 요청을 적절한 샤드로 전달해주는 중간 계층 | 안내데스크 직원 |
| 크로스 샤드 조인 | 서로 다른 샤드에 있는 데이터를 조인하는 연산 | 1관 책과 2관 책을 동시에 가져와 대조함 (매우 느림) |

## 깊이 이해
- **배경·문제의식**: RDBMS는 태생적으로 단일 서버 수직 확장(Scale-up)에 맞춰져 있다. 하지만 모바일 트래픽 폭증으로 단일 마스터 DB에 Write 부하가 집중되면 락(Lock) 경합과 디스크 한계로 시스템이 멈춘다. 이를 해결하기 위해 저렴한 범용 서버 여러 대를 묶어 쓰는 수평 확장(Scale-out)이 필수적이게 되었다.
- **작동 원리**: 애플리케이션이 데이터를 쓸 때, 샤딩 라우터가 '샤드 키'를 검사한다. 해시 함수를 거치거나 매핑 테이블을 확인하여 해당 데이터가 저장될 특정 DB 인스턴스(서버)를 찾아 연결(Connection)을 맺고 쿼리를 던진다.
- **샤딩 기법**:
  - **Range Sharding**: 1~100만은 A서버, 100만~200만은 B서버. 특정 구간에 부하가 몰릴 수 있다.
  - **Hash Sharding**: 회원ID를 해시(Hash)하여 서버 수로 나눈 나머지(Modulo)로 분배. 고르게 분산되지만, 서버를 추가/삭제할 때 기존 데이터의 해시값이 틀어져 대대적인 데이터 이동(Rebalancing)이 필요하다.
  - **Directory (Lookup) Sharding**: '라우팅 테이블'을 따로 두고, ID 5는 A서버, ID 6은 C서버식으로 자유롭게 매핑. 유연하지만 라우팅 테이블이 단일 장애점(SPOF)이 될 수 있다.
- **구체 예시**: 카카오톡이나 라인의 메시지 DB는 사용자 ID를 해시로 샤딩하여, 수만 대의 DB 서버에 사용자를 균등하게 쪼개어 초당 수백만 건의 메시지를 처리한다.
- **흔한 오해·주의점**: 샤딩을 도입하면 분산 트랜잭션(2PC) 처리와 샤드 간 조인(Join)이 사실상 불가능해진다. 따라서 샤딩은 '은탄환'이 아니며, 애플리케이션 단에서 로직을 복잡하게 변경해야 하는 최후의 수단이다.

## 연결 개념
- 파티셔닝 (Partitioning): 샤딩은 여러 서버에 분산하지만, 파티셔닝은 단일 서버 내에서 데이터를 논리/물리적으로 쪼갠다.
- 일관성 해시 (Consistent Hashing): 서버 증설 시 데이터 리밸런싱을 최소화하기 위해 해시 링(Ring)을 사용하는 기법. 해시 샤딩의 단점을 보완한다.

---

# 📝 【답안용】 시험 답안 템플릿

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 샤딩은 초대용량 데이터를 동일한 스키마를 가진 다수의 독립적인 물리적 데이터베이스 노드로 수평 분할(Scale-out)하는 분산 저장 기법이다.
> 2. **가치**: 단일 마스터 DB의 Read/Write 병목과 스토리지 한계를 극복하고, 저비용 범용 서버를 통해 선형적인 성능 확장을 제공한다.
> 3. **판단 포인트**: 샤드 키 선정에 따라 데이터 쏠림(Hotspot) 여부가 결정되며, 샤드 간 조인 제약 및 분산 트랜잭션의 복잡도를 애플리케이션 아키텍처에서 흡수해야 한다.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| 대규모 시스템 수평 확장 전략 이해 | 파티셔닝(단일 노드)과 샤딩(다중 노드)의 구조적 차이 명시 | 단순 테이블 분할 기법으로만 서술 |
| 샤딩 매커니즘과 분류 역량 | Range, Hash, Directory(Lookup) 샤딩 기법의 장단점 비교 | 해시 샤딩 시 서버 증설 리스크 누락 |
| 아키텍처 제약 및 극복 방안 제시 | Cross-shard Join 한계 및 리밸런싱 부하에 대한 대응 방안 | 분산 트랜잭션을 RDBMS처럼 쉽게 처리된다고 단정 |

> 요약: 샤딩은 단일 DB의 한계를 극복하는 강력한 수단이나, 분산 트랜잭션과 데이터 재분배(Rebalancing)라는 막대한 트레이드오프를 동반함을 논증해야 한다.

---

## Ⅰ. 개요 및 필요성

- 개요: VLDB 환경에서 동일 스키마 데이터를 다수의 물리적 독립 노드(Shard)에 분산 저장하여 처리하는 데이터베이스 수평 확장 기법
- 배경: 서비스 트래픽 폭증으로 단일 데이터베이스 서버의 Scale-up 한계(CPU, 디스크 I/O) 및 RDBMS 쓰기(Write) 병목 발생
- 필요성: 물리적 제약 탈피를 위한 Scale-out 구조 전환 및 시스템 전체의 가용성과 처리량(Throughput)의 선형적 증가 필요

---

## Ⅱ. 구조 및 구성요소

```text
Application -> Shard Router (Broker / Proxy) -> [Shard Key 분석]
                                                  |
                                                  +-> Shard Node 1 (Master/Slave)
                                                  +-> Shard Node 2 (Master/Slave)
                                                  +-> Shard Node 3 (Master/Slave)
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| 샤드 키 (Shard Key) | 데이터를 특정 샤드에 할당하기 위한 파티셔닝 기준 값 | 균등 분포도(Cardinality)가 높은 속성 선정 필수 |
| 라우터 (Router) | 애플리케이션 쿼리의 샤드 키를 분석해 적절한 샤드로 연결 | Middleware 또는 App 내장 Library(JDBC 등) 형태 |
| 샤드 노드 (Shard Node) | 분할된 데이터가 저장되는 독립적인 물리 DB 인스턴스 | 각 샤드는 자체적인 복제(Replication) 구성으로 HA 보장 |

> 요약: 애플리케이션은 투명하게 라우터에 접근하고, 라우터가 샤드 키 규칙에 따라 물리적으로 분리된 여러 샤드 노드로 요청을 분산시킨다.

---

## Ⅲ. 동작원리 및 흐름도

```text
Query Request -> Shard Key Parsing -> Routing Table Match 
-> Target Shard Connection -> Execute Query -> Result Merge & Return
```

- 1단계 [요청 인입 및 분석]: 라우터가 앱의 쿼리를 수신하고 WHERE 절에서 특정 샤드 키 값 식별
- 2단계 [라우팅 결정]: 해시 연산 또는 매핑 테이블(Directory)을 참조하여 대상 샤드 노드의 IP/Port 매핑
- 3단계 [대상 샤드 실행]: 특정 샤드 노드로 커넥션을 맺고 쿼리 전송 및 로컬 트랜잭션 실행
- 4단계 [결과 병합 반환]: 여러 샤드에 분산된 쿼리일 경우, 라우터가 각 샤드의 결과를 수합(Merge)하여 앱으로 응답

> 요약: 쿼리에 포함된 샤드 키를 기반으로 라우터가 물리적 위치를 찾아 단일/다중 샤드 쿼리를 수행하고 결과를 조합한다.

---

## Ⅳ. 특징

- 처리량 극대화: 쓰기(Write) 쿼리가 여러 서버로 완벽히 분산되어 단일 마스터의 락 경합 및 디스크 I/O 한계 극복
- 선형 확장성: 트래픽 증가 시 새로운 샤드 노드(범용 x86 서버)를 추가하여 성능과 용량을 비례하여 증설 가능
- SPOF 완화: 특정 샤드 장애 시 전체 시스템 중단이 아닌, 해당 샤드 영역의 데이터 서비스만 일시 제한되는 부분 장애로 격리

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | 해시 샤딩 (Hash) | 범위 샤딩 (Range) | 디렉터리 샤딩 (Directory) |
|:---|:---|:---|:---|
| 라우팅 방식 | 샤드 키의 해시 연산 값 (Hash Modulo) | 연속된 키 값의 구간 (예: 날짜, ID 대역) | 별도의 매핑 테이블(Lookup) 유지 |
| 장점 | 데이터가 모든 서버에 가장 균등하게 분산됨 | 특정 범위 대상의 범위 검색(Range Scan)에 유리 | 룰 적용이 자유로워 특정 서버에 동적 할당 용이 |
| 단점 | 노드 추가/삭제 시 대규모 리밸런싱 발생 | 특정 최신 대역으로 트래픽 쏠림(Hotspot) 발생 | 매핑 서버 자체가 단일 장애점(SPOF)이 될 위험 |

> 요약: 균등 분산이 핵심이면 해시 샤딩(안정성 확보 위해 Consistent Hashing 병행)을, 시계열 조회가 잦으면 범위 샤딩을 채택한다.

**리스크·대응:**
- Cross-shard Join 제약: 다른 샤드 간 조인 시 막대한 네트워크 비용 발생 → 비정규화(Denormalization) 설계 적용 및 앱 단에서 분리 조회(App Join)
- 데이터 리밸런싱 부하: 노드 증설 시 데이터 대량이동으로 인한 서비스 지연 → 초기 설계 시 논리적 샤드 개수를 충분히 크게 잡고 맵핑만 분산(Virtual Sharding)
- 분산 트랜잭션 한계: RDBMS의 ACID 보장 불가 → Saga 패턴 적용 및 보상 트랜잭션 기반의 최종 일관성(Eventual Consistency) 구조 도입

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개:**
1. 데이터 특성에 따른 샤드 키 선정: 회원 ID 등 고유 카디널리티가 높은 값을 키로 해시 샤딩을 구성하여 I/O 부하 쏠림 완전 해소
2. 무중단 확장을 위한 Consistent Hashing 적용: 노드 추가/삭제 시 인접 노드의 데이터만 이동시켜 리밸런싱 트래픽을 전체 데이터의 1/N로 통제
3. 아키텍처 재설계: Cross-shard 연산을 피하기 위해 도메인 주도 설계(DDD)를 적용해 연관 데이터를 하나의 샤드로 묶는 Bounded Context 매핑 설계

**결론 (2줄):**
- 기술사 판단: 샤딩은 획기적인 확장성을 제공하지만 쿼리 복잡도와 트랜잭션 제약이라는 큰 대가를 요구하므로, Scale-up과 Read-Replica 확장이 한계에 달했을 때 최후에 도입해야 한다.
- 향후 방향: 최근에는 애플리케이션의 복잡도를 낮추기 위해 Spanner, CockroachDB와 같은 분산 저장과 트랜잭션을 자체 지원하는 NewSQL 도입을 적극 검토해야 한다.

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅱ·Ⅲ 강조 | Ⅴ·Ⅵ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "샤딩을 설명하시오" | 샤딩 아키텍처 구성도, 라우팅 원리 | 3가지 기법 장단점, 도입 시 제약사항 |
| 방안형 | "DB 분산 트래픽 처리 방안을 제시하시오" | 파티셔닝과 샤딩 구조 비교 도식 | Cross-shard 이슈 해결 방안, 리밸런싱 방안 |
"""

files["113_database_replication.md"] = """---
title: "데이터베이스 복제 — 마스터-슬레이브·멀티마스터 (Database Replication)"
date: "2026-07-04"
tags:
  - "cspe-software"
weight: 113
---

# 📖 【암기용】 개념 완전 이해

## 한눈에
- **개요**: DB 데이터를 물리적으로 분리된 여러 서버(노드)에 실시간으로 복사하여 일치시키는 고가용성·분산 아키텍처
- **왜 필요한가**: DB 서버가 1대뿐이면 서버 고장 시 전체 서비스가 멈추고(SPOF), 트래픽이 몰릴 때 읽기 쿼리 부하를 견딜 수 없다.
- **핵심 직관**: 메인 작가(Master)가 원고를 쓰면, 보조 작가(Slave) 여러 명이 그 원고를 그대로 복사해 들고 있는다. 독자(사용자)들은 보조 작가들의 복사본을 나눠 읽어 메인 작가가 쓰는 데만 집중하게 한다.

## 핵심 용어 정리

| 용어/표기 | 의미 | 비유·예 |
|:---|:---|:---|
| Master (Primary) | 쓰기(Insert/Update/Delete) 트랜잭션을 처리하는 메인 서버 | 원본 장부를 쓰는 회계사 |
| Slave (Replica / Secondary) | Master의 변경 내역을 받아 반영하고, 읽기(Select)를 담당하는 서버 | 복사본 장부를 열람시켜주는 직원 |
| Binlog (Binary Log) | DB 변경 사항을 시간 순으로 기록한 로그 파일. 복제의 핵심 매개체 | 수정 내역이 적힌 작업 일지 |
| Replication Lag | Master에서 변경된 데이터가 Slave에 반영되기까지 걸리는 지연 시간 | 복사본이 아직 원본을 못 따라간 상태 |

## 깊이 이해
- **배경·문제의식**: 대부분의 웹 서비스는 읽기(Read)와 쓰기(Write) 비율이 8:2 또는 9:1로 읽기 요청이 압도적이다. 단일 DB로 쓰기와 수많은 읽기를 동시 처리하면 락(Lock) 경합과 자원 고갈이 발생한다.
- **작동 원리 (Master-Slave 기준)**:
  1. 클라이언트가 Master에 데이터를 INSERT하면, Master는 변경 사항을 자신의 Binlog에 기록한다.
  2. Slave의 I/O 쓰레드가 Master의 Binlog를 읽어와 자신의 Relay Log에 복사한다.
  3. Slave의 SQL 쓰레드가 Relay Log의 이벤트를 순차적으로 실행(Replay)하여 데이터를 똑같이 맞춘다.
- **복제 아키텍처 분류**:
  - **Master-Slave (Single Master)**: 쓰기는 1대, 읽기는 여러 대. 구조가 단순하고 정합성 유지가 쉽지만, 쓰기 확장은 불가능하다.
  - **Multi-Master**: 여러 대의 Master가 쓰기와 읽기를 모두 처리하고 서로 복제. 쓰기 확장이 가능하지만, 동시에 같은 데이터를 수정할 때 '충돌(Conflict)'이 발생하여 관리가 매우 까다롭다.
- **동기화 방식 (Sync vs Async)**:
  - **비동기(Asynchronous)**: Master가 Binlog만 쓰고 바로 클라이언트에 응답. 속도는 빠르나 Master 장애 시 데이터 유실 가능. (일반적 사용)
  - **반동기(Semi-synchronous)**: 최소 1대의 Slave가 로그를 받았다고 응답할 때까지 대기. 데이터 유실 방지와 속도의 타협점.
- **비유**: 은행 앱에서 송금을 완료(Master 쓰기)하고 바로 잔액을 확인했는데 이전 금액이 보이다가, 1초 뒤에 다시 새로고침하니 제대로 된 금액(Slave 반영 완료)이 보이는 현상이 Replication Lag 때문이다.
- **흔한 오해·주의점**: Multi-Master는 완벽해 보이지만, 양쪽 서버 네트워크가 단절된 상태(Split-Brain)에서 각자 쓰기를 받으면 나중에 데이터를 합칠 때 데이터 정합성이 박살 난다. 그래서 대부분 Master-Slave + 자동 페일오버(Auto Failover) 구조를 쓴다.

## 연결 개념
- 로드 밸런싱 (Load Balancing): 클라이언트의 Read 요청을 여러 대의 Slave로 분배해주는 기술 (예: ProxySQL, HAProxy)
- CQRS (Command Query Responsibility Segregation): 쓰기 모델(Command)과 읽기 모델(Query)을 애플리케이션 레벨에서 분리하는 패턴. 복제 아키텍처와 시너지를 낸다.

---

# 📝 【답안용】 시험 답안 템플릿

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 데이터베이스 복제는 변경 데이터를 여러 노드에 동기화하여 고가용성(HA)을 확보하고 읽기 부하를 분산(Scale-out)시키는 아키텍처다.
> 2. **가치**: 쓰기(Write)와 읽기(Read) 경로를 분리하여 단일 노드의 트랜잭션 경합을 완화하고, 메인 서버 장애 시 레플리카 승격을 통해 RTO를 획기적으로 단축한다.
> 3. **판단 포인트**: Master-Slave 구조의 Replication Lag(최신성 불일치)와 Multi-Master 구조의 충돌(Conflict) 해결 복잡도 사이에서 서비스 특성에 맞는 토폴로지와 동기화 수준(비동기/반동기)을 선택해야 한다.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| DB 고가용성 및 스케일아웃 전략 이해 | Master-Slave 분산을 통한 읽기 성능 향상 메커니즘 | 물리적 복제 구조만 쓰고 트랜잭션 분리(CQRS) 누락 |
| 복제 토폴로지 비교 역량 | Master-Slave와 Multi-Master의 충돌 제어 트레이드오프 | Multi-Master가 무조건 좋다는 단정 |
| 복제 지연 및 장애 극복 한계 인지 | Replication Lag 원인과 Asnyc/Semi-sync 동기화 수준 | 비동기 복제에서 데이터 유실이 없다고 착각 |

> 요약: 복제의 근본 목적이 부하 분산과 가용성 확보에 있음을 명시하고, Binlog 기반 동작 원리와 복제 지연(Lag)이라는 한계점 극복 방안을 제시해야 한다.

---

## Ⅰ. 개요 및 필요성

- 개요: 마스터 노드의 데이터 변경(트랜잭션) 내역을 로그 기반으로 다른 슬레이브 노드들에 동기화하여 동일한 상태를 유지하는 기법
- 배경: 웹 서비스 특성상 Read/Write 비율이 극단적(8:2 등)이어서, 단일 DB 인스턴스에 읽기 쿼리가 집중되면 I/O 병목 및 Lock 경합 유발
- 필요성: 단일 장애점(SPOF) 제거를 통한 고가용성 확보 및 슬레이브 노드를 통한 읽기 쿼리 수평 확장(Scale-out) 구현 필요

---

## Ⅱ. 구조 및 구성요소

```text
Client (Write) -> Master DB -> [Binlog] -> Network
                                              |
Client (Read)  <- Slave DB 1 <- [Relay Log] <-+ (I/O Thread가 수신)
Client (Read)  <- Slave DB 2 <- [Relay Log] <-+
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| Master Node | 클라이언트의 쓰기(Insert/Update/Delete) 트랜잭션 처리 원본 | 변경 사항을 직렬화하여 Binlog에 기록 |
| Slave (Replica) Node | Master의 로그를 복제해 적용하고 클라이언트 읽기 쿼리 전담 | Master 장애 시 프로모션(승격) 대상 |
| Binlog / Relay Log | 데이터 변경 이벤트(DDL/DML)가 기록되는 바이너리 파일 | Statement(쿼리) 방식과 Row(데이터) 방식 존재 |

> 요약: 쓰기를 전담하는 Master에서 생성된 Binlog를 네트워크를 통해 Slave가 수신하여 로컬 Relay Log에 적재 후 재실행(Replay)하는 구조이다.

---

## Ⅲ. 동작원리 및 흐름도

```text
Master Commit -> Binlog Write -> Slave I/O Thread 수신 -> Relay Log Write
-> Slave SQL Thread Replay -> Data Apply -> Slave Read 쿼리 제공
```

- 1단계 [트랜잭션 발생 및 기록]: Master에서 트랜잭션 커밋 시 변경 내역을 자체 디스크의 Binlog(Binary Log)에 기록
- 2단계 [로그 전송 및 수신]: Slave의 I/O 쓰레드가 Master에 접속하여 갱신된 Binlog 이벤트를 지속적으로 폴링/푸시 방식으로 수신
- 3단계 [Relay Log 적재]: 수신한 변경 이벤트를 Slave의 로컬 디스크인 Relay Log에 순차적으로 기록
- 4단계 [데이터 반영(Replay)]: Slave의 SQL 쓰레드가 Relay Log를 읽어 실제 스토리지 엔진에 변경 사항 적용 및 읽기 서비스 제공

> 요약: 복제는 동기적인 디스크 쓰기와 비동기적인 네트워크 전송, 그리고 쓰레드 분리를 통한 릴레이 반영으로 마스터의 부하를 최소화하며 진행된다.

---

## Ⅳ. 특징

- 읽기 수평 확장: 읽기(Read) 요청을 다수의 Slave로 로드밸런싱하여 시스템 전체의 초당 처리량(TPS/QPS)을 선형적으로 증대
- 고가용성 및 재해 복구: Master 장애 시 즉시 최신 Slave를 신규 Master로 승격(Failover)시켜 수 분(RTO) 내 서비스 정상화 보장
- 온라인 백업 유연성: Master에 락(Lock)을 걸지 않고, 특정 Slave 노드에서 스냅샷 백업 및 통계 배치를 수행하여 메인 서비스 간섭 배제

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | 단방향 (Master-Slave) | 양방향 (Multi-Master) | 판단 기준 |
|:---|:---|:---|:---|
| 트랜잭션 흐름 | Master에서 Slave로 단방향 흐름 | 양쪽 노드에서 모두 Read/Write 허용 | 쓰기 트래픽의 수평 확장 필요성 |
| 정합성 관리 | 구조가 단순하여 충돌 우려 없음 | 동시 업데이트 시 충돌(Conflict) 해결 로직 필수 | 데이터 일관성 요구 수준 |
| 네트워크 단절 | Slave 지연만 발생, 서비스 정상 | Split-Brain 발생 시 양쪽 데이터 병합 난이도 극상 | 장애 시 복구 복잡도 트레이드오프 |

> 요약: 일반적인 대규모 웹 서비스는 안정성이 높은 Master-Slave를 채택하며, Multi-Master는 충돌 회피가 완벽히 설계된 특정 도메인에 제한 적용한다.

**리스크·대응:**
- Replication Lag 현상: 트래픽 폭증 시 Slave 반영 지연으로 최신 데이터 조회 실패 → 사용자 쓰기 직후 자신의 데이터는 강제로 Master에서 조회하는 라우팅 로직 적용
- 비동기 복제 중 Master 장애: 전송되지 않은 Binlog 유실 발생 → 정합성이 핵심인 금융 도메인은 Semi-synchronous(반동기) 복제 적용
- 다중 Slave 로드밸런싱 문제: 특정 Slave 쏠림 발생 → ProxySQL, HAProxy 등 DB L7 프록시를 활용한 헬스체크 및 Connection 분배

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개:**
1. CQRS 기반 라우팅 분리: 애플리케이션 레벨(Spring의 @Transactional(readOnly=true) 등)에서 쿼리 유형을 판단해 Read는 Slave 그룹으로, Write는 Master로 분리 전송
2. HA 통합 클러스터링 구축: MHA(Master High Availability) 또는 오케스트레이터(Orchestrator) 툴을 연동하여, Master 노드 헬스체크 실패 시 VIP(Virtual IP) 스위칭을 통한 자동 페일오버 체계 확립
3. 지연(Lag) 모니터링: Prometheus를 활용해 Seconds_Behind_Master 메트릭이 1초를 초과할 경우 알람을 발생시키고, 임계치 초과 시 해당 Slave를 라우팅 풀에서 자동 제외

**결론 (2줄):**
- 기술사 판단: 복제 토폴로지는 단순한 데이터 백업이 아니라 C(Consistency)와 A(Availability) 간의 트레이드오프 설계이므로, 비동기의 지연 위험과 반동기의 성능 저하를 업무 중요도에 따라 믹스해야 한다.
- 향후 방향: 최근 클라우드 네이티브 환경에서는 스토리지 계층 자체를 공유해 복제 지연을 원천 제거하는 Aurora DB 방식이나 뗏목(Raft) 합의 알고리즘 기반 분산 DB 전환이 가속화되고 있다.

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅱ·Ⅲ 강조 | Ⅴ·Ⅵ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "DB 복제(Replication)를 설명하시오" | Binlog 기반 동기화 흐름도, 쓰레드 역할 | Master-Slave/Multi 비교, Sync/Async 차이 |
| 요구사항 명시형 | "고가용성 DB 클러스터링 설계 방안을 제시하시오" | Read/Write 트래픽 분리 아키텍처 도식 | 페일오버(Failover) 기준, Split-Brain 리스크 대응 |
"""

files["114_cache_layer.md"] = """---
title: "캐시 계층 — Redis·Memcached (Cache Layer)"
date: "2026-07-04"
tags:
  - "cspe-software"
weight: 114
---

# 📖 【암기용】 개념 완전 이해

## 한눈에
- **개요**: 데이터베이스 앞단에 인메모리(In-Memory) 저장소를 두어 반복적인 읽기 연산의 지연 시간을 극단적으로 단축하는 아키텍처 패턴
- **왜 필요한가**: 디스크 기반의 RDBMS는 트래픽이 폭증할 때 수십 밀리초(ms)의 I/O 병목이 발생해 시스템이 다운된다. 
- **핵심 직관**: 식당 주방장(DB)에게 매번 물어보는 대신, 홀 매니저(Cache)가 자주 찾는 인기 메뉴의 재고를 메모장에 적어두고 손님에게 0.1초 만에 바로 대답해주는 원리.

## 핵심 용어 정리

| 용어/표기 | 의미 | 비유·예 |
|:---|:---|:---|
| Cache Hit | 요청한 데이터가 캐시에 존재하여 DB 접근 없이 즉시 반환됨 | "매니저가 답을 알고 있음" (빠름) |
| Cache Miss | 캐시에 데이터가 없어 최종적으로 DB까지 조회해야 함 | "매니저가 몰라서 주방에 다녀옴" (느림) |
| TTL (Time To Live) | 캐시에 저장된 데이터의 유효 기간(만료 시간) | "메모장의 정보는 5분 뒤 폐기" |
| Eviction Policy | 캐시 메모리가 가득 찼을 때 어떤 데이터를 버릴지 결정하는 정책 | LRU (가장 안 찾은 것 버림), LFU |

## 깊이 이해
- **배경·문제의식**: 서비스 이용자의 80%는 20%의 데이터(예: 메인페이지 뉴스, 베스트 상품)만 반복 조회한다(파레토 법칙). 이 모든 요청을 디스크를 도는 DB가 처리하면 서버 비용이 기하급수적으로 늘고 병목이 생긴다.
- **작동 원리**: 메모리(RAM)의 읽기 속도는 디스크(SSD)보다 수백~수천 배 빠르다. Redis나 Memcached 같은 인메모리 NoSQL을 DB 앞단에 프록시처럼 배치하여, 한 번 읽은 결과를 Key-Value 형태로 저장해둔다.
- **캐시 읽기/쓰기 전략 (패턴)**:
  - **Cache-Aside (Look-Aside)**: 앱이 캐시를 먼저 확인(Hit)하고 없으면(Miss) DB에서 읽어와 캐시에 쓴다. 가장 흔한 읽기 전략.
  - **Write-Through**: 앱이 데이터를 쓸 때 캐시와 DB 모두에 동시 업데이트. 데이터 정합성은 좋으나 쓰기 지연 증가.
  - **Write-Behind (Write-Back)**: 앱이 일단 캐시에만 쓰고 응답. 나중에 배치로 DB에 모아서 기록. 속도는 최고이나 캐시 장애 시 데이터 날아감.
- **Redis vs Memcached**: Memcached는 단순 문자열 캐싱에 특화된 가벼운 구조이고, Redis는 리스트, 셋, 해시 등 다양한 자료구조를 지원하며 스냅샷(백업) 기능까지 갖추어 사실상 표준으로 쓰인다.
- **비유**: 브라우저 임시 파일 캐시. 매번 똑같은 로고 이미지를 서버에서 다운로드하지 않고 내 컴퓨터 메모리에서 불러와 로딩 속도를 높이는 것과 본질적으로 같다.
- **흔한 오해·주의점**: 무조건 캐시를 둔다고 빨라지지 않는다. '사용자 개인정보'처럼 한 번만 조회되고 말 데이터는 캐시에 넣으면 메모리만 낭비하고 Cache Hit율이 바닥을 쳐(Cache Miss 발생) DB 조회 비용에 캐시 접근 비용까지 더해져 더 느려진다.

## 연결 개념
- 캐시 스탬피드 (Cache Stampede): 인기 데이터의 만료(TTL) 시점에 수만 건의 요청이 동시에 DB로 쏟아져 DB가 죽는 현상. (Thundering Herd)
- 최종 일관성 (Eventual Consistency): DB 값이 바뀌었을 때 캐시가 즉각 반영 안 될 수 있으나, TTL 만료 후 결국 맞춰지는 개념.

---

# 📝 【답안용】 시험 답안 템플릿

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: 캐시 계층은 영구 저장소(DB) 앞단에 초고속 인메모리 데이터 스토어(Redis/Memcached)를 배치하여 I/O 레이턴시를 마이크로초(μs) 단위로 단축하는 패턴이다.
> 2. **가치**: 파레토 법칙(80/20)이 적용되는 읽기 헤비(Read-heavy) 환경에서 Cache Hit 비율을 높여 백엔드 DB의 과부하를 막고 시스템 전체의 Throughput을 방어한다.
> 3. **판단 포인트**: 도입 시 데이터 성격(변동성, 조회 빈도)에 따른 적절한 읽기/쓰기 전략(Cache-Aside, Write-Through) 선택과 스탬피드 현상을 막기 위한 TTL 분산 설계가 핵심이다.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| 대규모 트래픽 아키텍처 패턴 이해 | 인메모리 I/O의 속도 이점 및 Cache Hit/Miss 메커니즘 | 로컬 캐시와 분산 캐시(글로벌)의 용도 혼동 |
| 데이터 동기화 및 캐시 전략 판단 | Look-Aside(읽기)와 Write-Through/Behind(쓰기) 비교 | 캐시를 쓰면 무조건 데이터가 일치한다고 단정 |
| 운영 환경의 리스크 관리 역량 | Cache Stampede 현상 원인과 Jitter(난수) 기반 TTL 분산 대응 | 메모리 Eviction(LRU) 설정 누락 |

> 요약: 캐시는 획기적인 속도 향상을 주지만 데이터 원본(DB)과의 정합성 불일치 리스크를 동반하므로, 이를 통제하는 아키텍처 전략(전략 패턴, 만료 정책)을 함께 제시해야 한다.

---

## Ⅰ. 개요 및 필요성

- 개요: 디스크 기반 데이터베이스의 응답 속도 한계를 극복하기 위해 앞단에 In-Memory 캐시(Redis 등)를 두어 빠른 접근을 제공하는 계층
- 배경: 서비스 규모 확장에 따라 반복적이고 동일한 읽기 요청이 RDBMS에 집중되어 Lock 대기 및 커넥션 풀 고갈 현상 심화
- 필요성: 조회 쿼리의 응답 시간을 수십 ms에서 1ms 이하로 단축하고, DB의 Scale-up 비용을 오픈소스 기반 Scale-out으로 대체 절감 필요

---

## Ⅱ. 구조 및 구성요소

```text
Application Client
   | (1) Read Request
   v
[ Cache Layer (Redis / Memcached) ] --(2) Cache Hit 시 즉시 반환--> App
   | (3) Cache Miss 시
   v
[ Primary Database (RDBMS) ] ---------(4) Data Fetch 후 캐시에 적재 (Look-Aside)
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| Redis / Memcached | Key-Value 형태의 초고속 In-Memory 데이터 스토어 | Redis는 자료구조 및 영속성(AOF/RDB) 지원 |
| TTL (Time to Live) | 캐시 항목의 유효 만료 시간 지정 메커니즘 | 데이터 정합성 보장의 1차 방어선 |
| Eviction Policy | 메모리 부족 시 오래된 데이터를 밀어내는 축출 정책 | 통상 LRU(Least Recently Used) 정책 적용 |

> 요약: 애플리케이션은 원본 DB 접근 전 항상 캐시 계층을 먼저 프록시처럼 조회하며, Hit/Miss 여부에 따라 I/O 경로가 동적으로 결정된다.

---

## Ⅲ. 동작원리 및 흐름도

```text
App Request -> Cache 존재 확인 -> (Hit) -> 캐시 데이터 반환 
                               -> (Miss) -> DB 쿼리 실행 -> 데이터 캐시 기록(TTL 설정) -> 반환
```

- 1단계 [캐시 조회]: 애플리케이션이 특정 Key로 Redis 등 캐시 서버에 데이터 조회를 먼저 요청 (Cache Hit율 목표 80% 이상)
- 2단계 [상태 분기]: 데이터가 유효하면 즉시 반환(Hit), 없거나 만료되었으면(Miss) DB로 쿼리 경로 우회
- 3단계 [DB 패치 및 적재]: DB에서 원본 데이터를 조회한 후, 다음 요청을 위해 캐시 서버에 Write(통상 Cache-Aside 패턴)
- 4단계 [만료 및 축출]: 지정된 TTL이 지나면 캐시에서 자동 삭제되며 데이터 최신화 사이클 반복

> 요약: 조회 빈도가 높은 핫 데이터(Hot Data)를 메모리에 상주시켜 DB 디스크 접근을 논리적으로 차단(Pruning)하는 사이클로 동작한다.

---

## Ⅳ. 특징

- 초고속 응답성: 디스크 I/O 없이 메모리 직접 접근을 통해 수십만 QPS(Queries Per Second) 트래픽을 단일 노드에서 1ms 이내로 처리
- 백엔드 보호 (Offloading): 메인 DB로 향하는 읽기 트래픽의 80~90%를 차단하여, DB는 쓰기 트랜잭션과 복잡한 조인 연산에 자원 집중 가능
- 휘발성 한계: 본질적으로 메모리 기반이므로 서버 재부팅 시 데이터가 유실(Cold Start)되며, 워밍업 전 초기 부하 발생 위험 내포

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | Cache-Aside (Look-Aside) | Write-Through | Write-Behind (Write-Back) |
|:---|:---|:---|:---|
| 트랜잭션 흐름 | 읽기 시 앱이 캐시/DB 직접 조율 | 쓰기 시 캐시와 DB에 동시 동기 업데이트 | 캐시에 일단 쓰고, 비동기 배치로 DB 적재 |
| 장점 | 구조가 단순하고 캐시 장애가 DB에 치명타 안 줌 | 캐시와 DB 간의 강한 데이터 정합성 보장 | 쓰기 성능 극대화 (DB Insert 병목 회피) |
| 단점 | 캐시 Miss 시 DB 조회+캐시 적재로 지연(Delay) 발생 | 매 쓰기마다 두 번의 I/O 발생으로 지연 증가 | 캐시 장애/재부팅 시 비동기 큐 데이터 완전 유실 |

> 요약: 일반적인 웹 환경은 조회 성능 극대화를 위해 Cache-Aside를 기본 적용하며, 강한 일관성이 필요 시 Write-Through를 선택한다.

**리스크·대응:**
- Cache Stampede (캐시 스탬피드): 핫 키 만료 순간 수만 개 스레드가 동시에 DB 조회 및 캐시 갱신 시도 → TTL에 난수(Jitter)를 부여해 만료 시간 분산(예: 60분 ± 5분) 및 PER(Probabilistic Early Recomputation) 기법 적용
- 데이터 정합성 불일치: DB 수정 후 캐시가 미갱신되어 Stale 데이터 노출 → 원본 갱신 시 캐시 무효화(Invalidation) 이벤트 발행(CDC 연동)
- OOM (Out Of Memory) 및 Eviction 부하: 메모리 풀 스케일오프 → Maxmemory 제한 설정 및 allkeys-lru 정책으로 핫 데이터만 유지

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개:**
1. 아키텍처 다층 캐싱: 변동성이 극히 낮은 공통 코드는 앱 내부 로컬 캐시(Ehcache)로 1차 처리하고, 유저 세션·글로벌 데이터는 분산 캐시(Redis)로 2차 배치
2. Redis Cluster 구성 적용: 단일 노드 SPOF 방지를 위해 Sentinel 기반의 Failover 환경 또는 해시 슬롯(Hash Slot)을 활용한 Redis Cluster로 노드 분산 확장
3. 퍼포먼스 튜닝: 빅 키(Big Key, 수 MB 크기 객체)는 네트워크 병목을 유발하므로, 객체를 직렬화(Protobuf 등)하여 100KB 이하로 압축 저장하는 규칙 적용

**결론 (2줄):**
- 기술사 판단: 캐시 계층은 가성비 최고의 Scale-out 수단이지만, 필연적으로 발생하는 '일시적 정합성 불일치'를 비즈니스가 허용 가능한 수준으로 제어(통제)하는 전략이 아키텍처의 핵심이다.
- 향후 방향: 최근에는 캐시와 데이터베이스의 경계가 무너져, Redis 자체가 Primary DB 역할을 하거나 DB(예: Aurora) 내부에 캐싱이 투명하게 내장되는 형태로 진화하고 있다.

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅱ·Ⅲ 강조 | Ⅴ·Ⅵ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "캐시 적용 패턴을 설명하시오" | Read/Write 캐싱 패턴 구조도, Hit/Miss 흐름 | 패턴별 장단점 비교, 메모리 관리(Eviction) |
| 방안형 | "이벤트 트래픽 폭증 시 DB 보호 방안" | Cache-Aside 통제 흐름, 대기열 구조 | Stampede 원인과 극복, Jitter 분산 방안 |
"""

files["115_db_capacity_planning.md"] = """---
title: "데이터베이스 용량 산정 (DB Capacity Planning)"
date: "2026-07-04"
tags:
  - "cspe-software"
weight: 115
---

# 📖 【암기용】 개념 완전 이해

## 한눈에
- **개요**: 시스템 구축·운영 시 데이터베이스가 요구하는 적정 스토리지 크기와 메모리/CPU 자원을 수학적으로 예측하고 계획하는 활동
- **왜 필요한가**: DB 스토리지 부족은 단순한 저장 실패를 넘어 시스템 전체 다운으로 이어지며, 반대로 너무 크게 잡으면 막대한 클라우드 비용 낭비가 발생한다.
- **핵심 직관**: 이삿짐 센터를 부를 때, 가구 크기(레코드 사이즈) × 가구 개수(로우 수)를 계산하고, 버릴 물건(삭제)과 1년 뒤 살림 늘어날 것(연간 증가율)을 예측하여 적정 크기의 트럭(디스크)을 계약하는 것과 같다.

## 핵심 용어 정리

| 용어/표기 | 의미 | 비유·예 |
|:---|:---|:---|
| 로우/레코드 길이 | 테이블 1줄(Row)이 차지하는 바이트 수 | "책 1권의 두께" |
| 보존 기간 (Retention) | 데이터를 DB에 유지해야 하는 기간 | "서류 보관 연한(3년)" |
| 오버헤드 (Overhead) | 순수 데이터 외에 DBMS가 쓰는 관리용 공간 | "포장지 크기와 빈 공간" |
| 연간 증가율 | 매년 데이터가 늘어나는 퍼센티지 추정치 | "매년 살림 20% 증가" |

## 깊이 이해
- **배경·문제의식**: 개발 초기에는 테이블 스키마만 있고 데이터가 비어 있다. 시스템 오픈 후 3년, 5년이 지났을 때 디스크가 꽉 차지 않도록 물리적 H/W 도입 예산을 타거나 클라우드 볼륨(EBS) 사이즈를 할당하려면 과학적 근거가 필요하다.
- **산정 방식 (상향식 원리)**:
  1. 각 컬럼 데이터 타입의 바이트 수를 모두 더해 '초기 레코드 크기'를 구한다 (예: INT 4바이트 + VARCHAR 50바이트 = 54바이트).
  2. 여기에 발생 예상 건수를 곱해 '테이블 순수 데이터 용량'을 구한다 (예: 54B × 100만 건 = 54MB).
  3. 인덱스는 별도로 공간을 차지하므로, 생성될 인덱스 크기를 추정하여 더한다.
  4. 블록 헤더, 여유 공간(PCTFREE) 등 DBMS 구조적 오버헤드(보통 30~40%)를 얹는다.
  5. 최종적으로 연간 증가율(예: 년 20%)을 적용해 향후 3~5년 치 목표 디스크 용량을 확정한다.
- **비유**: 월 100만 건의 결제가 발생하는 앱이라면, 1건(100Byte) × 100만 × 12개월 × 5년 = 약 6GB. 여기에 인덱스 3GB, 시스템 공간 1GB 얹고, 넉넉히 2배수 적용해 20GB 디스크를 준비하는 논리 전개다.
- **구체 예시**: `VARCHAR(100)`은 100바이트를 꽉 채우지 않는 가변 길이이므로 산정 시 평균 채워짐(예: 50%)을 가정하여 50바이트로 계산하는 것이 실무적 노하우다.
- **흔한 오해·주의점**: 데이터베이스 용량은 단순히 '테이블 데이터'만의 총합이 아니다. 실제 디스크는 Undo/Redo 트랜잭션 로그, 임시 테이블스페이스(Temp), 인덱스 등이 데이터보다 더 많은 공간을 차지하기도 한다.

## 연결 개념
- 파티셔닝 / 데이터 아카이빙: 무한정 늘어나는 용량을 제어하기 위해 오래된 데이터를 삭제하거나 값싼 오브젝트 스토리지로 내리는 아키텍처 연계 필수
- 데이터 타입 최적화: 용량 산정 중 INT(4B) 대신 TINYINT(1B)로 줄이는 튜닝을 선행하면 전체 TCO(총소유비용)를 수억 원 절감할 수 있다.

---

# 📝 【답안용】 시험 답안 템플릿

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: DB 용량 산정은 논리적 데이터 모델을 물리적 스토리지 크기로 치환하여, 향후 시스템의 수명주기 동안 안정적인 I/O와 무중단 서비스를 보장하기 위한 자원 계획 활동이다.
> 2. **가치**: 감(Guessing)이 아닌 수리적 근거(레코드 길이 × 트랜잭션 건수 × 증가율)를 바탕으로 오버프로비저닝에 따른 예산 낭비를 막고 장애 리스크를 사전 차단한다.
> 3. **판단 포인트**: 순수 데이터 크기뿐 아니라 인덱스, 트랜잭션 로그(Redo/Undo), 블록 오버헤드, 임시 영역(Temp) 등 DBMS 고유의 부가 스토리지 요구량을 반드시 합산 설계해야 한다.

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| 물리 DB 설계 및 용량 예측 역량 | 기초데이터(로우) -> 테이블 -> 인덱스 -> 시스템 합산 절차 | 인덱스 및 시스템 오버헤드(Redo/Temp 등) 고려 누락 |
| 실무 가변 요인 반영 능력 | 데이터 보존 주기(Retention), 연간 증가율, VARCHAR 평균 길이 | 테이블 크기만 단순 곱셈으로 산정함 |
| 클라우드/현대 아키텍처 연계 | 아카이빙 전략 및 스케일아웃(분산) 고려 | 전통적 H/W 도입 시각에만 매몰된 결론 |

> 요약: 논리 모델을 물리 볼륨으로 산출하는 상향식(Bottom-up) 계산식의 타당성을 제시하고, 데이터 폭증에 대비한 성능·관리 측면의 버퍼(Buffer) 및 아카이빙 전략을 포함해야 한다.

---

## Ⅰ. 개요 및 필요성

- 개요: 서비스 요구사항과 논리 스키마를 바탕으로 테이블, 인덱스, 시스템 관리 영역의 목표 물리 스토리지 및 메모리 용량을 추정하는 작업
- 배경: 서비스 오픈 후 트래픽 폭증 시 디스크 풀(Disk Full) 장애로 인한 시스템 마비 및 추가 증설에 따른 다운타임 리스크 상존
- 필요성: 도입 예산(TCO) 최적화와 안정적인 트랜잭션 처리를 위해 수리적 모델 기반의 적정 디스크 확보와 인프라 계획 수립 필요

---

## Ⅱ. 구조 및 구성요소

```text
[논리 모델] -> [기초 용량] -> [테이블+인덱스] -> [오버헤드 반영] -> [최종 용량]
 (컬럼 타입)    (레코드 크기)    (건수/증가율 적용)  (System/Log/Temp)    (여유율 적용)
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| 테이블 데이터 영역 | 업무 실 데이터가 저장되는 순수 공간 | 초기 데이터 건수 및 월/년 트랜잭션 증가 건수 |
| 인덱스 영역 | 고속 검색을 위한 B-Tree 등 부가 데이터 공간 | 통상 데이터 영역의 30~50% 수준 배정 |
| 시스템 / 로그 영역 | Redo/Undo/Temp 등 트랜잭션 처리 지원 공간 | 통계 작업이나 대량 정렬(Sort) 시 Temp 영역 팽창 주의 |

> 요약: 전체 DB 용량은 단순 테이블 총합이 아니라 `(데이터 + 인덱스 + DBMS 메타 + 트랜잭션 로그) × 안전 여유율`의 복합 구조체다.

---

## Ⅲ. 동작원리 및 흐름도

```text
업무량 분석(건수 예측) -> Row 길이 산정 -> 초기 용량 도출
-> 증가율 및 보존 기간 반영 -> 인덱스 및 오버헤드 합산 -> 최종 스토리지 결정
```

- 1단계 [기초 메트릭 수집]: 논리 ERD를 분석해 각 테이블의 로우 길이 산정 (가변길이 VARCHAR는 평균 50~70% 율 적용 계산)
- 2단계 [순수 데이터 산출]: `로우 길이 × (초기 건수 + (일 발생 건수 × 보존일수))` 식으로 기준 테이블 용량 계산
- 3단계 [인덱스 용량 산출]: 테이블별 생성될 인덱스 컬럼 길이 합산 및 B-Tree 구조의 여유 공간(PCTFREE 보통 10~20%) 적용
- 4단계 [버퍼 및 최종 확정]: 시스템(Temp/Undo) 마진 30%, 연간 증가 예상치(예: 연 20% × 5년)를 가산하여 최종 물리 볼륨 확정

> 요약: 가장 작은 단위인 레코드 바이트 계산에서 시작하여 보존 주기, 인덱스, 시스템 오버헤드라는 배수를 단계별로 곱해 나가는 상향식 절차다.

---

## Ⅳ. 특징

- 비용-성능 트레이드오프: 디스크를 크게 잡으면 가용성은 보장되나 클라우드 EBS/SAN 비용이 낭비되며, 타이트하게 잡으면 장애 확률이 급증
- 정합성 보정 필수: LOB(Large Object, 텍스트/이미지) 컬럼이나 JSON 데이터 타입은 길이 편차가 극심하여 평균 추정 오류가 잦음
- 동적 변화: 비즈니스 성공 여부에 따라 일 트랜잭션 증가율이 최초 예측(예: 연 10%)을 아득히 초과(월 100% 등)할 수 있는 불확실성 내포

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | 보수적 산정 (안정성 중시) | 효율적 산정 (비용 중시) | 선택 기준 |
|:---|:---|:---|:---|
| 산정 기준치 | MAX(최대) 값 기준 (VARCHAR 풀 사이즈) | AVG(평균) 값 기준 + 타이트한 마진 | 서비스 중단 시 비즈니스 타격 규모 (금융 vs 커뮤니티) |
| 버퍼 여유율 | 연간 산출 결과의 200% 이상 할당 | 산출 결과의 120~130% 할당 | 클라우드(동적 확장 용이) vs 온프레미스(증설 어려움) |
| 보존 전략 | 전체 데이터 무기한 본DB 보존 (All-in-one) | 파티셔닝/오브젝트 스토리지 연계 아카이빙 | 규제 컴플라이언스(보존 의무) 및 비용 예산 |

> 요약: 전통적 온프레미스는 장비 증설이 어려워 보수적으로 크게 잡고, 클라우드 환경에서는 효율적 산정 후 Auto-scaling(볼륨 확장)에 의존하는 추세다.

**리스크·대 단 및 점검 지표:**
- 로그 파일 팽창 통제 실패: 야간 대량 배치 시 Undo/Temp 영역 급팽창으로 서비스 장애 → 산정 시 일괄 작업(Batch)의 최대 트랜잭션 롤백 사이즈 고려
- 인덱스 크기 역전: 튜닝 과정에서 인덱스가 남발되어 데이터보다 커짐 → 설계 단계에서 인덱스 컬럼 크기 제한 및 복합 인덱스 통폐합
- 예상 외 트래픽 폭증: 예측치 붕괴 → 스토리지 임계치(80%) 도달 시 알람 체계 수립 및 무중단 볼륨 확장(Online Resize) 플랜 사전 테스트

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개:**
1. 아카이빙(Archiving) 연계 설계: 3년 치 전체를 고비용 NVMe에 담지 않고, 최근 1년만 운영 DB에 두고 과거 데이터는 파티션 단위로 Drop하여 저렴한 S3/Hadoop 기반 Data Lake로 이관
2. 데이터 타입 최적화(Diet): 용량 산정 전 `BIGINT(8B)`를 `INT(4B)`로, 불필요한 `VARCHAR(100)`을 `VARCHAR(30)`으로 축소하는 스키마 다이어트 수행으로 기준 바이트 30% 감축
3. 클라우드 탄력성 활용: 초기부터 5년 치 목표 용량을 100% 사전 프로비저닝하지 않고, 초기 2년 치만 할당한 뒤 AWS EBS Elastic Volumes 등을 활용해 임계치 접근 시 무중단 자동 증설

**결론 (2줄):**
- 기술사 판단: DB 용량 산정은 단순한 곱셈 기교가 아니라, 데이터의 생명주기(발생-활용-보관-폐기)를 설계하고 시스템의 안정성과 예산의 균형을 맞추는 아키텍처 통제 행위다.
- 향후 방향: 최근 클라우드 네이티브 스토리지(Aurora 등) 환경에서는 용량이 테라바이트 단위로 자동 증가하므로, 예측 모델링의 초점이 "디스크 크기 확보"에서 "과금 제어 및 쿼리 비용 최적화(FinOps)"로 이동하고 있다.

### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅱ·Ⅲ 강조 | Ⅴ·Ⅵ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "DB 용량 산정 절차를 설명하시오" | 기초데이터 -> 최종용량 계산 단계 도식 | 데이터/인덱스/시스템 버퍼 산출 기준 |
| 방안형 | "DB 구축 시 비용 및 자원 최적화 방안" | 용량 다이어트, 타입 최적화 원리 | 아카이빙 연계 전략, 클라우드 확장 전략 제시 |
"""
