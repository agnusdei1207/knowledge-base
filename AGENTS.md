현재 포인터: 06과목 030

# 노트 전면 재작성·교정 지시

## 0. 시작 전에 반드시 할 일

1. **작성 규칙 정본은 이 저장소 안에 없다.** 진입점 경로는 `AGENTS.local.md`에 적혀 있다. 그 파일을 먼저 열고, 거기 적힌 정본을 지정된 순서대로 **완독한 뒤** 작업을 시작한다. `AGENTS.local.md`가 없으면 작업을 시작하지 말고 사용자에게 요청한다.
2. **정본의 규칙을 이 저장소의 어떤 파일에도 복사·요약·발췌하지 않는다.** 과거에 이 저장소가 이미 폐지된 조항을 문서로 들고 있다가 정본과 반대되는 지시로 작동한 사례가 있다. 규칙은 정본 한 곳에서만 산다.
3. 완독은 **작업 시작 시 1회**다. 파일마다 정본을 다시 읽지 말고, 정본이 제공하는 판정 체크리스트 한 장을 들고 파일을 처리한다. 체크리스트로 판정이 갈리거나 `아니오`의 이유를 설명하지 못할 때만 해당 정본을 펼친다.
4. 작업 중 정본이 바뀌면 그때까지의 검수 결과를 무효로 보고, 바뀐 정본을 다시 완독한 뒤 영향 범위의 첫 파일부터 재시작한다.

## 1. 대상과 순서

- 대상: `src/content/docs/notes/` 아래 **01~08 과목 970개 파일 전체**
- 순서: `01-basic-theory` → `02-hardware` → `03-software` → `04-network` → `05-security` → `06-evaluation` → `07-law-policy` → `08-latest-tech`. 과목 안에서는 파일 번호 오름차순
- 진행 상태는 이 파일 첫 줄의 포인터와 `agent-guides/CSPE_PROGRESS.md`가 소유한다. 파일을 끝낼 때마다 갱신한다
- **한 파일은 위에서 아래로 한 번 읽으면서 판정과 수정을 함께 끝낸다.** 같은 파일을 교정 목적으로 두 번 순회하지 않는다
- 앞선 과목이 `완료`로 적혀 있어도 이번 회차의 대상이다. 아래 2절의 이유로 완료 표시와 실제 품질이 어긋나 있다

## 2. 이번 회차가 잡으려는 두 가지 결함

판정 기준은 전부 정본이 소유한다. 아래는 **무엇을 집중해서 볼지**의 지시이지 규칙 본문이 아니다.

두 결함은 증상이 반대로 보이지만 원인이 같다. 둘 다 **키워드 개수를 채점 단위로 착각**한 결과다. 채점자가 점수를 주는 단위는 키워드가 아니라 키워드 사이의 관계다.

1. **이해 불가 수준의 과압축** — 관계를 지우고 키워드만 남겨 짧아진 문장. `최대 마진 초평면 기반 분류 모델`처럼 동사가 사라지면, 채점자는 작성자가 그 개념을 이해하고 쓴 것인지 외워서 쓴 것인지 판정할 수 없다
2. **키워드 적재** — 관계 없이 전문어를 `·`로 계속 이어 붙인 문장. `장치의 하드웨어·펌웨어·통신·물리 보안 결함`은 전문어가 다섯 개지만 서로의 관계가 없어 무엇에 관한 문장인지 잡히지 않는다

교정 방향은 **확실한 핵심 키워드 한두 개와 그 관계를 정확히 잇는 것**이다. 키워드를 더 넣는 것도, 더 줄이는 것도 목표가 아니다.

주의할 점이 하나 있다. **글자 수 하한은 없다.** 정본에 하한이 들어가 있지 않으므로, 짧다는 이유만으로 문장을 늘리지 않는다. 세 요소(대상·핵심 동작·상위 개념)와 그 관계가 짧게 담기면 짧은 것이 정답이다. 길이를 채우려는 수정은 과압축과 방향만 반대인 같은 실패다.

세 번째로, **이미 정확하고 완결된 문장은 보존한다.** 수정 건수를 만들려고 취향 변경을 하지 않으며, 수정 0건으로 통과하는 파일도 정상 완료다. 재검수를 돌릴수록 문장이 짧아지면 그 자체가 결함 신호다.

### 현재 알려진 결함 분포

작업 시작 시점에 970개 파일의 Ⅰ절 정의 불릿을 실측한 값이다. 대상을 잡는 데만 쓰고 판정에는 정본을 쓴다.

| 과목 | 파일 수 | 옛 라벨 잔존 | 정의에 관계 동사 있음 | 정의가 동작명사로 끝남 |
|:---|---:|---:|---:|---:|
| 01-basic-theory | 62 | 0 | 61% | 3% |
| 02-hardware | 94 | 0 | 43% | 1% |
| 03-software | 229 | 0 | 24% | 19% |
| 04-network | 116 | 31 | 16% | 22% |
| 05-security | 147 | 125 | 8% | 35% |
| 06-evaluation | 51 | 51 | 25% | 18% |
| 07-law-policy | 45 | 45 | 40% | 9% |
| 08-latest-tech | 226 | 226 | 51% | 6% |

관계 동사 비율이 **검수를 진행한 순서대로 61% → 8%로 떨어졌다.** 아직 손대지 않은 07·08 과목이 검수 완료 과목보다 수치가 좋다. 즉 이전 검수가 문장을 개선한 것이 아니라 관계를 지웠다는 뜻이며, 같은 실수를 반복하지 않는 것이 이번 회차의 목적이다.

`옛 라벨 잔존`은 Ⅰ절 둘째 불릿이 현행 계약과 다른 이름을 쓰고 있는 파일 수다. 현행 라벨과 그 내용 규칙은 정본이 소유한다.

## 3. 파일 하나를 처리하는 방법

1. frontmatter와 보호 구간을 먼저 확인한다
2. `미리 알고가기` → `Ⅰ~Ⅶ` → 각 절의 학습용 블록 순서로 한 번 정독하면서, 정본 체크리스트 항목을 **동시에** 판정하고 위반만 고친다
3. `Ⅰ~Ⅶ`만 실제 답안 영역이다. `미리 알고가기`와 `쉽게 이해하기`는 학습 블록이며 답안의 길이·종결 규칙을 적용하지 않는다
4. 마지막에 학습 블록을 가린 채 `Ⅰ~Ⅶ`만 다시 훑어, 주제의 대표 전문용어가 학습 블록에만 남고 답안 영역에서 빠지지 않았는지 확인한다
5. 고칠 때는 **무엇을 왜 고쳤는지 한 줄로 설명할 수 있어야 한다.** 설명하지 못하는 수정은 되돌린다

## 4. 검증과 커밋

- 커밋 단위는 **과목 하나 또는 파일 10개**다. 그보다 크게 묶지 않는다
- 커밋 전에 `npm.cmd run build`가 통과해야 한다. Mermaid·KaTeX 구문 오류는 빌드에서만 드러난다
- `git diff --check`로 공백 오류를 확인한다
- 커밋 메시지는 영문 한 줄로 무엇을 고쳤는지만 적는다. **작성 규칙 본문, 학습 전략, 정본 경로를 커밋 메시지에 남기지 않는다**
- 과목 하나가 끝나면 이 파일의 포인터와 `agent-guides/CSPE_PROGRESS.md`를 갱신해 함께 커밋한 뒤 푸시한다

## 5. 보호 규약

- `astro.config.mjs`, `src/content.config.ts`, `package.json`, `.github/`는 이번 작업의 대상이 아니다. 건드리지 않는다
- frontmatter 스키마와 `sidebar.order`·`weight`·`extra` 값의 의미를 바꾸지 않는다. 배지 문자열은 기존 표기를 따른다
- **자동 일괄 치환·정규식 변환·템플릿 주입을 금지한다.** 파일을 직접 읽고 수작업으로 고친다. 자동 명령은 빌드, `git diff --check`, Git 상태 확인에만 쓴다
- 다른 에이전트가 만든 미커밋 변경분이 작업 트리에 있으면 건드리지 말고 사용자에게 알린다
- 이 저장소는 공개된다. 정본 경로와 작성 전략을 파일이나 커밋 메시지로 남기지 않는다
