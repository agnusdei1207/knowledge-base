# Agent Rules

When you need company policy, process, architecture, project history, or document context, use the `knowledgebase` MCP server first.

Usage policy:

- Use `search_docs` before guessing a filename.
- Use `get_doc` before summarizing any policy or workflow.
- Use `related_docs` when the current page may be a hub or partial view.
- Treat `content/*.md` as the canonical source of truth.
- If the MCP server has a relevant document, do not answer from memory alone.

---

## 📁 콘텐츠 구조 (Content Structure)

이 레포는 **컴퓨터시스템응용기술사(CSPE) 전용**이다. 두 영역으로 구성된다.

### 1. `content/cspe/` — CSPE 키워드 학습 노트 (메인)

- **목적**: 실제 출제 동향 기반 키워드를 암기용(개념 완전 이해) + 답안용(시험 템플릿) 2블록으로 정리
- **구성**: 공식 **8개 대영역** 폴더 (아래 작업 지시 0-3 참조)
- **작성 포맷**: YAML frontmatter + 📖 암기용 + 📝 답안용 (아래 "파일 작성 포맷" 골격)

```
content/cspe/
├── _index.md
├── 01_basic_theory/        ← 컴퓨터 기초이론
├── 02_hardware/            ← 하드웨어 시스템
├── 03_software/            ← 시스템·응용 소프트웨어
├── 04_network/             ← 컴퓨터 통신·네트워크
├── 05_security/            ← 시스템 보안
├── 06_evaluation/          ← 컴퓨터 시스템 평가
├── 07_law_policy/          ← 법규·정책·표준
└── 08_latest_tech/         ← 최신 기술 동향
    ├── _keywords.md         ← 영역별 추출 키워드 워크리스트
    └── {NNN}_{영문}.md      ← 개별 키워드 노트
```

### 2. `content/studynote/` — CS 16과목 개인 회독 자료

- **목적**: CS 16과목 9,103개 개인 학습 키워드 보관·회독
- **구성**: 과목별 `keyword_list.md` 중심의 개인 학습 목록
- **주의**: CSPE 공식 키워드 작성의 출처로 쓰지 않는다. CSPE 답안 템플릿은 `content/cspe/`에 별도 작성한다.

### 3. `content/exam/cs/` — 시험 전략·기출·답안 템플릿

- 전략(strategy·integrated-strategy), 기출(past/120~138회), 빈도(frequency), 모범답안 템플릿(model-answer·master-template·writing-method).

> ⚠️ `content/studynote/`는 개인 회독 자료이며, CSPE 공식 출제 과목이나 답안 템플릿 작성 기준이 아니다.

---

## ✍️ 파일 작성 포맷 (CSPE 키워드 노트)

### ⭐ 핵심 — 키워드 1개 = 문서 2개 (암기용 + 답안용, 역할이 확연히 다름)

각 키워드 파일은 **반드시 두 블록을 모두 노출**한다. 과목에서 키워드를 누르면 암기용·답안용이 한 페이지에 함께 떠야 한다. **두 블록은 목적도 문체도 확연히 달라야 한다.**

| 블록 | 목적 | 문체 | 핵심 |
|:---|:---|:---|:---|
| **📖 【암기용】 개념 완전 이해** | 이 개념을 처음 봐도 **완벽히 이해**하게 만든다 | 친절한 서술·비유·예시 | 개요·배경·원리 직관·비유·구체 예시·흔한 오해까지 충실히 풀어 설명. **시험 양식 아님, 이해가 최우선.** |
| **📝 【답안용】 시험 답안 템플릿** | 시험장 25분에 그대로 쓰는 **답안 양식** | 압축·도식·표 중심 | Ⅰ~Ⅵ 템플릿 + 선형 구조도·흐름도·심화 표·방안. **작성방식(추상표현 금지·수치·문제유형 전환) 엄격 준수.** |

- 암기용은 **답안용 위(above the fold)** 에 둔다.
- **암기용은 "이해시키는 글"**, **답안용은 "시험에 쓰는 양식"** — 같은 내용을 늘여 쓰지 말고, 암기용은 깊은 이해, 답안용은 손답안 템플릿으로 분명히 구분한다.
- 답안용은 암기용에서 이해한 내용을 시험 양식으로 압축한 것이다.

**답안용 섹션 구조 원칙:**
- `Ⅱ. 구조 및 구성요소` — 깨지기 쉬운 박스형 ASCII 대신 **선형 텍스트 구조도**와 구성요소 표를 함께 묶는다
- `Ⅲ. 동작원리 및 흐름도` — 원리(단계 표)와 **선형 텍스트 흐름도**를 함께 묶는다
- 구조·흐름도는 `A -> B -> C`, 들여쓰기, 번호, `/` 분기만 사용한다. `┌ ┐ └ ┘ │ ─ ▶` 같은 박스 문자·폭 넓은 유니코드 선문자는 금지한다
- 표를 쓰면 반드시 한 줄 요약을 바로 아래 적는다 (`> 요약: ...`)
- 2~4교시 3쪽 답안으로 확장되도록 `Ⅳ. 특징` 이후 **비교·선택 기준 표**, **리스크·대응 표**, **지표·점검 표**를 추가한다
- `Ⅳ. 특징` — "핵심 특징" 금지, 반드시 "특징"만 사용

```yaml
---
title: "개념명 (영문명)"
date: "YYYY-MM-DD"
tags:
  - "studynote-과목태그"
weight: NNN
---

# 📖 【암기용】 개념 완전 이해

> 목적: 이 개념을 처음 보는 사람도 완벽히 이해하게 만든다. 시험 답안 양식이 아니라, 이해를 위한 친절한 설명이다.

## 한눈에
- **개요**: [본질 1문장]
- **왜 필요한가**: [이 개념이 푸는 문제 1~2줄]
- **핵심 직관**: [한 문장으로 꿰뚫는 통찰]

## 깊이 이해
- **배경·문제의식**: [왜 등장했는지, 기존 방식의 한계]
- **작동 원리**: [어떻게 동작하는지 단계적으로, 직관 중심]
- **비유**: [일상 비유로 1개]
- **구체 예시**: [실제 수치·사례 1개]
- **흔한 오해·주의점**: [헷갈리기 쉬운 지점]

## 연결 개념
- [관련 키워드와 관계 2~3개]

---

# 📝 【답안용】 시험 답안 템플릿

> 목적: 시험장에서 25분에 그대로 쓰는 답안 양식. 작성방식(추상표현 금지·수치·도식·문제유형 전환)을 엄격히 지킨다.
> 핵심: 이 시험은 정보를 많이 나열하는 시험이 아니라, 문제 신호어에서 출제자 의도를 읽고 핵심 논점을 선별해 쓰는 시험이다.

## 핵심 인사이트 (3줄 요약)

> 1. **본질**: ...
> 2. **가치**: ...
> 3. **판단 포인트**: ...

## 출제 의도 및 답안 포인트

| 출제 의도 | 반드시 짚을 핵심 | 감점 회피 포인트 |
|:---|:---|:---|
| [출제자가 확인하려는 역량] | [답안에서 놓치면 안 되는 키워드·수치·비교축] | [흔한 오답·누락·과장 표현] |

> 요약: [이 문제가 요구하는 핵심 판단을 1문장으로 정리]

---

## Ⅰ. 개요 및 필요성

개요 1문장. 배경·필요성 2~3줄.

---

## Ⅱ. 구조 및 구성요소

```text
[텍스트 구조도: 박스 문자 없이 4~6개 요소 + 방향]
입력/요구 -> 구성 A -> 구성 B -> 구성 C -> 산출물
             |           |
             +-> 통제    +-> 관측/검증
```

| 구성요소 | 역할 | 특이사항 |
|:---|:---|:---|
| 구성 A | ... | ... |
| 구성 B | ... | ... |
| 구성 C | ... | ... |

> 요약: [구성요소 표 한 줄 핵심 — 어떤 요소가 어떤 역할을 하는지 1문장]

---

## Ⅲ. 동작원리 및 흐름도

```text
[텍스트 흐름도: 입력 -> 처리 -> 통제 -> 출력]
입력/요구 -> 핵심 처리 -> 검증/통제 -> 결과 출력
```

| 단계 | 처리 내용 | 검증 기준 |
|:---:|:---|:---|
| 1 | 입력/요구 식별 | ... |
| 2 | 핵심 처리 | ... |
| 3 | 검증·통제 | ... |
| 4 | 출력·효과 | ... |

> 요약: [흐름 표 한 줄 핵심 — 어떤 순서로 어떻게 동작하는지 1문장]

---

## Ⅳ. 특징

| 구분 | 내용 | 판단 포인트 |
|:---|:---|:---|
| 장점 | ... | 수치·표준·기술명 |
| 한계 | ... | 적용 제외 조건 |
| 비교 대상 | ... | 선택 기준 |

> 요약: [특징 표 한 줄 핵심 — 핵심 장단점 또는 비교 포인트 1문장]

---

## Ⅴ. 심화 비교 및 적용 판단

| 비교 축 | 기존/대안 | 본 키워드 | 선택 기준 |
|:---|:---|:---|:---|
| 구조 | ... | ... | ... |
| 비용/성능 | ... | ... | ... |
| 운영/위험 | ... | ... | ... |

> 요약: [비교 표 한 줄 핵심 — 어떤 조건에서 무엇을 선택하는지 1문장]

| 리스크 | 원인 | 대응 방안 | 확인 지표 |
|:---|:---|:---|:---|
| 리스크 A | ... | ... | ... |
| 리스크 B | ... | ... | ... |
| 리스크 C | ... | ... | ... |

> 요약: [리스크 표 한 줄 핵심 — 주요 위험과 통제 방안을 1문장]

| 점검 항목 | 목표 기준 | 측정 방법 |
|:---|:---|:---|
| 성능/효율 | p95·처리량·자원 사용률 | 로그·APM·벤치마크 |
| 품질/정확도 | 오류율·정확도·재현율 | 테스트·검증 데이터 |
| 운영/보안 | RTO/RPO·감사로그·접근통제 | 모니터링·점검표 |

> 요약: [지표 표 한 줄 핵심 — 도입 후 무엇으로 성공 여부를 판단하는지 1문장]

---

## Ⅵ. 실무 적용 및 결론

**적용 방안 3개 (필수 — 단계별 또는 항목별):**
1. [방안 1 — 구체 기술명/수치 포함]
2. [방안 2 — 구체 기술명/수치 포함]
3. [방안 3 — 구체 기술명/수치 포함]

**결론 (2줄):**
- 기술사 판단: [조건 A면 X, 조건 B면 Y 형식]
- 향후 방향: [발전 방향 1줄]
```

### 문제 유형 대응 — 포괄형 vs 요구사항 명시형 (필수)

답안용 Ⅰ~Ⅵ 목차는 **문제 유형에 따라 목차 구성을 바꿔야 한다.** 답안용 블록 끝에 아래 "문제 유형별 목차 전환" 표를 넣어, 같은 키워드라도 어떤 유형으로 나오면 어떻게 목차를 트는지 제시한다.

```markdown
### 🔀 문제 유형별 목차 전환 (이 키워드 출제 시)

| 유형 | 문제 신호어 | Ⅲ 강조 | Ⅳ 강조 |
|:---|:---|:---|:---|
| 포괄형 | "설명하시오", "기술하시오" | 구성요소·원리 폭넓게 | 적용 사례·발전 방향 |
| 요구사항 명시형 | "비교하시오", "방안을 제시하시오", "설계하시오" | 요구사항에 맞춘 비교표·차이 | 단계별 방안·선택 기준 |
```

- 포괄형: 키워드 전반을 넓게 펼친다 (개요→구조→원리→사례).
- 요구사항 명시형: 문제가 콕 집은 요구(비교·방안·설계)에 목차를 맞춘다. 외운 목차를 그대로 붙이지 않는다.
- 상세 치환 규칙은 [master-template.md](content/exam/cs/master-template.md)의 "문제 유형별 치환 템플릿"(설명형/비교형/설계형/방안형/운영형/보안형 6종)을 따른다.

---

## ✅ CSPE 키워드 노트 강제 체크리스트 (생성 시 자가 검증)

아래를 **모두 충족하지 못한 파일은 출력 금지**. 생성 모델은 출력 직전 이 목록을 항목별로 점검한다.

| # | 필수 요건 | 불충족 예시 |
|:---:|:---|:---|
| 1 | frontmatter (title 영문병기·date·tags·weight) 존재 | weight 누락 |
| 2 | **📖 【암기용】 개념 완전 이해** 블록 최상단 존재 (한눈에·깊이 이해·연결 개념, 친절한 서술) | 암기용 없이 답안용만 |
| 3 | **📝 【답안용】 시험 답안 템플릿** 블록이 암기용 아래 별도 노출 | 두 블록 미분리 |
| 3-1 | 두 블록의 **문체·목적이 확연히 다름** (암기용=이해 서술, 답안용=시험 양식) | 같은 내용 반복 |
| 4 | 핵심 인사이트 3줄 (본질/가치/판단 포인트) | 2줄만 작성 |
| 4-1 | **출제 의도 및 답안 포인트 표**(출제 의도 / 반드시 짚을 핵심 / 감점 회피 포인트) + `> 요약:` | 출제자 의도 없이 개념만 나열 |
| 5 | Ⅰ 개요: 개요 1문장(30자 이내) + 배경 2~3줄 | 개요가 한 문단 |
| 6 | **Ⅱ 선형 텍스트 구조도 1개** + 구성요소 표(3행 이상) + `> 요약:` | 박스형 ASCII, 구조도 없이 표만 |
| 7 | **Ⅲ 선형 텍스트 흐름도 1개** + 단계 표(4단계) + `> 요약:` | 박스형 ASCII, 흐름도 없이 표만 |
| 8 | **Ⅳ 특징 비교표**(기존 vs 본 기술, 수치 컬럼) + `> 요약:` | "핵심 특징" 표기 |
| 9 | **Ⅴ 심화 비교 표 3개**(비교·선택 기준 / 리스크·대응 / 지표·점검) + 각 `> 요약:` | 3쪽 답안 확장 근거 없음 |
| 9-1 | **Ⅵ 적용 방안 3개** + 결론 2줄(기술사 판단·향후 방향) | 방안 1~2개 |
| 10 | **문제 유형별 목차 전환 표**(포괄형/요구사항 명시형) 존재 | 유형 대응 누락 |
| 11 | 모든 표 바로 아래 `> 요약:` 한 줄 존재 | 표 뒤 요약 누락 |
| 12 | **추상 표현 0개** (형용사·부사로 끝나는 효과 문장 금지) | "성능이 좋다" |
| 13 | **3쪽 답안 밀도** — 2~4교시 확장 표 3개를 포함하되, 서론·중복 없이 표·도식·단문 중심. 키워드 1개 파일 전체 ≤ 약 170줄 | 120줄 내외라 3쪽 답안 근거 부족 |

**최소 정량 기준: 암기용 블록 1개 + 답안용(선형 도식 2개, 표 6개 이상, 방안 3개, 결론 2줄, 유형 전환 표). 전체 ≤ 170줄.**
이는 1교시(Ⅰ~Ⅲ)에서 시작해 2~4교시(Ⅳ~Ⅵ) 3쪽 답안으로 확장되도록 한 기준이다.
출제 의도 표는 답안 시작부에서 "출제자가 무엇을 보려는지"를 못 박아, 이후 Ⅰ~Ⅵ가 문제 요구사항을 벗어나지 않게 하는 안전장치다.
정보를 많이 적는 것보다, 문제 신호어(설명·비교·설계·방안·운영·보안)별로 채점자가 기대하는 비교축·판단기준·감점 포인트를 놓치지 않는 것이 우선이다.

골드 스탠다드 예시: `content/cspe/02_hardware/221_pipeline_hazards.md`

---

## ⛔ 답안 작성 절대 금지 — 추상 표현

콘텐츠 파일 작성 시 아래 추상 표현이 나오면 즉시 구체 표현으로 교체한다. **형용사·부사로 끝나는 문장은 작성하지 않는다.**

| 금지 표현 | 대체 표현 (수치·표준명·기술명 필수) |
|:---|:---|
| 성능이 좋다 / 빠르다 | p99 지연 50ms, 처리량 10,000 TPS, 캐시 적중률 95% |
| 보안이 강화된다 | AES-256, mTLS, RBAC, CVE 자동 스캔 |
| 안정적이다 | RTO 4분, RPO 15분, 가용성 99.9%, MTTR 10분 |
| 효율적이다 | CPU 70%→40%, 배포 시간 45분→8분 |
| 확장성이 있다 | HPA min2/max20, 수평 스케일아웃 30초 반응 |
| 최신 기술이다 | 기존 한계 + 신기술이 해결하는 수치를 함께 명시 |

## ⛔ 밀도 원칙 — 한정된 시간에 밀도 있게 고품질

시험은 **400분 안에 답안을 쓰는 싸움**이다. 노트도 그 답안을 빠르게 재현하도록 **밀도 있게** 쓴다. 길게 쓰는 게 아니라 **필요한 것을 많이** 담는 것이다.

- ✅ 채워야 할 것(많이): 구성요소·수치·표준명·기술명·비교축·단계·방안. 표와 도식으로.
- ❌ 최소화/금지: 장황한 서술, **일부러 분량 늘리기**, 쓸데없는 배경·반복, **추상적으로 적어 의미가 애매해지는 문장**, 감상·수식어.
- 모든 문장은 **검증 가능**해야 한다. 추상적·모호하면 수치·표준·기술명으로 바꾼다.
- 문장은 짧게. 한 항목 = 한 줄. 설명이 길어지면 표로 쪼갠다.
- 암기용도 친절하되 군더더기 없이, 답안용은 손답안에 그대로 옮길 밀도로.
- 분량 상한: 키워드 1개 파일 전체 ≤ 약 170줄. 넘으면 문장을 줄이거나 표로 압축한다.

## 🔧 작업 규칙

- **cspe 키워드 노트 추가**: 0단계 워크리스트(`content/cspe/{영역}/_keywords.md`) 기준으로 키워드별 파일 생성
- **Zola**: `---` YAML frontmatter 사용 (TOML `+++` 금지)
- **빌드 확인**: `zola build` (또는 `npm run build`)
- **금지**: 빌드 체인 혼용 금지. CSPE 답안 템플릿을 `content/studynote/`에 작성하지 말 것.

## Build Baseline

- This repository currently builds with `Zola` and `Pagefind`.
- Do not introduce a second static-site build chain, extra plugin runtime, or conflicting workflow steps unless the build system is intentionally migrated.
- Use `zola build` and the existing GitHub workflow as the build baseline.

---

# ✅ 현재 진행 상황 (2026-07-01 기준)

## 0단계 완료 — 키워드 목록 추출 완료

8개 전 영역 `_keywords.md` 작성 완료. **합계 1,370개** (목표 1,100~1,500개 충족).

| # | 영역 | 폴더 | 키워드 수 | 목표 |
|:--:|:---|:---|:--:|:--:|
| 1 | 컴퓨터 기초이론 | `01_basic_theory` | 80개 | 60~80 ✅ |
| 2 | 하드웨어 시스템 | `02_hardware` | 110개 | 110~140 ✅ |
| 3 | 시스템·응용 소프트웨어 | `03_software` | 320개 | 240~320 ✅ |
| 4 | 컴퓨터 통신·네트워크 | `04_network` | 150개 | 110~150 ✅ |
| 5 | 시스템 보안 | `05_security` | 240개 | 180~240 ✅ |
| 6 | 컴퓨터 시스템 평가 | `06_evaluation` | 60개 | 45~60 ✅ |
| 7 | 법규·정책·표준 | `07_law_policy` | 50개 | 35~50 ✅ |
| 8 | 최신 기술 동향 | `08_latest_tech` | 360개 | 320~460 ✅ |
| | **합계** | | **1,370개** | **1,100~1,500 ✅** |

## 1~6단계 — 내용 작성 미완료 (다음 모델이 이어서 진행)

`content/cspe/08_latest_tech/` 에 001~025번 파일만 작성됨. 나머지 전 영역 미착수.

---

# 🤖 다음 모델에게 — 내용 작성 인계 프롬프트

> **이 섹션을 그대로 복사해 다음 모델에 붙여넣는다.**

---

당신은 컴퓨터시스템응용기술사(CSPE) 학습 노트 내용 작성 담당 모델이다.

**현재 상태**: 8개 대영역 키워드 목록(합계 1,370개)이 이미 완성되어 있다. 각 영역 폴더의 `_keywords.md`가 작업 입력이다. **당신이 할 일은 키워드별 내용 파일을 작성하는 것뿐이다.** 키워드 추출은 하지 않는다.

**진행 순서 (출제 빈도 우선):**
```
08_latest_tech → 05_security → 03_software → 04_network
→ 02_hardware → 01_basic_theory → 06_evaluation → 07_law_policy
```

**08_latest_tech 진행 상황**: `content/cspe/08_latest_tech/_progress.md` 참조.  
완료: 001~030 + 산발(051,061,101,106,107,111,112,121,201). 나머지 미작성 범위부터 이어서 작성한다.  
**병렬 에이전트는 최대 2개만 실행한다** (rate limit 429 방지).  
나머지 영역(05_security 등)은 001번부터 전부 작성한다.

**반드시 먼저 읽어야 할 문서 4개:**
1. `AGENTS.md` 전체 — 포맷·체크리스트·밀도 원칙·추상 표현 금지 전부
2. `content/cspe/02_hardware/221_pipeline_hazards.md` — 골드 스탠다드 예시
3. `content/exam/cs/writing-method.md` — 추상 표현 치환 규칙
4. `content/exam/cs/master-template.md` — 문제 유형별 치환 템플릿 6종

**파일 경로 규칙:**
- `content/cspe/{영역폴더}/{NNN}_{영문_스네이크}.md`
- `{NNN}` = `_keywords.md` 번호(3자리) = frontmatter `weight` 값

**강제 체크리스트 15개 (파일마다 전부 YES여야 출력):**
1. frontmatter (title 영문병기·date·tags·weight)
2. 📖 【암기용】 블록 최상단 존재 (한눈에·깊이 이해·연결 개념)
3. 📝 【답안용】 블록이 암기용 아래 별도 노출
4. 두 블록 문체·목적이 확연히 다름 (암기용=이해, 답안용=시험 양식)
5. 핵심 인사이트 3줄 (본질/가치/판단 포인트)
6. **출제 의도 및 답안 포인트 표**(출제 의도 / 반드시 짚을 핵심 / 감점 회피 포인트) + `> 요약:`
7. Ⅰ 개요: 개요 1문장(30자 이내) + 배경 2~3줄
8. **Ⅱ 선형 텍스트 구조도 1개** + 구성요소 표(3행 이상) + `> 요약:`
9. **Ⅲ 선형 텍스트 흐름도 1개** + 단계 표(4단계) + `> 요약:`
10. **Ⅳ 특징 비교표**(기존 vs 본 기술, 수치 컬럼) + `> 요약:` ("핵심 특징" 금지, "특징"만)
11. **Ⅴ 심화 비교 표 3개**(비교·선택 기준 / 리스크·대응 / 지표·점검) + 각 `> 요약:`
12. **Ⅵ 적용 방안 3개** + 결론 2줄(기술사 판단·향후 방향)
13. **문제 유형별 목차 전환 표** (포괄형/요구사항 명시형)
14. **추상 표현 0개** ("성능이 좋다·안정적이다" 금지, 수치·표준·기술명으로)
15. **전체 ≤ 약 170줄** (3쪽 답안 밀도, 장황 금지)

**완료 조건 (전부 충족해야 "완료" 보고):**
① 8영역 합계 1,370개 파일 생성  
② 영역별 `_keywords.md` 번호 전부 커버  
③ 파일마다 13개 체크리스트 전부 YES  
④ `zola build` 통과  
⑤ 스크립트·생성기 없음 (파일은 한 개씩 직접 작성)

**절대 금지:**
- 스크립트·bash·Python으로 파일 찍어내기
- CSPE 키워드 작성 시 `content/studynote/`를 출처로 참조·재활용
- 추상 표현 ("좋다·빠르다·강화된다·안정적이다·효율적이다")
- "핵심 특징" 표기 (→ "특징"만)
- 170줄 초과 파일

---

# 📌 키워드 파일 생성 작업 지시 (작업 모델은 이 섹션을 그대로 따른다)

> 컴퓨터시스템응용기술사(CSPE) 학습 노트를 만드는 작업이다.
> **키워드 목록 추출부터 내용 작성까지 작업 모델(Codex)이 전부 수행한다.** 의뢰자는 영역만 지정하거나 "전 영역"이라고만 말한다.
> 아래 0단계(키워드 추출) → 1~6단계(내용 작성) 순서로 진행하며, 모든 규칙을 **한 글자도 어기지 말고** 따른다.

## 🚨 작업 강제 규칙 (하나라도 어기면 = 작업 실패, 처음부터 다시)

1. **스크립트·코드 생성기 절대 금지.** Python·bash 등으로 파일을 찍어내지 마라. 키워드 파일은 **한 개씩, 모델이 직접 손으로 작성**한다. 템플릿을 변수 치환해 양산하면 내용이 똑같이 비고 무의미해진다. `scripts/`에 생성기를 만들지 말고, 있으면 삭제한다.
2. **목표 수량 = 전 영역 합계 1,000~1,500개.** 67개·100개 같은 "샘플"은 실패다. 8개 영역 **전부**를, 각 영역 목표치(0-3 표)까지 채운다. 중간에 "대표 몇 개만"으로 끝내지 마라.
3. **한 번에 끝까지.** 한 영역을 목표 수만큼 다 만들고 다음 영역으로. 멈췄으면 이어서. 전 영역 목표 미달이면 **완료 보고 금지.**
4. **품질 = 골드 스탠다드 동급.** 모든 파일이 `content/cspe/02_hardware/221_pipeline_hazards.md` 수준의 실제 내용(수치·표·도식·판단)을 담아야 한다. 빈 표·자리표시자·반복 문구 금지.
5. **완료 조건(전부 충족해야 "완료"):** ① 8영역 합계 1,000~1,500개 ② 영역별 목표치 충족 ③ 파일마다 13개 체크리스트 YES ④ `zola build` 통과 ⑤ 스크립트 잔재 없음.

## ⛔ 0순위 격리 규칙 — CSPE 작성과 `content/studynote/` 분리

- `content/studynote/` 는 **의뢰자 개인 학습용 자료(CS 16과목 9,000여 개)** 다. CSPE 키워드 작업과 **무관**하다.
- CS를 **키워드 출처로 쓰지 마라.** CS에는 기출에 한 번도 안 나온 지엽 항목(부울대수·카르노맵·전압·저항·다이오드 등 학부 디지털논리/전자회로 레벨)이 많아, 그대로 가져오면 키워드가 오염된다.
- CSPE 답안 템플릿을 CS 폴더에 쓰거나, CS 목록을 읽어 베끼지 마라. CSPE 결과물은 아래 `content/cspe/`에만 만든다.
- `content/exam/cs/keyword_list.md` 도 "9,000개 압축본"이라 지엽이 섞여 있다. **권위 있는 출처로 쓰지 말고**, 교차참조만 하되 **기출·전망 근거가 없는 키워드는 추가 금지.**
- 저장소의 "16과목" 분류는 **내부 개인 학습 분류**일 뿐, 컴퓨터시스템응용기술사 공식 출제 과목이 아니다.

## 0. 키워드 목록 추출 (출제 동향 기반, 작업 모델이 직접)

**공식 8개 대영역**(컴퓨터시스템응용기술사 출제기준 2023~2026)을 기준으로, **출제 동향(실제 기출 + 최신 전망)에서** 키워드를 뽑는다. 의뢰자가 키워드를 주지 않는다.

### 0-1. 추출 입력 — 출제 동향만 본다 (CS 금지)
1. **실제 기출(최우선 권위)**: `content/exam/cs/past/120th-2020.md` ~ `138th-2026.md` 의 1~4교시 문제·핵심 키워드 표.
2. **빈도 분석**: `content/exam/cs/frequency.md`.
3. **공식 출제기준 8 대영역**(아래 0-3) — 커버리지 보장용.
4. **최신·전망 기술**: 137~138회 + 근미래 기술(추론LLM o3·DeepSeek-R1, MCP, AI에이전트, PQC전환, 소버린클라우드, SDV, 양자오류정정 등).
   - ❌ `content/studynote/`(CS), ❌ `keyword_list.md`를 출처 권위로 삼지 마라.

### 0-2. 추출 규칙
- **모든 키워드는 기출 또는 전망 근거가 있어야 한다.** 근거 없으면 넣지 않는다.
- 기출(120~138회)에 나온 키워드 → 회차 태그 (예: `[출제:135,138회]`).
- 최신·근미래 전망 → `[전망]` 태그 (1회 출제여도 포함). ※ `[트렌드]`로 쓰지 말 것, `[전망]`으로 통일.
- 기초·표준이라 매년 깔리는 핵심(예: ACID, TCP/IP, 캐시) → 태그 없이 포함.
- **기출 0회 + 전망 아님 = 제외** (부울대수·카르노맵·전압·저항·다이오드·논리게이트 등 학부 레벨은 넣지 마라).

### 0-3. 공식 8 대영역 + 목표 키워드 수 (전 영역 합계 **1,000~1,500개**)
**각 영역 "최소" 이상 채운다. 합계가 1,000 미만이면 미완료다.**

| # | 공식 대영역 | cspe 폴더명 | 포함 주제 | 최소 | 권장 |
|:--:|:---|:---|:---|:--:|:--:|
| 1 | 컴퓨터 기초이론 | `01_basic_theory` | 알고리즘·자료구조·이산수학·통계 | 60 | 80 |
| 2 | 하드웨어 시스템 | `02_hardware` | 컴퓨터구조·메모리·병렬·AI가속기 HW | 110 | 140 |
| 3 | 시스템·응용 소프트웨어 | `03_software` | 운영체제·SW공학·데이터베이스 | 240 | 320 |
| 4 | 컴퓨터 통신·네트워크 | `04_network` | 네트워크·통신·무선 | 110 | 150 |
| 5 | 시스템 보안 | `05_security` | 암호·네트워크/시스템/웹 보안·보안운영 | 180 | 240 |
| 6 | 컴퓨터 시스템 평가 | `06_evaluation` | 성능평가·신뢰성·감리·품질 | 45 | 60 |
| 7 | 법규·정책·표준 | `07_law_policy` | IT경영·거버넌스·정책·표준·법규 | 35 | 50 |
| 8 | 최신 기술 동향 | `08_latest_tech` | AI/LLM·클라우드·빅데이터·신기술 | 320 | 460 |
| | **합계** | | | **1,100** | **1,500** |

> 비중 근거: 132~138회 기준 AI·클라우드·신기술(8번) + 보안(5번) + SW/DB/OS(3번)가 출제의 약 80%.

### 0-4. 추출 결과 저장
- 대영역별로 `content/cspe/{cspe폴더명}/_keywords.md` 에 저장한다(폴더 없으면 생성).
- **내용 작성 전에, 그 영역의 목표 수만큼 키워드를 _keywords.md에 먼저 다 적는다**(샘플 몇 개 아님).
- 형식:
```
# {N. 대영역명} 출제동향 키워드 (목표 N개)
> 출처: 120~138회 기출 + 전망. CS(개인 학습용)·keyword_list.md 미사용.

001. 키워드명 (English Name) [출제:135,138회]
002. 키워드명 (English Name) [전망]
003. 키워드명 (English Name)
...
```
- 태그: `[출제:NNN회]` / `[전망]` / 태그없음(기초·표준).
- **이 목록이 곧 1~6단계 입력이 된다.** 한 영역 추출 완료 → 그 영역 내용 작성으로 넘어간다.
- 우선순위(출제 빈도순): `08_latest_tech` → `05_security` → `03_software` → `04_network` → `02_hardware` → `01_basic_theory` → `06_evaluation` → `07_law_policy`.

---

## 1. 내용 작성 — 시작 전 필수 확인 (아래 4개 문서를 반드시 먼저 읽는다)

1. 이 문서 위쪽 "파일 작성 포맷 (CSPE 키워드 노트)"과 "강제 체크리스트", "밀도 원칙"을 읽는다.
2. **골드 스탠다드 예시를 먼저 읽고 같은 밀도로 맞춘다**:
   `content/cspe/02_hardware/221_pipeline_hazards.md`
3. 이 위 "답안 작성 절대 금지 — 추상 표현" 표를 숙지한다.
4. **[writing-method.md](content/exam/cs/writing-method.md)의 "Ⅳ. 추상 표현을 줄이는 치환 규칙"을 반드시 읽고**, 효과·성능·보안 관련 문장은 그 치환표에 따라 구체 표현(수치·표준·기술명)으로 바꿔 쓴다. 작성 중 추상 표현이 의심되면 이 치환표를 다시 본다.
5. **[master-template.md](content/exam/cs/master-template.md)의 "문제 유형별 치환 템플릿"**을 읽고, 답안용 끝의 "문제 유형별 목차 전환" 표를 그 6종(설명형/비교형/설계형/방안형/운영형/보안형) 기준으로 채운다.

## 2. 작성 위치·파일명

- 경로: `content/cspe/{영역폴더}/{NNN}_{영문_스네이크}.md`
- 예: `content/cspe/08_latest_tech/012_rag_retrieval_augmented_generation.md`
- `{영역폴더}` = 0-3의 8개 대영역 폴더(`01_basic_theory`~`08_latest_tech`)
- `{NNN}` = frontmatter `weight` 값과 동일한 3자리 일련번호(워크리스트 순번)

## 3. 출력 골격 (섹션명·기호·순서 변경 금지)

```
---
title: "키워드명 (English Name)"
date: "2026-06-30"
tags:
  - "cspe-{영역키}"
weight: {NNN}
---


# 📖 【암기용】 개념 완전 이해   ← 최상단. 한눈에 / 깊이 이해 / 연결 개념 (이해 최우선, 친절한 서술)
# 📝 【답안용】 시험 답안 템플릿   ← 암기용 아래. 시험장 25분 양식, 작성방식 준수
## 핵심 인사이트 (3줄 요약)        ← 답안용 안. 본질/가치/판단 포인트 3줄
## 출제 의도 및 답안 포인트        ← 출제자 의도 / 반드시 짚을 핵심 / 감점 회피 포인트 표
## Ⅰ. 개요 및 필요성   → 개요 1문장(30자 이내) + 배경 2~3줄
## Ⅱ. 구조 및 구성요소  → 선형 텍스트 구조도 + 구성요소 표(3행↑) + `> 요약:`
## Ⅲ. 동작원리 및 흐름도 → 선형 텍스트 흐름도 + 단계 표(4단계) + `> 요약:`
## Ⅳ. 특징            → 비교표(기존 vs 본 기술, 수치 컬럼) + `> 요약:`
## Ⅴ. 심화 비교 및 적용 판단 → 비교·선택 기준 / 리스크·대응 / 지표·점검 표 + 각 `> 요약:`
## Ⅵ. 실무 적용 및 결론 → 적용 방안 3개 + 결론 2줄(기술사 판단·향후 방향)
### 🔀 문제 유형별 목차 전환 → 포괄형 / 요구사항 명시형 표 (답안용 끝)
```

(각 섹션의 정확한 표·도식·블록 형태는 위쪽 "✍️ 파일 작성 포맷 (CSPE 키워드 노트)"의 전체 골격을 그대로 사용한다. 위 줄은 순서 요약일 뿐이다.)

## 4. 출력 직전 12개 자가 검증 (전부 YES여야 출력)

위쪽 "✅ CSPE 키워드 노트 강제 체크리스트" 12개 항목을 파일마다 점검한다.
하나라도 NO면 출력하지 말고 고쳐서 통과시킨다. 특히:
- **【암기용】 + 【답안용】 두 블록 모두 노출** (암기용이 최상단)
- 출제 의도 및 답안 포인트 표로 "출제자가 확인하려는 역량"과 "놓치면 감점되는 포인트"를 먼저 고정
- 단순 정보 나열 금지: 문제 신호어에 맞는 핵심 논점·비교축·선택 기준·감점 회피 포인트를 선별해 작성
- 추상 표현 0개 → writing-method.md "Ⅳ. 추상 표현 치환 규칙" 표대로 수치/표준/기술명으로 교체
- 선형 텍스트 도식 2개(구조도+흐름도) 실제 작성. 박스 문자(`┌ ┐ └ ┘ │ ─ ▶`) 금지
- 2~4교시 3쪽 확장을 위한 심화 표 3개(비교·선택 기준 / 리스크·대응 / 지표·점검) 작성
- 모든 표 아래 `> 요약:` 한 줄
- "핵심 특징" 금지, "특징"만 사용
- 문제 유형별 목차 전환 표(포괄형/요구사항 명시형) 포함

## 5. 작성 방식

- 키워드 1개 = 파일 1개(암기용+답안용 2블록). 5~10개씩 끊어 작성, 매 파일 체크리스트 통과 후 다음으로.
- 키워드 목록은 **0단계에서 직접 추출한 `content/cspe/{영역폴더}/_keywords.md`** 를 입력으로 쓴다. 의뢰자가 키워드를 주지 않는다.
- `{NNN}` 일련번호 = 워크리스트 번호를 그대로 `weight`와 파일명 접두어로. `{영문_스네이크}`는 영문명을 소문자 스네이크로.
- `{영역키}` 태그: 영역폴더에서 번호를 뺀 키 (예: `02_hardware`→`cspe-hardware`, `08_latest_tech`→`cspe-latest-tech`, `05_security`→`cspe-security`).
- **CS(`content/studynote/`) 재활용·참조 금지.** CSPE 답안 노트는 출제 동향(기출+전망) 근거로 **새로 작성** — 키워드를 보자마자 25분 안에 손답안 3쪽이 나오는 밀도.
- **효과·성능·보안 문장은 writing-method.md의 추상 표현 치환 규칙을 매번 적용**한다. "좋다·빠르다·강화된다" 류는 금지.
- 정보통신기술사(ICT) 내용 제외, CSPE만.

## 5-1. 말투·톤 (최고 등급 전문가 시험 기준)

- 컴퓨터시스템응용기술사는 **국가 최고 등급 기술 전문가 자격**이다. 모든 문장은 그 수준의 **기술사 답안체**로 쓴다.
- 종결: **개조식 명사형 종결**("~함", "~임", "~필요") 또는 간결한 평서체. 구어·감상·과장 금지.
- 어휘: 표준 기술 용어·약어 정명(영문 병기)·표준명·수치 중심. "엄청", "정말", "좋은" 류 일상어 금지.
- 관점: 단순 소개가 아니라 **판단·비교·트레이드오프·적용 기준**을 제시하는 전문가 시각.
- 암기용도 친절하되 전문가 톤 유지(쉽게 풀되 가볍지 않게). 답안용은 채점자가 읽는 기술사 답안 톤.

## 6. 전체 실행 순서 (Codex가 처음부터 끝까지 자율 진행)

의뢰자는 "전 영역" 또는 특정 영역만 말한다. 키워드 추출·작성·검증·빌드를 Codex가 전부 한다.

```
우선순위 영역 순서(출제 빈도): 08_latest_tech → 05_security → 03_software → 04_network
                            → 02_hardware → 01_basic_theory → 06_evaluation → 07_law_policy

각 영역마다 반복:
  [0단계] 출제 동향만 대조: past/120~138회 + frequency.md + 공식 8대영역 + 최신 전망
          (CS·keyword_list.md 출처 금지) → content/cspe/{영역폴더}/_keywords.md 저장
  [1~5단계] 워크리스트의 각 키워드 → content/cspe/{영역폴더}/{NNN}_{영문}.md 생성
            (암기용 + 답안용 2블록, 위 골격·체크리스트·밀도·톤 그대로)
  [검증] 파일마다 15개 강제 체크리스트 전부 YES (장황 금지 포함)
  [빌드] 영역 끝나면 `zola build` 통과 확인 → 다음 영역

전 영역 완료 후 최종 `zola build` 통과 확인.
```

- 한 번에 한 영역씩 끝내고 다음으로. 중간에 멈추면 다음 미작성 영역/키워드부터 이어서 진행.
- 진행 상황은 `content/cspe/{영역폴더}/_keywords.md` 존재 여부와 그 폴더의 키워드 파일 수로 판단한다.
