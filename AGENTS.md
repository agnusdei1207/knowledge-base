# Agent Rules

When you need company policy, process, architecture, project history, or document context, use the `knowledgebase` MCP server first.

Usage policy:

- Use `search_docs` before guessing a filename.
- Use `get_doc` before summarizing any policy or workflow.
- Use `related_docs` when the current page may be a hub or partial view.
- Treat `content/*.md` as the canonical source of truth.
- If the MCP server has a relevant document, do not answer from memory alone.

---

## 📁 콘텐츠 구조 (Content Structure)

이 레포는 **컴퓨터시스템응용기술사(CSPE) 전용**이다. 두 영역으로 구성된다.

### 1. `content/cspe/` — CSPE 키워드 학습 노트 (메인)

- **목적**: 실제 출제 동향 기반 키워드를 실제 시험 답안과 동일한 골격(Ⅰ~Ⅶ)의 단일 흐름으로 정리
- **구성**: 공식 **8개 대영역** 폴더 (아래 작업 지시 0-3 참조)
- **작성 포맷**: YAML frontmatter + Ⅰ~Ⅶ 단일 흐름 (아래 "파일 작성 포맷" 골격, 암기용/답안용 분리 없음)

```
content/cspe/
├── _index.md
├── 01_basic_theory/        ← 컴퓨터 기초이론
├── 02_hardware/            ← 하드웨어 시스템
├── 03_software/            ← 시스템·응용 소프트웨어
├── 04_network/             ← 컴퓨터 통신·네트워크
├── 05_security/            ← 시스템 보안
├── 06_evaluation/          ← 컴퓨터 시스템 평가
├── 07_law_policy/          ← 법규·정책·표준
└── 08_latest_tech/         ← 최신 기술 동향
    ├── _keywords.md         ← 영역별 추출 키워드 워크리스트
    └── {NNN}_{영문}.md      ← 개별 키워드 노트
```

### 2. `content/studynote/` — CS 16과목 개인 회독 자료

- **목적**: CS 16과목 9,103개 개인 학습 키워드 보관·회독
- **구성**: 과목별 `keyword_list.md` 중심의 개인 학습 목록
- **주의**: CSPE 공식 키워드 작성의 출처로 쓰지 않는다. CSPE 답안 템플릿은 `content/cspe/`에 별도 작성한다.

### 3. `content/exam/cs/` — 시험 전략·기출·답안 템플릿

- 전략(strategy·integrated-strategy), 기출(past/120~138회), 빈도(frequency), 모범답안 템플릿(model-answer·master-template·writing-method).

> ⚠️ `content/studynote/`는 개인 회독 자료이며, CSPE 공식 출제 과목이나 답안 템플릿 작성 기준이 아니다.

---

## ✍️ 파일 작성 포맷 (CSPE 키워드 노트)

> **2026-07-05 4차 개정(최신 확정본): 골격을 실제 기술사 수험가의 표준 목차로 단순화.** `개요(정의·배경/필요성) → 구성요소 → 절차 → 문제점 → 개선방안 → 전망`. Ⅰ~Ⅶ(심화비교·리스크를 별도 절로 뒀던) 2차 개정판은 폐기하고, 골격 정본은 [model-answer.md](content/exam/cs/model-answer.md)의 "2~4교시 서술형 템플릿"(Ⅰ~Ⅵ, 4차 개정판)이며, 이 문서에 사본을 두지 않는다.

- **골격**: frontmatter → `Ⅰ. 개요` → `Ⅱ. 구성요소` → `Ⅲ. 절차` → `Ⅳ. 문제점` → `Ⅴ. 개선방안` → `Ⅵ. 전망`. 6절 전부 필수(생략 가능한 절 없음), 번호는 고정한다.
- **단일 흐름, 분리 없음**: 【암기용】/【답안용】 두 블록으로 쪼개지 않고 Ⅰ~Ⅵ 하나의 흐름으로 쓴다. 비유·용어 설명·동작 예시 같은 "이해를 돕는 요소"는 별도 섹션으로 떼지 않고 해당 절 안에 녹여 쓴다:
  - 개념 정의·배경·비유는 `Ⅰ. 개요` 안에 (비유는 노트에서 이해를 돕는 용도 — 실전 답안엔 안 옮겨도 되지만 노트에서는 지우지 않는다)
  - 내부 용어·구성요소 설명은 `Ⅱ. 구성요소`의 표(`구성요소 | 설명 | 비유` 3칸 — 역할과 산출물은 "설명" 한 칸으로 합침, 비유는 별도 칸) 안에
  - 알고리즘 동작·버그 패턴 등 구체 예시는 `Ⅲ. 절차`의 단계 설명 안에
  - 대안 비교가 필요하면 별도 절을 만들지 않고 `Ⅱ`(방식이 여러 개일 때) 또는 `Ⅴ`(개선 대안을 고를 때) 표 안에 녹인다
  - "핵심 인사이트 3줄", "관련 개념 맵", "발전 흐름도", "어린이 설명" 같은 부가 섹션은 만들지 않는다.
- **출제 의도 표는 노트용, 삭제하지 않는다**: `Ⅰ. 개요`에 "출제 의도/반드시 짚을 핵심/감점 회피 포인트" 표를 둔다. 실전 답안지에는 표를 그대로 옮기지 않아도 되지만(노트에서 눈으로 보고 이해하기 위한 것), 노트 자체에서는 지우지 않는다.
- **등장 배경은 짧게, 역사 공부 금지**: `Ⅰ. 개요`의 배경/필요성은 "이 개념이 왜 논리적으로 필요해졌는가"를 1~2문장으로만 쓴다. 발표 연도·인명·논문 제목·저널명·수상 이력을 나열하는 역사 서술은 하지 않는다. 확신이 서지 않는 사실을 검증하겠다고 연도·인명을 늘어놓지 말고, 애초에 그런 문장 자체를 쓰지 않는다.
- **Ⅳ↔Ⅴ는 1:1로 짝짓는다**: Ⅳ에서 제기한 문제/한계 각각에 대응하는 개선방안이 Ⅴ에 있어야 한다. Ⅴ는 단기/중기/장기로 구분.
- frontmatter는 기존과 동일: `title`(영문 병기) · `date`(실제 작성일) · `tags`(`cspe-{영역키}`) · `weight`(따옴표 없는 정수, 문자열로 쓰면 Zola 빌드 실패).

---

## ✅ CSPE 키워드 노트 강제 체크리스트 (생성 시 자가 검증)

아래 항목을 모두 충족하지 못한 파일은 출력 금지. 생성 모델은 출력 직전 이 목록을 항목별로 점검한다.
**중요도순 배치다 — A등급이 곧 채점 점수다. 시간·분량이 부족하면 A등급부터 지킨다.**
각 규칙의 상세·예시 정본은 [writing-method.md](content/exam/cs/writing-method.md)(작성 품질)와 [model-answer.md](content/exam/cs/model-answer.md)(골격)다 — 이 표는 점검용 요약이며, 충돌 시 작성법 문서가 우선한다.

**🔴 A등급 — 내용·채점 직결 (어기면 답안 자체가 무너짐)**

| # | 필수 요건 | 정본 | 불충족 예시 |
|:---:|:---|:---|:---|
| 1 | **기술적 정확성** — 사실 오류 0건. 복잡도·표준 번호·수치 오류는 즉시 감점. 불확실하면 쓰지 않는다. 인과 방향 검증·범위 한정·용어 혼동 금지 | writing-method Ⅵ | "단위 행렬" 쓰고 "유니터리 행렬"이 맞는 경우 |
| 2 | **빈 추상 표현 0개** — 검증 불가 형용사 금지. 단 억지 수치화도 금지, 기준은 "구체·검증 가능" | writing-method Ⅳ | "성능이 좋다" / 과시용 수치 남발 |
| 3 | **모호 표현·이중 부정 0개** — 반대로 읽어도 성립하는 압축 표현 금지, 주어·목적어 명시 | writing-method Ⅴ | "최고차항·상수 제거" |
| 4 | **Ⅰ 출제 의도 및 답안 포인트 표** + `> 요약:` (노트용, 삭제 금지). 감점 회피 열은 피해야 할 실수를 **명사형으로**("~ 누락", "~ 단정") | writing-method Ⅴ(이중 부정 금지) | 표 누락, "누락하지 않음" 식 이중 부정 |
| 5 | **Ⅳ. 문제점 — 필수 절(생략 금지)**. 원인이 명시된 문제·한계를 다룬다. 비교가 의미 있을 때만 표(대상 2개↑ × 축 2개↑), 그 외엔 불릿 | writing-method Ⅶ 표·불릿 판단 | Ⅳ 생략, 원인 없이 결과만 나열 |
| 6 | **Ⅴ. 개선방안 — 필수 절(생략 금지)**. Ⅳ의 문제 각각에 대응하는 방안을 단기/중기/장기로 제시. 리스크·지표는 기본 불릿(예외 조건 충족 시만 표) | writing-method Ⅶ 표·불릿 판단 | Ⅴ 생략, Ⅳ와 대응되지 않는 방안 나열 |
| 7 | **Ⅵ. 전망 — 제목·번호 고정**(번호 당김 금지). 발전 방향 + 기술사적 판단·제언 | writing-method Ⅶ | "## Ⅴ. 전망"(번호 밀림) |
| 8 | **Ⅱ ASCII 박스 구조도 1개** + 구성요소 표(`구성요소 │ 설명 │ 비유` 3칸 고정) + `> 요약:` | model-answer 골격 | 구조도 없이 표만, 유니코드 박스 문자 |
| 9 | **Ⅲ ASCII 박스 흐름도 1개** + **단계 설명(기본 번호 불릿, 4단계)** — 흐름도가 순서를 보여주므로 단계 표를 중복으로 넣지 않는다. | writing-method Ⅶ 표·불릿 판단 | 흐름도+단계표 중복, 흐름도 없이 표만, 선형 텍스트 |
| 10 | **역사 서술 최소화** — Ⅰ 배경은 논리적 필요성 1~2문장뿐, 연도·인명·논문·저널·수상 이력 나열 금지 | 본 문서(파일 작성 포맷) | "1986년 Bentley가 저서에서 90%가..." 식 역사 나열 |
| 11 | **장황한 대조 수사·힘주기 부사·좁은 예시 금지** — "~이 아니라 ~해야 함"류 대조 수사, "직접/반드시/절대" 같은 정보량 없는 힘주기 부사, 일반 원리에 특정 환경 하나만 좁혀 붙이는 괄호 부연은 금지 | writing-method 핵심 집중 | "이론적 도구가 아니라 클라우드 비용을 직접 좌우하는" (→ "실행시간·메모리 사용량을 결정하는") |

**🟡 B등급 — 구조·문체 (어기면 가독성·변별력 하락)**

| # | 필수 요건 | 정본 | 불충족 예시 |
|:---:|:---|:---|:---|
| 12 | Ⅰ 개요: 정의(30자 내외)·배경/필요성·출제 의도(표), **순환 정의 금지**, 줄글 금지(불릿) | writing-method Ⅵ(순환 정의) | "X는 X를 위한 기술이다" |
| 13 | 3개 이상을 나열하는 설명은 한 문장에 욱여넣지 않고 도입 문장 + 번호 목록으로 쓴다 | 본 문서(파일 작성 포맷) | "(1)...(2)...(3)..."을 한 문장에 압축 |
| 14 | 변수·코드 표현은 백틱 코드로, LaTeX `\text{}` 남용 금지 — `$` 수식은 진짜 수학적 표기(복잡도·부등식·점화식·통계 라벨 등)에만 | 본 문서(파일 작성 포맷) | `$\text{low} = \text{high}$`처럼 변수명을 LaTeX로 감쌈 |
| 15 | 전체 문장 단위 `- 불릿`(문장 2개 이상이면 쪼갠다) + **종결어미 파일 내 통일**(~함/~임/~필요) | writing-method Ⅵ | 줄글 문단, "~함"과 "~한다" 혼재 |
| 16 | 쓴 표 바로 아래 **빈 줄 1개 + `> 요약:`** (불릿엔 강제 안 함) | writing-method Ⅶ | 표 마지막 행에 붙여 쓰기 |
| 17 | **유니코드 박스문자 금지, 일반 ASCII 박스 적극 활용** — `┌ ┐ └ ┘` 같은 특수문자는 금지하되, `+`, `-`, `|`를 사용해 구성요소와 절차 모두 실전처럼 깔끔한 박스형 도식을 그릴 것. | model-answer 골격 | 화살표만 있는 성의없는 도식, 유니코드 깨짐 문자 |

**🟢 C등급 — 메타 (어기면 관리·빌드 문제)**

| # | 필수 요건 | 정본 | 불충족 예시 |
|:---:|:---|:---|:---|
| 18 | frontmatter — title 영문병기 · date(실제 작성일) · tags `cspe-{영역키}` · weight=NNN(따옴표 없는 정수) | 본 문서 파일 작성 포맷 | weight 누락, `studynote-*` 태그, `weight: "020"`처럼 문자열로 써서 Zola 빌드 실패, tags에 `cspe-basic_theory`처럼 언더스코어 오타 |
| 19 | **답안 밀도** — Ⅳ~Ⅵ 확장을 갖추되 서론·중복·역사 나열 없이 표·도식·단문 중심. 파일 전체 ≤ 약 170줄 | writing-method 대전제(0) | Ⅲ에서 끝남, 장황한 역사·배경으로 분량만 채움 |

표는 개수 하한이 없다 — 대조 축이 있는 곳에만 쓰고, 나머지는 불릿으로 쓴다(writing-method Ⅶ). **표 남발 금지: 한 파일의 표는 대개 "출제 의도 + Ⅱ 구성요소" 정도면 충분하다. Ⅲ 단계·Ⅳ 문제점·Ⅴ 리스크·지표를 표로 그리는 것은 남용이다(기본 불릿, 정말 대조 축이 있을 때만 예외).** **논리 전개(기승전결)·중복 금지·억지 수치화 금지는 writing-method 대전제(0)와 Ⅵ을 따른다.**
출제 의도 표는 답안 시작부에서 "출제자가 무엇을 보려는지"를 못 박아, 이후 Ⅰ~Ⅵ가 문제 요구사항을 벗어나지 않게 하는 안전장치다(노트용 — 실전 답안엔 생략 가능해도 노트에선 유지).
정보를 많이 적는 것보다, 문제 신호어(설명·비교·설계·방안·운영·보안)별로 채점자가 기대하는 비교축·판단기준·감점 포인트를 놓치지 않는 것이 우선이다.

---

## 🚨 답안 작성법 — 정본은 writing-method.md

> **CSPE 콘텐츠 작성·수정의 품질 규칙은 [writing-method.md](content/exam/cs/writing-method.md)가 유일 정본이다.** 아래 규칙을 이 문서에 사본으로 두지 않고 링크로 참조한다. 하나라도 위반한 파일은 작성 완료로 인정하지 않는다.

| 규칙 | 정본 위치 |
|:---|:---|
| 추상 표현 치환 (빈 추상 금지, 단 억지 수치화도 금지 — "구체적·검증 가능"이 기준) | writing-method.md `Ⅳ` |
| 의미 반전·모호 표현 방지 (주어·목적어·동사 명시) / 이중 부정 금지 | writing-method.md `Ⅴ` |
| 기술적 정확성·순환 정의 금지 / 핵심 집중 / 논리 전개(기승전결)·중복 금지 / 문체(불렛·종결어미 통일) | writing-method.md `Ⅵ` |
| 표 vs 불릿 판단 / 표 요약(빈 줄 필수) / 절 생략·번호 규칙(Ⅰ~Ⅵ 전부 필수, 번호 고정) | writing-method.md `Ⅶ` |
| 내부 용어를 뜻+비유로 이해시키는 원칙(Ⅱ 구성요소 표의 "설명·비유" 칸에 적용) | writing-method.md `Ⅷ` |
| 인접 키워드 간 서사·배경 중복 방지(연쇄 학습) | writing-method.md `Ⅸ` |
| Ⅰ~Ⅵ 골격·각 절 형식(개요/구성요소/절차/문제점/개선방안/전망) | model-answer.md |
| 문제 유형별 치환 6종 / Ⅰ~Ⅵ 치환 예시 | master-template.md |

**AGENTS 고유 제약(위 정본에 더해 적용):**
- 채점자 관점: "이 사람 이해를 못 한 것 같은데?"라는 인상을 주면 감점. 불확실한 내용은 쓰지 않는다(틀린 답안 > 빈 답안 아님).
- 출제 의도 표(노트용)를 답안 시작부에 배치해 Ⅰ~Ⅵ가 문제 요구사항을 벗어나지 않게 고정한다.
- 분량 상한: **파일 전체(Ⅰ~Ⅵ) ≤ 약 170줄**(넘으면 중복 제거·압축). 암기용/답안용 분리가 없으므로 이 상한이 파일 전체에 적용된다 — 패딩 금지.
- 밀도: 길게 쓰는 게 아니라 **필요한 것을 많이** 담는다. 장황·분량 늘리기·쓸데없는 반복 금지.

---

## 🔧 작업 규칙

- **cspe 키워드 노트 추가**: 0단계 워크리스트(`content/cspe/{영역}/_keywords.md`) 기준으로 키워드별 파일 생성
- **Zola**: `---` YAML frontmatter 사용 (TOML `+++` 금지)
- **빌드 확인**: `zola build` (또는 `npm run build`)
- **금지**: 빌드 체인 혼용 금지. CSPE 답안 템플릿을 `content/studynote/`에 작성하지 말 것.

## Build Baseline

- This repository currently builds with `Zola` and `Pagefind`.
- Do not introduce a second static-site build chain, extra plugin runtime, or conflicting workflow steps unless the build system is intentionally migrated.
- Use `zola build` and the existing GitHub workflow as the build baseline.

## 🚑 CI/CD 배포 실패 대응 (푸시할 때마다 "터지는" 문제)

**결론 먼저: 답안/콘텐츠 문제가 아니다. 고치겠다고 글 파일을 건드리지 마라.**

실패한 런을 까보면 항상 이 모양이다:

```
✓ build   (zola build + pagefind)   ← 매번 성공. 링크·frontmatter·수식 다 정상
X deploy  (actions/deploy-pages)    → "Deployment failed, try again later."
```

즉 **콘텐츠 빌드는 통과**하고, GitHub Pages **백엔드가 간헐적으로 뱉는 일시 오류**에서만 터진다. 원인 두 가지:

1. **Pages 백엔드 플레이키** — "Deployment failed, try again later." / "deployment_queued" 는 우리 코드와 무관한 GitHub 쪽 일시 장애다. `deploy.yml`에 **재시도 3회**를 넣어 자동 흡수한다(이미 반영됨). 한 번 빨간불 떠도 재시도 안에서 초록불 나면 정상이다.
2. **연속 빠른 푸시** — `concurrency: cancel-in-progress: true` 때문에 이전 배포가 진행 중일 때 새 푸시가 들어오면 **이전 런이 `cancelled`** 로 죽는다(실패 아님, 의도된 취소). 10k+ 페이지라 배포가 2~3분 걸리므로, **연달아 푸시하지 말고 직전 배포가 끝난 뒤 다음 푸시**를 한다.

### 대응 수칙 (이 순서대로)

- **빨간불/취소가 떠도 먼저 당황하지 말 것.** `gh run view <id>` 로 **어느 잡이 죽었는지** 확인한다.
  - `build` 실패 → 진짜 콘텐츠 문제(링크 깨짐·frontmatter·LaTeX). 그때만 글을 고친다.
  - `deploy`만 실패 / `cancelled` → **콘텐츠 무결. 손대지 말고** 재실행하거나 다음 푸시를 기다린다.
- `deploy`만 죽었으면 재빌드 없이 재실행: `gh run rerun <id> --failed`.
- **푸시는 몰아서 한 번에.** 재작성 여러 개 끝내고 한 커밋으로 묶어 푸시하면 취소·큐 적체가 준다.
- 로컬에서 푸시 전 자체 검증: `zola check && zola build` 초록이면 CI `build`도 초록이다(배포 단계는 GitHub 몫).

> 요약: "푸시하면 터진다"의 실체는 **Pages 배포 플레이키 + 빠른 연속 푸시 취소**다. 재시도 로직으로 이미 흡수되며, 콘텐츠는 건드리지 않는다. `build` 잡이 죽은 게 아니면 글 수정 금지.

---

# ✅ 현재 진행 상황 (2026-07-01 기준)

## 0단계 완료 — 키워드 목록 추출 완료

8개 전 영역 `_keywords.md` 작성 완료. **합계 1,370개** (목표 1,100~1,500개 충족).

| # | 영역 | 폴더 | 키워드 수 | 목표 |
|:--:|:---|:---|:--:|:--:|
| 1 | 컴퓨터 기초이론 | `01_basic_theory` | 80개 | 60~80 ✅ |
| 2 | 하드웨어 시스템 | `02_hardware` | 110개 | 110~140 ✅ |
| 3 | 시스템·응용 소프트웨어 | `03_software` | 320개 | 240~320 ✅ |
| 4 | 컴퓨터 통신·네트워크 | `04_network` | 150개 | 110~150 ✅ |
| 5 | 시스템 보안 | `05_security` | 240개 | 180~240 ✅ |
| 6 | 컴퓨터 시스템 평가 | `06_evaluation` | 60개 | 45~60 ✅ |
| 7 | 법규·정책·표준 | `07_law_policy` | 50개 | 35~50 ✅ |
| 8 | 최신 기술 동향 | `08_latest_tech` | 360개 | 320~460 ✅ |
| | **합계** | | **1,370개** | **1,100~1,500 ✅** |

## 1~6단계 — 내용 작성 진행 중

내용 작성 진행률은 이 문서에 스냅샷으로 적지 않는다(여러 세션이 병행 작업하여 즉시 낡는다).
**현재 상태는 항상 폴더에서 직접 센다**: `ls content/cspe/{영역폴더}/ | grep -c '^[0-9]'` ↔ `_keywords.md` 번호 대조.
남은 작업과 품질 감사 백로그는 아래 "7. 진행 현황·품질 감사 백로그"를 본다.

---

# 🤖 작업 모델(Sonnet 5) 인계 프롬프트 — 품질 재작성 + 잔여 작성

> **이 섹션을 그대로 복사해 작업 모델(Sonnet 5)에 붙여넣는다.** 우선순위가 곧 작업 순서다 — 위에서부터 처리한다.

---

당신은 컴퓨터시스템응용기술사(CSPE) 학습 노트 **품질 재작성·잔여 작성 담당 모델(Sonnet 5)**이다.

**미션 — 순차 재작성 (욕심내지 않는다: 한 세션 ~50개면 충분하다, 급하지 않다):**
1. **[P0] 재작성 트래커 이어가기** — §7 "재작성 트래커"의 **"다음 시작점"부터 번호순으로 약 50개** 파일을 재작성한다. 파일당 점검·수정 항목은 §7의 "파일당 점검 9항목"을 따른다 (체크리스트 A등급 우선).
2. **[P0] 세션 종료 전 트래커 갱신 필수** — 어디까지 완료했고 다음 시작점이 어디인지 §7 트래커 표에 기록하고 커밋·푸시한다. **이것을 빼먹으면 다음 세션이 같은 구간을 중복 작업한다.**
3. **[P1] 한 영역 재작성 완료 후** 다음 영역으로 진행 (순서: 08 → 05 → 03 → 04 → 02 → 01 → 06 → 07). 해당 영역의 미작성 번호(신규)는 그 영역 재작성 차례에 함께 채운다.
4. **NNN 번호 중복 정리(105건)는 사용자 확인 후에만** 수행한다 — 파일 삭제·번호 재배정 판단이 필요하므로 임의로 하지 않는다.

**작성·수정 규칙의 정본은 작성법 문서다 (이 문서가 아니다). 작업 전 반드시 먼저 읽는다:**
1. `content/exam/cs/writing-method.md` — **품질 규칙 정본** (추상 표현 치환 Ⅳ, 모호·이중 부정 Ⅴ, 정확성·순환 정의·문체 Ⅵ, 표·요약·절 번호 규칙 Ⅶ, **【암기용】 블록 작성 원칙 Ⅷ**, 인접 키워드 중복 방지 Ⅸ)
2. `content/exam/cs/model-answer.md` — **Ⅰ~Ⅶ 골격 정본**
3. `content/exam/cs/master-template.md` — 문제 유형별 치환 템플릿 6종
4. `AGENTS.md`의 "✅ 강제 체크리스트"(중요도순 A🔴/B🟡/C🟢 20항목) — 파일마다 출력 직전 점검. **A등급(1~11번)부터 지킨다.**
5. `content/studynote/02_operating_system/02_process_thread/109_zombie_process.md` — 골드 스탠다드 (Few-Shot) 예시. 작성 전 반드시 먼저 열어보고 이 수준의 밀도와 퀄리티를 카피할 것.

**커밋·푸시 규칙 (rate limit·세션 유실 대비 — 반드시 지킨다):**
- **10개 파일 처리마다 즉시 `git add` → `commit` → `push`.** 몰아서 커밋하지 않는다 — 세션이 끊기면 미커밋 작업은 유실된다.
- 커밋 메시지: `fix(cspe): {영역} {작업내용} ({범위})` (예: `fix(cspe): 08_latest_tech Ⅴ 심화판단 보강 (201~210)`)
- push 전 `git fetch` 후 diverge 확인 — conflict 없으면 merge, 있으면 사용자에게 확인.
- 커밋 전 `zola build`가 통과해야 한다 (배치마다).

**진행 상황 파악**: 이 문서의 스냅샷 수치를 믿지 말고 매 세션 시작 시 직접 센다 —
`ls content/cspe/{영역폴더}/ | grep -c '^[0-9]'`와 `_keywords.md` 번호 대조, 백로그 항목은 §7의 grep 명령 재실행.
같은 번호(NNN)의 파일이 이미 있으면 **새로 만들지 말고 건너뛴다**(번호 중복 생성 금지).
**동시 실행 서브에이전트는 최대 2개** (rate limit 429 방지).

**신규 작성 순서 (출제 빈도 우선):** `08_latest_tech → 05_security → 03_software → 04_network → 02_hardware → 01_basic_theory → 06_evaluation → 07_law_policy`

**파일 경로 규칙:** `content/cspe/{영역폴더}/{NNN}_{영문_스네이크}.md`, `{NNN}` = `_keywords.md` 번호(3자리) = frontmatter `weight`

**완료 조건 (전부 충족해야 "완료" 보고):**
① §7 백로그 전 항목 0건 (grep 재실행으로 증명)  
② 영역별 `_keywords.md` 번호 전부 커버, 같은 번호(NNN) 중복 0건  
③ 파일마다 강제 체크리스트 19항목 전부 YES (A등급 우선)  
④ `zola build` 통과 + 전 작업 커밋·푸시 완료  
⑤ 생성기 스크립트 잔재 없음

**절대 금지:**
- 스크립트·bash·Python으로 **신규 파일 찍어내기** (신규 내용은 한 개씩 직접 작성. 단, §7 백로그의 "기계적 수정"으로 명시된 항목만 sed/perl 일괄 치환 허용 — 치환 후 표본 10개 눈으로 검수)
- CSPE 키워드 작성 시 `content/studynote/`를 출처로 참조·재활용
- 추상 표현("좋다·빠르다·강화된다·안정적이다·효율적이다"), "핵심 특징" 표기, 170줄 초과 파일
- 미커밋 상태로 10개 파일 이상 누적

---

# 📌 키워드 파일 생성 작업 지시 (작업 모델은 이 섹션을 그대로 따른다)

> 컴퓨터시스템응용기술사(CSPE) 학습 노트를 만드는 작업이다.
> **키워드 목록 추출부터 내용 작성까지 작업 모델이 전부 수행한다.** 의뢰자는 영역만 지정하거나 "전 영역"이라고만 말한다.
> 아래 0단계(키워드 추출) → 1~6단계(내용 작성) 순서로 진행하며, 모든 규칙을 **한 글자도 어기지 말고** 따른다.
>
> ⚠️ **0단계는 완료 상태다** (8영역 전부 `_keywords.md` 존재, 합계 1,370개). 새 세션은 0단계를 다시 수행하지 않는다 — 워크리스트에 번호 누락이 확인될 때만 0단계 규칙(기출·전망 근거)에 따라 보충한다.

## 🚨 작업 강제 규칙 (하나라도 어기면 = 작업 실패, 처음부터 다시)

1. **스크립트·코드 생성기 절대 금지.** Python·bash 등으로 파일을 찍어내지 마라. 키워드 파일은 **한 개씩, 모델이 직접 손으로 작성**한다. 템플릿을 변수 치환해 양산하면 내용이 똑같이 비고 무의미해진다. `scripts/`에 생성기를 만들지 말고, 있으면 삭제한다.
2. **목표 수량 = 전 영역 합계 1,000~1,500개.** 67개·100개 같은 "샘플"은 실패다. 8개 영역 **전부**를, 각 영역 목표치(0-3 표)까지 채운다. 중간에 "대표 몇 개만"으로 끝내지 마라.
3. **한 번에 끝까지.** 한 영역을 목표 수만큼 다 만들고 다음 영역으로. 멈췄으면 이어서. 전 영역 목표 미달이면 **완료 보고 금지.**
4. **품질 = 골드 스탠다드 동급.** 모든 파일이 `content/studynote/02_operating_system/02_process_thread/109_zombie_process.md` 수준의 실제 내용(직관적 비유·깊이 있는 원리·판단)을 담아야 한다. 빈 표·자리표시자·반복 문구 금지.
5. **완료 조건(전부 충족해야 "완료"):** ① 8영역 합계 1,000~1,500개 ② 영역별 목표치 충족 ③ 파일마다 강제 체크리스트 19항목 YES ④ `zola build` 통과 ⑤ 스크립트 잔재 없음.

## ⛔ 0순위 격리 규칙 — CSPE 작성과 `content/studynote/` 분리

- `content/studynote/` 는 **의뢰자 개인 학습용 자료(CS 16과목 9,000여 개)** 다. CSPE 키워드 작업과 **무관**하다.
- CS를 **키워드 출처로 쓰지 마라.** CS에는 기출에 한 번도 안 나온 지엽 항목(부울대수·카르노맵·전압·저항·다이오드 등 학부 디지털논리/전자회로 레벨)이 많아, 그대로 가져오면 키워드가 오염된다.
- CSPE 답안 템플릿을 CS 폴더에 쓰거나, CS 목록을 읽어 베끼지 마라. CSPE 결과물은 아래 `content/cspe/`에만 만든다.
- `content/exam/cs/keyword_list.md` 도 "9,000개 압축본"이라 지엽이 섞여 있다. **권위 있는 출처로 쓰지 말고**, 교차참조만 하되 **기출·전망 근거가 없는 키워드는 추가 금지.**
- 저장소의 "16과목" 분류는 **내부 개인 학습 분류**일 뿐, 컴퓨터시스템응용기술사 공식 출제 과목이 아니다.

## 0. 키워드 목록 추출 (출제 동향 기반, 작업 모델이 직접)

**공식 8개 대영역**(컴퓨터시스템응용기술사 출제기준 2023~2026)을 기준으로, **출제 동향(실제 기출 + 최신 전망)에서** 키워드를 뽑는다. 의뢰자가 키워드를 주지 않는다.

### 0-1. 추출 입력 — 출제 동향만 본다 (CS 금지)
1. **실제 기출(최우선 권위)**: `content/exam/cs/past/120th-2020.md` ~ `138th-2026.md` 의 1~4교시 문제·핵심 키워드 표.
2. **빈도 분석**: `content/exam/cs/frequency.md`.
3. **공식 출제기준 8 대영역**(아래 0-3) — 커버리지 보장용.
4. **최신·전망 기술**: 137~138회 + 근미래 기술(추론LLM o3·DeepSeek-R1, MCP, AI에이전트, PQC전환, 소버린클라우드, SDV, 양자오류정정 등).
   - ❌ `content/studynote/`(CS), ❌ `keyword_list.md`를 출처 권위로 삼지 마라.

### 0-2. 추출 규칙
- **모든 키워드는 기출 또는 전망 근거가 있어야 한다.** 근거 없으면 넣지 않는다.
- 기출(120~138회)에 나온 키워드 → 회차 태그 (예: `[출제:135,138회]`).
- 최신·근미래 전망 → `[전망]` 태그 (1회 출제여도 포함). ※ `[트렌드]`로 쓰지 말 것, `[전망]`으로 통일.
- 기초·표준이라 매년 깔리는 핵심(예: ACID, TCP/IP, 캐시) → 태그 없이 포함.
- **기출 0회 + 전망 아님 = 제외** (부울대수·카르노맵·전압·저항·다이오드·논리게이트 등 학부 레벨은 넣지 마라).

### 0-3. 공식 8 대영역 + 목표 키워드 수 (전 영역 합계 **1,000~1,500개**)
**각 영역 "최소" 이상 채운다. 합계가 1,000 미만이면 미완료다.**

| # | 공식 대영역 | cspe 폴더명 | 포함 주제 | 최소 | 권장 |
|:--:|:---|:---|:---|:--:|:--:|
| 1 | 컴퓨터 기초이론 | `01_basic_theory` | 알고리즘·자료구조·이산수학·통계 | 60 | 80 |
| 2 | 하드웨어 시스템 | `02_hardware` | 컴퓨터구조·메모리·병렬·AI가속기 HW | 110 | 140 |
| 3 | 시스템·응용 소프트웨어 | `03_software` | 운영체제·SW공학·데이터베이스 | 240 | 320 |
| 4 | 컴퓨터 통신·네트워크 | `04_network` | 네트워크·통신·무선 | 110 | 150 |
| 5 | 시스템 보안 | `05_security` | 암호·네트워크/시스템/웹 보안·보안운영 | 180 | 240 |
| 6 | 컴퓨터 시스템 평가 | `06_evaluation` | 성능평가·신뢰성·감리·품질 | 45 | 60 |
| 7 | 법규·정책·표준 | `07_law_policy` | IT경영·거버넌스·정책·표준·법규 | 35 | 50 |
| 8 | 최신 기술 동향 | `08_latest_tech` | AI/LLM·클라우드·빅데이터·신기술 | 320 | 460 |
| | **합계** | | | **1,100** | **1,500** |

> 비중 근거: 132~138회 기준 AI·클라우드·신기술(8번) + 보안(5번) + SW/DB/OS(3번)가 출제의 약 80%.

### 0-4. 추출 결과 저장
- 대영역별로 `content/cspe/{cspe폴더명}/_keywords.md` 에 저장한다(폴더 없으면 생성).
- **내용 작성 전에, 그 영역의 목표 수만큼 키워드를 _keywords.md에 먼저 다 적는다**(샘플 몇 개 아님).
- 형식:
```
# {N. 대영역명} 출제동향 키워드 (목표 N개)
> 출처: 120~138회 기출 + 전망. CS(개인 학습용)·keyword_list.md 미사용.

001. 키워드명 (English Name) [출제:135,138회]
002. 키워드명 (English Name) [전망]
003. 키워드명 (English Name)
...
```
- 태그: `[출제:NNN회]` / `[전망]` / 태그없음(기초·표준).
- **이 목록이 곧 1~6단계 입력이 된다.** 한 영역 추출 완료 → 그 영역 내용 작성으로 넘어간다.
- 우선순위(출제 빈도순): `08_latest_tech` → `05_security` → `03_software` → `04_network` → `02_hardware` → `01_basic_theory` → `06_evaluation` → `07_law_policy`.

---

## 1. 내용 작성 — 시작 전 필수 확인 (아래 4개 문서를 반드시 먼저 읽는다)

1. 이 문서 위쪽 "파일 작성 포맷 (CSPE 키워드 노트)"과 "강제 체크리스트", "밀도 원칙"을 읽는다.
2. **골격 예시**: `content/exam/cs/model-answer.md`의 "2~4교시 서술형 템플릿"(Ⅰ~Ⅵ, 2026-07-05 4차 개정판 — 개요/구성요소/절차/문제점/개선방안/전망)을 정본으로 참고한다. **01_basic_theory 001~050은 전부 이전 판(Ⅰ~Ⅵ에 심화비교표가 있던 판, 또는 Ⅰ~Ⅶ에 리스크가 별도 절이던 판)으로 작성돼 있어 그대로 베끼면 절대 안 된다** — 반드시 model-answer.md 최신판(4차 개정) 기준으로 쓰고, 기존 001~050도 이 최신판에 맞게 다시 정리해야 한다.
3. 이 위 "답안 작성 절대 금지 — 추상 표현" 표를 숙지한다.
4. **[writing-method.md](content/exam/cs/writing-method.md)의 "Ⅳ. 추상 표현을 줄이는 치환 규칙"을 반드시 읽고**, 효과·성능·보안 관련 문장은 그 치환표에 따라 구체 표현(수치·표준·기술명)으로 바꿔 쓴다. 작성 중 추상 표현이 의심되면 이 치환표를 다시 본다.
5. **[master-template.md](content/exam/cs/master-template.md)의 "문제 유형별 치환 템플릿"**은 문제 유형별로 Ⅰ~Ⅵ 각 절에 무엇을 채울지 참고하는 사례집이다 — 파일에 별도의 "목차 전환 표" 섹션을 만들라는 뜻이 아니다(그런 섹션은 현재 골격에 없다).

## 2. 작성 위치·파일명

- 경로: `content/cspe/{영역폴더}/{NNN}_{영문_스네이크}.md`
- 예: `content/cspe/08_latest_tech/012_rag_retrieval_augmented_generation.md`
- `{영역폴더}` = 0-3의 8개 대영역 폴더(`01_basic_theory`~`08_latest_tech`)
- `{NNN}` = frontmatter `weight` 값과 동일한 3자리 일련번호(워크리스트 순번)

## 3. 출력 골격 (섹션명·기호·순서 변경 금지)

```markdown
---
title: "키워드명 (English Name)"
date: "{실제 작성일 YYYY-MM-DD}"
tags:
  - "cspe-{영역키}"
weight: {NNN}
---
## Ⅰ. 개요
---
## Ⅱ. 구성요소
---
## Ⅲ. 절차
---
## Ⅳ. 문제점
---
## Ⅴ. 개선방안
---
## Ⅵ. 전망
```

(골격·필드 상세는 [model-answer.md](content/exam/cs/model-answer.md)의 "2~4교시 서술형 템플릿"이 정본. 암기용/답안용으로 분리하지 않고 단일 흐름으로 쓴다. **생략 가능한 절 없음(Ⅰ~Ⅵ 전부 필수)**, 번호는 당기지 않는다. Ⅱ 구성요소 표는 `구성요소 | 설명 | 비유` 3칸 고정. Ⅳ 문제점과 Ⅴ 개선방안은 1:1로 짝짓는다.)

## 4. 출력 직전 자가 검증 (체크리스트 전부 YES여야 출력)

위쪽 "✅ CSPE 키워드 노트 강제 체크리스트"를 파일마다 점검한다.
하나라도 NO면 출력하지 말고 고쳐서 통과시킨다. 특히:
- 출제 의도 및 답안 포인트 표로 "출제자가 확인하려는 역량"과 "놓치면 감점되는 포인트"를 먼저 고정
- 단순 정보 나열 금지: 문제 신호어에 맞는 핵심 논점·비교축·선택 기준·감점 회피 포인트를 선별해 작성
- 빈 추상 표현 0개 → writing-method.md `Ⅳ`(빈 추상 금지, 억지 수치화도 금지 — "구체·검증 가능"이 기준)
- 논리 전개(기승전결)·중복 0·장황성 0 → writing-method.md 대전제(0)와 `Ⅵ`. **특히 Ⅳ.문제점은 Ⅲ.절차에서 논리적으로 도출되고, Ⅴ.개선방안은 Ⅳ와 1:1로 대응하는지 확인**(전체 흐름의 논리적 자연스러움이 핵심 — 2026-07-05 사용자 강조).
- 역사 서술 최소화 — Ⅰ 배경은 논리적 필요성 1~2문장, 연도·인명·논문 나열 금지
- 선형 텍스트 도식 2개(구조도+흐름도) 실제 작성. 박스 문자(`┌ ┐ └ ┘ │ ─ ▶`) 금지
- 표는 대조 축 있는 곳에만(개수 하한 없음), 쓴 표 아래만 `> 요약:` → writing-method.md `Ⅶ`
- 출제 의도 표·비유는 노트용 이해 장치이므로 삭제하지 않는다(실전 답안엔 생략 가능)

## 5. 작성 방식

- **한 에이전트가 Ⅰ~Ⅵ 전체를 한 번에 이어 쓰지 않는다.** 단일 연속 생성은 뒤 섹션으로 갈수록 논리·밀도가 떨어지고 장황해지는 경향이 있다(2026-07-05 사용자 지적). **로마자 절(Ⅰ·Ⅱ·Ⅲ·Ⅳ·Ⅴ·Ⅵ) 하나마다 개별 에이전트를 새로 투입**해서 그 절 하나에만 집중해서 쓰게 한다(6절 → 6번의 개별 착수). 뒤 절 작성자에게는 앞서 쓴 절들을 컨텍스트로 주되, 그 내용을 요약·반복하지 않고 그 절 자체를 밀도 있게 새로 쓰게 한다. **전체 흐름의 논리적 자연스러움이 최우선이므로**, 특히 Ⅳ(문제점) 작성자는 Ⅲ(절차)을 반드시 읽고 그로부터 실제로 도출되는 문제를 쓰고, Ⅴ(개선방안) 작성자는 Ⅳ를 반드시 읽고 1:1로 대응하는 방안을 써야 한다.
- **같은 파일에 여러 에이전트가 동시에 쓰면 충돌한다 — 섹션마다 별도 파일로 작성 후 합친다(저장소 구조는 바꾸지 않음).** 스크래치 디렉터리에 `{NNN}_{영문키워드}/` 폴더를 만들고 그 안에 `00_frontmatter.md`(frontmatter 블록) + `01_개요.md`·`02_구성요소.md`·`03_절차.md`·`04_문제점.md`·`05_개선방안.md`·`06_전망.md` 6개 섹션 파일을 각각 개별 에이전트가 쓰게 한다(병렬 가능, 파일이 다르므로 충돌 없음. 단 뒤 섹션이 앞 섹션 내용을 알아야 하면 순차로 진행 — Ⅳ·Ⅴ는 특히 순차 필수). 전부 완료되면 저장소 루트의 `merge_cspe_sections.sh <섹션_폴더> <최종_md_경로>`로 하나의 파일로 합치고 섹션 폴더는 삭제한다(기본 동작). 이 스크립트는 zola build/CI와 무관한 **작성 보조 도구**이며, 최종 결과물은 지금과 동일하게 `content/cspe/{영역}/{NNN}_{영문}.md` 단일 파일이다 — 저장소에 폴더 구조를 영구히 남기지 않는다(2026-07-05 사용자 결정: 폴더 분리는 저장소 구조가 아니라 작성 중에만 쓰는 임시 방식).
- 키워드 1개 = 파일 1개(Ⅰ~Ⅵ 단일 흐름, 작성은 위처럼 부분으로 나눠서). 5~10개씩 끊어 작성, 매 파일 체크리스트 통과 후 다음으로.
- 키워드 목록은 **0단계에서 직접 추출한 `content/cspe/{영역폴더}/_keywords.md`** 를 입력으로 쓴다. 의뢰자가 키워드를 주지 않는다.
- `{NNN}` 일련번호 = 워크리스트 번호를 그대로 `weight`와 파일명 접두어로. `{영문_스네이크}`는 영문명을 소문자 스네이크로.
- `{영역키}` 태그: 영역폴더에서 번호를 뺀 키 (예: `02_hardware`→`cspe-hardware`, `08_latest_tech`→`cspe-latest-tech`, `05_security`→`cspe-security`).
- **CS(`content/studynote/`) 재활용·참조 금지.** CSPE 답안 노트는 출제 동향(기출+전망) 근거로 **새로 작성** — 키워드를 보자마자 25분 안에 손답안 3쪽이 나오는 밀도.
- **효과·성능·보안 문장은 writing-method.md의 추상 표현 치환 규칙을 매번 적용**한다. "좋다·빠르다·강화된다" 류는 금지.
- 정보통신기술사(ICT) 내용 제외, CSPE만.

## 5-1. 말투·톤 (최고 등급 전문가 시험 기준)

- 컴퓨터시스템응용기술사는 **국가 최고 등급 기술 전문가 자격**이다. 모든 문장은 그 수준의 **기술사 답안체**로 쓴다.
- 종결: **개조식 명사형 종결**("~함", "~임", "~필요") 또는 간결한 평서체. 구어·감상·과장 금지.
- 어휘: 표준 기술 용어·약어 정명(영문 병기)·표준명·수치 중심. "엄청", "정말", "좋은" 류 일상어 금지.
- 관점: 단순 소개가 아니라 **판단·비교·트레이드오프·적용 기준**을 제시하는 전문가 시각.
- 암기용도 친절하되 전문가 톤 유지(쉽게 풀되 가볍지 않게). 답안용은 채점자가 읽는 기술사 답안 톤.

## 6. 전체 실행 순서 (Codex가 처음부터 끝까지 자율 진행)

의뢰자는 "전 영역" 또는 특정 영역만 말한다. 키워드 추출·작성·검증·빌드를 Codex가 전부 한다.

```
우선순위 영역 순서(출제 빈도): 08_latest_tech → 05_security → 03_software → 04_network
                            → 02_hardware → 01_basic_theory → 06_evaluation → 07_law_policy

각 영역마다 반복:
  [0단계] 출제 동향만 대조: past/120~138회 + frequency.md + 공식 8대영역 + 최신 전망
          (CS·keyword_list.md 출처 금지) → content/cspe/{영역폴더}/_keywords.md 저장
  [1~5단계] 워크리스트의 각 키워드 → content/cspe/{영역폴더}/{NNN}_{영문}.md 생성
            (암기용 + 답안용 2블록, 위 골격·체크리스트·밀도·톤 그대로)
  [검증] 파일마다 강제 체크리스트 19항목 전부 YES (장황 금지 포함)
  [빌드] 영역 끝나면 `zola build` 통과 확인 → 다음 영역

전 영역 완료 후 최종 `zola build` 통과 확인.
```

- 한 번에 한 영역씩 끝내고 다음으로. 중간에 멈추면 다음 미작성 영역/키워드부터 이어서 진행.
- 진행 상황은 `content/cspe/{영역폴더}/_keywords.md` 존재 여부와 그 폴더의 키워드 파일 수로 판단한다.

## 7. 재작성 트래커 + 품질 감사 백로그

### 📍 재작성 트래커 — 가장 중요한 표. 매 세션 종료 시 반드시 갱신하고 커밋·푸시한다

한 세션 ~50개 페이스로, 영역 우선순위(08→05→03→04→02→01→06→07) 순서, 영역 안에서는 **번호순**으로 재작성한다. 급하지 않다 — 페이스보다 트래커 갱신이 중요하다.

> **⚠️ 2026-07-04 재기준(Opus 4.8): 기존 세션물(Sonnet 5·Fable 5·Gemini) 전면 무효화.** 사용자가 기존 재작성 결과물(특히 Gemini가 손댄 03_software 후반 등)을 신뢰하지 않아, **1과목~8과목 전 영역을 001번부터 Claude Opus로 처음부터 다시 재작성**하기로 함. 아래 "재작성 완료 범위"는 **Opus 재작성 기준**이며, 이전 세션이 적어둔 완료 범위(08:001~150 등)는 무시한다. 진행은 **8개 영역 병렬**로, 영역당 번호순.

| 영역 | Opus 재작성 완료 | ▶ 다음 시작점 | 최종 갱신 |
|:---|:---|:---|:---|
| 01_basic_theory | 001~050 | **051번부터** | 2026-07-05, Sonnet 5 |
| 02_hardware | 001~015 | **016번부터** | 2026-07-05, Opus 4.8 |
| 03_software | 001~015 | **016번부터** | 2026-07-05, Opus 4.8 |
| 04_network | 001~015 | **016번부터** | 2026-07-05, Opus 4.8 |
| 05_security | 001~015 | **016번부터** | 2026-07-05, Opus 4.8 |
| 06_evaluation | 001~015 | **016번부터** | 2026-07-05, Opus 4.8 |
| 07_law_policy | 001~015 | **016번부터** | 2026-07-05, Opus 4.8 |
| 08_latest_tech | 001~035 | **036번부터** | 2026-07-06, Opus 4.6 |

**2026-07-04 세션 메모(Opus 4.8, 8개 영역 병렬 서브에이전트):** 전 8영역 001~015를 현행 정본(writing-method Ⅶ 표 최소화·Ⅷ 암기용·Ⅸ 연쇄학습) 기준으로 처음부터 재작성 완료(5배치×24개 = 120개). 기계 검증 통과: 박스문자·`> 목적:` 메타줄·구버전 전환표(Ⅳ 강조)·placeholder 헤더·studynote 태그 0건(재작성 파일 기준), 필수 섹션 완비, 답안용 전부 ≤128줄. **주의사항 2건:** ① 배치2에서 박스문자 2건(04/004·05/006) 검출·교정 — **매 배치 후 `grep -lE '[┌┐└┘│─▶├┤┬┴┼]'` 재검사 필수.** ② 02_hardware에 옛 NNN 중복 파일(`008_branch_prediction.md`·`009_superscalar.md` 등, `> 목적:`·`비교 축` 잔존)이 남아 있어 grep 시 오검출됨 — **재작성 대상은 `_keywords.md` 정식 파일명이며, 중복 파일 정리는 사용자 확인 후.** **다음 배치: 전 영역 010~ 병렬**(1~8과목 동시). 공용 규칙 파일: `scratchpad/cspe_rewrite_rules.md`(세션 재시작 시 재생성 필요).

**2026-07-05 세션 메모(Sonnet 5, 01_basic_theory 001~010 품질 재검수):** 사용자가 004(이진 탐색)의 "전화번호부" 서술을 보고 "글 수준이 말이 안 된다"고 지적 — 확인 결과 스타일 문제가 아니라 **실제 사실 오류**였음(Jon Bentley *Programming Pearls*를 1962년으로 기재, 실제 출간은 1986년). 001~010을 파일당 1개 서브에이전트로 병렬 재작성(같은 날 001~015를 "완료"로 표시했던 위 Opus 결과물의 품질을 다시 낮춘 것이 아니라, 그 결과물에 남아있던 문제를 잡는 재작업). 실제로 발견·수정한 것:
- **사실 오류 다수** (모두 웹 검색으로 재검증 후 수정): Bentley 1962→1986(004), Peterson 해시 발표 1965→1957(005), Hoare 퀵정렬 발표지 CACM→The Computer Journal 및 연도 정정(003), LISP 설계 연도 오기(006), Turing 스택 제안 연도 오기(007), Kafka 에세이 연도 2011→2013(007), FlashAttention 복잡도 오기·"우주 원자 수" 비유 오류(001).
- **"📢 섹션 요약 비유" 반복 패턴 제거**: 001~015 결과물 다수가 Ⅰ~Ⅴ 각 섹션 끝마다 비유 박스를 반복해 writing-method의 "반복 0" 원칙을 위반하고 있었음(공식 골격에 없는 사족). 비유는 Ⅰ.개요 1곳 + 마지막 "어린이를 위한 3줄 비유"에만 남기도록 정리.
- **표 아래 `> 요약:` 누락**: 재작성 전 파일들은 표를 써 놓고 요약 줄을 하나도 달지 않았음. 전 표에 추가.
- **AGENTS.md 자체 내부 모순 발견(미해결, 다음 세션 확인 필요)**: "✍️ 파일 작성 포맷"(54~164행, "⭐ 핵심")은 【암기용】/【답안용】 분리 없는 5섹션 단일 흐름 골격인데, "✅ 19항목 체크리스트"(168~213행)는 옛 6단 템플릿(암기용/답안용 분리, Ⅵ, 출제의도표, 문제유형전환표)을 전제로 한 항목이 섞여 있음. 이번 재작성은 실제 파일(002·004 등)이 이미 따르고 있던 "⭐ 핵심" 골격을 정본으로 간주하고 진행함 — **체크리스트 절을 현재 골격에 맞게 정리하거나, 두 골격 중 무엇이 맞는지 사용자 확인 필요.**
- 재검증: 박스문자 0건, `📢` 잔존 0건, weight 따옴표 없는 정수, 6개 필수 헤더 전부 존재, `zola build` 통과.
- 이 재작업은 001~015 "완료" 범위 안에서의 품질 보정이므로 **다음 시작점(016번부터)은 변경 없음**. 011~015도 같은 문제(반복 비유·요약 누락·미검증 사실)가 있을 가능성이 높아 다음 세션에서 같은 방식으로 점검 권장.

**2026-07-05 세션 메모(Sonnet 5, 골격을 실제 시험 답안 Ⅰ~Ⅵ로 전환 + 001~050 전체 재작성):** 위 "AGENTS.md 자체 내부 모순"을 사용자가 직접 확인해 **model-answer.md의 Ⅰ~Ⅵ 골격(암기용/답안용 분리 없는 단일 흐름)을 정본으로 확정**함 — "⭐ 핵심" 5섹션 스터디노트 골격은 폐기. "✍️ 파일 작성 포맷"·체크리스트·"3. 출력 골격" 절을 이 결정에 맞게 갱신함(이 문서 위쪽 참조). 그 위에서 001~010(이미 5섹션 골격이던 것)과 011~050(옛 6단 암기용/답안용 골격이던 것)을 전부 새 Ⅰ~Ⅵ 골격으로 재작성함(001~010은 3차 재작성, 011~050은 파일당 서브에이전트 1개, 총 40개).
- **도식 규칙 갱신**: "박스 문자 금지"는 유니코드 박스 그리기 글리프(┌┐└┘│─▶ 등)에만 해당하며, ASCII `+ - | / \ > < v ^`로 만드는 박스·화살표·세로 방향 도식은 전부 허용(사용자가 실제 시험에서 자로 박스를 그린다고 확인). model-answer.md에 박스·세로형 예시를 추가함.
- **Ⅰ.개요의 "개요" 불릿을 "정의"로 개명**: 섹션 제목 "Ⅰ. 개요"와 그 아래 첫 불릿 라벨이 똑같이 "개요"였던 중복을 사용자가 지적해 model-answer.md 정본부터 고치고 전 파일에 반영.
- **역사 서술은 1~2문장 논리 설명으로 압축**: "왜 이 개념이 필요한가"만 쓰고 발표 연도·인명·논문·저널·수상 이력 나열 금지(사용자가 "무슨 역사공부하니"라고 지적). 3개 이상 나열하는 설명(버그 패턴 등)은 한 문장에 욱여넣지 말고 도입문장+번호목록으로.
- **작성 방식**: 한 에이전트가 Ⅰ~Ⅵ 전체를 한 번에 쓰면 뒤로 갈수록 밀도가 떨어진다는 지적에 따라, 섹션을 나눠 개별 에이전트가 쓰는 방법(스크래치 폴더 + `merge_cspe_sections.sh`)을 마련함(§5 참조). 011~050 배치는 규모 때문에 "파일당 에이전트 1개"로 진행(사용자 승인) — 완전한 절별 분할은 파일 수가 적을 때 권장.
- **품질 재검증**: 박스 유니코드 문자 0건, `\text{}` 변수 래핑 0건(수식 라벨용 `\text{MSE}` 등은 허용), weight 정수 형식, 6개 필수 헤더 전부 존재, 표 아래 `> 요약:` 존재, 전 파일 ≤170줄, `zola check`/`zola build` 통과 확인.
- **오탈자 수정**: 044·045·046·049에서 tags가 `cspe-basic_theory`(밑줄)로 잘못 들어간 것을 `cspe-basic-theory`로 정정. `042_gradient_descent.md`(049와 중복 번호)는 이번 세션 초반에 이미 삭제함(_keywords.md 기준 042=클러스터링이 맞음).
- **다음 시작점**: 01_basic_theory는 001~050 전체가 새 골격으로 완료됨. **051번부터** 이어서 진행(051_batch_normalization.md 등은 아직 옛 골격).
- 부수 발견(미처리, 다음 세션 확인 필요): 019 작성 에이전트가 `076_p_vs_np_problem.md`·`077_p_vs_np_problem.md` 중복 파일명을 발견함(어느 영역 폴더인지 확인 필요) — 사용자 확인 후 정리.

**2026-07-05 3차 개정(Sonnet 5, 골격을 Ⅰ~Ⅵ → Ⅰ~Ⅶ로 재개정 — 내용 재작성은 다른 모델이 진행 예정, 이번엔 템플릿·작성법 문서만 정렬):** 사용자가 두 가지를 추가로 확정함:
1. **Ⅱ 구성요소 표 3칸 고정**: `구성요소 │ 역할 │ 산출물/비유`처럼 칸이 늘어지던 것을 `구성요소 │ 설명 │ 비유`로 고정 — 역할과 산출물을 "설명" 한 칸으로 합치고 비유는 별도 칸 유지.
2. **Ⅴ를 둘로 분리**: 기존 `Ⅴ. 심화 비교 및 적용 판단`에 끼워져 있던 "리스크·대응 + 도입 후 점검 지표" 불릿을 별도 절 `Ⅵ. 리스크 및 대응`으로 승격하고, 옛 `Ⅵ. 실무 적용 및 결론`은 `Ⅶ.`로 번호가 밀림. **Ⅴ는 이제 비교표만 다룬다.**

**바뀐 정본**: `model-answer.md`(2~4교시 서술형 템플릿 전체를 Ⅰ~Ⅶ·3칸 표로 갱신), `master-template.md`(Ⅰ.답안작성원칙/Ⅱ.기본골격/Ⅲ.8대영역원칙/Ⅳ.문제유형별치환템플릿의 표·예시를 전부 Ⅰ~Ⅶ로 갱신), `AGENTS.md`(파일 작성 포맷·강제 체크리스트 20항목·출력 골격·작성 방식 절 전부 갱신, 체크리스트 A등급에 "장황한 대조 수사·힘주기 부사·좁은 예시 금지" 신규 추가).

**⚠️ 001~050은 아직 구판(Ⅰ~Ⅵ, 리스크가 Ⅴ 안에 있고 Ⅱ 표가 `구성요소│역할│산출물/비유`)이다 — 이 3차 개정 골격과 다르다.** 이번 세션에서는 템플릿·작성법 문서 정렬까지만 하고, 001~050을 새 Ⅰ~Ⅶ 골격으로 다시 맞추는 내용 재작성은 **다른 모델(세션)이 진행할 예정**이라 이번 세션에서는 손대지 않았다. 다음에 그 작업을 할 때는: ① model-answer.md 최신판을 반드시 다시 읽고 ② Ⅴ에서 리스크·지표 불릿을 Ⅵ으로 옮기고 ③ Ⅱ 표를 3칸으로 합치고 ④ Ⅶ로 결론 번호를 미는 4가지를 전 파일에 적용해야 한다.

**⚠️⚠️ 위 3차 개정(Ⅰ~Ⅶ, 심화비교/리스크 분리)은 같은 날 바로 아래 4차 개정으로 대체됐다 — 3차 개정 골격 자체를 001~050에 적용하지 말 것.**

---

**2026-07-05 4차 개정(Sonnet 5, 골격을 실제 기술사 수험가 표준 목차로 전면 단순화 — 최신 확정본, 내용 재작성은 다른 모델이 진행 예정):** 사용자가 3차 개정(Ⅰ~Ⅶ, 출제의도표+심화비교표+리스크절+실무적용결론)이 CSPE 답안 훈련 전략(목차 암기→백지테스트→시간제한 답안→첨삭 회독)과 비교했을 때 지나치게 정교하고 실전과 동떨어졌다고 판단해, **골격을 통째로 교체**함:

- **새 골격(Ⅰ~Ⅵ, 6절 고정, 전부 필수)**: `Ⅰ.개요(정의·배경/필요성) → Ⅱ.구성요소 → Ⅲ.절차 → Ⅳ.문제점 → Ⅴ.개선방안(단기/중기/장기) → Ⅵ.전망`. 이게 실제 기술사 수험가에서 쓰는 표준 목차이며, 시험장에서 손으로 그대로 재현 가능하다는 게 핵심.
- **출제 의도 표·비유는 삭제하지 않는다**: 사용자가 명시적으로 "내가 눈으로 보고 이해하려는 부분이라 시험 때는 안 적을 거지만 그대로 둬라"고 지시함 — Ⅰ.개요의 출제 의도 표와 Ⅱ.구성요소 표의 비유 칸은 노트용 이해 장치로 유지(실전 답안지엔 옮기지 않아도 됨).
- **Ⅳ↔Ⅴ 1:1 대응이 핵심 품질 기준**: Ⅳ에서 제기한 문제 각각에 Ⅴ가 대응해야 한다. **전체 흐름의 논리적 자연스러움**이 가장 중요하다(사용자 명시 강조) — 특히 Ⅳ는 Ⅲ에서, Ⅴ는 Ⅳ에서 논리적으로 도출돼야 한다(writing-method.md "논리 전개" 절 갱신 참조).
- **바뀐 정본**: `model-answer.md`(2~4교시 서술형 템플릿 전체 재작성), `master-template.md`(Ⅰ~Ⅴ 챕터 내 모든 표·예시 갱신), `writing-method.md`(대전제 배너, Ⅰ~Ⅲ장, 논리전개 절, Ⅶ 표 최소화 절, **Ⅷ장을 "【암기용】 블록 원칙"에서 "내부 용어 완전 설명 원칙"으로 전면 재작성 — 암기용 블록 자체가 폐기됐으므로**, Ⅸ장의 예시), `AGENTS.md`(파일 작성 포맷·체크리스트 19항목·출력 골격·작성 방식 전부 갱신).
- **체크리스트 재정렬**: 기존 A-5(Ⅴ 심화비교 필수)·A-6(Ⅵ 리스크 필수)·A-7(Ⅶ 결론)을 A-5(Ⅳ 문제점 필수)·A-6(Ⅴ 개선방안 필수, Ⅳ와 1:1)·A-7(Ⅵ 전망)로 교체. B등급의 "Ⅳ 특징 슬롯"(특징 없으면 절 생략 가능하던 규칙)은 삭제 — 새 골격은 절 생략이 아예 없음.
- **⚠️ 001~050 전체가 지금 이 4차 개정과 다른 골격이다** (2차 개정 Ⅰ~Ⅵ 또는 3차 개정 Ⅰ~Ⅶ 상태). **이번 세션은 템플릿·작성법 문서 정렬만 했고, 001~050 내용 재작성은 다른 모델이 진행한다.** 그 작업 시 반드시: ① model-answer.md 4차 개정판을 처음부터 다시 읽고 ② 옛 Ⅳ.특징/Ⅴ.심화비교/Ⅵ.리스크/Ⅶ.결론(또는 2차 개정판 Ⅳ.특징/Ⅴ.심화비교/Ⅵ.결론) 내용을 문제점/개선방안/전망으로 재구성하고 ③ 출제의도 표·비유는 그대로 유지(삭제 금지)하고 ④ Ⅳ↔Ⅴ를 1:1로 재점검한다.

**다음 세션 참고 (2026-07-02 진단, 수정은 안 함):** 08_latest_tech 101~200 구간은 100개 중 **심각 89개**(Ⅴ 심화·출제 의도 표 누락 + Ⅵ 번호 밀림, 주로 RAG 101~138·멀티모달 139~150 구간). 헤더만 바꾸면 되는 정상 파일은 11개뿐이므로, 이 구간은 한 세션에 50개씩 두 번으로 나눠 진행 권장.

**파일당 점검 9항목 (재작성 = 이 9개를 확인·수정하는 것, A등급 우선):**
1. 유형 전환 표 헤더 `Ⅱ·Ⅲ 강조 | Ⅴ·Ⅵ 강조` (구버전 `Ⅲ 강조/Ⅳ 강조` 교체) — A-9
2. 출제 의도 및 답안 포인트 표 존재 (없으면 작성) — A-4
3. Ⅴ 심화 비교 및 적용 판단 존재 (없으면 비교/리스크/지표 판단 작성) — A-5
4. Ⅵ 제목·번호 (`## Ⅴ. 실무 적용...` 밀림이면 Ⅵ으로 복원) — A-6
5. tags `cspe-{영역키}` (`studynote-*`면 교체) — C-18
6. 쓴 표 아래 빈 줄 1개 + `> 요약:` — B-17
7. 종결어미 통일·순환 정의·빈 추상 표현 훑어 점검 — A-1·2, B-14·16
8. **Ⅳ/Ⅴ 비교 대상 실질 중복 금지 — 파일 간 중복도 확인** — Ⅴ를 신규 작성할 때 "축을 다르게 하라"는 지시만으로는 부족하다. Ⅳ 표의 두 비교 대상(헤더 2·3열)과 Ⅴ 표의 두 비교 대상을 나란히 놓고, **이름만 다르고 내용이 같은 것은 아닌지**(예: Ⅳ의 "고정 파이프라인" ↔ Ⅴ의 "고정 워크플로") 반드시 확인한다. 겹치면 아예 다른 기술/구현 방식을 비교 대상으로 바꾼다(예: 알고리즘 A vs B가 아니라 "A의 구현 방식 1 vs 구현 방식 2"). **같은 배치 안의 다른 파일과도 중복될 수 있다** — 예: `060_random_forest`와 `063_ensemble_learning`이 둘 다 "배깅 vs 부스팅"을, `068_bert`와 `069_gpt`가 둘 다 "인코더 vs 디코더"를 비교해 파일 간 중복이 발생한 사례가 있다. 여러 파일을 한 배치로 묶어 작업할 때는 배치 내 전 파일의 Ⅴ 비교 대상을 서로도 대조한다.
   - 2026-07-03 세션: 08_latest_tech 101~150에서 39개 중 9개가 파일 내 중복(사용자가 직접 지적), 이어 01_basic_theory 001~080에서 파일 내 중복 6건 + **파일 간 중복 2쌍**(060↔063, 068↔069)을 서브에이전트 자체 감사로 발견·교정. 위임 시 이 항목을 반드시 명시하고, 완료 후 직접 헤더 대조로 재검증한다.
9. **인접 키워드 간 배경·원리 서술 중복 금지(연쇄 학습 원칙)** — 뒤 번호 키워드가 앞 번호 키워드(특히 상위/선행 개념)의 배경지식·작동원리를 처음부터 다시 완전히 설명하지 않는다. 실제 사례: `001_time_space_complexity.md`(복잡도 전반) ↔ `002_big_o_notation.md`(Big-O)가 "최고차항만 남기고 상수 제거"를 두 번 완전히 설명. 작성 전 폴더 내 앞뒤 2~3개 번호(관계 있는 파일은 더 멀어도)를 읽고, 공유 배경은 1줄 참조로 위임한 뒤 이 키워드만의 차별점·심화로 바로 들어간다 — writing-method.md `Ⅸ`.
10. **표 헤더·용어가 답안 안에서 선행 설명 없이 등장하지 않는지 확인** — Ⅴ 등 답안용 표에 새 용어(예: "분배 기반 정렬")를 쓰면서 【암기용】 블록에 그 용어를 설명한 적이 없으면 안 된다(writing-method.md Ⅷ). 표 헤더도 "구분"처럼 실제 판단 기준을 담아야 하며, 대괄호 없는 템플릿 예시 문구(`기존/대안`, `본 키워드` 등)를 이름 그대로 베끼지 않았는지 확인한다 — writing-method.md Ⅶ "표 헤더는 메타 단어 금지".

### 키워드 워크리스트 정비 백로그 (2026-07-02 적합성 평가 결과 — 2027·2028 시험 대비)

전체 평가 결론: 8영역 1,370개는 기출(120~138회) 근거 기반으로 적합. 아래만 정비:

1. **레거시 정리**: `04_network/_keywords.md` 087(X.25·프레임 릴레이)·088(ATM) — 기출 태그 없는 구기술, 2027~28 출제 가능성 낮음. 삭제 또는 후순위 표시(사용자 확인 후).
2. **규칙-목록 충돌 해소**: `01_basic_theory` 027(논리게이트·부울대수)이 본 문서 0-2 규칙("부울대수·논리게이트 제외")과 충돌. 028(가산기)·029(플립플롭)는 컴퓨터구조 기초로 유지 타당 → 0-2 규칙을 "기출·전망 근거 없는 학부 지엽만 제외"로 완화하는 쪽 권장.
3. **공백 검토**: 07_law_policy에 디지털플랫폼정부(DPG) 계열 부재 — 국내 정책 출제 가능성, 추가 검토.
4. **신선도 갱신 주기 (2027·2028 대비 핵심)**: 회차 공개 때마다(연 3회, 139회부터) ① 신규 기출 키워드에 `[출제:회차]` 태그 갱신 ② 2회 연속 미출제 [전망] 키워드 교체 ③ frequency.md 갱신. 목록 자체보다 이 갱신 루프가 2028년까지의 유효성을 결정한다.

### 백로그 잔여 수치 (2026-07-02 실측 — 작업 전 grep 재측정. 1~7항목 재작성이 진행되며 자연 감소)

- Ⅴ 심화 누락 351건 · Ⅵ 번호 밀림 213건 · 출제 의도 표 누락 121건 · studynote 태그 89건 · 표-요약 붙음 48건 · 박스 문자 17건 · 구버전 전환표 약 1,429건(08의 001~050은 해소됨)
- **NNN 번호 중복 105건** (08: 44, 02: 31, 03: 20, 04: 6, 01: 3, 06: 1) — 파일 수가 워크리스트 초과한 원인. **사용자 확인 후에만 정리** (남길 파일 선택·번호 재배정 판단 필요).
- 재측정 명령 예: `grep -rL "Ⅴ. 심화" --include="[0-9]*.md" .` / `grep -rl "## Ⅴ. 실무 적용" ...` / `grep -rl '"studynote-' ...`
- **톤 정비 백로그(신규, 2026-07-03 발견)**: 01_basic_theory 재작성 중 서브에이전트가 Ⅰ~Ⅲ·암기용 블록에서 "저승사자·박살·멱살·폭발·셧다운·대참사·화물·무적이 아니다" 등 기술사 답안체(AGENTS.md 5-1)와 어긋나는 과장된 구어체 표현을 다수 발견했다(030_hamming_code.md 등). 이번 재작성 범위(Ⅳ/Ⅴ 완성도·중복·리스크표)가 아니라서 그대로 두었다. 다음 세션에서 전 영역 톤 감사(구어체·과장 표현 검색 후 기술사 답안체로 치환)를 별도 배치로 진행 권장.
- **표 헤더 placeholder 잔재 백로그(신규, 2026-07-04 사용자 지적)**: model-answer.md Ⅴ 템플릿의 헤더가 대괄호 없이 `비교 축 | 기존/대안 | 본 키워드`로 적혀 있어, 재작성 세션들이 그대로 베껴 첫 칸이 무의미한 "비교 축"이 되는 사고가 전 영역(1,325개 파일)에서 발생했다. **"비교 축" → "구분" 기계적 치환은 완료**(2026-07-04, Fable 5, 04_network 작업 중이던 67개 파일 제외). 남은 것은 `기존/대안` · `본 키워드` 헤더를 실제 비교 대상 이름으로 바꾸는 작업(455개 파일, `grep -rl "기존/대안 | 본 키워드" content/cspe/`로 재측정) — 파일마다 무엇과 무엇을 비교하는지 판단이 필요해 기계적 치환이 불가하다. 재작성 트래커 진행 시 파일당 점검 10번 항목으로 함께 고친다. 템플릿(model-answer.md)은 대괄호 placeholder로 이미 수정됐다.

- **`> 목적:` 메타 줄 누출 백로그(신규, 2026-07-04 사용자 지적)**: 템플릿의 **작성자 지침**인 `> 목적: …` 블록쿼트가 독자용 콘텐츠가 아닌데 다수 파일에 그대로 복사됐다. 두 종류가 있다:
  - **암기용** — `> 목적: … 시험 답안 양식이 아니라, 이해를 위한 친절한 설명이다.` → **858개 파일**(측정: `grep -rl "이해를 위한 친절한 설명이다" content/cspe/ | wc -l`). `## 한눈에` 바로 위 1줄.
  - **답안용** — `> 목적: 시험장에서 25분에 그대로 쓰는 답안 양식. 작성방식(…)을 엄격히 지킨다.` → **1,386개 파일**(측정: `grep -rl "시험장에서 25분에 그대로 쓰는 답안 양식" content/cspe/ | wc -l`). `# 📝 【답안용】` 바로 아래 1줄.
  - 두 줄 모두 100% `> 목적:`로 시작하는 단일 블록쿼트라 **기계적 삭제(sed) 안전**: `grep -rl "이해를 위한 친절한 설명이다" content/cspe/ | xargs sed -i '/^> 목적: .*이해를 위한 친절한 설명이다/d'` 와 `grep -rl "시험장에서 25분에 그대로 쓰는 답안 양식" content/cspe/ | xargs sed -i '/^> 목적: 시험장에서 25분에 그대로 쓰는 답안 양식/d'`.
  - 템플릿(AGENTS.md)·작성법(writing-method.md Ⅷ)은 **작성자 지침(주석)으로 이미 수정**해 재발을 막았다. 남은 것은 **기존 파일 청소** — 재작성 트래커 진행 시 파일당 점검 10-1번으로 함께 제거하거나 위 sed로 일괄 청소한다.

**작업 방식 메모:** 동시 실행 서브에이전트 최대 2개, 10개 파일마다 커밋·푸시(`zola build` 통과 확인 후). push 전 `git fetch`로 diverge 확인 — conflict 없으면 merge, 있으면 사용자에게 확인.

**빌드 실패 사고 이력 — weight 타입 오류 (2026-07-05):** `01_basic_theory/020_automata_theory.md`의 frontmatter가 `weight: "020"`(따옴표+0패딩 문자열)로 작성되어 Zola가 `YAML deserialize error: weight: invalid type: string "020", expected usize`로 3회 연속 빌드 실패(커밋 3개 연속 CI 빨간불). 원인: 파일명 접두어 `{NNN}`(3자리, 0패딩)을 frontmatter `weight` 값에 그대로 문자열로 복사. **weight는 파일명과 별개로 항상 따옴표 없는 순수 정수**(`weight: 20`, `020`이나 `"020"` 금지)여야 함 — 위 "출력 골격"과 체크리스트 18번에 반영 완료. 재발 방지: 커밋 전 `grep -rnE '^weight: *"?0[0-9]' content/` 로 0패딩·따옴표 weight 없는지 확인 후 push.
