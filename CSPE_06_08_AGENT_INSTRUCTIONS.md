# CSPE 06·07·08 병렬 재검수 지시

이 문서는 `06-evaluation`, `07-law-policy`, `08-latest-tech`를 다른 에이전트가 병렬 재검수할 때 사용하는 프로젝트 공용 지시다. 저장소 루트의 `AGENTS.md`와 최신 사용자 지시를 먼저 따르며, 충돌 시 최신 사용자 지시가 우선한다.

## 1. 작업 범위

- `06-evaluation`: 001~051
- `07-law-policy`: 001~045
- `08-latest-tech`: 001~226
- 과목 번호순, 각 과목 안에서는 파일 번호순으로 처리한다.
- `extra.review_status: "사용자 확정"` 파일은 전체를 건너뛴다.

## 2. 권장 병렬 배정

서로 겹치지 않는 아래 묶음만 한 에이전트에 배정한다.

- 06과목: `001-020`, `021-040`, `041-051`
- 07과목: `001-020`, `021-040`, `041-045`
- 08과목: `001-020`부터 `201-220`까지 20개 단위, 마지막 `221-226`

한 파일을 두 에이전트가 동시에 수정하지 않는다. 작업자는 자신에게 지정된 범위 밖 파일과 `agent-guides/CSPE_PROGRESS.md`를 건드리지 않는다.

## 3. 파일별 처리 절차

1. 파일 하나를 위에서 아래로 한 번만 읽는다.
2. 읽는 동안 아래 일곱 검사를 함께 판정한다.
3. 필요한 부분만 `apply_patch`로 직접 수정한다.
4. 수정한 파일의 `date`만 실제 수정 시각의 RFC 3339 `+09:00` 값으로 갱신한다.
5. 수정할 곳이 없으면 파일과 날짜를 건드리지 않는다.
6. 다음 파일로 이동하며 같은 파일을 다시 순환하지 않는다.

자동 변환, 일괄 치환, 정규식 기반 본문 수정은 금지한다.

## 4. 필수 검사

### 4.1 채점 키워드 누출

- 학습 블록에만 있고 Ⅰ~Ⅶ 답안 영역에 없는 대표 용어•공식 약어•지표를 찾는다.
- 필요한 키워드는 답안 영역의 적절한 불릿이나 표 셀에 회수한다.
- 답안에서 쓰이지 않는 불필요한 선행 용어는 첫 불릿 예외를 제외하고 제거한다.

### 4.2 결론의 판단과 결정

- Ⅶ 결론 한 문장에 판단 축과 구체적인 선택•적용 대상을 함께 둔다.
- `~기반 선택`, `~활용`, `~고려`만 남은 일반론은 교정한다.

### 4.3 배경의 선행 한계

- Ⅰ의 배경/필요성은 정의 반복이 아니라 기존 방식의 구체적인 실패•제약을 설명한다.
- 억지 한계가 필요한 분류 주제는 판단 대상을 직접 제시한다.

### 4.4 과압축 복원

- 조어, 명사 세 개 이상의 연쇄, 관계 조사 누락, 유명 개념으로 오독되는 압축을 복원한다.
- 이미 의미가 완결된 문장은 더 줄이지 않는다.

### 4.5 답안 형식

- 고정 절 제목을 정확히 유지한다.
- 답안 불릿•표 셀•요약•결론은 50자 이하의 완결된 전문 명사구로 작성한다.
- 셀•불릿마다 채점 키워드 1~2개만 굵게 표시한다.
- Mermaid 레이블과 표의 축 이름 칸에는 굵은 글씨를 쓰지 않는다.
- 모든 Markdown 표의 열 개수를 일치시킨다.

### 4.6 Ⅲ 구조 및 구성요소

- 의미 있는 정적 관계가 있으면 손으로 재현 가능한 ASCII 구조를 사용한다.
- 기본 표현은 한글 폭에 안전한 세로 가지형 일반 텍스트다.
- 박스는 경계 표현이 꼭 필요하고 렌더링이 깨지지 않을 때만 사용한다.
- 계층•포함•소유는 가지형, 보안 경계는 필요한 경우 상자형, 동등 요소는 무방향 연결로 표현한다.
- 시간 순서•단계 번호•방향 화살표를 넣지 않는다.
- ASCII 내부 구성요소와 책임 표의 이름•개수•추상 수준을 맞춘다.
- 정적 관계가 없으면 종류•대안•단계를 구조 블록으로 위장하지 않는다.

### 4.7 Ⅳ 흐름도

- 흐름도는 강제가 아니며 구조적 이해를 실제로 높일 때만 ASCII로 작성한다.
- 아키텍처•플랫폼•프로토콜은 책임 경계의 큰 흐름을 우선한다.
- 알고리즘•핸드셰이크•복구 절차는 상태•분기•반복•종료 조건의 세부 흐름을 사용한다.
- 비교•선택 주제는 결정 트리를 사용한다.
- 독립 주체의 상호작용이 핵심일 때만 생명선형 표현을 사용한다.
- 무조건 시퀀스 다이어그램으로 만들지 않는다.
- 가로 폭을 제한하고 길어지면 세로로 늘리거나 작은 도식으로 나눈다.
- 한글 박스 정렬이 깨질 수 있으므로 세로 가지형 텍스트를 기본으로 한다.
- 외부 요청 진입과 결과 반환에는 번호를 붙이지 않는다.
- 번호 있는 동작은 5개 이하로 두고 `동작 원리`와 번호•단계명을 글자 그대로 1:1 대응시킨다.
- 반복과 분기는 자기 메시지가 아니라 조건과 범위가 보이는 ASCII 가지로 표현한다.

## 5. 핵심 용어 동기화

- 구조도나 흐름도에서 새로 쓰거나 바꾼 핵심 용어를 해당 절의 핵심 용어와 맞춘다.
- 약어는 처음 이해할 수 있도록 한국어 명칭, 영문 풀네임, 약어를 함께 설명한다.
- 예: `저장 프로그램 상태 레지스터(Saved Program Status Register, SPSR)`.
- 약어만 나열하거나 도식과 핵심 용어가 서로 다른 표현을 쓰지 않는다.

## 6. 검증·체크포인트

- 20개 완료마다 작업자는 수정 파일 목록과 판정 이유를 통합 담당자에게 보고한다.
- 작업자는 별도 지시가 없으면 `git add`, 커밋, 푸시, 진행표 갱신을 하지 않는다.
- 통합 담당자는 정확한 파일 경로와 진행표만 스테이징한다.
- 커밋 전 `git diff --cached --check`를 통과시킨다.
- 커밋 메시지에는 과목과 처리 번호 범위만 적는다.
- 커밋 후 `git push origin main`을 실행한다.
- 과목 마지막 파일 완료 시 `npm.cmd run build`를 실행한다.
- 기존 경고 `Entry docs → 404 was not found`는 무시한다.

## 7. 완료 보고 형식

작업자는 아래 항목만 간결하게 보고한다.

- 처리 과목과 번호 범위
- 검수 완료 수, 수정 수, 사용자 확정 건너뜀 수
- 수정 파일의 정확한 경로
- 각 수정에 적용한 검사 번호와 핵심 이유
- `git diff --check` 결과
- 발견한 기존 손상•모지바케•표 열 불일치•빌드 위험

전체 범위가 끝나기 전에는 과목 완료를 선언하지 않는다.
