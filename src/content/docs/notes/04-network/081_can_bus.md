---
sidebar:
  order: 81
  label: "081. CAN 버스 자동차 통신"
  badge:
    text: "기출 · 30%"
    variant: note
title: "차량용 임베디드 네트워크 : CAN 버스 통신 (CAN, CAN FD, CAN XL)"
date: "2026-08-26T13:59:12+09:00"
tags:
  - "notes-network"
weight: 81
extra:
  question_no: "81"
  source_status: "기출"
  source_history: "129회"
  priority: 30
  priority_note: "비파괴 비트 단위 중재(CSMA/CD+NBA), 우성/열성 비트, 차동 전압(CAN_H/L), CAN FD/XL 및 버스 오프"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **CAN (Controller Area Network)**: 차량 내 ECU들이 꼬임선 2선(CAN_H/CAN_L) 버스 상에서 상호 통신하는 직렬 통신 표준 (ISO 11898).
- **ECU (Electronic Control Unit)**: 엔진, 조향, 브레이크, 에어백 등을 소프트웨어 알고리즘으로 제어하는 차량용 임베디드 컴퓨터.

</details>

- 정의/개념: 차량 내 복수 ECU가 2선식 차동 버스에서 **메시지 ID 기반의 비파괴 비트 단위 중재와 차동 전압으로 충돌 없이 우선순위 통신을 보장하는 차량용 직렬 네트워크(ISO 11898)**
- 배경/필요성: 차량 전장화에 따른 점대점 배선(와이어링 하네스)의 **무게·부피 폭증, 단선 결함률 증가 및 다중 ECU 간 실시간 브로드캐스팅 불가**

#### 한줄 요약
- 비파괴 비트 중재, 차동 꼬임선 노이즈 상쇄, 다중 브로드캐스팅을 통해 신뢰성 있는 차량 내 통신을 보장한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **Dominant vs Recessive Bit**: 버스 상에서 전기적으로 '0'이 '1'을 덮어쓰는 우성('0') 비트와 전압 변위가 없는 열성('1') 비트.
- **Wired-AND Logic**: 여러 노드가 동시에 신호를 출력할 때 단 하나라도 '0'을 출력하면 버스 전체가 '0'으로 구동되는 하드웨어 특성.

</details>

- **비파괴 비트 단위 중재(CSMA/CR)**: 낮은 ID가 우선 전송
- **차동 신호 노이즈 내성**: CAN_H와 CAN_L 전압 차로 판정
- **메시지 지향 브로드캐스팅**: 노드 주소가 아닌 **메시지 식별자(ID)를 기반으로 전 노드에 동시 방송 및 필터링 수신**

#### 한줄 요약
- 비파괴 우선순위 중재, 차동 전압 기반 EMI 노이즈 면역, 메시지 지향 브로드캐스팅을 제공한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **CAN Controller (MAC)**: 프레임 조립, 비트 스터핑, CRC 계산, 비파괴 중재 및 에러 카운터(TEC/REC)를 관리하는 디지털 칩셋.
- **Termination Resistor ($120\Omega$)**: 전송 선로의 신호 반사파 왜곡을 방지하기 위해 버스 양 끝단에 장착하는 임피던스 정합 저항 (합성 $60\Omega$).

</details>

```text
[CAN 노드]
|-- CAN 컨트롤러
|-- CAN 트랜시버
`-- 차동 꼬임선 버스
    |-- CAN_H
    |-- CAN_L
    `-- 양단 종단 저항
```

선의 의미: MCU 데이터가 CAN 컨트롤러와 트랜시버를 거쳐 꼬임선 버스 상의 차동 전압으로 변환 송출되고 양단 종단 저항이 반사파를 억제하는 구조

| 구성요소 | 책임 |
|:---|:---|
| **CAN 컨트롤러** | 프레임·중재·CRC·오류 상태 관리 |
| **CAN 트랜시버** | 논리 신호와 차동 전압 변환 |
| **차동 꼬임선 버스** | CAN_H·CAN_L 물리 신호 전달 |
| **종단 저항** | 선로 임피던스 정합과 반사 억제 |

#### 한줄 요약
- CAN 컨트롤러, 트랜시버, 차동 꼬임선, 120옴 종단 저항이 결합된다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **ACK Slot Overwrite**: 송신 노드가 열성('1')으로 비워둔 ACK 슬롯에, 정상 수신한 모든 노드가 동시에 우성('0')을 덮어써서 무결성을 확인하는 메커니즘.

</details>

```text
CAN 비파괴 비트 중재 및 프레임 송수신 파이프라인
        │
   1. [유휴 감지 및 동시 송출] 버스 유휴 상태에서 복수 ECU가 SOF(우성 '0') 동시 송출
        │
   2. [비트 단위 ID 중재] MSB부터 1비트씩 버스에 인가하고 자신의 출력과 버스 전압 대조
        │
   3. [열성 노드 패배 및 양보] 열성('1') 송출 노드가 버스의 우성('0')을 감지하고 즉시 수신 전환
        │
   4. [최우선 노드 무손실 전송] 최상위 우선순위(최소 ID) 승자 노드가 중단 없이 페이로드/CRC 전송
        │
   ▼
5. [수신단 ACK 구동] 수신 노드들이 CRC 검증 후 ACK 슬롯에 우성('0')을 인가하여 송신 성공 확정
```

- 1. 유휴 감지 및 동시 송출: SOF 전송
- 2. 비트 단위 ID 중재: 송신값과 버스값 비교
- 3. 열성 노드 패배 및 양보: 수신 상태로 전환
- 4. 최우선 노드 무손실 전송: 승자 프레임 계속 전송
- 5. 수신단 ACK 구동: 정상 수신 노드가 ACK 표시

#### 한줄 요약
- SOF 송출 → 비트 단위 ID 중재 → 패자 수신 전환 → 승자 데이터 전송 → ACK 수신 확인 순으로 동작한다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **Classic CAN vs CAN FD vs CAN XL**: 8B/1Mbps(CAN 2.0), 64B/5Mbps(CAN FD), 2048B/20Mbps(CAN XL).

</details>

| 비교 항목 | 클래식 CAN (CAN 2.0 / CC) | CAN FD (Flexible Data-rate) | CAN XL (Extra Long) |
|:---|:---|:---|:---|
| 페이로드 | 최대 8바이트 | 최대 64바이트 | 최대 2,048바이트 |
| 비트율 | 단일 비트율 | 데이터 구간 가속 | 데이터 구간 고속화 |
| 중재 방식 | 비트 단위 중재 | 중재 구간과 데이터 구간 분리 | 중재 구간과 데이터 구간 분리 |
| 상위 데이터 | 제한된 제어 데이터 | 더 큰 제어·센서 데이터 | 이더넷 프레임 전달 지원 |
| 적용 영역 | 차체·파워트레인 제어 | 고속 센서·제어 | 존 아키텍처 백본 후보 |

#### 한줄 요약
- CAN 2.0은 기본 차체 제어, CAN FD는 ADAS 센서 고속화, CAN XL은 SDV 대용량 백본에 쓰인다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Bus-Off Isolation**: 노드가 전송 에러 카운터(TEC > 255)를 초과할 때 버스 전체 마비(Babbling Idiot)를 막기 위해 스스로 트랜시버를 전기적으로 차단하는 고장 격리 기제.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 고우선순위 트래픽 폭주 시 저우선순위 ECU 메시지의 **전송 기아(Starvation)** | **`최악 응답 시간(WCRT) 해석 기반 ID 설계` 및 버스 부하율 70% 통제** | 모든 기능의 실시간 데드라인 보장 및 스케줄링 안정성 확보 |
| 종단 저항 누락 또는 긴 스터브 라인으로 인한 **신호 반사파 왜곡** | **양단 `120Ω 분할 종단(Split Termination)` 및 스터브 0.3m 이내 제한** | 신호 링잉 및 공통 모드 노이즈 제거, 통신 무결성 확보 |
| 결함 노드의 연속 에러 프레임 방출로 인한 **버스 전체 마비(Babbling Idiot)** | CAN 컨트롤러의 **`TEC/REC 기반 자동 버스 오프(Bus-Off) 격리`** 활성화 | 결함 노드의 전면 마비 차단 및 시스템 자율 복원력 확보 |
| 평문 브로드캐스팅 특성으로 인한 메시지 위변조 및 스푸핑 위협 | AUTOSAR 표준 **`SecOC (Secure Onboard Communication) MAC 인증`** 적용 | 비인가 제어 명령 차단 및 주행 안전 무결성 보증 |

#### 한줄 요약
- WCRT 분석, 120옴 종단, Bus-Off 자동 격리, SecOC 보안 인증으로 운영한다.

## Ⅶ. 결론

- 소형 제어는 **Classic CAN**, 큰 프레임은 CAN FD·XL 선택

#### 한줄 요약
- CAN 버스는 비파괴 비트 중재와 차동 신호 전송을 통해 차량 임베디드 제어의 실시간 신뢰성을 보장하는 핵심 전장 네트워크다.
