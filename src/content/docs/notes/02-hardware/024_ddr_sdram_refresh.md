---
sidebar:
  order: 24
  label: "024. DDR SDRAM과 리프레시 방식 (DDR SDRAM Refresh)"
  badge:
    text: "기출 • 50%"
    variant: note
title: "DDR SDRAM과 리프레시 방식 (DDR SDRAM Refresh)"
date: "2026-08-17T16:42:00+09:00"
tags:
  - "notes-hardware"
weight: 24
extra:
  question_no: "024"
  source_status: "기출"
  source_history: "129회"
  priority: 50
  priority_note: "양 엣지 전송과 리프레시 지연"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **DDR SDRAM(Double Data Rate Synchronous DRAM)**: 클록의 상승·하강 양 엣지에서 데이터를 전송해 동일 클록 주파수 대비 두 배 대역폭을 제공하는 동기식 DRAM.
- **전하 누설(Charge Leakage)**: DRAM 커패시터의 저장 전하가 시간에 따라 감소하는 현상; 방치 시 데이터 손실.
- **리프레시(Refresh)**: 누설되기 전 행의 전하를 재충전해 데이터를 보존하는 필수 동작; 접근 차단 원인.

</details>

- 정의/개념: 클록의 **상승·하강 양 엣지 전송**과 커패시터 전하 보존을 위한 **주기적 리프레시**를 수행하는 고속 동기식 메모리
- 배경/필요성: 단일 엣지 전송의 대역폭 한계 및 커패시터의 **자연 방전(Leakage)**에 따른 데이터 소실 위험

#### 한줄 요약
- DDR은 한 클록에서 데이터를 두 번 보내 대역폭을 확보하고, DRAM은 전하가 사라지기 전에 반복 충전해 저장값을 유지한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **뱅크 수준 병렬성(Bank-Level Parallelism, BLP)**: 여러 독립 메모리 뱅크를 동시 활용해 요청을 겹쳐 처리하는 구조.
- **버스트 길이(Burst Length)**: 하나의 명령으로 연속 전송하는 데이터 단위 수; 내부 버스 효율 결정.
- **양 엣지 전송(Double-Edge Transfer)**: 클록의 상승·하강 두 전이 시점에서 데이터를 전달해 핀당 전송률을 두 배로 높이는 방식.

</details>

- **양 엣지 전송**으로 동일 클록에서 핀당 전송률 두 배 향상
- **뱅크 수준 병렬성**으로 다른 뱅크의 요청을 중첩 처리해 유효 대역폭 증가
- **리프레시** 명령 실행 중 해당 뱅크 접근 차단으로 꼬리 지연 발생

#### 한줄 요약
- DDR은 전송 시점을 늘려 대역폭을 얻고 뱅크를 나눠 요청을 겹치지만, 리프레시가 진행되는 동안 일부 접근은 기다려야 한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **메모리 제어기(Memory Controller)**: DRAM 명령·주소·타이밍과 리프레시를 스케줄링하는 제어기.
- **DDR PHY(DDR Physical Interface)**: 메모리 제어기와 DRAM 사이의 고속 전기 신호를 송수신하고 타이밍을 맞추는 인터페이스.
- **행 버퍼(Row Buffer)**: 활성화된 DRAM 행의 데이터를 래치해 연속 열 접근을 가속하는 버퍼.

</details>

```text
[DDR SDRAM 시스템]
├─ 메모리 제어기  (명령·리프레시 스케줄링)
├─ DDR PHY  (고속 신호·타이밍 정합)
└─ DRAM 배열
   ├─ 뱅크·행 버퍼  (행 활성화·열 접근)
   └─ 리프레시 제어  (전하 복원 스케줄)
```

선의 의미: 가지(`├─`)는 소유 관계; DDR PHY는 제어기와 DRAM 배열 사이의 신호 변환 계층

| 구성요소 | 책임 |
|:---|:---|
| 메모리 제어기 | 읽기·쓰기·**리프레시** 명령 스케줄링 |
| DDR PHY | 고속 **양 엣지** 데이터 신호·샘플링 타이밍 관리 |
| 뱅크·행 버퍼 | 행 활성화 후 **BLP** 중첩 열 접근 제공 |
| 리프레시 제어 | 누설 전하 주기 복원으로 데이터 보존 |

#### 한줄 요약
- 메모리 제어기가 명령 순서를 정하고 PHY가 신호를 맞추며, DRAM 내부 뱅크와 리프레시 회로가 실제 저장을 담당한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **리프레시 간격(tREFI)**: 리프레시 명령 사이에 허용되는 최대 시간; 초과 시 전하 누설로 데이터 손실.
- **리프레시 사이클 시간(tRFC)**: 리프레시 명령 실행 동안 해당 뱅크의 일반 접근이 차단되는 시간.
- **프리차지(Precharge)**: 다음 행 접근 전 뱅크 비트라인을 기준 전압으로 초기화하는 동작.

</details>

```text
메모리 제어기 타이머 확인
      │
      ├─ tREFI 도래 → [ 1. 리프레시 명령 발행 및 차단 ]
      │                    │
      │              리프레시 명령 발행 → tRFC 동안 차단 → 완료 후 재개
      │
      └─ [ 2. 일반 접근 행 활성화 및 열 전송 ]
             │
         행 활성화 (RAS) → 열 전송 (CAS) → 양 엣지 전송 → 프리차지 (PRE)
```

**동작 원리**

1. **리프레시 명령 발행 및 차단**: tREFI 도래 시 명령 발행, tRFC 동안 차단 후 재개
2. **일반 접근 행 활성화 및 열 전송**: RAS로 활성화, CAS로 양 엣지 전송 후 프리차지

#### 한줄 요약
- 제어기는 리프레시가 늦지 않았는지 먼저 보고, 아니면 일반 요청을 RAS→CAS→PRE 순서로 처리한다.

## Ⅴ. 종류 및 비교

| 리프레시 방식 | 전체 뱅크 리프레시 | 뱅크별 리프레시 | 자체 리프레시(Self-Refresh) |
|:---|:---|:---|:---|
| 적용 기준 | 단순 제어 우선·저빈도 접근 | 접근 차단 분산 필요 | 저전력 대기 상태(전원 절약) |
| 핵심 특징 | 전체 범위 한 번에 복원 | 일부 뱅크 중심 복원·중첩 가능 | DRAM 내부 타이머 기반 자율 복원 |
| 한계 | 넓은 접근 차단·긴 **tRFC** | 스케줄링 복잡도 증가 | 일반 접근 차단 상태 |

#### 한줄 요약
- 단순 제어가 중요하면 전체 리프레시, 꼬리 지연 분산이 중요하면 뱅크별 리프레시, 저전력 대기는 자체 리프레시를 선택한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **표적 행 리프레시(Target Row Refresh, TRR)**: 특정 행의 반복 활성화로 인접 행 오류 우려 시 주변 행을 추가 복원하는 완화 기법.
- **메모리 트레이닝(Memory Training)**: 부팅 시 데이터 신호와 샘플링 타이밍을 맞추는 초기화 절차.
- **오류 정정 코드(Error Correcting Code, ECC)**: 저장 데이터의 오류를 검출하고 일부를 정정하는 코드.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 전체 리프레시의 긴 **tRFC** 꼬리 지연 | 뱅크별 리프레시·요청 스케줄 조정 | 접근 차단 범위 분산 |
| 특정 행 반복 접근으로 인접 행 오류 | **TRR**·**ECC** 적용 및 범위 검증 | Row Hammer 완화·오류 정정 |
| 고온에서 **전하 누설** 증가 | 온도 기반 **tREFI** 조정 | 데이터 보존 여유 확보 |
| 고속 양 엣지 신호의 타이밍 오류 | **메모리 트레이닝**·신호 무결성 검증 | 샘플링 오류 감소 |

#### 한줄 요약
- DDR 성능은 전송률만 높이는 문제가 아니라, 리프레시 차단과 신호 오류를 워크로드·온도 조건에서 함께 통제하는 문제다.

## Ⅶ. 결론

- 지연 민감 워크로드는 **뱅크별 리프레시**, 대역폭 극대화는 **BLP 및 양 엣지 전송** 선택

#### 한줄 요약
- DDR 설계는 빠른 데이터 전송과 리프레시 대기, 고속 신호의 오류 가능성을 함께 비교해 정해야 한다.
