---
sidebar:
  order: 20
  label: "020. MMU 메모리 관리 장치 (Memory Management Unit)"
  badge:
    text: "기출 • 70%"
    variant: note
title: "MMU 메모리 관리 장치 (Memory Management Unit)"
date: "2026-08-19T17:22:07+09:00"
tags:
  - "notes-hardware"
weight: 20
extra:
  question_no: "020"
  source_status: "기출"
  source_history: "135회, 137회"
  priority: 70
  priority_note: "가상 주소 변환과 메모리 보호"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **메모리 관리 장치(Memory Management Unit, MMU)**: 가상 주소를 물리 주소로 변환하고 접근 권한을 하드웨어로 검사하는 장치.
- **가상 주소(Virtual Address)**: 프로세스가 사용하는 논리 주소 공간의 주소; 물리 주소와 독립.
- **물리 주소(Physical Address)**: 실제 메모리 장치에서 데이터 위치를 지정하는 주소.

</details>

- 정의/개념: **가상 주소**를 **물리 주소**로 실시간 변환하면서 접근 권한까지 같은 경로에서 검사하는 **메모리 관리 장치(Memory Management Unit, MMU)**
- 배경/필요성: 주소 변환을 소프트웨어로 하면 모든 접근에 지연이 붙고, 검사 코드를 우회할 여지가 남아 격리를 강제할 수 없음

#### 한줄 요약
- 변환과 검사를 한 경로에 합친 것이 MMU의 핵심이며, 덕분에 검사를 건너뛰는 접근 경로 자체가 존재하지 않게 된다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **페이지 권한(Page Permissions)**: 메모리 페이지의 읽기(R)·쓰기(W)·실행(X) 허용 여부를 나타내는 권한 비트.
- **메모리 속성(Memory Attributes)**: 메모리 영역의 캐시 가능 여부·장치 메모리·쓰기 순서 등의 접근 특성 속성.
- **변환 색인 버퍼(Translation Lookaside Buffer, TLB)**: 최근 가상→물리 변환 결과를 캐싱해 반복 페이지 테이블 순회를 생략하는 캐시.

</details>

- **TLB** 기반 빠른 변환으로 매 메모리 접근마다 페이지 테이블 순회 생략
- R/W/X **페이지 권한** 하드웨어 검사로 프로세스 간 불법 접근 즉시 차단
- **메모리 속성**으로 캐시 가능 여부·장치 메모리 등 접근 특성을 페이지 단위로 제어

#### 한줄 요약
- **메모리 속성**까지 같은 표에 실린 덕분에 캐시 정책이 페이지 단위로 갈리며, 이 때문에 성능 문제와 정합성 문제가 같은 자료구조에서 결정된다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **페이지 테이블 워커(Page Table Walker)**: TLB 미스 시 다단계 페이지 테이블을 하드웨어로 순회해 가상→물리 매핑을 탐색하는 기능.
- **권한 검사기(Access Checker)**: 현재 실행 모드와 접근 종류를 PTE 권한 비트와 비교하는 회로.
- **예외 제어기(Exception Controller)**: 무효 매핑과 권한 위반을 CPU 예외(페이지 폴트·보호 예외)로 전달하는 제어기.

</details>

```text
[MMU]
├─ TLB  (가상→물리 변환 캐시)
├─ 페이지 테이블 워커  (TLB 미스 시 순회)
├─ 권한·속성 검사기  (R/W/X·메모리 속성)
└─ 예외 제어기  (변환·권한 오류 → CPU 예외)
```

선의 의미: 가지(`├─`)는 MMU가 소유한 변환 캐시·미스 처리·검사·예외 책임

| 구성요소 | 책임 |
|:---|:---|
| TLB | 최근 가상→물리 매핑 즉시 제공 |
| **페이지 테이블 워커** | TLB에 없는 변환을 페이지 테이블에서 순회·리필 |
| **권한 검사기** | **페이지 권한** R/W/X와 **메모리 속성** 검증 |
| **예외 제어기** | 변환 실패·권한 위반을 CPU 예외로 전달 |

#### 한줄 요약
- 네 구성요소 중 셋은 성능을 위한 것이고 예외 제어기만 정확성을 위한 것이지만, 그 하나가 없으면 나머지 셋의 결과를 신뢰할 수 없다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **실행 금지(No-Execute, NX)**: 데이터 페이지에 명령어 실행을 금지하는 권한 비트; 코드 주입 공격 방어.
- **페이지 테이블 엔트리(Page Table Entry, PTE)**: 물리 프레임 번호·권한·유효 상태를 담은 매핑 항목.

</details>

```text
CPU 가상 주소 접근
      │
      ▼
1. TLB 조회 및 미스 처리
      │
      ├─ TLB 적중 → 권한 검사 단계 진입
      │
      └─ TLB 미스 → 페이지 테이블 워커 순회 → PTE 확보 → TLB 리필 → 권한 검사
                          │
                          └─ 유효 매핑 없음 → 페이지 폴트 예외
      │
      ▼
2. 권한 및 속성 검사
      │
      ├─ 권한 위반·NX 위반 → 보호 예외 발생
      │
      └─ 통과 → 3. 물리 주소 생성 및 접근 (캐시·메모리 요청)
```

분기 결과: 변환 실패는 매핑이 없다는 뜻이라 OS가 채워 주면 재개되지만, **실행 금지(No-Execute, NX)**를 포함한 권한 위반은 매핑이 있는데도 거부된 것이라 재시도해도 결과가 같음

#### 한줄 요약
- **페이지 테이블 엔트리(Page Table Entry, PTE)** 하나가 위치와 권한을 함께 담기 때문에, 매핑을 고치는 일과 권한을 고치는 일이 같은 갱신 경로를 공유한다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **메모리 보호 장치(Memory Protection Unit, MPU)**: 주소 변환 없이 정해진 물리 영역의 접근 권한을 검사하는 임베디드 전용 장치.

</details>

| 메모리 보호 방식 | MMU | MPU |
|:---|:---|:---|
| 적용 기준 | 가상 메모리·멀티태스킹 OS·프로세스 격리 | 결정적 응답·단순 보호 필요 임베디드 |
| 핵심 특징 | 가상→물리 변환·**페이지 권한** 보호 | 물리 영역 고정 권한 검사·변환 없음 |
| 한계 | 페이지 워크 지연 변동·TLB 슛다운 비용 | 가상 주소 공간·비연속 배치 미지원 |

#### 한줄 요약
- **MPU**가 선택되는 진짜 이유는 저렴함이 아니라 변환 단계가 없어 접근 시간이 항상 일정하다는 점이며, 실시간 시스템은 평균보다 이 일정함을 산다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **TLB 슛다운(TLB Shootdown)**: 한 코어의 매핑 변경을 다른 코어의 TLB에도 알려 오래된 변환 항목을 무효화하는 처리.
- **중첩 페이징(Nested Paging)**: 가상 머신의 주소를 게스트 페이지 테이블과 호스트 페이지 테이블로 두 단계 변환하는 방식.
- **페이지 고정(Memory Pinning)**: 특정 페이지가 교체되지 않도록 물리 메모리에 고정해 폴트를 방지하는 방식.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 다단계 페이지 워크로 변환 지연 증가 | 대형 페이지·페이지 워크 캐시 적용 | 반복 변환·메모리 접근 감소 |
| 매핑 변경마다 **TLB 슛다운** 비용 | 변경 범위 최소화·선택적 무효화 | 코어 간 동기화 지연 감소 |
| **중첩 페이징**으로 가상화 변환 증가 | 중첩 대형 페이지·변환 캐시 활용 | 게스트 주소 변환 비용 완화 |
| 실시간 경로에서 페이지 부재 예외 발생 | **페이지 고정**과 사전 접근으로 상주 강제 | 지연 변동과 예외 경로 제거 |

#### 한줄 요약
- 네 문제 모두 변환 자체가 아니라 변환 상태를 바꾸거나 겹칠 때 생기므로, MMU 비용은 정적인 조회가 아니라 동적인 갱신에서 나온다.

## Ⅶ. 결론

- 가상 메모리와 프로세스 격리가 필요하면 **MMU**, 접근 시간의 결정성이 우선이면 **MPU**를 선택하고, 두 요구가 겹치면 실시간 경로만 **페이지 고정**으로 분리

#### 한줄 요약
- MMU와 MPU의 선택은 보호 수준의 문제가 아니라 지연의 평균을 살 것인지 최악값을 살 것인지의 문제이며, 두 값은 같은 방향으로 움직이지 않는다.
