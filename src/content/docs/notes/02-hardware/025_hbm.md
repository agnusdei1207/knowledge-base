---
sidebar:
  order: 25
  label: "025. HBM 고대역폭 메모리 (High Bandwidth Memory)"
  badge:
    text: "기출 • 85%"
    variant: note
title: "HBM 고대역폭 메모리 (High Bandwidth Memory)"
date: "2026-08-19T17:25:02+09:00"
tags:
  - "notes-hardware"
weight: 25
extra:
  question_no: "025"
  source_status: "기출"
  source_history: "129회, 131회, 137회"
  priority: 85
  priority_note: "적층 DRAM과 광폭 인터페이스"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **고대역폭 메모리(High Bandwidth Memory, HBM)**: DRAM 다이를 TSV로 수직 적층하고 로직 다이와 광폭 인터페이스로 연결한 고대역폭 패키지 메모리.
- **메모리 벽(Memory Wall)**: 프로세서 연산 속도에 비해 메모리 데이터 공급 대역폭이 부족해 성능이 제한되는 현상; AI·HPC 병목의 주원인.
- **실리콘 인터포저(Silicon Interposer)**: 로직 다이와 HBM 스택 사이에 고밀도 배선을 제공하는 패키지 기판.

</details>

- 정의/개념: 복수의 DRAM 다이를 **TSV(실리콘 관통 전극)** 로 수직 적층하고 **인터포저**로 연결한 초고속 패키징 메모리
- 배경/필요성: AI·HPC 연산량이 데이터 공급 속도보다 빠르게 증가해 **메모리 벽**이 가속기 활용률 제한

#### 한줄 요약
- HBM은 메모리 칩을 높이 쌓고 가속기 가까이에 넓은 데이터 길을 놓아 많은 데이터를 한 번에 전달하는 구조다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **실리콘 관통 전극(Through-Silicon Via, TSV)**: 다이를 수직으로 관통해 상하 칩의 전기 신호를 연결하는 배선; HBM 적층의 핵심 기술.
- **광폭 인터페이스(Wide Interface)**: 수천 개의 데이터 선을 병렬로 사용해 핀당 속도를 낮추고 전체 대역폭을 높이는 인터페이스.
- **채널 인터리빙(Channel Interleaving)**: 메모리 요청을 여러 독립 채널에 분산해 병렬 전송을 극대화하는 방식.

</details>

- **TSV** 수직 적층으로 다이 간 배선 길이를 단축하여 신호 지연 및 에너지 소모 절감
- **광폭 인터페이스**로 핀당 속도를 낮추면서 전체 메모리 대역폭 확대
- **채널 인터리빙**을 통한 요청 병렬 분산으로 유효 대역폭 극대화

#### 한줄 요약
- HBM은 한 선을 극도로 빠르게 달리는 대신 여러 선을 동시에 사용하고, 여러 채널에 요청을 분산해 대역폭을 높인다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **베이스 다이(Base Die)**: HBM 스택 하단에서 DRAM 다이와 외부 로직 사이의 신호·명령 연결을 담당하는 인터페이스 다이.
- **마이크로범프(Microbump)**: 다이와 인터포저 사이의 전기적·기계적 연결을 위한 미세 접합부.
- **DRAM 코어 다이(DRAM Core Die)**: 실제 메모리 셀 배열을 포함하는 HBM 적층 다이.

</details>

```text
[가속기 패키지]
AI·GPU 로직 다이 ─ 마이크로범프 ─ 실리콘 인터포저 ─ 마이크로범프 ─ HBM 스택
                                                                     ├─ DRAM 코어 다이 × N
                                                                     │     ↕ TSV
                                                                     └─ 베이스 다이
```

선의 의미: `─`는 로직 다이와 HBM의 광폭 수평 연결, `↕`는 적층 다이 사이의 TSV 수직 연결

| 구성요소 | 책임 |
|:---|:---|
| 로직 다이 | 메모리 요청 발생·가속 연산 수행 |
| **실리콘 인터포저** | 로직·HBM 사이 광폭 마이크로범프 배선 제공 |
| DRAM 코어 다이 | 대용량 데이터 저장 |
| **TSV** | 적층 다이 간 수직 고밀도 신호 전달 |
| 베이스 다이 | 외부 인터페이스 수신·**채널 인터리빙** 분산 |

#### 한줄 요약
- HBM은 가속기·인터포저·베이스 다이·복수 DRAM 다이가 하나의 고밀도 패키지에서 협력하는 구조다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **메모리 컨트롤러(Memory Controller)**: 가속기 메모리 요청을 채널·뱅크에 배치하는 제어기.
- **버스트 전송(Burst Transfer)**: 연속 데이터를 한 명령으로 묶어 광폭 인터페이스로 전송하는 방식.

</details>

```text
가속기 메모리 요청
      │
      ▼
1. 채널 및 뱅크 인터리빙: 주소 분산 및 병렬 요청 생성
      │
      ▼
2. 광폭 명령 및 주소 전달: 인터포저 경유 베이스 다이 전달
      │
      ▼
3. TSV 수직 전송 및 반환: DRAM 코어 다이 수직 접근 및 버스트 반환
      │
      ▼
가속기 연산 유닛으로 데이터 공급
```

**동작 원리**

1. **채널 및 뱅크 인터리빙**: 메모리 컨트롤러가 주소를 분산해 병렬 요청 생성
2. **광폭 명령 및 주소 전달**: 인터포저 경유 광폭 병렬 신호로 베이스 다이 전달
3. **TSV 수직 전송 및 반환**: 베이스 다이에서 DRAM 코어 다이로 수직 접근

#### 한줄 요약
- 요청을 여러 채널에 나누고 넓은 배선과 수직 연결로 동시에 옮기므로 대규모 가속 연산의 데이터 공급이 빨라진다.

## Ⅴ. 종류 및 비교

| 메모리 유형 | HBM | GDDR | DDR |
|:---|:---|:---|:---|
| 적용 기준 | 가속기·HPC의 **대역폭** 최우선 | 그래픽의 대역폭·비용 균형 | 범용 시스템의 용량·확장성 우선 |
| 핵심 특징 | 적층 DRAM·**광폭 인터페이스**·낮은 핀 전력 | 협폭 고속 인터페이스·GDDR 세대 향상 | DIMM 기반 범용 확장성 |
| 한계 | 패키징·열·비용 부담 | 신호 전력·공간 제약 | 가속기 대역폭 병목 가능 |

#### 한줄 요약
- 가속기 대역폭이 최우선이면 HBM, 범용 확장성과 비용이 중요하면 DDR, 그래픽 균형은 GDDR을 선택한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **양품 다이(Known Good Die, KGD)**: 적층 전 시험을 통과해 정상 동작이 확인된 다이; 적층 수율 핵심.
- **직접 칩 냉각(Direct-to-Chip Liquid Cooling)**: 냉각판·냉각 유체로 고밀도 패키지의 열을 직접 제거하는 방식.
- **메모리 RAS(Reliability, Availability, Serviceability)**: 오류 검출·격리·복구를 포함하는 시스템 신뢰성 설계.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 적층 다이 결함으로 패키지 수율 저하 | **KGD** 선별·적층 전후 시험 | 결함 조기 검출·수율 향상 |
| 가속기+HBM 집중 발열 | 패키지 열 설계·**직접 칩 냉각** 검토 | 열 제한·성능 저하 완화 |
| 특정 채널 요청 쏠림 | **채널 인터리빙**·주소 배치 조정 | 유효 대역폭 향상 |
| 적층 연결·메모리 오류 | ECC·**RAS** 기능 보장 범위 검증 | 검출·정정 가능 오류 신뢰성 확보 |

#### 한줄 요약
- HBM의 대역폭만 보지 않고 적층 수율·열 제거·채널 균형·오류 처리까지 제품 수준에서 함께 확인해야 한다.

## Ⅶ. 결론

- 실측 성능이 메모리 대역폭에 제한되고 패키지·열·용량 비용을 감당할 수 있을 때 **HBM**을 선택하며, 비용·확장성이 우선이면 **GDDR·DDR** 유지

#### 한줄 요약
- HBM은 무조건 빠른 메모리가 아니라, 가속기가 요구하는 대역폭이 패키지와 냉각 비용을 정당화할 때 선택하는 메모리다.
