---
sidebar:
  order: 69
  label: "069. 펌웨어 보안 취약점"
  badge:
    text: "기출 · 70%"
    variant: note
title: "펌웨어 보안 취약점"
date: "2026-07-28T13:09:56+09:00"
tags:
  - "notes-hardware"
weight: 69
extra:
  question_no: "069"
  source_status: "기출"
  source_history: "138회"
  priority: 70
  priority_note: "공급망·부트·복구의 고확장 기출 핵심"
---

## 미리 알고가기

- **펌웨어(Firmware)**: 부팅과 하드웨어 제어를 맡는 저수준 코드
- **운영체제(Operating System, OS)**: 응용 프로그램의 실행과 프로세서·메모리·장치 자원을 관리하는 시스템 소프트웨어
- **베이스보드 관리 제어기(Baseboard Management Controller, BMC)**: 운영체제와 독립적으로 서버의 전원·센서·펌웨어를 원격 관리하는 제어기
- **보안 부팅(Secure Boot)**: 부팅 구성요소의 서명을 검증해 실행
- **펌웨어 매니페스트(Firmware Manifest)**: 모델·버전·해시·의존성·서명 정보
- **소프트웨어 자재 명세서(Software Bill of Materials, SBOM)**: 펌웨어에 포함된 구성요소·버전·공급처·의존성을 추적하는 명세
- **합동 테스트 작업 그룹(Joint Test Action Group, JTAG) 인터페이스**: 칩의 경계 스캔·시험·디버깅에 사용하는 물리 인터페이스
- **범용 비동기 송수신기(Universal Asynchronous Receiver-Transmitter, UART)**: 펌웨어 로그와 부팅 콘솔을 제공하는 비동기 직렬 통신 장치
- **서명 업데이트(Signed Update)**: 승인 키로 서명한 패키지만 설치
- **A/B 업데이트(A/B Update)**: 새 펌웨어를 비활성 슬롯에 설치·검증하고 부팅 실패 시 기존 정상 슬롯으로 복귀하는 갱신 방식
- **롤백 방지(Anti-Rollback)**: 보안 버전보다 낮은 서명 이미지 설치 차단
- **공급망(Supply Chain)**: 펌웨어의 설계·구성요소 조달·빌드·서명·배포에 참여하는 조직과 절차의 연결망
- **권한 상승(Privilege Escalation)**: 취약점을 이용해 원래 허용 범위보다 높은 시스템 권한을 얻는 공격
- **의존성(Dependency)**: 펌웨어가 빌드나 실행에 필요로 하는 외부 구성요소와 버전 관계
- **자격 증명(Credential)**: 장치나 관리자의 신원을 증명하는 키·인증서·암호 정보
- **무결성 측정(Integrity Measurement)**: 펌웨어의 해시를 기록·비교해 실행 코드의 변조 여부를 확인하는 절차
- **관리망(Management Network)**: BMC 같은 관리 인터페이스만 분리해 연결하는 전용 네트워크

## Ⅰ. 개요

- 정의/개념: 부팅·장치 제어를 담당하는 **고권한 코드의 취약성**
- 기존 한계: OS 보안만으로는 **부팅 전 침해·재설치 후 잔존** 통제 곤란

### 쉽게 이해하기 (학습용)

- 운영체제보다 먼저 실행되고 하드웨어를 직접 제어하는 펌웨어가 침해되면 운영체제 보안만으로 신뢰를 복구할 수 없다.

## Ⅱ. 특징

- **OS 이전 고권한 실행**으로 침해 지속성 증가
- **긴 수명·공급망 분산**으로 패치 지연 확대
- 서명 검증만으로 **취약 코드·오설정** 차단 불가

### 쉽게 이해하기 (학습용)

- 관리실 코드는 운영체제보다 먼저 켜지고 여러 업체가 오래 관리해 침해와 패치 누락이 오래 남는다.

## Ⅲ. 아키텍처

**도표안 A — 구조도**

```mermaid
flowchart LR
    S([공급사 서명 패키지])
    O([관리·정비 주체])
    subgraph F["펌웨어 보안 체계"]
        A["자산·배포 관리"] -->|"대상·매니페스트"| U["업데이트 검증"]
        T["신뢰 데이터·A/B 슬롯"] -->|"키·보안 버전"| U
        U -->|"검증 후보"| T
        T <-->|"후보·검증본"| B["부트·복구 제어"]
        M["관리·정비 경계"] -->|"BMC 갱신"| U
        M -->|"통제된 JTAG·UART"| T
    end
    S -->|"이미지·매니페스트"| A
    O -->|"인증·물리 접근"| M
```

**도표안 B — sequenceDiagram**

```mermaid
sequenceDiagram
    participant O as 운영·배포 시스템
    participant U as 업데이트·정책 검증기
    participant T as 신뢰 데이터 저장소
    participant S as A/B 슬롯·부트 제어기
    participant H as 부팅 건강 감시기

    O->>U: ① 이미지·매니페스트·SBOM 배포
    U->>T: ② 승인 키·폐기 상태·최소 버전 조회
    T-->>U: ③ 보호된 신뢰 기준 반환
    alt 서명·버전·SBOM 정책 실패
        U-->>O: ④ 설치 거부·원인 보고
    else 모든 사전 검사 통과
        U->>S: ⑤ 비활성 슬롯에 이미지 기록
        S-->>U: ⑥ 읽기 검증용 해시 반환
        U->>S: ⑦ 후보 슬롯 시험 부팅 요청
        S->>H: ⑧ 제한 횟수의 건강 검사 요청
        H-->>S: ⑨ 서비스·장치 상태 반환
        alt 건강 검사 통과
            S->>T: ⑩ 활성 슬롯·보안 버전 확정
            S-->>O: ⑪ 갱신 성공·측정값 보고
        else 건강 검사 실패
            S-->>U: ⑫ 후보 부팅 실패 통지
            U->>S: ⑬ 기존 검증 슬롯 재선택
            S-->>O: ⑭ 롤백·오류 원인 보고
        end
    end
```

| 설계 요소 | 입력·상태 | 역할 |
|:---|:---|:---|
| 자산·배포 관리 | 기기·SBOM·버전·공급처 | 대상–패키지 추적 |
| 업데이트 검증 | 이미지·서명·모델·의존성 | 설치 적합성 판정 |
| 신뢰 데이터·A/B 슬롯 | 키·보안 버전·활성 상태 | 비밀·후보·검증본 분리 |
| 부트·복구 제어 | 후보 건강 상태·검증 결과 | 활성화·롤백 결정 |
| 관리·정비 경계 | BMC·JTAG·UART 자격 증명 | 원격·물리 접근 인증·격리 |

> 요약: 자산·갱신·부트·관리 경계를 분리해 통제한다.

**동작 원리**

1. **패키지 배포**: 이미지·매니페스트·SBOM을 함께 전달한다.
2. **신뢰 기준 조회**: 승인 키·폐기 상태·최소 버전을 읽는다.
3. **보호 기준 반환**: 저장소가 변경이 제한된 검증 기준을 반환한다.
4. **사전 거부**: 서명·버전·SBOM 정책 실패 시 기록 전에 거부한다.
5. **후보 기록**: 통과한 이미지만 비활성 슬롯에 쓴다.
6. **기록 검증**: 다시 읽은 해시로 저장 손상을 확인한다.
7. **시험 부팅**: 다음 부팅에서 후보 슬롯을 제한적으로 실행한다.
8. **건강 검사**: 핵심 서비스와 장치 초기화를 확인한다.
9. **상태 반환**: 감시기가 제한시간 내 정상 여부를 반환한다.
10. **갱신 확정**: 성공 후에만 활성 슬롯과 최소 버전을 확정한다.
11. **성공 보고**: 새 버전과 무결성 측정값을 보고한다.
12. **실패 통지**: 후보의 건강 검사 실패를 갱신기에 알린다.
13. **검증본 복구**: 기존 정상 슬롯을 다시 선택한다.
14. **롤백 보고**: 실패 원인과 복구 결과를 보고한다.

### 쉽게 이해하기 (학습용)

- 새 펌웨어는 빈 방에 설치해 서명·취약점·부팅 상태를 모두 확인한 뒤에만 주 방으로 확정한다.

## Ⅳ. 종류 및 비교

| 취약점 위치 | 펌웨어 | 애플리케이션 |
|:---|:---|:---|
| 적용 기준 | 공급망·부트·**BMC·디버그 보호** | 입력·라이브러리·**응용 권한 보호** |
| 핵심 특징 | OS 밖 **고권한 부팅·장치 제어** | OS 위 **사용자 프로세스 실행** |
| 한계 | 재설치 후 잔존·**복구 경로 제한** | 패치 지연·**권한 상승** |

> 요약: 펌웨어 보안은 OS 밖 실행·갱신 경계를 통제한다

### 쉽게 이해하기 (학습용)

- 방 안 프로그램은 다시 깔 수 있지만 관리실 코드는 예비 관리실과 수리 통로까지 준비해야 한다.

## Ⅴ. 실무 고려사항 및 대책

| 운영 위험 | 대응 | 기대 효과 |
|:---|:---|:---|
| 자산·SBOM 누락으로 취약 펌웨어 식별 불가 | 기기 식별자·구성·해시·공급처를 배포 이력과 연결 | 영향 범위 신속 파악 |
| 서명은 유효하지만 알려진 취약 코드 설치 | 취약점·의존성·최소 보안 버전을 서명과 함께 검증 | 승인된 취약 코드 차단 |
| BMC·JTAG·UART의 기본·공유 자격 증명 노출 | 기기별 자격 증명·관리망 격리·출하 잠금 적용 | 고권한 경로 보호 |
| 키 폐기·복구 이미지 실패로 장치 영구 중단 | 다중 신뢰 키·검증본·A/B 롤백과 복구 훈련 적용 | 보안 갱신 복원력 확보 |

### 적용 사례

- 서버 BMC는 업무망과 분리된 관리망에서 장치별 인증서로 관리자를 인증하고, 서명된 펌웨어만 비활성 슬롯에 설치한다.

### 쉽게 이해하기 (학습용)

- 서버 관리실은 업무망과 분리하고 기기마다 다른 열쇠로만 들어간다.

## Ⅵ. 결론

- 서명 갱신·측정 부팅·키 관리·검증본 복구를 결합해 펌웨어 수명주기의 변조와 복구 실패를 통제한다.

### 쉽게 이해하기 (학습용)

- 관리실마다 승인된 변경·변조 확인·정상본 복구 방법을 선택한다.
