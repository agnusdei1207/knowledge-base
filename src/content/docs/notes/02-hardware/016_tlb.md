---
sidebar:
  order: 16
  label: "016. TLB 변환 색인 버퍼 (Translation Lookaside Buffer)"
  badge:
    text: "기출 • 70%"
    variant: note
title: "TLB 변환 색인 버퍼 (Translation Lookaside Buffer)"
date: "2026-08-17T16:35:00+09:00"
tags:
  - "notes-hardware"
weight: 16
extra:
  question_no: "016"
  source_status: "기출"
  source_history: "135회, 137회"
  priority: 70
  priority_note: "가상 주소 변환 캐시와 미스 처리"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **변환 색인 버퍼(Translation Lookaside Buffer, TLB)**: 최근 가상 페이지와 물리 프레임의 변환 결과를 캐싱해 반복 페이지 테이블 순회를 줄이는 주소 변환 캐시.
- **메모리 관리 장치(Memory Management Unit, MMU)**: 주소 변환과 메모리 접근 권한 검사를 수행하는 하드웨어.
- **페이지 테이블(Page Table)**: 가상 페이지 번호와 물리 프레임 번호의 대응 정보를 저장하는 메모리 구조.

</details>

- 정의/개념: **가상 페이지 번호(VPN)**와 **물리 프레임 번호(PFN)**의 매핑 정보를 캐싱하는 MMU 내 고속 하드웨어 캐시
- 배경/필요성: 다단계 페이지 테이블 참조 시 발생하는 **반복 메모리 접근 오버헤드** 및 지연 급증

#### 한줄 요약
- TLB는 자주 쓰는 주소 번역 결과를 가까이 보관해 매번 페이지 테이블을 처음부터 찾지 않게 하는 장치다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **주소 공간 식별자(Address Space Identifier, ASID)**: TLB 엔트리가 어느 프로세스 주소 공간에 속하는지 구분하는 식별자; 문맥 전환 시 전체 TLB 플러시 없이 공존 허용.
- **TLB 도달 범위(TLB Reach)**: TLB 엔트리 수 × 페이지 크기로 결정되는 변환 커버리지.
- **대형 페이지(Huge Page)**: 기본 4KB보다 큰 단위(2MB·1GB)로 주소 공간을 매핑해 단일 엔트리의 커버리지를 확대하는 방식.

</details>

- 최근 변환 재사용으로 **페이지 테이블 순회** 횟수 감소해 주소 변환 지연 단축
- **ASID** 태깅으로 문맥 전환 시 TLB 플러시 없이 다수 프로세스 엔트리 공존
- **대형 페이지**로 단일 엔트리가 커버하는 **TLB 도달 범위** 확대

#### 한줄 요약
- 프로세스가 같은 영역을 반복해서 접근하면 변환 결과를 재사용하고, ASID로 전환 비용을, 대형 페이지로 커버리지 한계를 줄인다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **가상 페이지 번호(Virtual Page Number, VPN)**: 가상 주소에서 페이지를 식별하는 상위 비트.
- **물리 프레임 번호(Physical Page Number, PPN)**: 변환 결과인 물리 메모리 프레임을 식별하는 번호.
- **페이지 테이블 워커(Page Table Walker)**: TLB 미스 시 다단계 페이지 테이블을 하드웨어로 순회해 변환을 찾는 기능.

</details>

```text
[주소 변환 경로]
├─ TLB 조회기  (VPN·ASID 병렬 비교)
├─ TLB 엔트리
│  ├─ VPN·ASID
│  ├─ PPN
│  └─ 권한·유효 비트
├─ 페이지 테이블 워커  (미스 시 순회)
└─ 페이지 테이블  (메모리 내)
```

선의 의미: 가지(`├─`)는 소유·연결 관계; 페이지 테이블 워커와 페이지 테이블은 TLB 미스 경로에서만 활성화

| 구성요소 | 책임 |
|:---|:---|
| TLB 조회기 | **VPN**·**ASID**와 엔트리 병렬 비교 |
| TLB 엔트리 | VPN·PPN·권한·유효 비트 저장 |
| 페이지 테이블 워커 | **TLB 미스** 시 다단계 페이지 테이블 순회 |
| 페이지 테이블 | 가상 페이지·물리 프레임 전체 매핑 보관 |

#### 한줄 요약
- TLB 조회기가 빠른 비교를 맡고, 워커와 페이지 테이블은 TLB에 없는 변환을 찾아 TLB를 다시 채우는 역할을 한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **TLB 적중(TLB Hit)**: 요청 가상 페이지 변환이 TLB에 있어 즉시 물리 주소를 생성할 수 있는 상태.
- **TLB 미스(TLB Miss)**: 변환이 TLB에 없어 페이지 테이블 순회가 필요한 상태.
- **페이지 폴트(Page Fault)**: 페이지 부재 또는 권한 오류로 OS 예외 처리가 필요한 상황.

</details>

```text
가상 주소 입력
      │
      ▼
1. VPN 및 ASID TLB 조회
      │
      ├─ 적중 → PPN·오프셋 결합 → 물리 주소 생성
      │
      └─ 미스
             │
             ▼
      2. 미스 시 페이지 테이블 워커 순회
             │
             ├─ 유효 매핑 → TLB 리필 → 물리 주소 생성
             │
             └─ 부재·권한 오류 → 3. 페이지 폴트 및 예외 처리 → OS 처리
```

**동작 원리**

1. **VPN 및 ASID TLB 조회**: 엔트리 병렬 비교 후 적중 시 물리 주소 생성
2. **미스 시 페이지 테이블 워커 순회**: 다단계 순회로 유효 매핑 리필
3. **페이지 폴트 및 예외 처리**: 유효 매핑 부재 또는 권한 오류 시 OS 처리

#### 한줄 요약
- TLB에 번역이 있으면 곧바로 물리 주소를 만들고, 없으면 표를 다시 찾으며, 표에도 없거나 권한이 없으면 OS로 넘긴다.

## Ⅴ. 종류 및 비교

| 주소 변환 실패 | TLB 미스 | 페이지 폴트 |
|:---|:---|:---|
| 적용 기준 | 유효 매핑이 존재하지만 **TLB**에 미캐싱 | 페이지 부재 또는 접근 권한 오류 |
| 핵심 특징 | 워커가 페이지 테이블 순회 후 **TLB** 리필 | OS 예외 처리·페이지 적재·권한 변경 |
| 한계 | 페이지 테이블 깊이만큼 메모리 접근 지연 | 저장장치 접근 시 매우 큰 지연 |

#### 한줄 요약
- TLB 미스는 번역표를 다시 찾는 지연이고, 페이지 폴트는 번역표의 상태나 권한을 OS가 해결해야 하는 예외다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **페이지 워크 캐시(Page Walk Cache)**: 페이지 테이블 상위 항목을 캐싱해 반복 순회의 메모리 접근을 줄이는 구조.
- **선택적 무효화(Selective Invalidation)**: 주소 공간 전체 또는 특정 엔트리만 골라 TLB에서 제거하는 처리.
- **내부 단편화(Internal Fragmentation)**: 대형 페이지 사용 시 실제 데이터보다 많은 물리 메모리가 할당되어 낭비되는 현상.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 작업 집합이 **TLB 도달 범위** 초과로 미스 증가 | **대형 페이지**·페이지 배치 최적화 | 변환 미스·워크 횟수 감소 |
| 문맥 전환마다 전체 TLB 플러시 비용 | **ASID**·**선택적 무효화** 적용 | 전환 직후 미스 급증 완화 |
| 다단계 순회 반복으로 메모리 지연 증가 | **페이지 워크 캐시** 적용 | 순회 메모리 접근·지연 감소 |
| 대형 페이지의 **내부 단편화** | 큰 연속 할당 영역에만 선택 적용 | 커버리지 이득과 공간 낭비 균형 |

#### 한줄 요약
- TLB 용량만 키우기보다 작업 집합 크기·문맥 전환 빈도·페이지 크기를 함께 조정해야 변환 비용을 줄일 수 있다.

## Ⅶ. 결론

- TLB 도달 범위 확장은 **대형 페이지 및 ASID**, 세밀한 보호는 **기본 페이지** 유지

#### 한줄 요약
- TLB 최적화는 엔트리 수를 늘리는 문제가 아니라, 프로그램이 실제로 접근하는 페이지 범위와 OS의 페이지 관리 비용을 함께 맞추는 설계다.
