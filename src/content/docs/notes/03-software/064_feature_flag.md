---
sidebar:
  order: 64
  label: "064. 피처 플래그"
  badge:
    text: "기출 · 50%"
    variant: note
title: "피처 플래그 (Feature Flag)"
date: "2026-08-25T11:00:00+09:00"
tags:
  - "notes-software"
weight: 64
extra:
  question_no: "064"
  source_status: "기출"
  source_history: "138회"
  priority: 50
  priority_note: "138회 기출, 배포 분리•기능 제어 현안"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **피처 플래그(Feature Flag / Toggle)**: 소스 코드를 다시 빌드하거나 배포하지 않고, 런타임에 조건부로 특정 기능을 켜거나 끄는(ON/OFF) 소프트웨어 공학 기법.
- **배포(Deploy)와 릴리즈(Release)의 분리**: 코드를 서버에 배포하는 물리적 행위(Deploy)와 사용자에게 기능을 노출하는 비즈니스 행위(Release)를 독립 분리.

</details>

- 정의/개념: 소스코드 재배포 없이 런타임에 특정 기능의 활성화(ON/OFF)와 대상 사용자를 동적 제어하는 **배포와 릴리즈 분리(Decoupling)** 기법
- 배경/필요성: 배포와 릴리즈의 강결합으로 인한 **출시 일정 지연 및 프로덕션 결함 발생 시 긴급 롤백 불가 해결 불가**

#### 한줄 요약
- 재배포 없이 런타임에 기능 ON/OFF와 사용자 타겟팅을 동적 제어하여 배포와 릴리즈를 분리한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **Kill Switch(킬 스위치)**: 장애 발생 시 관리 콘솔에서 버튼 하나로 1초 만에 해당 기능을 OFF시켜 장애 확산을 즉시 차단하는 비상 메커니즘.
- **Trunk-Based Development 연계**: 미완성된 기능도 Feature Flag(OFF)로 감싼 채 main 브랜치에 수시로 병합하여 Merge Hell을 방지.

</details>

- **배포(Deploy)와 릴리즈(Release)의 완전 분리** 기반의 비즈니스 주도 출시 제어
- 장애 발생 시 재배포 없이 즉각 비활성화하는 **킬 스위치(Kill Switch)** 기능
- 사용자 속성(VIP, 지역, 베타 테스터)에 따른 **정밀 타겟팅 및 A/B 테스트** 지원

#### 한줄 요약
- 배포-릴리즈 분리, 즉각 킬 스위치, 사용자 타겟팅 및 A/B 테스트를 실현한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **Unleash / LaunchDarkly**: 중앙에서 플래그 규칙을 관리하고 애플리케이션 SDK로 실시간 푸시/폴링해주는 피처 관리 플랫폼.

</details>

```text
[피처 플래그 중앙 관리 및 런타임 평가 구조]
|-- 중앙 플래그 관리 콘솔 (LaunchDarkly / Unleash Server)
|   |-- 플래그 상태 설정 (ON / OFF / Percentage Rollout)
|   `-- 타겟팅 룰셋 정의 (예: user.grade == 'VIP' or user.id % 100 < 20)
|-- 클라이언트/서버 애플리케이션 (Feature Flag SDK 내장)
|   |-- 로컬 룰셋 캐시 (Local Rule Cache: 네트워크 장애 방어)
|   `-- 플래그 평가 엔진 (Flag Evaluation Engine)
|       |-- [True 판정]  -> 신규 결제 UI 렌더링 / 신규 비즈니스 로직 실행
|       `-- [False 판정] -> 기존 레거시 로직 안전 실행 (기본값)
`-- 분석 플랫폼 (Datadog / Google Analytics: A/B 테스트 전환율 지표 수집)
```

선의 의미: 계층 및 룰셋 배포-로컬 평가-기능 분기 실행 구조

| 구성요소 | 책임 |
|:---|:---|
| **중앙 플래그 콘솔** | 웹 UI를 통해 플래그 ON/OFF 토글 및 **타겟팅 룰셋 중앙 관리** |
| **플래그 평가 SDK** | 애플리케이션 메모리에 룰셋을 캐싱하고 **사용자 컨텍스트 기반 초고속 평가** |
| **코드 분기 로직** | `if (flagService.isEnabled("new_feature", user))` 형태의 **조건부 실행** |
| **감사 로그 및 메트릭** | 플래그 변경 이력 추적 및 기능 노출별 성능/전환율 계측 |

#### 한줄 요약
- 중앙 콘솔, 로컬 캐싱 SDK, 조건부 코드 분기, 메트릭 분석 플랫폼이 결합된다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **Percentage Rollout(점진 롤아웃)**: 사용자 ID 해시를 기반으로 10% $\to$ 30% $\to$ 100% 비율로 점진적으로 기능을 활성화하는 방식.

</details>

```text
사용자 HTTP 요청 수신 (사용자 ID, 디바이스, 등급 컨텍스트 추출)
        │
   1. 애플리케이션 내부 플래그 SDK 호출 (`flagService.isEnabled('dark_mode', context)`)
        │
   2. 로컬 메모리 캐시의 타겟팅 룰셋과 사용자 컨텍스트 대조 평가
        │
   플래그 평가 결과는 무엇인가?
   ┌────┴───────────────────────────┐
[True: 기능 활성화]               [False: 비활성화 또는 콘솔 통신 장애]
신규 다크모드 화면 렌더링           안전한 기본 화이트모드 화면 렌더링
        │                                │
   3. 결과 응답 반환 및 Datadog으로 A/B 테스트 클릭 이벤트 전송
```

#### 한줄 요약
- 컨텍스트 추출 → 로컬 룰셋 평가 → True/False 코드 분기 → 결과 반환 및 통계 수집 순으로 진행된다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **Canary 배포 vs Feature Flag**: 인프라 레벨의 네트워크 트래픽 분할(Canary)과 코드 레벨의 사용자 속성 조건 분기(Feature Flag).

</details>

| 비교 항목 | 카나리 배포 (Canary Deployment) | 피처 플래그 (Feature Flag) |
|:---|:---|:---|
| 제어 계층 | **인프라 L7 라우터 (Ingress, Service Mesh)** | **애플리케이션 소스 코드 (if-else 조건문)** |
| 제어 주체 | DevOps 엔지니어, 인프라 운영자 | **소프트웨어 개발자, PO, 프로덕트 매니저** |
| 타겟팅 정밀도 | IP, 무작위 트래픽 비율 기반 분할 | **사용자 ID, 회원 등급, 지역 등 정밀 타겟팅** |
| 롤백 방식 | 라우터 트래픽 비율 원복 | **콘솔 토글 스위치 OFF (0.1초 즉시 적용)** |

#### 한줄 요약
- 카나리는 인프라 수준 트래픽 분할, 피처 플래그는 애플리케이션 수준의 비즈니스 타겟팅이다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Flag Debt(플래그 부채)**: 릴리즈가 100% 완료된 후에도 코드베이스에서 제거되지 않은 죽은 플래그(Dead Flag)가 쌓여 복잡도를 높이는 기술 부채.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 릴리즈 완료 후 플래그 미제거로 코드 스파게티화(**Flag Debt**) | **플래그 유효기간(TTL: 30일) 설정 및 만료 시 제거 스프린트 강제** | 코드 복잡도 및 테스트 경우의 수 최소화 |
| 플래그 관리 서버 장애 시 앱 전체 먹통 위험 | **로컬 인메모리 룰 캐싱 및 Fallback Default(False) 보장** | 네트워크 단절 시에도 0-Downtime 정상 서비스 |
| 복수 플래그 간 상호 결합으로 인한 예외 상황 폭증 | **플래그 간 의존성(Nesting) 절대 금지 및 직교성 유지** | 조합 폭발(Combinatorial Explosion) 버그 차단 |
| 플래그 변경 권한 오남용으로 인한 운영 사고 | **RBAC(역할 기반 접근 제어) 및 변경 승인 워크플로우 적용** | 비인가 플래그 조작 원천 차단 |

#### 한줄 요약
- TTL 기반 플래그 수거, 로컬 인메모리 캐싱, 중첩 금지, RBAC 통제로 기술 부채를 예방한다.

## Ⅶ. 결론

- 지속적 배포(CD)와 트렁크 기반 개발을 정착시키기 위해 **Unleash/LaunchDarkly 기반 피처 플래그**를 표준 도입하고, **플래그 생애주기 관리(TTL 및 정기 수거)** 를 통해 안전한 출시 통제 완성

#### 한줄 요약
- 피처 플래그는 코드 배포와 비즈니스 릴리즈를 분리하여 프로덕션 환경에서의 빠른 검증과 즉각적인 롤백을 가능하게 하는 핵심 딜리버리 패턴이다.