---
sidebar:
  order: 23
  label: "023. UNIX 커널·쉘·파일시스템 3요소 (UNIX Kernel Shell)"
  badge:
    text: "기출 · 30%"
    variant: note
title: "UNIX 커널·쉘·파일시스템 3요소 (UNIX Kernel Shell)"
date: "2026-07-27T23:59:59+09:00"
tags:
  - "notes-software"
weight: 23
extra:
  question_no: "023"
  source_status: "기출"
  source_history: "125회"
  priority: 30
  priority_note: "125회 기출 후 저빈도, UNIX 구성 기초"
---

## 미리 알고가기

- **유닉스(UNIX)**: ‘유닉스’로 읽는 공식 상표·운영체제 계열 이름으로 별도 영문 확장어를 만들지 않으며 커널·쉘·파일시스템을 분리한 다중 사용자 구조를 제공함
- **커널(Kernel)**: 특권 모드에서 프로세스·메모리·장치 자원 관리
- **쉘(Shell)**: 명령을 해석해 프로세스 실행을 요청하는 사용자 프로그램
- **파일시스템(File System)**: 파일·디렉터리의 경로·권한·저장 블록 관리
- **시스템 호출(System Call)**: 사용자 프로그램이 커널 기능을 요청하는 인터페이스
- **파이프(Pipe)**: 한 프로세스 출력을 다른 프로세스 입력에 연결
- **리다이렉션(Redirection)**: 표준 입출력을 파일·장치로 바꾸는 쉘 기능
- **프롬프트(Prompt)**: 쉘이 다음 명령을 기다린다는 표시
- **운영체제(Operating System, OS)**: ‘오에스’로 읽고 Operating System의 머리글자를 딴 표기이며 사용자 프로그램과 하드웨어 사이에서 프로세스·메모리·장치·파일을 관리
- **특권 모드·사용자 모드(Privileged·User Mode)**: 특권 모드는 하드웨어 제어 명령을 허용하고 사용자 모드는 응용 실행을 제한해 시스템 호출로 커널 기능을 요청하게 하는 프로세서 권한 상태
- **사용자 공간·커널 공간(User·Kernel Space)**: 사용자 공간은 쉘과 응용이 격리 실행되는 주소 범위이고 커널 공간은 운영체제 핵심과 파일시스템이 특권으로 실행되는 범위
- **표준 입출력(Standard Input/Output, Standard I/O)**: ‘스탠더드 아이오’로 읽고 Input과 Output의 머리글자를 빗금으로 묶은 표기이며 프로그램의 기본 입력·출력·오류 스트림
- **스크립트·인수(Script·Argument)**: 스크립트는 여러 쉘 명령을 저장한 파일이고 인수는 명령 동작에 전달하는 값
- **스크립트 주입(Script Injection)**: 신뢰하지 않은 입력이 쉘 문법으로 해석되어 의도하지 않은 명령이 실행되는 취약점
- **소유자·권한 비트(Owner·Permission Bits)**: 소유자는 파일 관리 주체이고 권한 비트는 소유자·그룹·기타 사용자의 읽기·쓰기·실행 허용 여부를 기록함
- **로그 필터·집계(Log Filter·Aggregation)**: 필터는 조건에 맞는 로그 줄만 통과시키고 집계는 여러 결과를 개수·합계로 묶어 파이프 조합의 처리 결과를 만듦

## Ⅰ. 개요

- 정의/개념: 커널·쉘·파일시스템의 **책임을 계층화한 운영체제 구조**
- 기존 한계: 사용자 프로그램의 직접 특권 접근은 **자원 충돌·권한 침해** 유발

### 쉽게 이해하기 (학습용)

- 쉘이 말을 해석하고 커널이 자원을 움직이며 파일시스템이 데이터를 보관한다

## Ⅱ. 특징

- **커널 시스템 호출**로 프로세스·메모리·장치 통제
- **쉘 명령 해석·파이프**로 단일 기능 프로그램 조합
- **파일시스템 계층**으로 경로·권한·영속 블록 관리

### 쉽게 이해하기 (학습용)

- 작은 명령의 출입구를 파이프로 이어 복잡한 작업을 만든다

## Ⅲ. 아키텍처 및 구성요소

```mermaid
flowchart TB
    U[사용자]
    H[하드웨어]
    subgraph X[UNIX 시스템 경계]
        subgraph US[사용자 공간]
            S[쉘]
        end
        subgraph KS[커널 공간]
            K[커널]
            F[파일시스템]
            K -->|파일 연산| F
        end
        S -->|시스템 호출| K
    end
    U -->|명령·스크립트| S
    K -->|장치 제어| H
```

| 설계 요소 | 설명 |
|:---|:---|
| 쉘 | 사용자 공간에서 시스템 호출 경계에 연결 |
| 커널 | 호출 경계 아래에서 하드웨어 자원 소유 |
| 파일시스템 | 커널 공간에서 파일 경로·저장 블록 연결 |

> 요약: 쉘 요청은 시스템 호출을 거쳐 커널·파일시스템으로 전달

### 쉽게 이해하기 (학습용)

- 쉘의 요청은 시스템 호출 문을 지나 커널과 파일 서고로 내려간다

## Ⅳ. 원리 및 절차 흐름도

```mermaid
sequenceDiagram
    participant U as 사용자
    participant S as 쉘
    participant K as 커널
    participant F as 파일시스템
    U->>S: 명령 입력
    S->>S: 명령 해석
    S->>K: 실행 요청
    K->>F: 파일 처리
    K-->>S: 종료 상태 반환
    S-->>U: 프롬프트 출력
```

| 절차 | 설명 |
|:---|:---|
| 명령 입력 | 사용자가 쉘에 명령·인수를 전달 |
| 명령 해석 | 쉘이 명령·인수·파이프·리다이렉션 분석 |
| 실행 요청 | 쉘이 시스템 호출로 프로세스 실행 요청 |
| 파일 처리 | 커널이 파일시스템에 실행 파일·데이터 요청 |
| 종료 상태 반환 | 커널이 프로세스 종료 상태를 쉘에 반환 |
| 프롬프트 출력 | 쉘이 다음 명령 입력 상태를 사용자에게 표시 |

> 요약: 쉘은 명령 해석·실행 요청 후 종료 상태 수신

### 쉽게 이해하기 (학습용)

- 창구가 요청을 해석해 관리실과 서고에 전달하고 다음 입력을 기다린다

## Ⅴ. 종류 및 비교

| UNIX 핵심 요소 | 커널 | 쉘 | 파일시스템 |
|:---|:---|:---|:---|
| 적용 기준 | 프로세스·메모리·**장치 통제** | 명령·스크립트·**파이프** | 영속 파일·**디렉터리 관리** |
| 핵심 특징 | 특권 **자원 관리** | 명령 해석·**실행 조합** | 경로·권한·**블록 관리** |
| 한계 | 결함의 **시스템 전파** | 입력·스크립트 **주입** | 권한 오류·**메타데이터 손상** |

> 요약: 시스템 호출·명령·파일 연산으로 요소 경계 구분

### 쉽게 이해하기 (학습용)

- 커널은 자원, 쉘은 명령, 파일시스템은 저장을 맡는다

## Ⅵ. 실무 사례

1. 로그 분석은 **파이프**로 필터·집계 명령 연결

### 쉽게 이해하기 (학습용)

- 로그는 필터와 집계 도구를 파이프로 이어 필요한 내용만 뽑는다

## Ⅶ. 결론

- 유닉스의 기능을 책임별로 분리하기 위해 **명령 해석·자원 통제·영속 저장 요구**를 검토하고, 쉘·커널·파일시스템의 경계를 지켜 구성한다

### 쉽게 이해하기 (학습용)

- 명령은 창구, 자원은 관리실, 데이터는 서고가 각각 맡는다
