---
sidebar:
  order: 4
  label: "004. 이진 탐색 (Binary Search)"
  badge:
    text: "미출 • 30%"
    variant: note
title: "이진 탐색 (Binary Search)"
date: "2026-08-10T10:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 4
extra:
  question_no: "004"
  source_status: "미출"
  source_history: ""
  priority: 30
  priority_note: "탐색 절차와 복잡도 계산의 대표 기초"
---

## Ⅰ. 개요

<details>
<summary>핵심 용어</summary>

- **이진 탐색(Binary Search)**: 정렬된 배열의 중앙 원소와 목표값을 비교해 후보 구간을 절반씩 줄이는 탐색 알고리즘이다.
- **선형 탐색(Linear Search)**: 첫 원소부터 차례로 비교해 목표값을 찾는 탐색이다.
- **중앙 키**: 현재 후보 구간의 가운데 원소에서 목표값과 비교하는 값이다.

</details>

- 정의/개념: 정렬 배열의 중앙 키와 목표값을 비교해 불가능한 절반을 반복 제외하는 **이진 탐색 알고리즘**이다.
- 배경/필요성: 선형 탐색은 데이터의 정렬 순서를 이용하지 않아 최악의 경우 모든 원소를 비교해야 한다.

#### 한줄 요약

- 정렬 배열에서 중앙 원소와 비교하여 후보 탐색 공간을 분할 정복 방식으로 1/2씩 축소한다.

## Ⅱ. 특징

<details>
<summary>핵심 용어</summary>

- **정렬 불변식(Sorted Invariant)**: 탐색 중에도 후보 구간의 원소가 키 순서를 유지한다는 전제이다.
- **로그 시간 $O(\log n)$**: 입력이 배수로 늘어도 처리 단계가 일정한 수만큼 증가하는 시간이다.
- **하한(Lower Bound)**: 목표값 이상인 첫 위치이다.
- **상한(Upper Bound)**: 목표값을 초과하는 첫 위치이다.

</details>

![이진 탐색과 선형 탐색의 비교 횟수 증가율](/study/diagrams/binary-search-growth.svg)

> 입력이 두 배가 될 때 파란 이진 탐색은 한 단계만 늘지만 붉은 선형 탐색은 입력 크기만큼 늘며, 정렬 배열을 가정한 이론적 비교 횟수다.

- 정렬 불변식에 따른 **후보 구간 절반 제거**가 핵심이다.
- 입력 2배당 비교 1회가 느는 **로그 증가율**이 핵심이다.
- 정확 일치와 경계 위치를 찾는 **하한•상한 변형**이 핵심이다.

#### 한줄 요약

- 입력 데이터(N)가 2배 확장되어도 비교 횟수는 1회 증가하는 점근적 로그 시간 $O(\log N)$ 성능을 제공하며 Lower/상한 탐색으로 확장 가능하다.

## Ⅲ. 구조 및 구성요소

<details>
<summary>핵심 용어</summary>

- **임의 접근(Random Access)**: 인덱스로 원하는 배열 원소를 일정한 시간에 읽는 방식이다.
- **후보 구간**: 목표값이 존재할 가능성이 남아 있는 배열의 인덱스 범위이다.
- **탐색 방향**: 중앙 키와 목표값의 대소 관계에 따라 다음 후보를 왼쪽 또는 오른쪽 절반으로 정하는 방향이다.

</details>

```text
이진 탐색 구조
├─ 이진 탐색기
├─ 정렬 배열
└─ 후보 구간
```

가지의 의미: 탐색 제어와 데이터•상태의 정적 책임 분리를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 이진 탐색기 | 중앙 키 대소로 **탐색 방향** 결정 |
| 정렬 배열 | **키 순서•임의 접근** 제공 |
| 후보 구간 | 양 끝 인덱스로 **후보 구간**•유효성 보관 |

#### 한줄 요약

- 탐색 제어 로직, 정렬 배열 데이터, 인덱스 경계(low, high) 상태가 상호 작용하여 중앙 키 비교 및 탐색 범위를 축소한다.

## Ⅳ. 흐름도

<details>
<summary>핵심 용어</summary>

- **중앙 인덱스**: 후보 구간을 좌우로 나누기 위해 선택하는 가운데 위치이다.
- **정수 오버플로(Integer Overflow)**: 계산 결과가 정수형의 표현 범위를 넘어 잘못된 값으로 바뀌는 현상이다.
- **후보 경계**: 다음 탐색에서 포함할 구간의 시작과 끝 인덱스이다.

</details>

```text
탐색 요청
   │
   ▼
후보 구간 유효성
├─ 무효: 미발견 반환
└─ 유효
   │
   ▼
1. 중앙 키 조회
   │
   ▼
2. 목표값 대소 판정
├─ 일치: 위치 반환
└─ 불일치
   │
   ▼
3. 후보 경계 갱신
   │
   └─ 후보 구간 유효성으로 반복
```

**동작 원리**

1. **중앙 키 조회**: 현재 후보 구간에서 오버플로를 피하는 식으로 중앙 인덱스를 계산한다.
2. **목표값 대소 판정**: 목표값을 중앙 키와 비교하여 일치 여부와 탐색할 좌•우 구간을 판정한다.
3. **후보 경계 갱신**: 목표값이 존재할 수 없는 절반을 제외하고, 찾거나 후보 구간이 빌 때까지 반복한다.

#### 한줄 요약

- 중앙 인덱스 계산, 목표값 대소 비교, 탐색 경계 갱신 과정을 종결 조건(탐색 성공 또는 범위 수렴)을 만족할 때까지 루프 수행한다.

## Ⅴ. 종류 및 비교

<details>
<summary>핵심 용어</summary>

- **해시 조회(Hash Lookup)**: 키를 버킷 위치로 변환해 정확한 키를 조회하는 방식이다.
- **해시 충돌(Hash Collision)**: 서로 다른 키가 같은 버킷 위치로 변환된 상태이다.
- **해시값(Hash Value)**: 키를 해시 함수에 넣어 얻은 버킷 위치 계산값이다.
- **버킷(Bucket)**: 같은 해시 위치로 매핑된 원소를 저장하는 공간이다.
- **범위 조회(Range Query)**: 정렬 순서에서 특정 조건에 속하는 연속된 키 구간을 찾는 조회이다.
- **정렬 유지 비용**: 삽입•수정 뒤에도 이진 탐색이 가능하도록 원소 순서를 보존하는 비용이다.

</details>

| 탐색 방식 | 이진 탐색 | 선형 탐색 | 해시 조회 |
|:---|:---|:---|:---|
| 적용 기준 | 정렬된 **범위 조회** | 미정렬 소규모 **선형 탐색** | 정확 키 **해시 조회** |
| 핵심 특징 | **이진 탐색**으로 후보 절반 제거 | **선형 탐색**으로 순차 비교 | **해시값**으로 버킷 접근 |
| 한계 | 삽입 시 **정렬 유지 비용** | 입력 증가만큼 비교 증가 | **충돌**•범위 조회 제약 |

#### 한줄 요약

- 정렬 배열의 범위 조회에는 이진 탐색, 소규모 미정렬 검색에는 선형 탐색, 단일 키 점조회(Point Query)에는 해시 조회가 적합하다.

## Ⅵ. 실무 고려사항 및 대책

<details>
<summary>핵심 용어</summary>

- **반열린 구간 $[low, high)$**: 시작 인덱스는 포함하고 끝 인덱스는 제외하는 후보 범위이다.
- **비교 함수**: 두 키의 순서와 일치 여부를 동일한 규칙으로 판정하는 함수이다.
- **배치 정렬(Batch Sorting)**: 여러 갱신을 모은 뒤 한 번에 정렬해 유지 비용을 줄이는 방식이다.
- **균형 탐색 트리(Balanced Search Tree)**: 높이를 로그 수준으로 유지해 조회와 삽입을 함께 지원하는 트리이다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 잦은 삽입의 **정렬 유지 비용**이 조회 이득을 초과 | 갱신 누적 후 **배치 정렬** 또는 **균형 탐색 트리** 적용 | 갱신 빈도에 맞춰 총 처리비용 절감 |
| 중복 키의 **하한•상한** 계약이 불명확해 결과 불일치 | **반열린 구간**과 하한•상한 계약 고정 | 중복 구간의 반환 범위 일관성 확보 |
| 큰 중앙 인덱스 합산으로 **정수 오버플로** 발생 | $low+(high-low)/2$ 방식 적용 | 유효 **중앙 인덱스** 산정 |
| 탐색•정렬의 비교 기준 불일치로 누락•오탐 | 정렬과 탐색에 동일 **비교 함수** 적용 | 이진 탐색의 **정렬 불변식** 보장 |

#### 한줄 요약

- 정렬 규칙과 탐색 비교 함수의 일치성을 보장하고, `low + (high - low) / 2`로 오버플로를 예방하여 정밀한 분할 판정을 수행한다.

## Ⅶ. 결론

<details>
<summary>핵심 용어</summary>

- **조회 패턴**: 정확 키 한 건과 연속 키 범위 중 반복적으로 요구되는 조회 형태이다.

</details>

- **조회 패턴**이 범위면 이진 탐색, 정확 키 반복이면 **해시 조회**를 선택한다.

#### 한줄 요약

- 정렬 상태 유지 비용과 범위 검색(Range Search) 및 경계 탐색 필요성을 종합 판단하여 이진 탐색과 해시 구조를 선별 적용한다.

