---
sidebar:
  order: 106
  label: "106. SHAP 설명기법 (SHapley Additive exPlanations)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "SHAP 설명기법 (SHapley Additive exPlanations)"
date: "2026-07-25T22:58:00+09:00"
tags:
  - "notes-latest_tech"
weight: 106
extra:
  question_no: "106"
  source_status: "기출"
  source_history: "122회, 135회"
  priority: 50
  priority_note: "기여도 기반 설명은 비교 출제 가치"
---

## 미리 알고가기

- **SHAP(Shapley Additive exPlanations)**: 특징별 기여도를 게임이론 기반으로 배분하는 기법
- **개별 예측 설명**: 개별 예측과 전체 모델 해석을 지원함
- **변수 수**: 변수 증가 시 계산 비용 급증으로 Explainer 선택 필요



## Ⅰ. 개요

- **정의/개념**: 예측과 기준의 차이를 특징별 기여도로 분해
- **배경/필요성**: 특징 조합의 기여를 일관된 규칙으로 배분

### 쉽게 이해하기 (학습용)

- 특징별 기여도를 합해 예측값과 기준값의 차이를 설명합니다.

## Ⅱ. 특징

- 특징 추가에 따른 출력 변화의 평균으로 기여도를 구한다.
- 기준값과 모든 특징 기여도의 합이 예측값이 된다.
- 개별 기여도를 모아 전역 중요도·분포를 분석한다.
- 배경분포·마스킹이 상관 특징의 몫을 바꿀 수 있다.

### 쉽게 이해하기 (학습용)

- 특징의 몫을 더하면 예측과 기준의 차이가 되지만 기준 집단과 상관 특징 처리 방식에 따라 몫이 달라집니다.

## Ⅲ. 구성요소 및 구조

| 설계 요소 | 설명 |
|:---|:---|
| 모델 출력 함수 | 연합별 모델 예측값 계산 |
| 배경분포·마스커 | 기준 데이터 샘플링·대체 |
| 특징 연합 가치 | 특징 집합별 모델 기대 출력 |
| SHAP 계산기 | 평균 한계 기여도 산출 구현 |
| 기준값·기여도 | 특징별 기여 분해 결과 |

```text
[모델 출력 함수] ─────┐
[배경분포·마스커] ────┴─ [특징 연합 가치]
                              │
                       [SHAP 계산기]
                              │
                       [기준값·기여도]
```

### 쉽게 이해하기 (학습용)
- 모델·배경분포·특징 연합·계산기가 기준값과 특징별 기여도를 산출합니다.

## Ⅳ. 원리 및 절차

```text
배경선택 → 연합구성 → 출력산출 → 한계기여 → 결과검증
```

| 절차 | 설명 |
|:---|:---|
| 배경선택 | 기준 표본과 특징 개입 규칙 설정함 |
| 연합구성 | 마스킹을 통한 특징 조합 생성함 |
| 출력산출 | 연합별 모델 출력 차이 도출함 |
| 한계기여 | 연합 크기별 가중치 적용 평균화함 |
| 결과검증 | 가산성과 배경 민감도 확인 수행함 |

### 쉽게 이해하기 (학습용)

- 비교 기준을 정하고 특징 조합의 출력 차이로 평균 몫을 구한 뒤 합이 실제 예측과 맞는지 확인합니다.

## Ⅴ. 종류 및 비교

| 적용 기준 | Kernel SHAP | Tree SHAP | Deep SHAP |
|:---|:---|:---|:---|
| 계산방식 | 범용 가중 선형회귀 근사 | 트리 경로 구조 연산 | 계층별 기여 규칙 적용 |
| 특성 | 표본 수에 따른 분산 증가 | 트리 구조에 따른 최적화 | 배경 표본별 근사 지원 |
| 대상 | 내부 미접근 모델 설명 | 트리 앙상블 반복 분석 | 신경망 입력 기여 분석 |

> 요약: 모델 구조별 최적 계산기를 선택하여 특징 기여도를 도출함

### 쉽게 이해하기 (학습용)

- 범용 표본 근사, 트리 전용 계산, 신경망 근사 중 모델 구조와 허용 시간에 맞는 방법을 고릅니다.

## Ⅵ. 실무 사례

- 사기 탐지는 배경 기간별 SHAP 변화를 비교한다.
- 대출 설명은 SHAP을 인과가 아닌 예측 기여로 본다.

### 쉽게 이해하기 (학습용)

- 두 특징이 함께 움직이면 몫이 가정에 따라 달라지므로 기준 집단과 의존성 가정을 함께 보여 줍니다.

## Ⅶ. 결론

- 배경분포·특징 의존성·근사 오차를 함께 검증한다.

### 쉽게 이해하기 (학습용)

- 특징별 몫은 유용하지만 기준 집단과 특징 의존성 가정을 숨기면 같은 예측도 다르게 설명될 수 있습니다.
