---
sidebar:
  order: 114
  label: "114. 동형암호 (Homomorphic Encryption)"
  badge:
    text: "기출 · 60%"
    variant: note
title: "동형암호 (Homomorphic Encryption)"
date: "2026-08-26T17:09:04+09:00"
tags:
  - "notes-latest_tech"
weight: 114
extra:
  question_no: "114"
  source_status: "기출"
  source_history: "132회"
  priority: 60
  priority_note: "기출 동형암호의 연산 비용•정확도 비교가 유력함"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **동형암호(Homomorphic Encryption, HE)**: 데이터를 암호화한 상태(평문 복호화 없이)에서 덧셈이나 곱셈 등의 연산을 수행하고 그 결과를 복호화하면, 평문 상태에서 연산한 결과와 동일함을 보장하는 차세대 암호 기술
- **처리 중 평문 노출**: 외부 연산을 위해 암호 데이터를 복호화하면서 원본 값이 메모리나 처리자에게 보이는 위험이다.

</details>

- 정의: 암호문 연산 결과가 평문 연산과 대응하는 **HE** 기법
- 배경/필요성: 일반 암호의 외부 연산 중 복호화에 따른 **평문 노출 위험**

#### 한줄 요약
- 암호문 상태로 계산하고 키 소유자만 **결과 복호화**

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **암호문 평가**: 비밀키 없이 암호문에 덧셈•곱셈 등의 연산 회로를 적용하는 과정이다.
- **암호문 노이즈**: 보안을 위해 포함되며 곱셈을 반복할수록 커져 복호화 가능 범위를 제한하는 오차 성분이다.
- **부트스트래핑**: 암호문 자체의 복호화 회로를 동형 연산해 노이즈와 연산 여유를 새로 고치는 기법이다.

</details>

- 비밀키 없이 입력•중간값을 계산하는 **암호문 평가**
- 곱셈 깊이•노이즈에 따른 **매개변수•복호화 한계**
- 부트스트래핑 기반 **암호문 연산 여유 회복**

#### 한줄 요약
- 곱셈 노이즈 누적 전 **부트스트래핑** 기반 연산 여유 회복

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **패킹**: 여러 평문 값을 하나의 암호문 슬롯에 묶어 병렬 연산하는 기법이다.
- **재선형화**: 암호문 곱셈 뒤 커진 암호문 크기를 평가 키로 줄이는 연산이다.
- **스케일 관리**: 근사 실수 연산에서 값의 정밀도와 암호문 모듈러스 수준을 조정하는 처리이다.

</details>

```text
                    [키•매개변수 관리자]
                    /          |          \
             [인코더•패커] [암호문 평가기] [결과 복호화기]
                                 |
                       [노이즈•스케일 관리자]
```

선의 의미: 키•매개변수 관리자는 인코더•패커, 암호문 평가기, 결과 복호화기의 공통 암호 경계를 정의하고, 노이즈•스케일 관리자는 암호문 평가기의 연산 가능 범위를 통제한다.

| 구성요소 | 책임 |
|:---|:---|
| 키•매개변수 관리자 | 보안•깊이별 **암호•평가 키 생성** |
| 인코더•패커 | 평문 값의 **암호문 슬롯 배치** |
| 암호문 평가기 | 덧셈•곱셈•회전의 **평가 회로 실행** |
| 노이즈•스케일 관리자 | 재선형화•부트스트래핑의 **연산 한계 통제** |
| 결과 복호화기 | 비밀키 기반 **결과 평문 복원** |

#### 한줄 요약
- 키•매개변수를 정하고 **암호문 평가•결과 복호화** 분리

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **곱셈 깊이**: 연산 회로에서 순차적으로 이어지는 암호문 곱셈의 최대 횟수이다.
- **평가 키**: 암호문 연산의 재선형화•회전 등을 수행하도록 공개되는 보조 키이다.

</details>

```text
데이터 소유자 ── 연산 함수•보호 목표 ──▶ 키•매개변수 관리자
키•매개변수 관리자 ── 1. 키•암호 매개변수 생성 ──▶ 인코더•암호화기
데이터 소유자 ── 평문 입력 ──▶ 인코더•암호화기
인코더•암호화기 ── 2. 입력 인코딩•암호화 ──▶ 암호문 평가기
암호문 평가기
   │ 3. 암호문 회로 평가
   └── 결과 암호문 ──▶ 데이터 소유자
데이터 소유자
   └ 4. 결과 복호화•오차 확인
```

### 동작 원리

1. 키•암호 매개변수 생성: 곱셈 깊이•정확도에 맞춘 설정
2. 입력 인코딩•암호화: 평문 벡터의 슬롯 패킹•암호화
3. 암호문 회로 평가: 노이즈•스케일 관리와 연산 실행
4. 결과 복호화•오차 확인: 비밀키 복원값•허용 오차 검증

#### 한줄 요약
- 회로 깊이에 맞춰 키를 생성하고 **복호화 오차** 검증

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **부분 동형암호(Partially Homomorphic Encryption, PHE)**: 덧셈이나 곱셈 한 종류를 반복 지원하는 방식.
- **제한 동형암호(Somewhat Homomorphic Encryption, SHE)**: 정해진 깊이 안에서 덧셈•곱셈을 지원하는 방식.
- **완전 동형암호(Fully Homomorphic Encryption, FHE)**: 부트스트래핑으로 임의 깊이 연산을 지원하는 방식.

</details>

| 구분 | 부분 동형 | 제한 동형 | 완전 동형 |
|:---|:---|:---|:---|
| 적용 기준 | 한 종류 연산 반복 | 사전 정의 깊이 회로 | 임의 깊이 반복 계산 |
| 핵심 특징 | 덧셈 또는 곱셈의 **단일 동형성** | 제한 깊이의 **덧셈**•**곱셈** | 부트스트래핑 기반 **깊이 연장** |
| 한계 | 지원 연산 제한 | 깊이별 큰 매개변수 | 높은 부트스트래핑 비용 |

#### 한줄 요약
- 지원 연산 종류와 **회로 깊이•부트스트래핑** 비교

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **복호화 실패**: 암호문 노이즈가 허용 범위를 넘어 결과 평문을 올바르게 복원하지 못하는 문제이다.
- **천-김-김-송 근사 오차(Cheon-Kim-Kim-Song Approximation Error, CKKS 근사 오차)**: 실수 벡터 연산의 스케일 조정과 반올림으로 결과 오차가 누적되는 문제이다.
- **부트스트래핑 지연**: 암호문 연산 여유를 회복하는 복잡한 회로 때문에 처리 시간이 급증하는 문제이다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 과소 매개변수의 복호화 실패 | 회로 깊이•노이즈 예산 기반 매개변수 산정 | 결과 복호화 **성공 보장** |
| CKKS 방식의 근사 오차 누적 | 스케일•재조정 위치별 평문 대비 오차 시험 | 추론 정확도 **검증** |
| 부트스트래핑의 지연 급증 | 회로 깊이 축소•패킹•실행 횟수 최소화 | 암호문 연산 비용 **절감** |

#### 한줄 요약
- 회로 깊이에 맞춰 **노이즈 예산•근사 오차**와 지연 검증

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **모듈러스**: 암호문 계수의 연산 범위와 노이즈 여유를 정하는 나머지 연산 기준값이다.
- **암호 매개변수**: 보안 수준•연산 깊이•정확도에 맞춰 정하는 차원•모듈러스•스케일 등의 값이다.

</details>

- 단일•고정 깊이는 **부분•제한 동형**, 임의 깊이는 **완전 동형** 선택

#### 한줄 요약
- 회로 깊이•오차•지연에 맞춰 **동형 방식•매개변수** 결정
