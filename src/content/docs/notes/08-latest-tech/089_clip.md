---
sidebar:
  order: 89
  label: "089. CLIP (대조 언어-이미지 사전학습)"
  badge:
    text: "기출 • 50%"
    variant: note
title: "CLIP (대조 언어-이미지 사전학습)"
date: "2026-08-14T05:08:00+09:00"
tags:
  - "notes-latest_tech"
weight: 89
extra:
  question_no: "089"
  source_status: "기출"
  source_history: "137회"
  priority: 50
  priority_note: "대조학습은 시각•언어 정렬의 기반"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **대조 언어-이미지 사전학습(Contrastive Language-Image Pre-training, CLIP)**: 이미지•텍스트를 공동 공간에 정렬하는 이중 인코더이다.

</details>

- 정의/개념: 이미지•텍스트를 공동 임베딩에 정렬하는 **CLIP**
- 배경/필요성: 고정 라벨 분류기는 새 개념마다 **라벨 데이터 재학습 필요**

#### 한줄 요약

- 이미지•설명을 **공동 임베딩 공간**에서 유사도 비교

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **대조학습(Contrastive Learning)**: 정답 쌍은 가깝게, 오답 쌍은 멀게 학습한다.
- **제로샷 분류(Zero-shot Classification)**: 클래스 표본 없이 문구 유사도로 분류한다.
- **교차 모달 검색(Cross-modal Retrieval)**: 이미지와 텍스트를 상호 검색한다.

</details>

- 정답 쌍은 가깝게, 오답 쌍은 멀게 하는 **대조학습**
- **이중 인코더** 기반 이미지•텍스트 임베딩 독립 계산
- 클래스 문구 유사도 기반 **제로샷 분류•교차 모달 검색**

#### 한줄 요약

- **대조학습•이중 인코더•제로샷 전이** 결합

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **이중 인코더(Dual Encoder)**: 이미지와 텍스트를 독립 인코딩해 표현을 재사용한다.
- **온도 계수(Temperature)**: 유사도 분포와 대조 손실의 구분 강도를 조절한다.

</details>

```text
                [이미지 인코더]   [텍스트 인코더]
                         \         /
                    [공동 임베딩 공간]
                               |
                      [유사도•온도 계수]
                               |
                       [제로샷 판별기]
```

선의 의미: 두 인코더와 공동 공간•유사도•판별기의 정적 결합 관계이다.

| 구성요소 | 책임 |
|:---|:---|
| 이미지 인코더 | 이미지의 **시각 임베딩** 변환 |
| 텍스트 인코더 | 클래스 문구의 **텍스트 임베딩** 변환 |
| 공동 임베딩 공간 | 이미지•텍스트의 **정렬 공간** |
| 유사도•온도 계수 | **코사인 유사도•온도** 기반 점수 조정 |
| 제로샷 판별기 | 클래스 문구와의 **유사도 판별** |

#### 한줄 요약

- **이미지•텍스트 인코더•공동 공간•유사도•판별기** 구성

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **유사도 행렬(Similarity Matrix)**: 배치의 모든 이미지•텍스트 쌍 점수 행렬이다.
- **대조 손실(Contrastive Loss)**: 정답•오답 쌍의 거리를 조정하는 손실 함수이다.

</details>

```text
┌──────────── 사전학습 종료 전 반복 ────────────┐
│ 이중 인코더                                  │
│   │ 1. 이미지•텍스트 임베딩                 │
│   ▼                                          │
│ 유사도 계산기                                │
│   │ 2. 쌍별 유사도 행렬                     │
│   ▼                                          │
│ 대조 손실                                    │
│   └── 3. 대조 손실 역전파 ──▶ 이중 인코더   │
└──────────────────────────────────────────────┘
이중 인코더
   └── 4. 클래스 문구 유사도 판별 ──▶ 제로샷 판별기
```

### 동작 원리

1. **이미지•텍스트 임베딩**: 이중 인코더로 정규화된 **공동 좌표**를 생성함.
2. **쌍별 유사도 행렬**: 온도 계수를 적용해 배치의 **모든 쌍을 비교**함.
3. **대조 손실 역전파**: 정답 쌍은 가깝게, 오답 쌍은 멀게 **표현을 갱신**함.
4. **클래스 문구 유사도 판별**: 자연어 클래스와 이미지의 **제로샷 분류**를 수행함.

#### 한줄 요약

- **임베딩•유사도 행렬•대조학습•제로샷 판별** 수행

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **지도 분류기(Supervised Classifier)**: 라벨 데이터로 학습해 클래스 확률을 출력한다.
- **생성 모델(Generative Model)**: 시각 조건으로 자연어 설명을 생성한다.

</details>

| 시각 모델 | CLIP | 지도 분류기 | 생성 모델 |
|:---|:---|:---|:---|
| 적용 기준 | **검색•제로샷 분류** | **고정 라벨 분류** | **자연어 설명 생성** |
| 핵심 특징 | 이미지•텍스트 **유사도** | 학습 클래스 **확률** | 시각 조건 **언어 생성** |
| 한계 | **프롬프트•도메인 민감도** | 새 클래스 **재학습** | **생성 비용•환각** |

#### 한줄 요약

- **제로샷•고정 라벨•자연어 생성**에 따라 모델 구분

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **프롬프트 민감도(Prompt Sensitivity)**: 설명 문구에 따라 예측 점수가 달라지는 성질이다.
- **도메인 전이(Domain Transfer)**: 다른 업무 분야에 학습 표현을 적용하는 능력이다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 클래스 문구에 따른 **프롬프트 민감도** | 복수 프롬프트 임베딩 앙상블 | **문구 민감도 완화** |
| 학습 분포 밖 **도메인 전이 오류** | 도메인 검증셋•선형 프로브 평가 | **전이 가능 범위 식별** |

#### 한줄 요약

- **프롬프트 편차•도메인 전이•제로샷 성능** 검증

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **프롬프트 앙상블(Prompt Ensemble)**: 여러 클래스 문구 임베딩으로 예측 편차를 줄인다.
- **전이 가능 범위(Transfer Range)**: 재학습 없이 허용 성능을 유지하는 영역이다.

</details>

- **전이 가능 범위**를 검증하고 문구 편차가 크면 **프롬프트 앙상블** 적용

#### 한줄 요약

- **전이 성능•문구 편차**에 따라 제로샷•앙상블 결정
