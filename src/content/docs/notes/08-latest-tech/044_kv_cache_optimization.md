---
sidebar:
  order: 44
  label: "044. KV Cache 최적화 (KV Cache Optimization)"
  badge:
    text: "기출 · 60%"
    variant: note
title: "KV Cache 최적화 (KV Cache Optimization)"
date: "2026-07-25T01:25:00+09:00"
tags:
  - "notes-latest_tech"
weight: 44
extra:
  question_no: "044"
  source_status: "기출"
  source_history: "138회"
  priority: 60
  priority_note: "메모리 병목 완화가 장문 추론의 핵심"
---

## 미리 알고가기

- **키·값(Key-Value, KV) 캐시**: 이전 토큰의 어텐션 값을 재사용함
- **대규모 언어모델(LLM)**: KV 캐시를 사용하는 생성 모델
- **그래픽 처리장치(GPU)**: 모델 연산과 캐시 접근 수행
- **중앙처리장치(CPU)**: 캐시 오프로딩 계층으로 활용
- **비디오 메모리(VRAM)**: KV 캐시를 저장하는 GPU 메모리
- **다중 헤드 어텐션(MHA)**: 헤드별 키·값을 저장
- **그룹 질의 어텐션(GQA)**: 여러 질의 헤드가 키·값 공유
- **다중 질의 어텐션(MQA)**: 모든 질의 헤드가 키·값 공유
- **최소 최근 사용(LRU)**: 오래 안 쓴 캐시 우선 회수
- **유효 시간(TTL)**: 캐시 보존 시간 제한
- **핵심 판단**: 재계산 감소와 메모리 비용 최적화 균형 중시



## Ⅰ. 개요

- **정의/개념**: 이전 토큰 K·V 재사용·메모리 관리
- **배경/필요성**: 재계산 감소·긴 문맥 메모리 통제

### 쉽게 이해하기 (학습용)

- 이전 계산 재사용을 위해 메모해 두되, 문장 증가에 따른 공간 부족 문제 대응

## Ⅱ. 특징

- 캐시가 없으면 과거 K·V를 매번 재계산한다
- 크기는 층·헤드·토큰·배치·정밀도에 비례한다
- GQA·MQA·양자화가 저장량을 줄인다
- 페이징·공유·회수가 메모리 낭비를 줄인다

### 쉽게 이해하기 (학습용)

- 메모 절약, 앞부분 공유, 부족 시 회수/지움 수행

## Ⅲ. 구성요소 및 구조

| 설계 요소 | 설명 |
|:---|:---|
| Cache Layout | 층·요청·Head·Token별 배치 |
| Allocator | Paged Block 단위 동적 할당 |
| Compression | 양자화·공유·잠재표현 압축 |
| Reuse·Eviction | Prefix 공유 및 우선순위 회수 |
| Scheduler | 계층 이동 및 Batch 실행 조정 |

```text
[ KV Cache Memory Plane ]
  +-- Shape: 2(K·V) × Layer × KV Head × Head Dim × Token × Byte
  +-- Layout·Allocator: 요청/Layer별 연속 영역 또는 Paged Block
  +-- Representation: MHA·GQA·MQA·Quantization·잠재 표현
  +-- Reuse·Lifetime: Prefix 참조·Refcount·TTL·LRU·Eviction
  +-- Scheduler·Tier: Admission·Preemption·GPU/CPU Offload·Recompute
```

> 요약: 할당·압축·공유·회수 정책으로 메모리 통제

### 쉽게 이해하기 (학습용)

- 계산 결과를 층별 블록에 저장하고 토큰마다 추가함

## Ⅳ. 원리 및 절차 흐름도

```text
산정
↓
할당
↓
저장
↓
갱신
↓
감시
```

| 절차 | 설명 |
|:---|:---|
| 산정 | 크기 및 정밀도 계산 |
| 할당 | 요청별 블록 동적 할당 |
| 저장 | 입력 KV 값 저장 |
| 갱신 | 캐시 읽고 신규 값 추가 |
| 감시 | 회수 정책 및 지표 측정 |

> 요약: 요청별 KV 할당·갱신 및 종료 시 회수함

### 쉽게 이해하기 (학습용)

- 요청 전 용량을 계산하고 부족하면 회수·이동함

## Ⅴ. 종류 및 비교

| 판단 | 미사용 | 사용 |
|:---|:---|:---|
| 처리 | 매번 재계산 | 저장된 K·V 재사용 |
| 적용 | 매우 짧은 생성 | 일반 LLM 서빙 |
| 비용 | 연산량 증가 | 메모리 용량 증가 |

> 요약: 캐시는 재계산 비용을 메모리 사용과 교환함

### 쉽게 이해하기 (학습용)

- 계산을 다시 하는 비용과 계산 결과를 저장하고 읽는 비용 사이의 선택임

## Ⅵ. 실무 사례

- 장문/테넌트별 메모리·히트율·대역폭 측정
- 양자화 등은 품질/지연 변화 회귀 검증 필요

### 쉽게 이해하기 (학습용)

- 메모 절약/지움 시 정보 정밀도 및 재계산 시간 발생

## Ⅶ. 결론

- KV Cache는 재계산 절감 및 메모리 자원 관리 기술
- 부하별 품질/지연 고려해 압축·공유·오프로드 선택 필요

### 쉽게 이해하기 (학습용)

- 빠른 생성을 위해 남기는 메모가 서버의 가장 큰 자원이 될 수 있어 수명주기 관리가 필요함
