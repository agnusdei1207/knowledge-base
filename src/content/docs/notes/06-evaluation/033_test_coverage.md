---
sidebar:
  order: 33
  label: "033. 테스트 커버리지"
  badge:
    text: "미출 • 50%"
    variant: note
title: "테스트 커버리지(구문•분기•MC/DC)"
date: "2026-08-06T23:27:50+09:00"
tags:
  - "notes-evaluation"
weight: 33
extra:
  question_no: "033"
  source_status: "미출"
  source_history: ""
  priority: 50
  priority_note: "구문•분기•MC/DC를 비교하는 시험 핵심축"
---

## Ⅰ. 개요

<details><summary>핵심 용어</summary>

- **테스트 커버리지(Test Coverage)**: 지정된 테스트 스위트(Test Suite)가 실행될 때 전체 대상 소스 코드의 구문, 분기 조건, 제어 흐름 구조 중 실제로 런타임에 실행을 거쳐 검증이 완료된 요소의 비율을 나타내는 정량적 지표이다.
- **시험 충분성 지표(Test Adequacy Criterion)**: 수행된 테스트가 시스템 내부 구조를 얼마나 촘촘하고 폭넓게 다루었는지를 입증하는 근거 데이터이나, 실행되었다는 사실만 증명할 뿐 결괏값의 정답성이나 기능적 무결성 자체를 보장하지는 않는 한계를 가진 지표이다.
- **미시험 요소(Untested Element)**: 프로젝트에서 설정한 커버리지 목표치 기준 내에 포함되지만 아직 어떠한 테스트 케이스로도 도달(실행)하지 못한 구조 영역으로, 추가 시나리오 보완이나 불가피한 예외 승인 절차가 요구되는 관리 대상이다.
- **수정 조건•결정 커버리지(Modified Condition/Decision Coverage, MC/DC)**: 복합 조건식 내의 각 개별 조건들이 다른 조건의 상태에 관계없이 독립적으로 전체 논리 결정 결과에 영향을 미침을 시험 쌍(Test Pair)으로 수학적으로 증명하도록 요구하는 가장 엄격한 커버리지 기준이다.

</details>

- 정의/개념: 소프트웨어 전체 내부 구조 대비 실제 테스트 케이스를 통해 한 번 이상 도달하고 실행된 요소의 비율을 수치화한 정량적 **테스트 커버리지(Test Coverage)**이자 객관적 **시험 충분성 지표(Test Adequacy Criterion)**이다.
- 배경/필요성: 10,000건의 테스트 케이스 성공이나 99%의 테스트 패스율만으로는 단 한 번도 실행되지 않아 숨겨진 치명적 버그를 내포하고 있을지 모르는 **미시험 요소(Untested Element)**를 구조적으로 식별해낼 수 없다.

#### 한줄 요약

- 준비한 수천 개의 테스트가 모두 성공(Pass)하더라도 단 한 번도 지나가지 않은 코드 경로에는 장애 폭탄이 남을 수 있으므로, 커버리지는 무엇을 잘했는지가 아니라 무엇을 놓쳤는지 식별하여 시나리오를 촘촘히 보완하기 위한 도구.

## Ⅱ. 특징

<details><summary>핵심 용어</summary>

- **포섭 관계(Subsumption Relationship)**: 논리적으로 더 엄격한 커버리지 기준(예: 분기 커버리지)을 100% 만족하면, 그보다 약한 하위 기준(예: 구문 커버리지)도 자연스럽게 100% 만족하게 되는 종속적이고 계층적인 포함 관계이다.
- **품질 게이트(Quality Gate)**: 소프트웨어 개발 각 단계(예: 빌드, 배포)에서, 코드 중요도별로 설정된 필수 커버리지 목표치를 미달할 경우 다음 스테이지로의 진입을 시스템적으로 강제 차단하는 품질 통제 정책이다.
- **위험 기반 목표(Risk-based Targeting)**: 사람의 생명과 직결되는 항공, 자율주행, 핵심 금융 원장 모듈일수록 구문 단위를 넘어 MC/DC 같은 가장 강력한 구조 검증 기준과 100%에 근접한 달성 목표를 부여하는 차등화 원칙이다.

</details>

- 코드 실행 궤적을 맵핑하여 사각지대에 놓인 위험한 **미시험 요소(Untested Element)**를 명시적으로 가시화하고 추적한다.
- 강한 기준이 약한 기준을 포함하는 **포섭 관계(Subsumption Relationship)**의 논리에 따라, 구문, 분기, MC/DC 순으로 오류 검출력과 증명 강도를 높이는 것이 핵심이다.
- 모듈별 중요도를 고려한 **위험 기반 목표(Risk-based Targeting)** 설정과 이를 강제하는 CI/CD 파이프라인의 **품질 게이트(Quality Gate)**를 통해 검증 밀도를 통제한다.

#### 한줄 요약

- 구문은 코드가 단지 읽혔는지, 분기는 참거짓 길이 모두 뚫렸는지, MC/DC는 조건 간 간섭이 없는지를 검증하므로, 생명과 돈이 직결된 치명적 시스템일수록 혹독한 기준을 대입하되 숫자 100%가 요구사항 완벽 구현을 담보하지 않음을 명심.

## Ⅲ. 구조 및 구성요소

<details><summary>핵심 용어</summary>

- **구문 커버리지(Statement Coverage)**: 소스 코드 내부의 실행 가능한 명령어 구문들이 테스트 중 최소 한 번 이상 호출되어 실행된 백분율로, 가장 기초적인 커버리지 기준이다(Line Coverage).
- **분기 커버리지(Branch Coverage/Decision Coverage)**: if, switch 등 조건 결정문이 가질 수 있는 최종 참(True)과 거짓(False) 방향의 제어 흐름 경로가 모두 최소 한 번 이상 실행된 비율이다.
- **조건 커버리지(Condition Coverage)**: 전체 결정의 참/거짓 결과와는 무관하게, 조건식 내부를 구성하는 각각의 개별 조건식(예: A>0) 자체가 참과 거짓을 모두 한 번씩 가져본 비율이다.
- **실행 흔적(Execution Trace)**: 코드 내부에 삽입된 센서(Instrumentation)를 통해, 테스트 구동 시 정확히 몇 번째 라인, 어느 분기, 어떤 개별 조건이 참/거짓으로 튕겨 나갔는지 빠짐없이 기록한 런타임 로그 궤적이다.

</details>

```text
              [검증 대상]         [시험 집합]
                       \           /
                        [계측 도구]
                             |
                        [실행 흔적]
                             |
                        [품질 게이트]
```

선의 의미: 컴파일된 검증 대상 바이너리에 계측 도구가 추적 코드를 심고, 입력된 시험 집합을 돌려 생성된 런타임 실행 흔적을 추출한 뒤, 최종적으로 품질 게이트가 목표 커버리지 달성 여부를 판정하는 동적/정적 융합 검증 흐름을 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 검증 대상 | 커버리지 측정의 바탕이 되는 소스 코드 또는 모델 기반 구조 요소 집합 제공 |
| 시험 집합 | 해피 케이스는 물론 한계 경곗값 및 예외 시나리오가 포함된 테스트 입력 및 기댓값 공급 |
| 계측 도구 | 빌드 또는 런타임 시 코드 분기점마다 센서를 심어(Instrumentation) 테스트별 실행 흐름 정밀 계측 |
| 실행 흔적 | 측정된 데이터를 바탕으로 구문, 분기점 미도달 내역을 추적 가능한 **실행 흔적(Execution Trace)**으로 시각화 및 문서화 |
| 품질 게이트 | 확보된 추적 데이터를 목표 임계치와 비교하여, 강제 보완 지시 또는 정당한 사유 기반 예외 처리 등 **품질 게이트(Quality Gate)** 역할 수행 |

#### 한줄 요약

- 소스 코드에 기록 장치를 달고 무수히 많은 테스트 데이터를 쏟아부으면 코드가 어느 골목을 안 거쳐 갔는지 선명하게 드러나므로, 그 흔적의 공백을 찾아내어 놓친 시나리오를 정밀하게 추가 타격.

## Ⅳ. 흐름도

<details><summary>핵심 용어</summary>

- **계측(Instrumentation)**: 소스 코드 컴파일 시점 혹은 런타임 시 구문 진입점 및 논리 분기점마다 별도의 카운터 코드를 삽입하여, 실제 테스트 실행 시 경로 활성화 여부를 정밀하게 수집하는 기술적 처리 과정이다.
- **도달 불가 코드(Dead Code / Unreachable Code)**: 어떠한 정상 및 비정상 입력값 조합과 제어 흐름의 조합으로도 절대 진입할 수 없는 논리적 모순 코드 블록으로, 커버리지 100% 달성을 가로막으므로 분석 후 삭제 또는 승인(Waiver) 처리가 필요하다.
- **예외 승인(Justification / Waiver Approval)**: 방어적 프로그래밍(예: 발생 확률이 극도로 낮은 하드웨어 결함 대비 코드)이나 도달 불가 코드로 인해 목표 커버리지에 미달한 경우, 개발팀이 작성한 타당성 근거와 파급 영향을 평가위원이 검토해 한시적 수용을 허가하는 행위이다.
- **위험별 커버리지 목표 설정**: 시스템 아키텍처 내 모듈별 장애 파급도(Criticality)를 분류하고, 그에 맞는 커버리지 레벨(구문, 분기, MC/DC)과 필수 달성 비율(예: 80%, 100%)을 차등 할당하는 계획 단계이다.
- **대상 코드•바이너리 계측**: 테스트 대상 코드 또는 실행 파일에 경로 추적을 위한 프로브(Probe)를 삽입하는 빌드 및 환경 구성 단계이다.
- **시험 집합•코드 경로 실행**: 자동화된 테스트 프레임워크를 통해 준비된 시나리오 세트를 구동시켜 대상 코드의 논리 궤적을 활성화하는 수행 단계이다.
- **달성률•미충족 요소 분석**: 생성된 실행 로그와 커버리지 리포트를 분석하여, 목표치 미달 영역과 단 한 번도 실행되지 않은(Red Line) 코드 블록을 식별해내는 단계이다.
- **시험 보완•예외 승인 결정**: 누락된 로직은 테스트 케이스 추가 설계로 메꾸고, 물리적으로 닿을 수 없는 코드는 안전 분석 근거를 첨부해 예외를 확정 짓는 마무리 단계이다.

</details>

```text
시험관리자
   │ 1. 위험별 커버리지 목표 설정
   ▼
커버리지도구
   │ 2. 대상 코드•바이너리 계측
   ▼
시험환경
   │ 3. 시험 집합•코드 경로 실행
   ▼
커버리지도구
   │ 4. 달성률•미충족 요소 분석
   ▼
시험관리자
   │ 5. 시험 보완•예외 승인 결정
```

### 동작 원리

1. **위험별 커버리지 목표 설정**: 시스템 등급에 따른 **위험 기반 목표(Risk-based Targeting)**로 구문, 분기, MC/DC 적용 기준을 선명히 분리한다.
2. **대상 코드•바이너리 계측**: 소스 코드나 런타임 환경에 커버리지 추적을 위한 **계측(Instrumentation)** 기능을 정밀하게 삽입한다.
3. **시험 집합•코드 경로 실행**: 경곗값 및 오류 케이스가 포함된 입력을 주입하여 복잡한 논리 제어 경로를 실제로 관통시킨다.
4. **달성률•미충족 요소 분석**: 수집된 실행 흔적 로그와 최초 설정한 목표 달성 비율 간의 갭을 도출해 사각지대를 분석한다.
5. **시험 보완•예외 승인 결정**: 접근 가능한 누락 경로는 즉시 테스트를 추가하고, 물리적인 **도달 불가 코드(Dead Code)**는 근거를 첨부하여 **예외 승인(Waiver)** 처리한다.

#### 한줄 요약

- 모듈 중요도에 따라 목표 잣대를 세우고 계측 테스트를 돌린 뒤 붉게 물든(미도달) 소스 라인 원인을 추적해야, 뚫을 수 있는 로직은 시나리오를 보충하고 쓸모없는 죽은 코드는 도려낼 수 있음.

## Ⅴ. 종류 및 비교

<details><summary>핵심 용어</summary>

- **결정(Decision)**: "if (A > 0 && B == true)"와 같이 하나 이상의 개별 조건(Condition)들이 AND/OR 논리 연산자로 결합되어, 제어 흐름을 분기시키는 전체 참/거짓 판단의 최종 결과 논리식이다.
- **커버리지 선택(Coverage Selection)**: 업무용 일반 웹 애플리케이션은 구문 및 분기 커버리지 정도로 타협하고, 자율주행이나 항공기 엔진 제어 등 복합 논리 오류 시 대참사가 발생하는 영역은 MC/DC를 의무 적용하는 전략적 분리 기준이다.

</details>

| 테스트 커버리지 기준 | 구문 커버리지 (Statement) | 분기 커버리지 (Branch/Decision) | 수정 조건•결정 커버리지 (MC/DC) |
|:---|:---|:---|:---|
| 시스템 적용 기준 | 일반 비즈니스 로직 등 기본 **구문 커버리지** 타깃 | 흐름 제어가 중요한 중요 모듈 대상 **분기 커버리지** | 항공/원전/차량 등 생명 안전 치명 로직 대상 **MC/DC 커버리지** |
| 핵심 검증 특징 | 코드 내 모든 실행 단위(구문)를 최소 1회 구동 | 전체 조건식인 **결정(Decision)**의 참/거짓 분기와 개별 **조건 커버리지** 만족 | 복합 조건 내 각 단일 조건이 전체 결과에 미치는 독립적 영향 증명 |
| 한계 및 기회 비용 | `if(A) do();`에서 조건이 거짓인 경우의 동작 누락 위험 | 복합 논리식 내부 특정 조건이 마스킹(Masking)되어 숨겨진 결함 은폐 | 독립 영향을 증명할 시험 쌍(Pair) 추출 비용 및 테스트 설계 복잡도 폭증 |

> **커버리지 선택(Coverage Selection)** 원칙에 따라 일반 IT 서비스 코드는 구문/분기를 적용하여 가성비를 높이고, DO-178C나 ISO 26262가 요구되는 안전 중요 복합 논리에는 철저한 MC/DC를 적용한다.

#### 한줄 요약

- 코드 100줄을 다 읽었다고 분기 경우의 수를 다 거친 게 아니고, 참/거짓 분기를 다 탔다고 복잡한 조건식 안쪽의 사소한 변수 결함을 잡아낼 수 없으므로 치명적 핵심 로직에는 다른 변수를 통제하고 오직 타깃 변수 하나만 바꿔서 결괏값 뒤집기를 증명하는 MC/DC가 필수.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>핵심 용어</summary>

- **높은 커버리지의 한계(Limitations of High Coverage)**: 커버리지 100%라는 숫자는 대상 코드가 한 번씩 활성화되었다는 의미일 뿐, 기획 의도와 다른 잘못된 기댓값 설정, 극한의 경곗값 누락, 동시성 오류 등 런타임의 기능적 결함은 잡아낼 수 없는 태생적 한계이다.
- **예외 근거(Justification/Waiver Evidence)**: 서드파티 라이브러리 내부 코드, 방어적 예외 처리 구문 등 현실적으로 커버하기 불가능한 구조 요소에 대해 커버리지 미달을 허용하기 위해 제출하는 시스템 영향도 평가 및 아키텍트 승인 기록이다.
- **위험별 게이트 운영(Risk-based Gate Operation)**: 단일한 80% 잣대를 모든 코드에 들이대는 대신, 프론트엔드 UI 로직은 구문 70%, 백엔드 결제 코어는 분기 95% 등 아키텍처 리스크에 비례하여 CI 파이프라인의 통과 장벽을 유연하면서도 엄격하게 운영하는 통제 기법이다.
- **시험 오라클(Test Oracle)**: 커버리지의 빈틈인 '정답 검증'을 보완하기 위해, 실제 코드 실행 결과가 요구사항이나 명세서의 기댓값과 정확히 일치하는지 자동화된 메커니즘으로 판정하고 거짓 성공을 걸러내는 잣대이다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 달성률 수치에 취해 잘못된 비즈니스 로직 연산 결과 결함 방치 | 강력하고 정교한 **시험 오라클(Test Oracle)** 결합을 통한 기댓값 검증 자동화 | **높은 커버리지의 한계**인 기능적 버그 누락 보완 및 실질 품질 향상 |
| 도달 불가 및 하드웨어 예외 처리 코드로 인한 전체 달성률 하락 병목 | 리팩터링으로 데드 코드 제거 또는 명확한 **예외 근거(Waiver Evidence)** 문서화 후 통과 | 왜곡 없는 정확한 달성 지표 관리 및 이해관계자 해석 가능성 확보 |
| 안전 치명 모듈에서 복합 조건식 내 일부 조건의 은폐/마스킹 위험 | 모듈 리스크 타깃팅 **위험별 게이트 운영**을 통한 MC/DC 필수 의무화 통제 | 수많은 조건 간 간섭 배제 및 단일 조건 결함의 독립적 파급력 완벽 입증 |

#### 한줄 요약

- 자동차 브레이크 제어 코드에서 속도, 노면, 페달 압력 등 수많은 변수가 얽힌 복합 조건은 다른 조건을 고정시킨 채 오직 타깃 조건 하나만 반전시켰을 때 최종 제동 논리가 정확히 뒤바뀌는 독립적 시험 쌍(Pair)을 설계해야만 치명적 논리 오류 예방 가능.

## Ⅶ. 결론

<details><summary>핵심 용어</summary>

- **구조 커버리지와 정답성(Structural Coverage & Correctness)**: 화이트박스 관점의 물리적 코드 실행 범위 증거(Coverage)와 블랙박스 관점의 비즈니스 요구사항 결과 정답 증거(Oracle)를 상호 결합하여 융합적으로 퀄리티를 평가하는 포괄적 소프트웨어 품질 확보 철학이다.
- **미충족 요소 처리(Handling Untested Elements)**: 커버리지 측정 후 도출된 미실행 경로(Red Line)에 대해, 입력 조합이 가능한 곳은 집요하게 테스트 코드를 보완하고 물리적 진입이 불가한 곳은 아키텍처 예외 승인 절차를 태우는 체계적 완결 프로세스이다.
- **보완 결정(Supplementation Decision)**: 측정된 수치 이면의 리스크를 꿰뚫어 보고 모듈 중요도에 따라 추가 테스트 강제 작성과 데드 코드의 합법적 예외 승인을 판가름하는 엔지니어링 리더의 의사결정 행위이다.

</details>

- **구조 커버리지와 정답성(Structural Coverage & Correctness)**을 상호 교차 검증하고 철저한 **미충족 요소 처리(Handling Untested Elements)** 과정을 기반으로 한 최종 **보완 결정(Supplementation Decision)** 체계 구현 필수.

#### 한줄 요약

- 수치 채우기용 무의미한 테스트 양산에 매몰되지 않고, 실행되지 못한 구문과 분기 및 복합 조건을 도출해 위험도에 맞게 케이스를 보충하며, 도달 불가 코드는 기술적 근거를 남겨 예외 수용하는 절차를 정착시킬 때 커버리지는 비로소 진짜 시스템 안전성을 증명.
