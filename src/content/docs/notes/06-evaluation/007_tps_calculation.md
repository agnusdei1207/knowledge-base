---
sidebar:
  order: 7
  label: "007. TPS 계산 - 동시 사용자•응답 시간 공식 (TPS Calculation)"
  badge:
    text: "기출 • 50%"
    variant: note
title: "TPS 계산 - 동시 사용자•응답 시간 공식 (TPS Calculation)"
date: "2026-08-13T23:10:00+09:00"
tags:
  - "notes-evaluation"
weight: 7
extra:
  question_no: "007"
  source_status: "기출"
  source_history: "131회"
  priority: 50
  priority_note: "동시 사용자와 응답시간으로 처리량을 산정"
---

## Ⅰ. 개요

<details><summary>핵심 용어(Core Terms)</summary>

- **TPS(Transactions Per Second)**: 1초 동안 오류나 지연 없이 성공적으로 완료한 비즈니스 업무 거래 또는 쿼리 건수.
- **폐쇄형 부하(Closed Workload Model)**: 인간의 반복적 상호작용 행동(요청-응답-생각시간)을 모사한 테스트 부하 모델.
- **처리율 산정(Throughput Estimation)**: 목표 동시 접속 사용자 수와 서버 응답시간, 사용자의 생각시간을 활용하여 초당 처리량을 도출하는 기법.

</details>

- 정의/개념: 동시 사용자·응답·생각시간의 **처리율 산정**
- 배경/필요성: 회원 수만으로는 **실제 TPS 역산 불가**

#### 한줄 요약

- $N$, $R$, $Z$로 **목표 TPS 산정**

## Ⅱ. 특징

<details><summary>핵심 용어(Core Terms)</summary>

- **대화형 응답시간 법칙(Interactive Response Time Law)**: 폐쇄형 큐 네트워크에서 동시 사용자(N)와 처리량(X), 응답시간(R), 생각시간(Z)이 $N=X(R+Z)$ 관계를 유지한다는 용량 검증 원칙.
- **동시 사용자(N, Concurrent Users)**: 서비스 루프를 반복하며 인프라 부하를 유발하는 유효 사용자 수.
- **부하 보정(Load Calibration)**: 이론적 처리량에 피크타임 집중률, 호출 비율 등 실제 환경 요인을 가중치로 반영하는 작업.

</details>

- **대화형 응답시간 법칙**으로 처리량 관계 산출
- 업무 TPS와 내부 API RPS를 분리 측정
- 피크·업무 비율을 **부하 보정**에 반영

#### 한줄 요약

- 생각시간 증가 시 동일 사용자 대비 **요구 TPS 감소**

## Ⅲ. 구조 및 구성요소

<details><summary>핵심 용어(Core Terms)</summary>

- **응답시간(R, Response Time)**: 요청 출발부터 최종 결과값이 화면에 렌더링을 시작하기 전까지의 왕복 지연 시간.
- **생각시간(Z, Think Time)**: 응답 수신 후 다음 요청 전송까지 사용자가 소비하는 휴지 시간.
- **처리량(X, Throughput)**: 특정 기준 시간 동안 오류 없이 성공적으로 방출된 비즈니스 거래 건수.
- **업무 거래 경계(Business Transaction Boundary)**: 단일 비즈니스 의도 달성을 위해 수행되는 여러 API 호출을 하나로 묶은 트랜잭션 판정 기준.

</details>

```text
TPS 산정 구조
├─ 업무 거래 경계
├─ 동시 사용자 N
├─ 응답시간 R
├─ 생각시간 Z
└─ 처리량 X·피크 보정
```

| 구성요소 | 책임 |
|:---|:---|
| 업무 거래 경계 | 거래 TPS와 API RPS 분리 |
| 동시 사용자 N | **동시 사용자** 규모 측정 |
| 응답시간 R | 목표 **응답시간** 설정 |
| 생각시간 Z | 실제 **생각시간** 반영 |
| 처리량 X·피크 보정 | **처리량·피크 계수** 산출 |

#### 한줄 요약

- $X=N/(R+Z)$ 기반 **처리량·피크 용량 산출**

## Ⅳ. 흐름도

<details><summary>핵심 용어</summary>

- **사용자 요청 주기**: 요청·응답·생각시간을 합친 반복 구간이다.
- **업무 혼합**: 운영 거래 유형을 실제 비율로 구성한 부하이다.
- **피크 TPS 보정**: 평균 처리량에 집중률을 반영하는 활동이다.
- **N·R·Z 측정 조건 확정**: 거래 경계와 단위를 정하는 단계이다.
- **N·R·Z 표본 수집**: 동시성·응답·생각시간을 모으는 단계이다.
- **X=N/(R+Z) 계산**: 폐쇄형 평균 처리량을 구하는 단계이다.
- **업무 비율·피크 TPS 보정**: 최종 시험 부하를 정하는 단계이다.

</details>

```text
업무 책임자
   │ 거래 경계·피크 비율 전달
   ▼
분석가·측정기
   │ 1. N·R·Z 측정 조건 확정
   │ 2. N·R·Z 표본 수집
   ▼
계산·검증기
   │ 3. X=N/(R+Z) 계산
   │ 4. 업무 비율·피크 TPS 보정
   ▼
목표 TPS·시험 부하 결과
```

### 동작 원리

1. **N·R·Z 측정 조건 확정**: 사용자 요청 주기 정의
2. **N·R·Z 표본 수집**: 동시성·주기 지연 수집
3. **X=N/(R+Z) 계산**: 폐쇄형 평균 TPS 산출
4. **업무 비율·피크 TPS 보정**: 피크 용량 결정

#### 한줄 요약

- 평균 TPS에 **업무 비율·피크 계수 보정**

## Ⅴ. 종류 및 비교

<details><summary>핵심 용어</summary>

- **폐쇄형 부하 모델(Closed Workload Model, 제어형 유입 모델)**: 시스템 내에 머무는 가상 사용자의 총수를 엄격하게 제한하고 유지시키면서, 앞선 요청의 응답과 생각시간이 지난 후에만 다음 트래픽이 쏟아지는 웹 유저 시뮬레이션 기반 성능 모델.
- **개방형 부하 모델(Open Workload Model, 발산형 유입 모델)**: 대상 시스템의 처리 응답이나 혼잡 여부에 일체 아랑곳하지 않고, 사전에 입력된 초당 도달률(Arrival Rate)에 따라 맹목적으로 무한 트래픽 신규 요청을 쏟아내는 가혹한 인프라 융단폭격 부하 모델.
- **RPS(Requests Per Second, 초당 시스템 호출 빈도)**: 사용자 비즈니스 거래 단위가 아닌, 프론트엔드에서 서버로 쏘는 개별적인 HTTP 스크립트나 API 쿼리 단위의 초당 처리 속도 횟수 지표.
- **API(Application Programming Interface)**: 애플리케이션 간에 데이터를 주고받고 시스템 기능을 제어할 수 있도록 약속된 기술적 연결 호출 통신 규격.
- **모델 선택(Load Model Selection Strategy)**: 시스템의 특성에 맞게, 사람이 화면을 바라보며 조작하는 B2C 서비스는 생각시간을 품은 폐쇄형 모델을 택하고, 기계 간 무한 릴레이 호출이 잦은 B2B API 중계 서버는 무자비한 개방형 모델을 채택하는 아키텍트의 분석 기조.

</details>

| 시뮬레이션 부하 생성 모델 | 동시 사용자 모델 (폐쇄형 부하) | 요청률 기반 모델 (개방형 부하) |
|:---|:---|:---|
| 시스템 적용 기준 | 사람의 웹·앱 요청 | B2B·서버 간 **API** 호출 |
| 핵심 메커니즘 | **폐쇄형 부하 모델**의 사용자 주기 | **개방형 부하 모델**의 도달률 |
| 검증 한계 | 생각시간 오류로 TPS 왜곡 | 혼잡 중 요청 누적으로 과부하 |

> 요약: 타겟 애플리케이션의 유입 패턴 주체를 인간으로 볼 것인지 기계 통신으로 볼 것인지에 따라 **모델 선택** 전략을 이원화 적용.

#### 한줄 요약

- 사람은 폐쇄형, 기계 호출은 **개방형 모델 선택**

## Ⅵ. 실무 고려사항 및 대책

<details><summary>핵심 용어(Core Terms)</summary>

- **업무 거래와 내부 요청(Business Transaction vs Internal Calls)**: 사용자 단위 트랜잭션과 이를 달성하기 위한 하위 API 종속 요청의 구분.
- **피크 계수(Peak Factor)**: 일일 평균 대비 트래픽 폭증 시점의 용량 방어를 위해 곱셈 적용하는 보정 배수.
- **경계 혼동(Boundary Confusion)**: 비즈니스 TPS와 내부 API RPS를 혼용하여 발생하는 분석 오류.

</details>

| 실무 용량 산출 시 범하는 치명적 문제점 | 현장 엔지니어링 대책 및 수식 튜닝 전략 | 용량 최적화 획득 효과 |
|:---|:---|:---|
| 사용자 생각시간 누락 | 실제 **생각시간 Z** 적용 | 과잉 TPS·투자 방지 |
| 거래와 API 단위 혼용 | **업무 거래·내부 요청** 분리 | **경계 혼동** 방지 |
| 평균이 폭주 시간 은폐 | 로그 기반 **피크 계수** 적용 | 이벤트 용량 확보 |

#### 한줄 요약

- 결제 TPS와 내부 API **RPS 분리 계측**

## Ⅶ. 결론

<details><summary>핵심 용어</summary>

- **부하 현실화(Load Realization, 테스트 인프라 정합성 동기화)**: 책상머리에서 도출한 이론 수식에서 벗어나, 현재 운영 중인 시스템 로그 통계 기반의 진짜 동시 유저 규모, 유효 체감 응답시간, 유저별 페이지 체류시간(생각시간), 그리고 트랜잭션 유형 믹스 비율을 테스트 툴에 가미하여 시뮬레이터 신뢰도를 실제 수준으로 끌어올리는 조정 단계.
- **과부하(Overload, 포화 붕괴 상태)**: API 게이트웨이 등으로 쏟아져 들어오는 외부 트래픽 도착률이 시스템 백엔드 큐의 순수 완료 방출 속도 능력을 한참 초과하여, 대기열 메모리가 터져나가고 연쇄 503 오류가 폭증하는 통제 불능 인프라 상태.
- **목표 부하 선택(Target Workload Paradigm Decision)**: 시스템 아키텍처 특성에 입각하여, 프론트웹 사용자 행동 평가는 N/(R+Z) 주기 폐쇄식 공식을, 서버 간 API B2B 통신 부하 검증은 RPS 기준의 개방형 무한 주입식을 양분하여 채택하는 설계적 의사결정 원칙.

</details>

- 사람은 $N/(R+Z)$, API는 도달률로 목표 부하 산정

#### 한줄 요약

- 운영 로그로 보정해 **과소 용량·과잉 투자 방지**
